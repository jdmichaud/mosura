<!-- Watcom 10.0a signed division by a power of two (the subject's 0x23108's `x / 32` sites, W3): the SBB
     template `MOV EDX,EAX; SAR EDX,0x1f; SHL EDX,5; SBB EAX,EDX; SAR EAX,5` = (x + 31) >> 5 for a
     negative x, x >> 5 otherwise. Wrapped as `mov eax,[esp+4]; <template>; ret`. -->
<binaryimage arch="x86:LE:32:default:watcom">
  <bytechunk space="ram" offset="0x6000" readonly="true">
8b44240489d0c1fa1fc1e2051bc2c1f805c3
  </bytechunk>
</binaryimage>

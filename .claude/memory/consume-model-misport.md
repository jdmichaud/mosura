---
name: consume-model-misport
description: "CLOSED ✅ `608d7d3` (2026-07-17): fresh-varnode consume default fixed to Ghidra's ~0 (varnode.cc:586) after the long park-unpark arc (parked @94eef0f → C1 probe falsified root (i) → Brick D confirmed+cleared root (ii)). switchloop→0.851, case-7 wrong-code class retired. Residual = switchloop merge/naming C."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9beadf25-4682-4e85-94fa-f326d85ed777
---

retw1 grounding + build @94eef0f (2026-07-12; 7th task re-frame of the session — the "R8 return-width" framing was STALE, already fixed by subvar+#21(a)).

**THE MIS-PORT:** mosura `alloc_varnode` sets `consume: 0` (funcdata.rs:255); Ghidra's Varnode constructor sets `consumed = ~((uintb)0)` (varnode.cc:586, conservative until ActionDeadCode recomputes). Consequences:
- Faithful `RuleOrConsume` (rules.rs:8281 = ruleaction.cc:353-371) fires on fresh varnodes → switchloop case 7 folds LIVE `x^0x87`→`#0x87` = **semantically WRONG output on master today**. orconsume fires in 17 fixtures vs Ghidra 0.
- Roots the subvar/pullsub over-fire: consume=0 passes every narrowing gate (subvarflow.rs:227/545/588 mirror subflow.cc:112) trivially — subvar_zext 99x vs Ghidra 12x.

**Brick 1 BUILT + GATED (UNCOMMITTED WIP in tree @94eef0f-dirty):**
- 3 edits: funcdata.rs consume→`!0u64`; pipeline.rs ActionConsume::apply return 1→0 (3rd instance of the return-1 action mis-port class, cf. [[task22-typespacebase-campaign]]); `.then(ActionConsume)` after ActionNonzeroMask in the reheritage restart group (completes the nzmask/consume pairing; Ghidra recomputes consume every mainloop iter, coreaction.cc:3925+).
- 24 tests updated via a `recompute_consume` helper (runs real calc_consume in setup = the pipeline precondition; 2 RETURN/CALL pull tests also seed nzm; 1 hand-sets 0xff, documented). They were asserting the mis-port default.
- MEASURE: switchloop 0.830→0.840 (case-7 SEMANTIC FIX, renders `uVar2^0x87; 8-(uVar2&1)`), subvar over-fire → Ghidra-exact (28→11 vs 12 on piecestruct), modulo's 60 orconsume firings benign (unchanged). **BUT piecestruct 1.000→0.873 + hard test `piecestruct_folds_shifts_to_concat` (decompile_corpus.rs:220) FAILS — it pins Ghidra-CORRECT output → suite-green invariant blocks the land.**

**THE EXPOSED GAP — refineInput hypothesis FALSIFIED (2026-07-15 grounding, do NOT resurrect):** Ghidra's refinement gate `size>4 && max<size` (placeMultiequals, heritage.cc:2610) does NOT fire on piecestruct's RDI range — the call-arg `lea` writes RDI:8 so collect's max=8=size. Ghidra ALSO heritages an 8-byte input and narrows TWO-STEP via subvar's input arm (`SUB42(EDI(i),0)→DI(i)`, ghidra.trace DEBUG 469-474). So the refineInput/guardInput split is NOT what saves Ghidra. What saves it: (i) the **actmainloop repeats** {heritage → ActionDeadCode(consume recompute) → nzmask → oppool} to quiescence — unbounded rounds, so a mid-pool-blocked narrowing step re-fires next iteration before the shape is lost; mosura has 3 rounds + the reheritage restart, and empirically R8's chain loses the round-1 race to the non-consume-gated `subzext` (`zext(SUB(x,0))`→`x&0xff` destroys RuleShiftPiece's pattern; probe: r0x80:4's consume is CORRECT 0xff from round 2 but the SUBPIECE shape is already gone; DI/SI/R9B chains complete, only R8 — the doubly-copied register — loses). (ii) Ghidra's second-struct packing runs through STACK-SLOT RMW (`or [rbp-0x14],eax`: LOAD/OR/STORE promoted mid-mainloop) where mosura's pre-pool `recover_stack` yields a register-form graph → a different rule race entirely. Both roots = the tracked architectural rocks: [[task8-mainloop-repeat]] + the recover_stack/placement family ([[task22-typespacebase-campaign]] #22-B). **VERDICT: DEEP — no bounded faithful brick; unblock = task #8-class.**

**PARKED TURNKEY (2026-07-15, lead-directed):** exact diff = `brick1-consume-default.patch` (this memory dir; 342 lines, 4 files: funcdata.rs/pipeline.rs/rules.rs/subvarflow.rs; diffed @`94eef0f`, re-verified `git apply --check` clean on `19f1ebb` after Brick 2 landed). Tree reverted clean to `94eef0f`. RESUME: apply on a task#8-advanced base, re-measure piecestruct FIRST (hard test `piecestruct_folds_shifts_to_concat`), then land Brick 1 → Brick 2. NOTE: until Brick 1 lands, master EMITS WRONG CODE on the case-7 class — any OR/XOR mysteriously collapsing to a constant is this landmine (check `MOSURA_TRACE` for `orconsume` at the site).

**BRICK-C1 PROBE (ml8c 2026-07-16, on task#8 C1-uncommitted over C0 `52cac08`, transient, revert join-verified): RED — the mainloop-cadence root (i) is now FALSIFIED as well.** C1 (the full two-phase fullloop{mainloop} restructure) already contains 2 of this patch's 3 semantic edits (ActionConsume return 1→0 + consume-in-loop after nzmask); the probe hand-applied the third (funcdata `consume: !0u64`). Result: `piecestruct_folds_shifts_to_concat` still FAILS, piecestruct 1.000→0.873 (unchanged magnitude vs pre-C1), switchloop 0.837→0.851 (+0.014 — the case-7 semantic fix fires, better than the memo's 0.840 since Brick 2 landed). The failure is precisely root (ii): the FIRST struct's `CONCAT22(param_2,param_1)` now folds; the SECOND struct's stack-word packing stays the unfolded shift-OR chain (`((p6<<8|p5&0xff)<<8|p4&0xff)<<8|p3`) — the `or [rbp-0x14],eax` STACK-SLOT RMW class that mosura's pre-pool recover_stack turns into a register-form graph. **Unpark gate = Brick D (#22-B: cancel recover_stack + per-iteration alias recompute); re-probe there. Post-C1 the patch will NOT git-apply (pipeline.rs regions rewritten) — but only the funcdata.rs hunk + the 24 test hunks (rules.rs/subvarflow.rs) are still needed; the pipeline.rs hunks are landed C1 content.**

**✅✅ ARC CLOSED — BRICK 1 LANDED `608d7d3` (ml8d 2026-07-17, lead GO, on Brick D `a76d4fb`+`47d082f`).** Re-probe on D1 was GREEN: Hand-applied only the funcdata default (`consume: !0u64`) on the D0+D1 tree: `piecestruct_folds_shifts_to_concat` PASSES, piecestruct HOLDS 1.000, switchloop 0.837→**0.851** (+0.014, the case-7 `x^0x87` semantic fix), avg 0.9345→0.9347, ONE mover total. Root (ii) CONFIRMED: with recover_stack cancelled the `or [rbp-0x14],eax` stack-slot RMW runs in-pool (RuleLoad/StoreVarnode + heritage re-entry) and the second struct's packing folds. LANDED per piecestruct-first protocol: hard test green FIRST, suite 420/0 (24 test hunks applied clean), corpus 0.9347/57 @608d7d3, ONE mover (switchloop). `brick1-consume-default.patch` DELETED (the commit is the record). master no longer emits the case-7 wrong-code class. REMAINING from this file: switchloop residual C (merge/naming: accumulator≠param_1, div-result merge cover conflict = #26/#27 class, uint4-vs-int4) — now re-groundable.

**Brick 2 ✅ LANDED `19f1ebb`** (2026-07-15, independent of parked Brick 1): RulePiece2Sext (ruleaction.cc:232-260) wired @coreaction.cc:5615 slot. Corpus 0.9274→0.9276, ONE mover: switchloop 0.830→0.837 (case 4 `CONCAT44(x>>0x1f,x)/10` → `iVar2/10` via piece2sext→subcommute-SDIV→subextcomm). Suite 382/0 + all targets. NOTE mosura fires it 2× vs Ghidra 0× on switchloop = overlapping-coverage, not over-fire (Ghidra's shiftpiece CDQ arm catches the shape earlier; both converge to the same SEXT/div). modulo-`%2`/floatconv NOT this class (Ghidra also 0× there — hypothesis corrected; separate root). REMAINING switchloop residual = C (merge/naming: accumulator≠param_1, div-result merge cover conflict = #26/#27 class, uint4-vs-int4) — re-ground after Brick 1 unparks.

---
name: task22-typespacebase-campaign
description: "Task #22 TypeSpacebase is a CAMPAIGN, not one arc: #22-A (stack NAMING, retires the print-time adaptation) + #22-B (recover_stack cancel = #7-S2/#3-S2b, subsumes #28). First brick = #22-A-1. sb8's -0.0037 is STALE."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9beadf25-4682-4e85-94fa-f326d85ed777
---

tspace1 grounding @`0143097` (baseline `avg 0.9228 / 56-of-60`), 2026-07-12. Board label "#22 = TypeSpacebase subsystem" bundles TWO independent faithful mechanisms keyed on `Varnode::isSpacebase()`; split them:

**#22-A = TypeSpacebase NAMING** (the varcross headline, "retires the print-time stack-naming adaptation"). Ghidra: `Funcdata::spacebase` types RSP-input `Pointer(8, TypeSpacebase(stack))` (funcdata.cc:230-264) → `AddTreeState::calcSubtype` TYPE_SPACEBASE arm (ruleaction.cc:6286, `hasMatchingSubType` trivially true) → `RulePtrArith` emits **`PTRSUB(RSP,off)`** in IR → `PrintC::opPtrsub` TYPE_SPACEBASE arm (printc.cc:1057) reads the symbol from **ScopeLocal** and prints `axStack_NN`(array)/`&xStack_NN`(scalar)/unnamed. Independent of recover_stack. **This is #22 proper.**
- mosura's adaptation to retire: PRINT-time INT_ADD-off-RSP naming — `stack_addr`/`stack_slot_name`/`anchor_stack_arrays` (printc.rs:525-666, INT_ADD-only) + the RSP-input `Pointer(8, Unknown(1))` stand-in (funcdata.rs:388). mosura's ScopeLocal is ALREADY faithful+running: `varmap::recover_scope` (varmap.rs:588). isSpacebase marking, split_uses, both ActionSpacebase passes = LANDED. Missing piece = `Datatype` has NO Spacebase variant + `ptrarith.rs` calc_subtype has NO TYPE_SPACEBASE arm → RSP arith goes through build_degenerate→PTRADD(Unknown(1)) instead of PTRSUB.

**#22-A-1 ✅ LANDED master `8d29ed8`** (tspace1, 2026-07-12; base 0143097 → corpus 0.9228/56 → **0.9206/55**, net −0.0022, suite 409/0, switch 6/6). Faithful ports: `Datatype::Spacebase`/TYPE_SPACEBASE (+8-file arms + lanedivide ARRAY/STRUCT_META renumber) → funcdata RSP-input `Pointer(8,Spacebase(stack))` → ptrarith `calc_subtype` TYPE_SPACEBASE arm→**PTRSUB** → **`nearestArrayedComponent`** array-hint over recover_scope (type.cc:2971/3020) → **`RulePushPtr`** (ruleaction.cc:6834, wired actprop2-order before RulePtrArith — the REAL missing rule re-rooting a shared frame base at the pointer) → `render_spacebase_ptrsub` = faithful opPtrsub-over-ScopeLocal, **RETIRES `anchor_stack_arrays`/`stack_addr`** (the print-time stack-naming adaptation) → 2nd ActionSpacebase moved INTO the reheritage restart group → (offset,width) decl-dedup. Confirmed NOT decomposable into a byte-identical sub-brick (sb8's PTRSUB/naming entanglement). sb8's "-0.0037" was @59cedd6-STALE; fresh measure landed BETTER. **Wins:** varcross +.031, wayoffarray +.043 (recovered, above baseline), switchind +.041, loopcomment +.037, stackstring +.024, partialsplit +.003; enum held 1.000, stackreturn restored. **Sole regression: offsetarray −.314** (1.000→0.686) = #22-A-2b diagnostic (below), landed per [[faithful-ports-land-not-held]] (guard_returns→P6 precedent), NOT bundled.

**#22-A-2b (task #29) ✅ LANDED — Brick1 `cde89ac` + Bricks2+3+4 `390effc`** (tspace2, 2026-07-12; corpus 0.9206/55 → **0.9269/56**, +0.0063, ALL-POSITIVE zero-regression, suite 409/0). offsetarray 0.686→**1.000** (+0.314 = #22-A-1's exact payback), wayoffarray 0.963→**1.000** (SUBSUMES the old #22-A-2 loop-ptr poison), partialsplit 0.870→0.899. tspace1's -0x98/-0x68 offset premise was PHANTOM (offsetarray uses -0x98 throughout — killed by oracle IR; the +0x98..-0x98 was the un-folded residual of the missing PTRADD, not an offset bug). The faithful two-pass PTRADD (pass-1 ptrarith→PTRSUB; pass-2 infertypes types it array-elem-pointer; oppool1 folds compensation; pass-2 ptrarith→PTRADD subscript) needed SIX faithful pieces:
- Brick1 (byte-identical): `ActionInferTypes::apply` return **1→0** (MIS-PORT, coreaction.cc:5415) + `localcount>=7` cap + set_type_recovery_exceeded.
- `ActionNonzeroMask::apply` return **1→0** (MIS-PORT, coreaction.hh:301) — the actual restart CONVERGENCE BLOCKER (reported a change every pass → infinite loop). [Two latent return-1 mis-ports found — valuable independent of corpus; unblock putting more actions in the mainloop.]
- `get_local_type` honors the varnode's OWN type-lock (`Varnode::getLocalType`: `if isTypeLock() return type`) — mosura only checked the empty-in-pipeline external locks map, so the typelocked spacebase RSP input was overridden `Int(8)` by PTRSUB descendants → pointer never propagated.
- spacebase `getSubType` in the type-prop `down_chain` path (`TypePointer::downChain`→`TypeSpacebase::getSubType`+getTypePointerStripArray) via existing `recover_scope` — types PTRSUB output `Pointer(element)`, bypassing the `Datatype::get_subtype` Unknown(1) stub where Funcdata reachable. ROOT-FIXES #22-A-2.
- Brick4: `ActionNonzeroMask`+`ActionInferTypes`+`default_rule_pool`+deadcode INTO the reheritage restart before ptrarith (actmainloop order coreaction.cc:5506-5509/5666). Converges (no cap-hit @30 iters, 2.3s).

**MAINLOOP-COMPLETENESS ADVANCE:** the reheritage restart now runs infertypes+nzmask+oppool1+deadcode+ptrarith+heritage — much closer to Ghidra actmainloop. Relevant to [[task8-mainloop-repeat]] and de-risks #22-B (stack re-heritage). NOT mloop2's hazard (infertypes/nzmask are monotone type/mask passes, not SSA re-versioning). ✅ **#22-A sub-campaign COMPLETE** (naming + array-subscript + loop-ptr poison all fixed; adaptation retired; net 0.9228→0.9269 over the whole arc). Remaining #22 = **#22-B only**.

**#22-B (= [[task-sb-spacebase-placeholder]] #7-S2/#3-S2b; subsumes [[task28-partialsplit-deadstore-escaped-stack]]):** Ghidra Mechanism B — `RuleLoadVarnode`/`RuleStoreVarnode` spacebase-register branch via `checkSpacebase`/`vnSpacebase`/`correctSpacebase` (ruleaction.cc:4173-4334) converts stack LOAD/STORE→direct addrtied stack COPY IN-POOL (post-heritage). mosura's adaptation = `stackvars::recover_stack` (stackvars.rs:160-183) does this PRE-heritage (a LOOSE symbolic tracker resolving COPY/MULTIEQUAL/IntAdd/IntSub-of-RSP — a strict SUPERSET of correctSpacebase, which resolves ONLY RSP_input / INT_ADD(RSP_input,const) and DECLINES COPY/MULTIEQUAL-of-RSP → the strict-vs-loose gap = varcross array-truncation + partialsplit dead-stores).
- **Brick 1 ✅ LANDED `41ab722`** (tspace3, byte-identical DORMANT, self-approved, 413/0 +4 strict-boundary tests): ported correctSpacebase spacebase-register branch into RuleLoad/StoreVarnode (shared check_spacebase + Space::contain). Dormant because recover_stack over-converts → nothing reaches the pool for it to fire on. Banks the mechanism, S2a-`59cedd6` pattern.
- **Brick 2 GROUNDED (tspace3, gated, NOT built):** the re-heritage half is PROVEN working (stack = delay-1 heritaged space, same path ram-globals already land — the mainloop advance closed the old #7-S2 blocker). Plan = (a) cancel recover_stack LOAD/STORE arms (KEEP call_push_restores = ret-addr-push neutralization, else post-call RSP drifts) + (b) recompute alias_boundary LATE inside the reheritage restart after ptrarith_pool (option i, matches Ghidra's per-iteration re-analysis) + (c) extend aliased_stack_offsets with PTRSUB/PTRADD arms. BOUNDED re-sequence, NOT a new subsystem. KEY reframe: mosura's up-front alias_boundary/probe-clone is itself an ADAPTATION standing in for Ghidra's `characterizeAsInputParam`/`hasEffect` guardCalls param-trials (P6-adjacent, stays deferred — NOT AliasChecker timing). **REAL RISK (the depth) = early-vs-late resolution timing:** cancel defers ALL stack resolution from pre-heritage to the LATE reheritage restart, later than Ghidra's actmainloop (RuleLoadVarnode from iter-1) → mid-pipeline passes (condconst/determinedbranch/early InferTypes/first oppool1) see raw LOAD/STORE → may need RuleLoadVarnode/RulePtrArith in an EARLIER pool too = [[task8-mainloop-repeat]]-coupled (mosura's mainloop is only the late tail, not full actmainloop). Correctness-sensitive ~20-fixture atomic mover (cancel+alias-recompute inseparable; cancel-alone over-guards corpus-wide). **VERDICT: Brick 2 is TASK8-GATED (deferred)** — tspace3 premise-check @41ab722, decisive evidence: Ghidra actmainloop resolves stack (actprop2/RuleLoadVarnode :5662) BEFORE ActionDeterminedBranch(:5672)/ActionConditionalConst(:5676) EVERY iteration; mosura runs determinedbranch(pipeline.rs:474)/condconst(:484) ONCE before the late reheritage restart resolves stack (:539) = inverted order. condconst/condmulti/elseif branch conditions derive from stack-spilled params re-LOADed from frame — condconst only propagates once those LOADs unify (needs stack resolved); Brick 1's STRICT correctSpacebase declines the `INT_ADD(COPY(RSP),-4)` spill until copy-prop folds it (inside the restart, AFTER condconst). So cancel-recover_stack regresses condconst/condmulti/elseif → the faithful fix IS Ghidra's ordering = the iterating mainloop placing resolution before those passes = **[[task8-mainloop-repeat]]**. Brick 1 banked dormant, activates once task8 provides ordering. **Multiple deep tasks now converge on task8** (Brick 2, #23 mainloop-reheritage, iterative condconst, revisit re-versioning, S2-stack) = the central gate.

**★ #22-B BRICK 2 = task#8 BRICK D — RE-GROUNDED POST-C1 (ml8c 2026-07-16 @`8a5fdc3`, read-only + one transient probe, revert join-verified; plan for gate, NOT built):**
- **THE TASK8 GATE IS CLEARED.** tspace3's blocker (determinedbranch/condconst ONCE before the late stack resolution = inverted order) no longer exists: task#8 C1 `8a5fdc3` made the whole pipeline the iterating `fullloop{mainloop}` — mosura's cycle is now Ghidra's exactly (oppool1 → actprop2/RuleLoad/StoreVarnode → determinedbranch → condconst → heritage, wrap), from iteration 1.
- **FRESH CANCEL PROBE @8a5fdc3-dirty** (transient: recover_stack LOAD/STORE arms cancelled, ret-addr store conversion + push neutralization KEPT — the probe found the retaddr stores by same-pc scan next to call_push_restores): avg 0.9294→0.9217 (−0.0077), 57/60, **exactly 20 movers, MIXED** — wins: noforloop_iterused +0.104, partialunion +0.059→**1.000**, partialsplit +0.042 (the #28 fixture!), stackstring +0.034, **varcross +0.021 (the gauge, toward Ghidra)**; losses: forloop_varused −0.212, forloop1 −0.182, stackreturn −0.058, deindirect −0.057, noforloop_globcall −0.050, ptrtoarray −0.029, forloop_thruspecial −0.025, noforloop_alias −0.024, nan −0.022, concatsplit −0.015, switchhide −0.015, deindirect2 −0.013, pointerrel −0.006; **condmulti −0.012 / elseif −0.005 only → tspace3's inverted-order regression prediction is FALSIFIED post-C1** (the gate really is open).
- **⚠ WRONG-CODE FINDING (the D0 co-requisite (d)):** probe forloop1 reads `iStack_1c` UNINITIALIZED — the param-spill STORE converts in-pool (RuleStoreVarnode) but is then DEAD-CODE-REMOVED: rule-created mid-loop stack varnodes are NOT addrtied (ActionMarkAddrTied runs once pre-loop; Ghidra sets addrtied at varnode CREATION — the known pipeline.rs backlog note). Cancel-alone emits wrong code → **(d) addrtied-at-creation (or per-iteration re-mark) is a hard co-requisite**, the forloop losses are largely this artifact.
- **(c) confirmed live:** `aliased_stack_offsets` (alias.rs:27) walks COPY/IntAdd/IntSub only, catch-all counts LOAD/STORE-deref as ESCAPE → with raw derefs surviving to the alias probe, every slot classifies aliased = corpus-wide over-guard. `gather_additive_base` (alias.rs:105) already has the PTRADD/PTRSUB arms — extend or unify.
- **(b) shape:** alias_boundary today = once, ActionHeritage first-call probe clone. Target = per-iteration recompute at mainloop body TOP (Ghidra ActionRestructureVarnode slot :5505 = between mosura's body-end heritage/deadcode and the wrap-around spacebase), gated `aliasyes = pass != 0` (coreaction.cc:2279-2281), on the REAL graph (the probe-clone retirement can come later — it stands in for guardCalls param-trials, P6-adjacent).
- **STAGING RECO (gate this plan before build):** **D0** (bank first, measure — likely small/byte-identical): (d) addrtied-at-creation for rule-created stack/ram varnodes + (c) alias arms. **D1** (THE atomic mover): (a) cancel general LOAD/STORE arms (keep ret-addr machinery) + (b) per-iteration alias recompute. Then the consume-patch re-probe (its unpark gate). The probe's 20-mover table is the blast floor; forloop/stackreturn losses should collapse once (c)/(d) land with it.

**★★ #22-B BRICK 2 = task#8 BRICK D ✅✅ LANDED (ml8d 2026-07-17, lead GATE-CONFIRMED GO): D0 `a76d4fb` + D1 `47d082f`** — post-land verified: corpus **0.9345/57 @47d082f** (then 0.9347 @608d7d3 with consume Brick 1), suite 420/0, jumptable 6/6, per-fixture == gated measurements, tree clean. **#22-B is COMPLETE — recover_stack's general conversion is retired, `41ab722` is ACTIVE.** Build detail (as gated):
- **D0 (d) addrtied-at-creation**: `alloc_varnode` (funcdata.rs) sets `MAPPED|ADDRTIED` for stack (SpaceKind::Spacebase) and `MAPPED|ADDRTIED|PERSIST` for ram (Processor && delay>0) at CREATION — Ghidra site CITED: `Funcdata::newVarnode`/`newVarnodeOut` (funcdata_varnode.cc:162-167/:115-120) → `Scope::queryProperties` (database.cc:1263-1282, in-scope-no-symbol → `mapped|addrtied` +persist-if-global). `mark_addrtied` (varnodeprops.rs) rewritten as the faithful SYNC/reconcile side (`syncVarnodesWithSymbols` funcdata_varnode.cc:939/:1046 + `markUnaliased` nolocalalias): skip free vns, set ram/aliased-stack, CLEAR addrtied+ADDRFORCE on unaliased stack ("if addrtied is cleared, so should addrforce" :1060-1062).
- **D0 (c) alias arms** (alias.rs `aliased_stack_offsets`): PTRSUB(base,#c)/PTRADD(base,idx,#elem) propagate (Ghidra gatherAdditiveBase varmap.cc:783-798; non-const idx ⇒ escape); LOAD/STORE-through-pointer = deref, NOT escape (STORE of the pointer AS VALUE still escapes) — compensates the probe running pre-conversion where Ghidra's checker runs post-conversion (aliasyes passes).
- **D0 MEASURE @8a5fdc3-dirty: avg 0.9294→0.9336 (+0.0042), 57/60, suite 420/0, clippy 80==80. TWO movers, both POSITIVE + Ghidra-CONVERGENT:** revisit 0.724→**0.894** (+0.170: `void func`+`return;` = oracle EXACT — the C1-era P6 void-return residual FIXED) + noforloop_iterused 0.767→**0.852** (+0.085: spurious `uVar2=uRam...;return uVar2` capture gone → void = oracle). Mechanism: mid-loop-created ram vns born persist/addrtied → the faithful subflow gates (trace_forward persist, useSameAddress addrtied) fire. (c) proven byte-INERT in D0 (decomposition join identical). Mover ⇒ gated lane, held.
- **D1 (a) cancel** (stackvars.rs): general STORE/LOAD arms GONE; `call_push_restores` returns (store,push,amt); ONLY the retaddr store converts + push neutralization kept; module doc rewritten. **(b) per-iteration**: `ActionRestructureVarnode{numpass}` (pipeline.rs, faithful coreaction.hh:848/cc:2274-2296) as mainloop FIRST member (slot :5505 before spacebase): `aliasyes=numpass!=0` gates the real-graph `alias_boundary` recompute, then mark_addrtied sync; pass 0 syncs against the probe boundary; pre-fullloop once-MarkAddrTied REMOVED (late pre-mergesnip one kept). RuleLoad/StoreVarnode docs → LIVE.
- **D1 MEASURE (vs D0): avg 0.9336→0.9345, 57/60, suite 420/0, jumptable 6/6, perf flat (~2.3s vs ~2.2s), convergence fixpoint-clean (mainloop ~2.5 extra rounds/fn, no hangs). SIX movers:** concatsplit +0.067→0.930, partialsplit +0.042→0.941 (#28 fixture), stackstring +0.034→0.933, **varcross +0.021→0.936 (GAUGE: `xunknown4*`/`[6]`/`*pVar1=0`/`+1` = oracle EXACT — reverses the C1-era 3-lines-away regression AND fixes baseline's wrong loop store `axStack_68[0]=0`; the filed RulePtraddUndo follow-on may be moot for this shape)**; pointerrel −0.006 = Ghidra-CONVERGENT (gains oracle's `fStack_18 = fRam...` init line; order-only LCS noise); **wayoffarray −0.106 (1.000→0.894) = the ONLY real regression, FAITHFUL-EXPOSES (bisect-proven: the per-iteration recompute, not the cancel):** the retaddr-push store `xStack_a0 = 0x100013` (Ghidra keeps it) dies — pass-0 probe boundary ≤ −0xa0 kept it aliased/addrforce'd; the faithful real-graph recompute reclassifies it unaliased → sync clears addrforce → chain dead-coded. Ghidra keeps it via `isUnmappedUnaliased`'s PARAM-REGION carve-out (varmap.cc:494-501: stack offsets in [minParamOffset,maxParamOffset] from `markNotMapped(parameter)`/FuncCallSpecs are never nolocalalias) — the callee-stack-param modeling mosura lacks (P6/#22-C family). **FOLLOW-UP FILED: port the param-region carve-out.**
- **⭐ THE PROBE'S 20-MOVER BLAST COLLAPSED**: forloop1/forloop_varused HOLD 1.000 (C inspected: NO uninitialized reads — the wrong-code artifact is gone, (d) was the right co-requisite); stackreturn/deindirect/nan/switchhide/ptrtoarray/noforloop_* all byte-flat vs baseline. partialunion stayed 0.941 (probe's +0.059 was a cancel-alone artifact). **Net vs 8a5fdc3: +0.0051, 8 movers, 6 Ghidra-convergent + 1 metric-noise + 1 named faithful-exposure.**
- **CONSUME RE-PROBE ON D1: GREEN → Brick 1 LANDED `608d7d3`** (see [[consume-model-misport]] — arc closed) — `piecestruct_folds_shifts_to_concat` PASSES, piecestruct HOLDS 1.000, switchloop 0.837→0.851 (+0.014 case-7 semantic fix), avg →0.9347, ONE mover only. The [[consume-model-misport]] unpark gate is OPEN on the D1 base (root (ii) recover_stack-placement CONFIRMED as the root — the stack-slot RMW now runs in-pool and the shift-OR chain folds).

**#22-C (defer):** `resolveSpacebaseRelative` (fspec.cc:4870) SP-across-call — only if a fixture needs it. **wayoffarray now names a concrete need** (the `isUnmappedUnaliased` param-region carve-out is the cheaper first slice — varmap.cc:494 needs minParamOffset/maxParamOffset from markNotMapped(parameter), i.e. callee stack-param recognition).

Gauge fixture = **varcross** (cleanest Mechanism-A diagnostic: mosura PTRADD(Unknown1)/2-scalars vs Ghidra PTRSUB/2-arrays). partialsplit=#22-B/low-value; stackreturn=#22-adjacent slot-granularity, muddier.

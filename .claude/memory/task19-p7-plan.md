---
name: task19-p7-plan
description: Task
metadata: 
  node_type: memory
  type: project
  originSessionId: 9beadf25-4682-4e85-94fa-f326d85ed777
---

## ★★ P7 CAMPAIGN MAP (p7g1 grounding @`f49aa34`, 2026-07-17 — USER-PICKED; gate one brick at a time). THE PRIZE = RETIRE `rule_goto` (structure.rs:351), the largest non-faithful adaptation left; gauge payback small (~+0.002-0.004, loopcomment+elseif only — HEAD goto-scan: only elseif/loopcomment/promotecompare emit gotos, promotecompare = red herring/broken decompile).
**Ghidra subsystem (blockaction.cc/block.cc/block.hh):** `CollapseStructure::collapseAll` (blockaction.cc:1877) = orderLoopBodies→collapseConditions(ruleBlockOr)→collapseInternal→{selectGoto→collapseInternal} loop. Spine = the EDGE-FLAG model (block.hh:108-117 edge labels f_goto/f_irreducible/f_back_edge/f_loop_exit; :92-103 block flags; :320-347 derived isDecisionOut/isLoopDAGOut/isGotoOut/isBackEdgeOut/isSwitchOut/isInteriorGotoTarget). Labels from 3 passes: (1) `structureLoops`=findSpanningTree+findIrreducible+calcLoop (block.cc:2194, ~250L, sets back/irreducible); (2) `LoopBody`+orderLoopBodies/labelLoops (blockaction.cc:40-497/1021-1044/1126, ~600L, sets loop_exit); (3) `TraceDAG` (blockaction.cc:499-1014, BranchPoint/BlockTrace/BadEdgeScore, ~500L, scores non-DAG gotos). Rules (blockaction.cc:1284-1762) CONSUME the flags; goto emitted LAST-resort (selectGoto:1260→setGotoBranch→ruleBlockGoto:1450). Placement: ActionBlockStructure IN mainloop (coreaction.cc:5659, getStructure().clear():4560 rebuilds/pass); refinements in final cleanup (PreferComplement:5714/StructureTransform/NormalizeBranches/FinalStructure:5736).
**mosura today (structure.rs 785L, printc.rs):** `structure(f)` (:526) = FLAG-FREE topological re-derive, called 3× (OrientBranches:601, PreferComplement:635, printc:1909) — NO persistent graph, NO edge flags, NO structureLoops/LoopBody/TraceDAG, NO back/irreducible classification (dominator.rs has postorder+doms+frontiers but not spanning-tree). Collapse-rule analogues PRESENT: rule_cat/short_circuit/proper_if/if_else/while_do/do_while/if_no_exit/switch. MISSING: ruleBlockInfLoop/ruleCaseFallthru/checkSwitchSkips. **rule_goto (:351) = THE ADAPTATION:** fires when nothing structural did (:567), cuts ANY merge-block in-edge (no loop/back/exit awareness) → on an un-collapsed loop head it CUTS THE BACK-EDGE → destroys the loop. Gap (A) goto-selection = the target; gap (B) persistent graph = deeper, NOT needed for A (LoopBody/TraceDAG run inside one structure() call).
**Divergence (instrumented):** loopcomment loses 3 loops→gotos (missing loop-exit/break labeling + back-edge preservation; also non-P7-capped by a `(uint1)param_3` dataflow bug + stack naming → can't reach 1.0 on structuring alone). elseif = else-if cascade byte-IDENTICAL, only the loop diverges: Ghidra 1 goto (the interior jump = genuinely IRREDUCIBLE, findIrreducible keeps it, back-edge stays→for structures) vs mosura 4 gotos (no irreducible detection→rule_goto cuts back-edges). Gap layer for BOTH = the LABELING passes (structureLoops+LoopBody), NOT the print walk (detect_for_loops printc.rs:1077 ready) nor the rule set.
**★ CRITICAL ROI (instrument-confirm at Brick 1, do NOT assume):** both movers may be reachable WITHOUT TraceDAG — elseif=irreducible (structureLoops), loopcomment=reducible loop-exits (LoopBody); TraceDAG traces loop INTERIORS for non-DAG gotos → should contribute ZERO on these clean reducible bodies. If confirmed, **TraceDAG (Brick 3, ~500L) = deferrable byte-identical completion**, mover = structureLoops+LoopBody+rewire (~1000-1200L = HALF the naive ~2000).
**STAGED BRICKS (land order):** **B0 ✅ LANDED `dfb160d`** (edge-flag model — edge_flags/block_flags modules + out_labels + 6 predicates, byte-identical self-approved). **B1 ✅ LANDED `b06077e`** (structureLoops = findSpanningTree block.cc:1009 + findIrreducible :1147 Tarjan + calcLoop :2104, sets BACK/LOOP/IRREDUCIBLE, byte-identical). **★ INSTRUMENT VERDICT (b06077e): TraceDAG DEFERRABLE** — elseif's 1 goto = edge b33→b36 marked IRREDUCIBLE (the only one; back-edge preserved); loopcomment = ZERO irreducible, all 3 loop back-edges BACK, interiors clean reducible DAGs → TraceDAG contributes ZERO surviving gotos on both movers (oracle-cross-checked). Mover = structureLoops(done)+LoopBody+rewire. **B2 ✅ LANDED `2dbb3f4`** (LoopBody + orderLoopBodies, byte-identical; probe-validated: loopcomment's 3 loops with correct tails/exits, elseif's for — exactly what emitLikelyEdges consumes). **B3 ✅ LANDED `8ce9e33`** (p7b3 2026-07-18, byte-identical self-approve — see the ★★★★ B3 LANDED section below). **P7 CAMPAIGN NOW COMPLETE: B0-B4b all landed; no deferred pieces remain.**
**★ B4/B4b SPLIT (p7g1 material finding @2dbb3f4, lead-confirmed):** condition-exit loops (loopcomment for/while, elseif for) recover with NO gotos (collapseConditions merges the compound condition → ruleBlockWhileDo fires). But GENUINE breaks (loopcomment's do-while b11→b13 `if(1999<...) break`) are goto-marked by selectGoto, structured, then flipped to `f_break_goto` by **`BlockGraph::scopeBreak` (block.cc:1270) in ActionFinalStructure** → PrintC emits break/continue. B4-as-mapped = loops recovered but genuine breaks render `goto` (CORRECT code, not oracle-matching) → **B4 = the collapse-driver mover; B4b = scopeBreak + PrintC break/continue rendering.**
**B4 TURNKEY PLAN (atomic, ~500-800L, all cites in hand — p7g1's 8 steps):** (1) parent-map on Structured (`getParent`/`getCurrentEdge` blockaction.cc:27), updated in install; (2) composite edge-label propagation in install (Ghidra newBlock* constructors carry sub-block exit labels — B0's composites currently label-0); (3) flag-aware rule guards on cat/or/proper_if/if_else/while_do/do_while/if_no_exit/switch (blockaction.cc:1284-1723) + ruleBlockInfLoop (:1579) + ruleCaseFallthru (:1729) + checkSwitchSkips (:1607); (4) selectGoto TraceDAG-deferred = stateful updateLoopBody→LoopBody::update (:94) + emitLikelyEdges (:364) → setGotoBranch, clipExtraRoots fallback, state on a collapse-scope struct; (5) ruleBlockGoto (:1450) consumes GOTO-marked edges → existing gotos map, **RETIRE rule_goto**; (6) driver → collapseAll shape (:1877); (7) re-verify OrientBranches/PreferComplement negations (coupling risk); (8=B4b) scopeBreak (block.cc:1270) + PrintC break/continue for f_break_goto/f_continue_goto. ATOMIC — flag-aware rules without composite-label propagation misbehave; flips together. NO shortcut ("rule_goto avoids back-edges" = the forbidden invented heuristic). ccompare-gotcha = loop RECOVERY moves the gauge (unlike else-if bracing).

**★★ B4 ✅ LANDED `9dd2443` (p7b4 2026-07-18, lead GO; verified post-land: suite 443/0, corpus 0.9456/57, elseif 1.000 / loopcomment 0.869 / condmulti 0.845).** Steps 1-7 ALL IN + faithful ruleBlockSwitch replacing the deferral-heuristic switch (checkSwitchSkips slots in; DEFAULTSWITCH leaf labels from `switch_defaults` = installSwitchDefaults funcdata_block.cc:687 at its blockaction.cc:2176 slot) + `BlockBasic::isComplex` (block.cc:2388, >2 statements; BlockCondition delegates to side-0 hh:635) as the ruleBlockOr orblock guard — found LOAD-BEARING mid-build (without it condition-folds swallow statement blocks). `rule_goto` RETIRED. Two extra faithful pieces the plan didn't name: (a) install remaps compset-internal out-targets → composite SELF-EDGE labeled LOOP|BACK (selfIdentify internal-drop + forceOutputNum block.cc:888) — REQUIRED: loopcomment's do-while (cat-chain swallowing the loop bottom) otherwise loses its back-edge = wrong code; (b) gotos map → `HashMap<BlockId, Vec<GotoRecord{target,negated,conditional}>>` (a block can carry if-goto + goto; negated = `(i==0)^is_oriented` — the old rule_goto never XOR'd orientation). **Stuck-fallback:** where Ghidra would run TraceDAG/throw (`selectGoto` exhausted + clipExtraRoots false), `trace_dag_placeholder_goto` (the retired picker, clearly labeled B3-pending) marks one merge in-edge so collapse never silently truncates to a partial root — instrumented ZERO firings on the corpus. **MEASURED @2dbb3f4-dirty: 0.9432→0.9456/57; movers exactly {elseif 0.923→1.000 byte-1.000 (irreducible goto Ghidra-exact incl. label-into-else placement), loopcomment 0.838→0.869 (ALL 3 loops recovered: while+&&, while, do-while+break-goto — breaks render `goto`=B4b-expected), condmulti 0.806→0.845 (baseline DROPPED the `param_1==10` guard = wrong code, now correct nested-if)}; other 59 fixtures dump_all BYTE-IDENTICAL; suite 443/0 (437+6 new unit tests), jumptable 6/6, perf flat (401→387ms), clippy 79==79. Convergence: loopcomment spins=2/cap=67, elseif 0 selectGoto rounds (ruleBlockGoto eats the IRREDUCIBLE edge directly). 2 wrong-code classes closed (condmulti dropped-condition; loopcomment loop-3 back-edge-loss under the new driver, fixed by (a)).** FOLLOW-ONS NAMED: (i) **ruleBlockOr swapped-sense variant** (j-search + negateCondition orblock, blockaction.cc:1345-1365) — mosura's same-index matcher folds 2 of Ghidra's 4 orientation cases; probe-proven blocker for loopcomment loop-1's 3-way fold + for-recovery (b8 outs=[exit,body] mirrored); needs a deferred per-side flip record on CondAnd/CondOr + render/orient consumption; (ii) B4b scopeBreak+break/continue (loopcomment's 2 break-gotos); (iii) PrintC comma-statement condition rendering (emitBlockCondition statements — condmulti's `(xRam=10, param_2<7)` oracle form; gated behind the dead-double-store upstream IR gap that keeps mosura's block isComplex); (iv) while-do overflow syntax (isComplex cond → `do{}while(true)`+break form, printc side; mosura currently hoists cond statements pre-loop — pre-existing); (v) B3 TraceDAG = replace the placeholder. Probe recipe: examples/dumpprobe.rs (git-ignored) dumps leaf outs+composites+gotos per fixture.

**★★★ p7fin2 (2026-07-18, base master `46fa4d6`, baseline RE-MEASURED = avg 0.9456/57, suite 499/0): B4b + ruleBlockOr BUILT+MEASURED, HELD FOR GATE (two gated movers); B3 PARKED with assessment.** Turnkey patch of both movers = `p7-b4b-ruleblockor.patch` in this memory dir (structure.rs +206/−37 net, printc.rs; applies clean on 46fa4d6). Combined: **0.9456→0.9462/57**, suite **500/0** (+1 swapped-sense unit test), jumptable 6/6, clippy 102==102. Movers = {loopcomment 0.869→**0.903**, elseif holds 1.000, sbyte unscored faithful `&&` fold}; all other 59 fixtures dump_all BYTE-IDENTICAL. NO wrong-code.
- **B4b ✅ scopeBreak + break/continue rendering (own delta 0.9456→0.9460, loopcomment 0.869→0.889).** Faithful port of `BlockGraph::scopeBreak` + the per-block overrides (block.cc:1270/2866/3034/3075/3324/3434/3462/3613) run in the `ActionFinalStructure` slot (blockaction.cc:2193), + PrintC `emitGotoStatement` break render (printc.cc:2303). **KEY FINDING: the C++ decompiler NEVER assigns `f_continue_goto`** — grep-proven, only `f_break_goto` is ever set (a back-edge to the loop head IS the loop's structure, so no explicit `continue` arises); the spec's "break/continue" is break-only in fact. Impl = `GotoRecord::is_break` field + `Structured::scope_break`/`scope_break_walk` (records `curloopexit` per leaf, flips a goto whose target == curloopexit) + label rebuild (a break references no label — Ghidra `markUnstructured` labels only surviving `f_goto_goto`). loopcomment: BOTH gotos → break (do-while `if(1999<..) break` = oracle-EXACT; first-loop goto→break = correct code, oracle folds it via the for/ruleBlockOr next). elseif's goto STAYS a goto (irreducible in-jump into loop interior — target is not a loop exit; premise-verified + held 1.000). 2 existing unit tests updated (break reclassification + irreducible-stays-goto).
- **ruleBlockOr ✅ swapped-sense 4-case fold (own delta 0.9460→0.9462, loopcomment 0.889→0.903 = the 3-way `&&`).** mosura's `rule_short_circuit` was a re-parameterized 2-case adaptation (only (i=0,j=1)/(i=1,j=0)); rewrote it FAITHFUL to Ghidra's full i/j loop (blockaction.cc:1321) — all 4 orientations + the composite out-order Ghidra's `newBlockCondition` forces (false=orblock's non-clause continuation, true=clause) + kind `(i==0)?OR:AND`. **mosura never swaps CFG edges** (funcdata.rs:664 comment — re-collapse would diverge), so the two `negateCondition` reversals (i==1 flips bl, j==0 flips orblock) are recorded as `FlowBlock::cond_flip:(bool,bool)` and consumed at print. **THREE coupled pieces, all needed:** (a) `render_cond_expr` XORs `cond_flip` into each operand's neg; (b) `compound_leaves` threads an `acc_flip` — a leaf at ODD accumulated flip was already fold-negated so the while's distributed NOT cancels (Ghidra flips it twice → net raw) → it is left UN-oriented, and its cleanliness no longer gates the all-or-nothing decision. Without (b) the third operand rendered `!(199 < iStack_c)` (double-neg) instead of oracle's clean `iStack_c < 200`; with (b) it's oracle-exact. Byte-identical for elseif/nan (their folds are the canonical (i=0,j=1), cond_flip=(false,false)). loopcomment loop-1 target = comp15(b6&&b7) ⊕ b8, the (i=0,j=0) case (b8's shared-exit edge on out-0). 1 new unit test (`short_circuit_swapped_sense_records_fold_flip`). NO corpus fixture uses CondAnd → the (i=1) out-order change (from the old adaptation's [clause,cont] to Ghidra's [cont,clause]) is corpus-inert.
- **EXPOSED FOLLOW-ON (loopcomment loop-1 stays `while`, oracle wants `for`):** the 3-way `&&` condition is now oracle-exact, but for-recovery does NOT fire — `for_parts` (printc.rs) uses `exit_basic(cond_idx)` = the LAST condition leaf (b8) as the phi head, but the loop-variable phi lives at the loop TOP (b6). Compound-condition for-recovery = a separate mechanism gap this fold exposed (NOT ruleBlockOr; loop-1 was `while` before AND after → no regression). Plus the pre-existing loop-2 `(10<iStack_28 & (uint1)param_3)` INT_AND-vs-BOOL_AND dataflow bug (FINDING 3) caps loopcomment below 1.0.
- **B3 TraceDAG PARKED (assessed, NOT attempted).** [SUPERSEDED — LANDED `8ce9e33`, see below.] blockaction.cc:499-1014 = ~380 non-blank lines of intricate pointer-graph C++ (BranchPoint/BlockTrace/BadEdgeScore with parent pointers, markPath, edge-distance scoring, pushBranches/selectBadEdge/checkOpen/retireBranch). ZERO gauge value — the `trace_dag_placeholder_goto` fires 0× on the corpus, so a correct TraceDAG is byte-identical.

**★★★★ B3 TraceDAG ✅ LANDED `8ce9e33` (p7b3 2026-07-18, byte-identical self-approve — the FINAL P7 piece).** Faithful port of Ghidra's `TraceDAG` (blockaction.cc:499-1014) over mosura's index-based collapse graph: `BranchPoint`/`BlockTrace`/`BadEdgeScore` → `BpNode`/`TrNode`/`BadEdgeScore` pools (pointers→indices; parent/pathout/derivedbp all indices), the `std::list<BlockTrace*>` active set → intrusive doubly-linked list threaded through `TrNode.anext/aprev` (Ghidra's per-trace `activeiter` = the trace index; std::list erase/insert semantics replicated so the `current_activeiter` cursor never dangles). ALL methods ported: createTraces/markPath/distance/getIndex(=block_index, composite = min component rpo per BlockGraph::addBlock block.cc:862)/removeTrace/processExitConflict/selectBadEdge(operator< group + compareFinal pick)/checkOpen(isLoopDAGIn count vs edgelump+visitcount)/openBranch/checkRetirement/retireBranch/initialize/pushBranches. `getPathStart` is dead in Ghidra (grep-proven) → skipped. Wired into `update_loop_body` (Ghidra's updateLoopBody): a live loop PREPENDS the interior gotos (tracer root=looptop, finishblock=loopbottom, bracketed by newly-ported `set_exit_marks`/`clear_exit_marks` = LoopBody::setExitMarks/clearExitMarks blockaction.cc:416/430) ahead of `emit_likely_edges`; the final DAG traces from every in-degree-0 root. **`trace_dag_placeholder_goto` RETIRED** + its `SelectGoto::Stuck`→placeholder consumer (Ghidra throws there; a faithful trace never reaches it on a well-formed graph → now a debug_assert+break). **PREMISE (instrumented on HEAD `7c6a359` via probes in dump_all): placeholder 0×, clipExtraRoots-goto 0×, DAG-case regen 0× — the ONLY tracer path exercised on the corpus is the loop-interior trace (3× firings), and those loops all fully collapse today, so a faithful trace scores no interior goto → byte-identical.** MEASURED: all 62 dump_all fixtures BYTE-IDENTICAL, corpus **0.9462/57 @8ce9e33** (unchanged), suite **503/0** (500+3 new tracer unit tests: clean-diamond-needs-none / forward-jump-needs-one[exercises selectBadEdge+removeTrace] / clean-loop-interior-needs-none), jumptable 6/6, clippy 102==102. structure.rs +696/−46. The DAG-tracer path is live-validated by the 3 structure()-level tests that previously hit the placeholder (complex_second_condition / irreducible_collapses / tangled_graph) — all pass through the real tracer now.

## --- prior grounding (p7struct1 @d3637fa, superseded by the map above but findings hold) ---
Task #19 P7 structuring (owner p7struct1). Premise-first READ-ONLY instrument @d3637fa (corpus avg 0.9222, 56/60). The lead's framing was "else-if flattening / for-while loop recovery / short-circuit &&"; the instrument CORRECTED it:

**FINDING 1 — else-if flattening is gauge-INERT (empirically confirmed).** ccompare (`ccompare.rs:53`) drops `(){};,` as noise, so mosura's `else { if (...) {...} }` and Ghidra's `else if (...)` normalize to the IDENTICAL token stream → 1.0000 both ways. Ghidra's mechanism is `PrintC::emitBlockIf` (printc.cc:2925-2942 + pending_brace 2882-2905: when an IfElse's else-block is a `t_if`, emit with `pending_brace` so the nested if's brace cancels → `else if`). A faithful printc.rs port is available but moves NO fixture. Low value.

**FINDING 2 — the ONLY P7 gauge lever is complex-loop recovery, and it is gated on the open-ended CollapseStructure subsystem.** Goto-count scan: ONLY loopcomment (2 gotos) and elseif (3 gotos) fall back to goto; condmulti/noforloop_iterused/revisit/floatconv/stackstring/varcross/concatsplit all have **0 gotos** (fully structured — their gaps are non-P7: IR/type/naming). So P7 moves at most 2 fixtures (loopcomment 0.751, elseif 0.915), est. ~+0.003–0.005 avg for a ~2000-line subsystem — narrow ROI.

Root cause (structure.rs `struct_probe` dump): mosura recovers loops only when the body already collapsed to a SINGLE block (rule_while_do requires `sizeIn==1,sizeOut==1,getOut(0)==head`, blockaction.cc:1518 — mosura's rule is FAITHFUL). When the body is multi-block (internal branching / rotated loop / break-to-else), it can't collapse (its blocks have a back-edge to the head), so `rule_while_do` never fires. Then mosura's `rule_goto` (structure.rs:368) — a loop-UNAWARE last resort — cuts the loop **back-edge** (loopcomment cut b5→b6) and DESTROYS the loop → goto spaghetti. Ghidra NEVER cuts a back-edge: `ruleBlockGoto` (blockaction.cc:1450) only cuts edges already `isGotoOut`-marked by `selectGoto`→`TraceDAG` (blockaction.cc:1260/1193), which traces looptop→loopbottom and marks only forward/exit/break edges. mosura has NO TraceDAG/LoopBody.

**The faithful fix = port Ghidra's CollapseStructure goto-selection: `LoopBody` (labelLoops/findBase/extend/findExit/labelExitEdges/orderLoopBodies, ~1000 lines) + `TraceDAG` (BranchPoint/BlockTrace/BadEdgeScore/selectBadEdge, ~450 lines) + driver (selectGoto/updateLoopBody/collapseAll, ~500). No smaller FAITHFUL brick exists** — "skip back-edges in rule_goto" is an invented heuristic (Ghidra picks the RIGHT forward edge via TraceDAG scoring, not "avoid back-edges"), forbidden by CLAUDE.md. mosura's rule_switch/cat/proper_if/if_else/while_do/do_while/if_no_exit ARE near-faithful analogues of the blockaction.cc rules; the divergent non-faithful piece is `rule_goto` (loop-unaware), fixable only by the TraceDAG+LoopBody feed.

**FINDING 3 — loopcomment's `while ((10<iStack_28 & (uint1)iVar1)!=0)` is a DATAFLOW bug, not structuring.** The while STRUCTURE is recovered; the second `&&` operand `aiStack_1c[0]<100` is LOST → uninitialized `iVar1`, fused as bitwise INT_AND (Ghidra has BOOL_AND). Tied to the missing stack-copy-in / stack-naming (task #22 TypeSpacebase territory), NOT P7.

✅ #19 else-if LANDED master `0f37185` (byte-identical corpus 0.9222/56, suite 406/0, switch 6/6): faithful port of PrintC::emitBlockIf pending_brace (printc.cc:2882-2943) as `emit_if` in printc.rs — merges `else { if … }` → `else if …` (Ghidra-C-exact), unless the else-arm's cond block emits a leading statement (brace fires → `else { … }`, e.g. elseif List else-arm). Gauge-inert (ccompare drops braces). Then declared spent; #19 done, loop-recovery lever carried by #25.

RESOLUTION: lead DEFERRED the subsystem as **TaskList #25** ("P7 loop-recovery subsystem: faithful CollapseStructure/TraceDAG/LoopBody — DEFERRED big low-ROI campaign") and kept #19 assigned. Staging plan for #25 when picked up: S0 edge-flag/mark/visitcount data model on FlowBlock (byte-identical) → S1 LoopBody labelLoops/findBase/extend/findExit/labelExitEdges (inert) → S2 TraceDAG BranchPoint/BlockTrace/BadEdgeScore (inert) → S3 rewire collapseAll driver (orderLoopBodies→collapseConditions→collapseInternal→while-stuck selectGoto) + make rule_goto consume isGotoOut-marked edges (THE MOVER, gated; recovers loopcomment/elseif loops). For #19's remaining bounded piece: else-if flattening (faithful emitBlockIf pending_brace port, gauge-inert self-approve). Sent lead a fork: (a) land else-if for #19; (b) subsume #19 into #25 + pick higher-ROI (#21 floatcast / #22 TypeSpacebase). My lean (b). Awaiting pick.

Tools: `cargo run --example dumpc -- <stem>` (mosura C); `oracle/capture ../ghidra <datatest.xml> --c` (Ghidra C); a throwaway `examples/struct_probe.rs` dumps the Structured tree + gotos + labels (removed to keep git clean — recreate from this memory if the campaign proceeds). Related: [[printc-structuring-adaptation-conflicts]], [[task10-isbooleanflip-condnegate]], [[faithful-type-of-wrong-ir]].

---
name: retirement-track-wave1
description: "Wave 1 of the invention-retirement track: A3 (RSP hardcode -> cspec) LANDED 9439fcf — the long-held regression was CURED by the heritage core, 3820 stack trials vs a structural zero, corpus unmoved. Plus the undefined-label wrong-code class."
metadata:
  type: project
---

**WAVE 1 ITEM A3 LANDED `9439fcf`; master fast-forwarded to `1ac5a6d` then advanced.** First item of the [[direction-retire-inventions-first]] track.

## ⭐⭐ THE HELD REGRESSION WAS CURED — the retirement protocol validated on its first hard case
The stackpointer patch was HELD FOR WEEKS for silently dropping real calls (7730→7707; a body became `while(true){}` — see [[war2-stackptr-wrong-code]]). **On the landed heritage core that regression is GONE:** absolute gauge identical to the c0ac350 base, deficit set unchanged BY MEMBERSHIP (5 fns/10 calls), per-function audit over all 1303 fns = **0 lose a call, 0 gain, totals 3974/3974.** ⇒ **the drop was EVIDENCE OF THE MISSING HERITAGE CORE, exactly as retirement-protocol step 2 says to assume — and it dissolved when the real mechanism landed, with NO gate invented.**

## ⭐ THE PRICE TAG, PAID IN FULL — and the thesis made empirical
With the placeholder no longer inert on x86-32: **4614 placeholders created / 4525 resolved (98.1%) / 3820 SPACEBASE INPUT TRIALS registered — against a STRUCTURAL ZERO.** Battery: suite 21/21 targets 0 failures · clippy 0 · **corpus 0.9534 / 58-60 UNCHANGED** (the right control: x86-64's own cspec declares RSP, so a faithful query MUST reproduce the hardcode exactly there) · all 12 byte-clean fns emit byte-identical C. **THE INVENTION-BOTTLENECK ARGUMENT IN ONE MEASUREMENT: one invention retired ⇒ a subsystem that was structurally inert ON THE TARGET BINARY went live at 3820 trials with zero call-graph regression, while the corpus we had been steering by did not move a single fixture.**

## 🔴 NEW WRONG-CODE CLASS NOBODY WAS SCANNING FOR: undefined `goto` labels
Renders containing `goto LAB_x` where **LAB_x is never defined** — cannot compile, unambiguous wrong code. Measured: **08ca850 and c0ac350 both carry 19 such labels across the SAME 11 files** (heritage core introduced none) → A3 takes it to **18 across 10**. **Every land battery run — including the lead's, and including the one the lead RATIFIED — reported "clean" without ever looking.** Now a STANDING GATE (baseline to beat: 19/11; specimen set: the 10 files).
**⭐ META-RULE ADDED: when adding a wrong-code scan, ENUMERATE THE SHAPES IT DOES NOT COVER and file them as gaps — never let a new scan imply completeness.** Second instance of a scan predicate's blind spot hiding a real defect (first: `switch(){}` missing from the empty-body scan).
**⚠️ FALSE-REGRESSION AVOIDED (agent checked instead of reporting): `empty for` 2→3 is NOT a regression** — the new one is a skip-whitespace loop `for (p=p; *p==0x20; p=p+1) {}` and **Ghidra emits the same empty body**; the work is in the increment. Wrong-code shapes are ONLY `switch(){}` and `while(true){}` (both 0); `empty for` is informational.

## FUN_00077dcb: expectation WRONG, not premature (task #5)
Re-checked first per sequence: **still dropped** ⇒ don't retry the stackptr angle. But the prototype converged to Ghidra's exact `(param_1, param_2)` (from 4 params + spurious frame stores) ⇒ **the stack-arg machinery works there; the body loss has a different cause.** It is one of the ten undefined-label cases (`goto LAB_00077dfd`/`LAB_00077e0a`). **RULING: treat the LABEL CLASS as the unit of work — take it as a block-removal-with-surviving-branch bug via the SHAPE instrument (not by chasing the call), fix the mechanism, then re-scan all ten.**

**ORDER:** finish compile+compare for the COMPILE_FAIL/byte-clean half of the impact record (also re-measures the ladder counts, stale since the prelude fix) → **the label class / 00077dcb** → **then G1** (wrong code outranks entanglement cleanup, the rule that has held all campaign).

## 🎯🎯 ROOT CAUSE (2026-07-30): THE DROPPED CALLS ARE AN **EMITTER** BUG — and the gauge has been mis-attributing them
**For FUN_00077dcb THE FINAL IR IS CORRECT**: the "missing" call is present and LIVE — `0x77e0a:218: CALL r0x63c35:4 ...` = exactly Ghidra's `FUN_00063c35()`; live ops at 19 distinct addresses. The emitted C has one label plus gotos to `LAB_00077dfd`/`LAB_00077e0a`, and **the blocks at both — including the one holding the live CALL — are never emitted at all.** ⇒ **the decompiler is RIGHT and the EMITTER/STRUCTURER loses the block. The undefined-label class and the "dropped call" class are ONE defect, not two** (predicted from the 4-of-10 overlap with the deficit set, now proven on a specimen).
**⚠️⚠️ THEREFORE THE ABSOLUTE CALL GAUGE — our BLOCKING gate — CANNOT DISTINGUISH "not recovered" from "recovered but not emitted", and has been attributing an EMITTER bug to the DECOMPILER. Four of the five remaining deficit functions are also in the label set ⇒ most of that 5 fns/10 calls is plausibly emitter-side.** ⭐ **REQUIRED TOOLING FIX (ordered, part of this work item, not later): give the gauge a COMPANION MEASURE — calls present in the final IR vs calls present in the RENDER, per function — so every deficit row classifies as *decompiler* (missing from IR) or *emitter* (in IR, absent from render). Small change to an existing instrument; retro-classifies the whole residual for free.** Our most-used instrument was failing the rule we apply to every other predicate: state what it literally tests.
**THREE INDEPENDENT INSTRUMENTS AGREE**, one not ours: the label scan (18/10) · wcc386's own `E1018 Label not defined` (8→7) · **a PRIOR SESSION reached the same verdict independently** (docs/war2-typeinfer-checkpoint.md:289 — "the goto-TARGET block is DROPPED from the structured output entirely… a structurer collapse bug… DEEP structurer"). **Today REFINES it: that note guessed a folded/absorbed block, but the block is ALIVE with real ops ⇒ nothing folded it; the structurer simply fails to place it while the in-edge survives as a goto.** Repro, no harness, ~20s: `cargo run -q --release --example dumpwar2 -- /home/jd/WAR2.EXE 0x77dcb [--raw]`.
**PRIME SUSPECT = WAVE 3's C1** (structure.rs:2889 re-derives the BlockGraph at 18 call sites where Ghidra mutates ONE persistent BlockGraph under ActionBlockStructure — a tree rebuilt 18× is exactly what loses a block while an edge to it survives). **LEAD RULING: confirm the mechanism first (diff CFG block set vs emitted set, expect {0x77dfd, 0x77e0a}); if C1 ⇒ C1 IS PROMOTED TO WAVE 1 — that FOLLOWS the user's blast-radius ordering rather than overriding it, since C1 would own a 10-fn compile-blocker class AND most of the residual deficit = the largest measured radius on the register. DO NOT force it standalone, and NEVER add a label-emission fallback** (it converts wrong code into differently-wrong code and destroys the evidence — how the multi-exit heuristic was born).

## A3 IMPACT RECORD (sentinel `ok=1201 fail=102 objects=1201 prelude=0ad5b82627de`)
**byte-clean 12 → 15** · MISMATCH 1213 → 1186 · COMPILE_FAIL 78 → 102 · DECOMPILE_FAIL 0 · **`E1052 void type` 48 → 0 (an ENTIRE error class eliminated)** · NEW `E1011 Symbol 'spacebase' not declared` ×20 (0 files → 21, rendering `spacebase * pVar6;`) = **wave 2's B2 exposed by A3 ⇒ B2 PROMOTED to wave 1's tail** (faithful ≠ compilable — not grounds to revert A3; same pattern as E1052/the prelude).
**⚠️ AGENT-OWNED MISLABEL:** the saved base `results.prev-c0ac350.tsv` actually PREDATES the heritage core ⇒ the ladder delta is heritage-core+A3 COMBINED, not A3 alone. Renamed `results.base-pre-heritage-core.tsv` and flagged (the very misattribution its own doc section warns against). **Recoverable without guessing: `src.landed-c0ac350` was pinned for exactly this — compile+compare it in the background and the A3-alone ladder falls out; do NOT hold the label class for it.**
**⚠️ LEAD-FLAGGED ARITHMETIC GAP (name it, don't absorb it): 78→102 while E1052 went 48→0 means the non-E1052 classes went ~30 → 82. E1011 accounts for 20; ~50 are UNEXPLAINED by anything reported. Some is the combined-baseline artifact, not obviously all. Class-by-class delta required once the c0ac350 base is measured — a +50 swing in unnamed classes is exactly the shape that becomes a phantom later.**

## ⛔⛔ MECHANISM PROVEN — AND THE DEFECT IS FAR LARGER THAN THE LABEL SCAN SEES (2026-07-30)
**`structure()` (structure.rs:2889) builds a FlowBlock for EVERY basic block but the tree it RETURNS reaches only a SUBSET; unreached blocks are never emitted.** Instrumented at the `structure(f)` call site (`MOSURA_BLOCKSET=1`), reached-vs-CFG:
```
FUN_00077dcb  cfg=8  reached=4  MISSING=4  [0x77dfd 0x77e06 0x77e08 0x77e0a]
FUN_0001081c  cfg=40 reached=36 MISSING=4  [0x108c3 0x108c5 0x108cf 0x109ae]
FUN_00043f04  cfg=9  reached=8  MISSING=1  [0x43f3a]
FUN_00051298  cfg=50 reached=39 MISSING=11 [0x513c5 … 0x51466]
```
Every undefined label is in its function's MISSING set — 4 for 4. **BUT 00051298 loses ELEVEN of fifty blocks and surfaces only TWO labels; 0001081c loses 4 and surfaces 3. The label scan only catches a dropped block that HAPPENS TO RETAIN A GOTO EDGE — a dropped block with NO surviving in-edge is INVISIBLE to every gate we have. Entire blocks of real code vanish with no diagnostic and the recompile "succeeds" while compiling the WRONG PROGRAM.**
**⭐ THIRD INSTANCE OF THE BLIND-SPOT PATTERN, and by far the worst** (`switch(){}` absent from the empty-body scan → the whole label class → now silent block loss; the first two at least produced a compiler error). **⭐⭐ GENERALIZED RULE: A WRONG-CODE GATE MUST TEST THE INVARIANT, NOT A SYMPTOM. `reached == cfg` is an invariant; "no undefined labels" was a symptom of it. Apply that lens to EVERY existing gate — ask what invariant it is a symptom of, and replace it where the invariant is checkable.**
**RULING 1 — BLOCKSET IS THE STANDING GATE** (`reached == cfg` per function; label scan demotes to a subset check): **HARD-FAIL, not warn** (lost code with no compiler error is worse than a panic — a panic is loud) · lives in the print/structure path as a debug-assert + opt-in `MOSURA_BLOCKSET` so corpus+suite enforce it on every fixture and the survey enumerates per function · **COMMIT AS PERMANENT** · it WILL fail on the known specimens on landing — record those MISSING sets as an accepted ENUMERATED baseline and drive to zero; **do NOT soften it or scope it to clean-functions-only to keep CI quiet.**
**RULING 2 — C1 PROMOTED into the active wave; this class lands UNDER it.** 11-of-50 is not local: a tree reaching only a subset of the blocks it builds, re-derived at 18 call sites where Ghidra mutates ONE persistent BlockGraph (ActionBlockStructure), is the mechanism in plain sight. **BLOCKSET diff = the oracle: reduce until `reached == cfg` on all four specimens with MISSING=0 on the negative controls. NEVER synthesize labels / add an emission fallback — it would paper over only the cases that still have an edge, leaving the invisible ones invisible, i.e. MAKE THE INSTRUMENT LIE.**
**LAYER ATTRIBUTION SETTLED:** for 00077dcb the IR is CORRECT (call live at 0x77e0a = Ghidra's FUN_00063c35()) inside a block the structurer never places ⇒ **most of the residual 5 fns/10 calls is EMITTER-side.** FOUR independent instruments agree, one not ours (BLOCKSET · label scan · wcc386's E1018 · the prior session's checkpoint, whose "constant-folded" guess is REFUTED — the blocks are ALIVE with a CALL and an INT_LESS).
**⚠️ NEGATIVE CONTROLS MATTER AS MUCH AS THE SPECIMENS: if ANY of the 12 byte-clean functions shows MISSING>0, blocks are being dropped from functions we call BYTE-CLEAN and that claim needs re-examination. Confirm explicitly, never by inference.**

## COMPILE_FAIL DELTA CLOSED + BOTH INSTRUMENTS COMMITTED `15a0768` (2026-07-30)
**⚠️ AGENT SELF-CORRECTION, found because the lead demanded the arithmetic close: E1011 went 1 → 60 (+59), NOT the "+20 spacebase" first reported — under-reported by 39.** Full decomposition, nothing unexplained: E1011 +59 · E1032 +7 · E1079 +4 · E1045/E1034 +2 each · E1081 +1 · three classes −1 · E1052 −48. **⭐ RULE: an unexplained residual in a delta is NOT noise to absorb — it is an UN-RUN MEASUREMENT, and running it changes the number.**
**All 59 are ONE root area in three sub-classes (⇒ one work item, task #6, not a scatter):** 20× `spacebase` — the internal TYPE_SPACEBASE name leaking into a declaration (`spacebase * pVar6;`), the types.rs:125 stub = **B2, promoted to wave 1's tail** · 26× **undeclared stack locals** (`xStack_<off>`×19, `iStack_<off>`×7 — used in expressions, never declared) · 14× **bogus width type names** (`uint6/int6/uint10/xunknown12/uint20/uint12/int10` — mosura synthesizes a type name per byte width and 6/10/12/20-byte integers are not C types; **likely the cheapest COMPILE_FAIL win on the board — needs a padded-struct/byte-array rendering, not a synthesized name**). **⇒ A3 traded a 48-function RECOVERY gap for a 59-function RENDERING gap: a strictly better class of problem, all locally fixable, none of them recovery failures.**
**GAUGE COMPANION IMPLEMENTED (layer attribution):** the survey manifest carries `ir_calls` (live CALL/CALLIND in the FINAL IR, counted the way the gauge counts) and the gauge classifies every deficit row — `ir == rendered` ⇒ **decompiler**, `ir > rendered` ⇒ **EMITTER**, `ir < rendered` ⇒ unknown/predicates-disagree — with a per-row `[layer]` + totals line, degrading to `ir=?`/`[unknown]` with a note on older manifests rather than claiming a layer. Column APPENDED (nothing index-based shifted; compare.py reads orig_hex at f[8]); `--selftest` passes.
**⭐ AND THE AGENT EXTENDED THE PRINCIPLE (asked for a sanity-check; LEAD RATIFIED — keep it, NOT env-gated): `structure::reached_basic_blocks` is now a shared helper and the survey records `blocks_cfg` + `blocks_reached` per function ⇒ `reached == cfg` becomes measurable across all 1303 WAR2 functions in ONE emit instead of by hand.** Precedent reasoning: the silent cases are invisible to every other gate, so the only way to know the population is to measure it EVERYWHERE; a column in an emit we already run is the cheapest possible way; and it converts a hand-run specimen list into a binary-wide census — the same move that turned the missing-calls class from anecdote into a 92-function measurement.
**NEXT REPORT'S HEADLINE (the single re-emit populating both columns for all 1303) — two figures required explicitly:** (1) **the population of SILENT block loss binary-wide** — functions with `reached < cfg`, and **how many of those carry NO undefined label** (i.e. were invisible to every prior gate; that second number is how much wrong code we have been shipping blind); (2) **negative controls: `reached == cfg` for ALL 12 byte-clean functions — confirm EXPLICITLY; if any byte-clean function is missing a block, the byte-clean claim needs re-examination and that OUTRANKS everything else in the queue.** Then the gauge's emitter-vs-decompiler layer totals, then back to the C1 question: which reduction drops an active block without re-parenting it (BLOCKSET diff = exact oracle).

## GATE LANDED `bf8f423` + A3 ISOLATED — THREE CORRECTIONS + A LADDER FLAW (2026-07-30)
**Gate:** debug_assert in the print path (corpus+suite enforce `reached==cfg` forever; release compiles it out so the survey censuses); precondition verified BEFORE commit — all 68 corpus scans MISSING=0, suite green; ten known failures recorded in-commit as the accepted baseline. **All ten specimens: 45 blocks lost, 18 labels — THE SYMPTOM SEES 40% OF THE DEFECT; 27 of 45 lost blocks raise NO diagnostic** (68902: 10 lost, ONE label; 77dcb's missing set includes the block with the live CALL).
**⚠️ THREE CORRECTIONS (from results.c0ac350.tsv — pre-heritage 12/1213/78 · c0ac350 12/1246/45 · +A3 15/1186/102):** (1) "A3 +24 CFAIL" WRONG — **A3 alone +57; the heritage core alone −33**; (2) **"A3 eliminated E1052" WRONG — that was the HERITAGE CORE's win**; (3) the +3 byte-clean (12→15) is entirely A3's; core flat. **CORRECTED FRAMING (ruled): the heritage core paid down RECOVERY debt; A3 exposed RENDERING debt that was always there and became visible when stack machinery finally ran on x86-32. Task #6 is A3's bill.** Dead claims, do not re-quote: "heritage core cost 24 CFAILs", "A3 eliminated E1052".
**⚠️ LADDER METHODOLOGY FLAW (invariant-not-symptom applied to the ladder itself): wcc386 reports only the FIRST error per function ⇒ every class count is conditional on no earlier class growing.** E1018 "−4" was really **−1** (3 "fixed" were MASKED by a new earlier E1011; source scan 19→18 is authoritative). **RULED: source scans are authoritative for every tracked class (labels: exists · spacebase: grep · bogus widths: grep · undeclared stack locals: declared-vs-used diff); the compiler ladder is a coarse can-it-compile total ONLY.** Worked example goes in the methodology doc beside manifest-idx and reference-sides.
**⭐ PINNED (status doc): all confirmed byte-clean negative controls are cfg=1 SINGLE-BLOCK functions — the explanation for why they're byte-clean. The 15 is real but its growth path runs DIRECTLY through the block-loss fix: no multi-block function can reliably join it until reached==cfg holds. The truest statement of where the campaign is.**
Census in flight (all 1303: blocks_*, ir_calls) → the two headline numbers (silent-loss population; how many with NO label = shipping-blind figure) + byte-clean controls completion + layer totals → then the C1 reduction hunt (which reduction drops an active block without re-parenting; BLOCKSET diff = oracle).

## 🎯 ROOT CAUSE OF THE BLOCK LOSS (b765b81, 2026-07-30) — an early-exiting collapse loop
**`MOSURA_COLLAPSE=1`: `collapse_all` finishes with TWO active top-level components** (77dcb: roots=[12,13]; 51298: roots=[77,85]) **and `structure()` then sets `s.root = current_form(0)` — the ENTRY block's form — orphaning the other component, which is never emitted.** For 77dcb the orphan holds 4 of 8 blocks INCLUDING THE LIVE CALL. **Why it is silent: the termination test is `while isolated_count < self.order.len()`, so once `order` has shrunk to 2, TWO isolated components satisfy it and the loop exits believing it succeeded** — no debug_assert bail-out fires. **Ghidra's `collapseAll` reduces to a SINGLE node, which is exactly why a goto there always finds its label.**
**⇒ LEAD RULING: C1 FIRST, not B2.** The cause is EXACT AND SMALL — a one-invariant fix with an exact oracle, not the open-ended BlockGraph rewrite C1 was feared to be — and while it sits unfixed, 6 of the 10 residual calls plus the entire undefined-label class stay open. B2's 59 are mechanical and will keep. **METHOD: fix the CONDITION, not the consequence — do NOT "re-parent the orphan into the root"; that is synthesizing structure to cover a collapse that didn't finish (same species as the label-emission fallback, already refused). If Ghidra's loop reduces to one node, ours must too; why ours stops early is what needs porting.** ORACLE: `active_top_level_roots == 1` AND `reached == cfg` on all ten, MISSING=0 preserved on controls, plus the existing battery.
**⚠️ AGENT SELF-CORRECTION AGAINST ITS OWN INTEREST (the standard): the census bounds the exposure — `reached < cfg` in 10 of 1303 fns (0.8%), 45 of 13328 blocks (0.34%), and THE TEN ARE EXACTLY THE TEN UNDEFINED-LABEL FUNCTIONS.** So the label symptom undercounted BLOCKS (18 of 45 = 40%) but identified **100% of affected FUNCTIONS**. There is NO hidden population; the blind part was 27 extra blocks inside functions we already knew. The earlier "shipping wrong code blind" framing was overstated — corrected unprompted. **The invariant-vs-symptom rule still stands on better grounds: it was the INVARIANT GATE that PROVED the bound — a symptom scan cannot tell you it is complete, an invariant can.**
## ⭐ LAYER ATTRIBUTION WORKS ON FIRST USE: deficit 5 fns/10 calls = **decompiler 4, EMITTER 6, unknown 0**
`00079130` (ghidra 12 / mosura 8 / ir 8) is **the ONLY function on the board where decompiler recovery work is warranted**; 00068902, 00051298, 0006529f, 00077dcb are all EMITTER (ir ≥ ghidra, render short). **Closing the block-loss class closes most of the residual call deficit** — invisible without the companion measure.
## BYTE-CLEAN CLAIM SURVIVES EXPLICIT CHECK: all 12 at `reached == cfg` — every one `cfg=1` single-block (which is WHY they are byte-clean). Growth path for byte-clean runs straight through the collapse fix.

## ⚠️ C1 PREMISE FALSIFIED (12f0280, 2026-07-30) — the defect is a MISSING EMISSION LOOP, not the re-derived graph
**Ghidra's `CollapseStructure::collapseAll` uses the SAME termination test mosura already ports (`while isolated_count < graph.getSize()`) ⇒ finishing with SEVERAL isolated top-level components is EXPECTED AND FAITHFUL.** The agent's own earlier "termination test permits multiple components where Ghidra reduces to one node" was WRONG, and so was the lead's "fix the condition, not the consequence" routing built on it (lead's 12th dead hypothesis — routes on a plausible mechanism, the instrument names the real one). **THE CONDITION IS RIGHT; THE EMISSION IS MISSING:** Ghidra `PrintC::emitBlockGraph` (printc.cc:2660, `emitBlockGraph(&fd->getStructure())`) loops `bl->getList()` and emits **EVERY** component; mosura does `s.root = current_form(0)` and emits ONE. Measured: 77dcb `order=[12,13] root=13`; 51298 `order=[77,85] root=85` — **`s.order` IS the `getList()` analogue (matches the active-parentless set exactly) ⇒ the blocks are collapsed CORRECTLY and sit in a component nobody emits. A missing loop, not a lost block.**
**⇒ LEAD RULING: STANDALONE, UNBUNDLED FROM C1** — a faithful port of two NAMED Ghidra functions, not a local patch under a structural cause (exactly the distinction the multi-exit-heuristic warning was about). C1 (re-derived BlockGraph) stays a real invention on the register, just not this defect's owner.
**⭐ THE LOAD-BEARING JUDGEMENT (agent's, correct): TWO PORTS MUST LAND TOGETHER.** (1) emit every component of `order` (`PrintC::emitBlockGraph`); (2) **maintain Ghidra's list invariant that the entry is `order[0]` — `BlockGraph::getStartBlock` (block.cc:1649) THROWS unless `list[0]` carries `f_entry_point`.** **Part 1 ALONE would emit the orphan BEFORE the entry** (the entry's form is at index 1 on both specimens) — a NEW correctness bug shipped while fixing one, and it would have LOOKED like progress (labels resolve, reached==cfg, blocks emitted) while reordering the program. **Ghidra throwing is Ghidra saying the invariant is not optional ⇒ port it as an INVARIANT, not a sort step: if our list can end up with the entry elsewhere, whatever puts it there is also unfaithful and should be FOUND, not compensated for at emission time** (check construction-vs-repair before settling).
**ORACLE UPDATED: `reached == cfg` on all ten · MISSING=0 on controls · entry emitted first. `active_top_level_roots == 1` is DROPPED (multiple components are faithful).** BLAST RADIUS = every function (1303 WAR2 + 60 corpus) ⇒ **FULL battery, no abbreviation:** corpus per-fixture vs pinned baseline · suite+clippy · **BLOCKSET census over all 1303 = the PRIMARY success measure (10 fns/45 blocks → zero)** · absolute gauge blocking WITH layer attribution (expect the 6 emitter calls to close, `00079130` to remain the lone decompiler row) · label scan → 0 · byte-clean re-measured · ladder as coarse total with source-scan predicates. **IF BYTE-CLEAN GROWS BEYOND SINGLE-BLOCK FUNCTIONS, SAY SO EXPLICITLY — that would be the campaign's FIRST MULTI-BLOCK BYTE-EXACT FUNCTION, a bigger milestone than the count.**

## 🎯 THE DEFECT IS **ONE CUT** in `select_goto` (9d6e4df, 2026-07-30) — a collapse-side scoring divergence
**Two agent conclusions superseded (recorded so nobody re-buys them): (1) "the termination test permits multiple components where Ghidra reduces to one node" — WRONG, same test; (2) "the fix is the missing `PrintC::emitBlockGraph` loop" — the loop IS genuinely unported, but it is NOT the fix: emitting the orphan would synthesize structure to cover a collapse that didn't finish (the label-fallback species).** The lead's method note — fix the condition, not the consequence — landed before the wrong repair shipped.
**TRACED (`MOSURA_STRUCT=1`, FUN_00077dcb, blocks ≥8 are composites):** cut 1 `blk10→blk2` (BlockIfGoto) · cut 2 `blk10→blk5` (BlockGoto; blk10 now 0 outs) · **cut 3 `blk12→blk10` = blk12's ONLY out-edge ⇒ blk12 left with no outs AND no ins = the isolated component holding the four orphaned blocks including the live CALL.**
**⭐ THE DEGREES SHOW THE FAITHFUL MOVE: the target blk10 was ins=2/outs=0 (terminal, two predecessors). `rule_cat` cannot absorb it (cat needs ONE in-edge), so something must be cut — but cutting one of blk10's IN-edges leaves ins=1 and lets cat absorb the other, whereas cutting blk12's sole OUT-edge can only isolate blk12. mosura picked the isolating edge.** ⇒ **`select_goto` is choosing an edge whose removal disconnects the graph.** Remaining work: diff mosura's `select_goto`/TraceDAG scoring against Ghidra's `TraceDAG` + `CollapseStructure::selectGoto` (blockaction.cc:499-1014 scores non-DAG gotos; :1260 marks the likely one) and port the difference.
**⛔ TEMPTATION REFUSED AND NOW A STANDING ENTRY: a guard in `select_goto` that skips edges whose removal would disconnect is an INVENTED HEURISTIC of the multi-exit species — it makes the oracle go green while the scoring stays wrong. ⭐ THE TELL FOR THE REGISTER: any fix whose shape is "skip the case that breaks" rather than "compute the same answer Ghidra computes" is that species, however local it looks.**
**LEAD READING GUIDANCE for the TraceDAG diff (guidance, not hypotheses — lead is 0-for-12):** (1) take the degree analysis in as THE question — among candidate non-DAG gotos, what does Ghidra's scoring prefer when the TARGET IS TERMINAL WITH MULTIPLE PREDECESSORS? If it ranks by something we flattened (edge depth, loop membership, the DAG/non-DAG classification itself) the difference shows up as a RANKING divergence, not a missing check. (2) **Establish CANDIDATE-SET EQUALITY BEFORE comparing scoring** — :499-1014 scores and :1260 marks, two stages; a divergence in which edges are even considered non-DAG yields a different pick with identical scoring and looks exactly like a scoring bug from outside. Cheaper, and it partitions the problem. (3) **Instrument the comparison; don't read it into agreement** — if a stamped trace can emit Ghidra's candidate list + scores for this specimen, that beats any amount of side-by-side reading (the campaign's record: the trace NAMES the mechanism, the source EXPLAINS what was named, in that order).
Three instruments committed env-gated off-by-default: `MOSURA_STRUCT` (GOTO marks with source/target in+out degrees and the live `order`, plus every cut — made the isolating cut visible), `MOSURA_COLLAPSE`, `MOSURA_BLOCKSET`. Repro ~20s: `MOSURA_STRUCT=1 cargo run -q --release --example dumpwar2 -- /home/jd/WAR2.EXE 0x77dcb`. Corpus unchanged 0.9534/58, clippy clean.

## 🛑 THE STOP THAT SAVED THE WORK (2026-07-30) — Ghidra's OUTPUT kills the approved plan and three diagnoses
**Agent checked the ground truth (already on disk) BEFORE writing a line of the approved change. Ghidra's `FUN_00077dcb`: a clean `while` loop with a compound `||` condition — GOTO/LAB count = 0 (mosura: 3); compound `||`/`&&` = 1 (mosura: 0).** ⇒ **Ghidra's collapse FULLY STRUCTURED this function: never multiple components, never `selectGoto`, never a goto.**
**THREE SUPERSEDED DIAGNOSES (recorded by name so none is re-bought): (1) the termination test — same as Ghidra's; (2) the missing `PrintC::emitBlockGraph` loop — genuinely unported but NOT this defect's owner (no multi-component case exists here); (3) `select_goto` scoring — DOWNSTREAM. Also withdrawn: the entry-`list[0]` reading — `identifyInternal` copies only interior-goto flags to a composite (not `f_entry_point`), `addBlock` push_backs so a composite absorbing the entry lands at the END, and `getStartBlock`'s assertion is about the CFG (`bblocks`, callers in flow.cc), NOT the collapsed structure graph.** Two lead ratifications died with them.
**⭐ REAL ROOT CAUSE, UPSTREAM: mosura FAILS TO FORM THE COMPOUND CONDITION.** Ghidra merges the two tests into `(A) || (B, C)` via `collapseConditions`/BlockCondition; mosura forms no compound condition anywhere in the function ⇒ the CFG keeps a shape its remaining rules can't reduce ⇒ `select_goto` runs as a FALLBACK (3×) ⇒ one cut isolates a component ⇒ orphaned blocks + the "missing" call. **CAUSAL CHAIN: condition-merge failure → unreducible shape → goto fallback → isolating cut → orphaned component → dropped blocks. BLOCK LOSS IS THE THIRD-ORDER SYMPTOM — which is why every fix aimed at it was aimed too low.**
**REFRAMED WORK ITEM (approved): the CONDITION-COLLAPSE port. ORACLE: zero gotos on this function matching Ghidra, and compound conditions formed where Ghidra forms them** (not anything about components). **CHECK THE REGISTER FIRST for a prior filing** — there are existing compound-condition items (the for-recovery/loopcomment gap and the INT_AND/BOOL_AND dataflow item from P7); if same mechanism it has just acquired a MEASURED price tag (10 fns, 45 blocks, 6 emitter-side calls, the whole label class) the way menu-A did; if distinct, say so. **⚠️ CAVEAT so artifacts aren't chased: that ground truth is the per-function isolated-import recipe — `in_EAX`/`extraout_ECX` are ITS artifacts. The STRUCTURAL evidence (zero gotos, one while, compound ||) is what's solid; 00077dcb is NOT among the four fns where the recipe under-emits.**

## 🎯 LOCALIZED UPSTREAM OF STRUCTURING ENTIRELY: **BASIC-BLOCK GRANULARITY** (591bb23, 2026-07-30) — 5 dead hypotheses
The lead's "establish SET-EQUALITY before comparing scoring" note generalized past gotos and paid: **Ghidra's goto candidate set for this function is EMPTY**, which redirected the hunt.
**TWO MORE HYPOTHESES DIED TO INSTRUMENTS, NOT ARGUMENT:** (a) suspected `is_complex` rejected the second condition (assignment `puVar2 = *puVar2` as a comma-expression) — **MEASURED FALSE**: instrumenting `rule_short_circuit`'s per-candidate decline reason shows **every decline is on SHAPE, never `is_complex`** (`orblock` candidates have **outs=1** — straight-line blocks — where the rule needs outs=2). *That instrument stopped a code change one step from being made.* (b) suspected rule ORDERING (Ghidra retrying the OR inside the main loop after `rule_cat` merges straight-line blocks) — **ALSO FALSE, and this is the useful part: Ghidra's `collapseInternal` has `ruleBlockOr` COMMENTED OUT** ⇒ Ghidra tries the OR **exactly once**, in `collapseConditions`, before the main loop — **identical to mosura. Neither side gets a second chance.**
**⇒ THE DIVERGENCE IS UPSTREAM OF STRUCTURING ENTIRELY: at `collapseConditions` time Ghidra's graph ALREADY HAS 2-out condition blocks where ours has a 1-out straight-line block feeding one. That is BASIC-BLOCK GRANULARITY — `cfg::build_cfg` vs Ghidra's `BlockBasic` construction — not structuring, not scoring, not emission.**
**FULL CAUSAL CHAIN: CFG splits too finely → the compound `||` is unrecognizable at the ONE moment it is tried → graph stays unreducible → `select_goto` fires 3× as fallback → one cut isolates a component → 4 blocks orphaned → the "missing" call + dangling labels.**
**FIVE DEAD HYPOTHESES, each with why it died, recorded in task #5 so none is re-bought:** collapse termination test · `emitBlockGraph` loop · entry-`list[0]` invariant · `select_goto` scoring · `is_complex`.
**NEXT (stated concretely, not started — agent stopped at a clean boundary near its context limit, 3rd time this campaign): diff the two BASIC-BLOCK PARTITIONS for `0x77dcb` — mosura reports n=8; GET GHIDRA'S FROM THE ORACLE rather than inferring it — with mosura's live-op addresses and block boundaries listed.**
**AGENT'S OWN LESSON, stated unprompted: "I should have read Ghidra's output for the specimen at the START of this hunt. It was in `ghidra-all.txt` the entire time, costs one awk, and would have skipped three wrong conclusions — including one the lead approved on my analysis. That's my error, not a gap in the rules."** ⇒ [[measurement-determinism-first]] RULE ZERO exists precisely so the next agent does not repeat it.

## ✅✅ BLOCK LOSS FIXED AND LANDED (5d517ee → 17666ad, 2026-07-30) — and the ORACLE finding is the bigger result
**(1) THE HYPOTHESIS DIED BY MEASUREMENT: `cfg::build_cfg` IS FAITHFUL.** New `oracle/ghidra_scripts/DumpBlocks.java` prints Ghidra's own `HighFunction::getBasicBlocks` (the exact list CollapseStructure runs on) + per-block p-code; new `MOSURA_CFG=1` prints the same fields so they diff line-for-line. Ghidra 6 blocks vs mosura 8 — **but NOT a split difference: Ghidra has NO OPS THERE AT ALL; it DELETED that code** (isolated import ⇒ FUN_00063c35's prototype defaults to no-params ⇒ dead-code kills EDX ⇒ kills `xor %edx,%edx` ⇒ empties one block and makes the other's CBRANCH redundant). **Wherever both sides have code, mosura's cuts are IDENTICAL to Ghidra's.** With forced storage (`63c35=EDX`) Ghidra reproduces mosura's partition **block for block, all 8, same spans, same edge sets — and emits THREE GOTOS of its own, in the same places.** ⇒ block-granularity hypothesis DEAD; `rule_short_circuit` not implicated at all (its decline is CORRECT on both sides).
**(2) THE ACTUAL DEFECT WAS THE `PrintC::emitBlockGraph` LOOP — the hypothesis previously marked DEAD** (printc.cc:2746 from :2660 iterates EVERY top-level component; mosura kept the collapse as one `root` and emitted only that). Multi-component is NORMAL (`collapseAll` stops on list.size(); `orderBlocks` guards its sort with `if (list.size()!=1)`). **Ghidra's output for this function IS mosura's two components in mosura's order, and Ghidra prints both.** Its death certificate was true of the 6-block graph it was measured on and false of the real one.
**(3) SECOND, PRE-EXISTING MIS-PORT exposed by emitting them:** unconditional cut edges were keyed on the source's exit BASIC BLOCK, but `newBlockGoto` (block.cc:1702) wraps the whole NODE ⇒ the goto followed the entire body: fine for a List, wrong for an If (goto buried in the then-arm, other path fell off the end of a non-void function).
**MEASURED, EVERY GATE:** `reached==cfg` **10 fns/45 blocks → 0/0** · undefined labels **18 in 10 fns → 0** · falls-off-end (NEW class) **1 → 0** · **call-gauge deficit 5 fns/10 calls → 1 fn/4 calls; layer split decompiler 4/EMITTER 6 → decompiler 4/EMITTER 0** (both lead pre-filed predictions CONFIRMED: the six emitter calls closed, `00079130` is the lone decompiler row) · strict-subset 0 lose / 4 gain +6 · COMPILE_FAIL 102 → 95 · corpus 0.9534/58 unchanged · suite 496/0 · clippy 0.
**⚠️ BYTE-CLEAN DID NOT GROW: 15 → 15, IDENTICAL member set (verified by joining the EXACT/RELOC_EXACT VA sets). No multi-block function became byte-exact — emitting the lost blocks was NECESSARY, NOT SUFFICIENT; the entire gain is 7 functions that now compile. ⇒ THE OPEN QUESTION: what actually blocks a multi-block function from byte-exactness?** Strong candidate = the prototype disagreement below (a call site binding wrong registers produces wrong argument bytes however well the structure recovers).
**NEW BLOCKING GATE CLASS: falls-off-end** (in war2-wrongcode-scan.py) — found by HAND-READING output, not by any gate, and it had been sitting in FUN_00070d83 the whole time. Agent applied Rule Zero to `FUN_00056c3c` before counting it and found **Ghidra emits the identical switch with the identical missing return ⇒ never a defect** — removed a false defect from its own finding rather than inflating it.
**NEXT (approved order):** **(1) CALL-SITE vs CALLEE PROTOTYPE DISAGREEMENT** — mosura's own FUN_00063c35 recovers one param in EAX while the call site binds EDX, EBX and more; both answers cannot be right, that EDX binding is what forces the 8-block shape, **blast radius = every WAR2 call**, and it plausibly gates the byte-clean milestone · (2) label naming (Ghidra's `BlockBasic` stores the ORIGINAL range — RangeList cover, block.hh:465 — while `Funcdata::block_range` recomputes from live ops, so labels move when a leading op dies; needs a full re-EMIT, batch it) · (3) label placement above a `do` · (4) deferred `ruleBlockIfNoExit` on a loop head — faithful today, DO NOT TOUCH before (1).

## ⚠️ TASK #7 PREMISE FALSIFIED BY MEASUREMENT — the agent overruled the lead, correctly (e07e009, docs-only)
**The "widest reach on the board" justification for taking prototype-disagreement first is FALSE.** Argument counts at every unknown-callee call site across all 1303 emitted .c (same binary, same cspec): **0 args 4786 (72.9%)** · 1 arg 749 (11.4%) · 2 args 463 (7.1%) · 3 args 266 (4.1%) · 4 args 114 (1.7%) · 5 args 187 (2.8%). **mosura's param trials are DISCRIMINATING, not blanket-binding — no systemic over-binding defect exists**; the 5-arg shape at 0x77e0a is in a 2.8% tail. On reach it ranks BELOW label naming (every label in every function) and below B2 (59 COMPILE_FAILs, three mechanical sub-classes). **⇒ DEFERRED behind #6.**
**The lead's byte-clean-gating argument also weakened: "wrong argument registers ⇒ wrong argument bytes" is sound in principle but 72.9% of unknown-callee sites have NO argument bytes to get wrong. RULING: do not treat #7 as gating byte-clean without measuring it — check whether the functions NEAREST byte-exact actually contain unknown-callee calls WITH arguments (start from the near-misses, since all 15 byte-clean are single-block). That measurement moves into #6's follow-on rather than remaining an assumption.**
**TWO MORE DEAD HYPOTHESES:** (a) the hardcoded-x86-64 class — `fspec::sysv_effect_list` IS hardcoded x86-64 (read literally on x86-32 it would mark ESP killedbycall and leave EBX unknown_effect) **but it is TEST-ONLY** (callers: the A1 premise-dump test + one unit test); production reads the cspec effect list ⇒ the pre-existing cosmetic `sysv_*` tidy item, NOT a defect. (b) "the call site ignores a callee prototype mosura recovered" — **there is no propagation to ignore**: `decompile_function` decompiles one function in isolation with no callee prototype input, and the call renders `func_0x...` (mosura's own unknown-target marker). Callee-says-(EAX) vs site-says-(EDX,EBX,…) is **one subsystem answering a question the other never asked**; the architectural gap (no Ghidra-style prototype commit into a database callers read) is REAL but FOUNDATION-SIZED.
**⭐ RULE ZERO'S SECOND CLAUSE APPLIED TO A TASK'S OWN PREMISE: the oracle CANNOT adjudicate #7.** Ghidra rendering `FUN_00063c35()` is NOT evidence it thinks the callee takes nothing — **forcing the storage changed the CALLER**, proving the caller read the callee's DATABASE prototype (the empty default of a sparse import). Both answers are context artifacts ⇒ settle on the BYTES (does the callee chain read EDX before writing it — flow-following disassembly, never a linear sweep).
**ENTRY POINT WHEN RESUMED (sharpest fact, needs NO oracle): mosura binds different input counts at STRUCTURALLY IDENTICAL sites.** FUN_00063c35's entire live IR is three ops — `CALL r0x63cbf` with ZERO inputs, `CALL r0x722c8 r0x0(i)` with ONE (EAX, a function-input varnode), and the RETURN. **The function's EAX input reaches the first call exactly as it reaches the second, yet one binds it and the other doesn't.** Two calls, one function, one cspec, opposite outcomes, two-line repro (0x63c38 vs 0x63c41). Instrument the trial path: Ghidra `FuncCallSpecs::checkInputTrialUse` (fspec.cc:5585) → register branch `AncestorRealistic::execute` then `ancestorOpUse`, driven by `ActionActiveParam::apply` (coreaction.cc:1725). No committed per-trial instrument exists — adding one is step one, and per precedent it will outlive the task.
**DOCS LANDED e07e009 (all four required):** the SEVERE oracle ledger entry (the recipe CAN CHANGE BLOCK STRUCTURE of any function that calls anything; an unforced run is NOT valid structural ground truth; EVERY prior structural conclusion from one is SUSPECT — with the bias direction recorded, since that is what let it survive) · DecompileWithForcedParams.java + DumpBlocks.java as permanent recipe parts with exact invocations + the callees-before-callers ordering requirement · AGENT.md carries Rule Zero with BOTH clauses, the death-certificate rule with both worked examples, and the per-specimen-instrument-must-filter-by-function trap · war2-recompile-remeasure.md carries the positional-args lesson verbatim + the falls-off-end class with its two exclusions and why FUN_00056c3c is not a member. Script re-verified (`bash -n` clean, DumpBlocks still returns the 6-block partition).

## ✅ B2 LANDED `66570a4` (2026-07-30) — stack-symbol naming/typing retired; COMPILE_FAIL 95 → 61
**GATES:** undeclared stack locals **32 → 0** · duplicate declarations **2 → 0** · `spacebase` **21 → 2** · `StackX_`/`StackY_` **0** (matching Ghidra) · reached==cfg **0/1303** · undefined-label **0** · falls-off-end **0** · call-gauge deficit 1 fn/4 calls with a **byte-identical deficit SET** (strict subset, 0 newly deficient, EMITTER still 0) · **corpus per-fixture IDENTICAL across all 60** · suite 566/0 · clippy 0. **Byte-clean UNCHANGED at 15 (none lost, none gained) ⇒ still NO multi-block byte-exact function.** The 2 new failures are `E1011: uint6 undeclared` — declaring the Symbol's recovered 6-byte type instead of a 2-byte reference's; `uint6` is a width GHIDRA ITSELF EMITS and the survey prelude has no typedef ⇒ emitter/prelude-side, filed.
**⭐ THE CAMPAIGN'S CLEANEST FALSIFIER-BEFORE-LANDING: steps 1-3 alone emitted `StackX_` 1516 times across 129 fns; Ghidra emits it ZERO times over WAR2 — not rarely but STRUCTURALLY IMPOSSIBLE** (`ProtoModel::defaultLocalRange`, fspec.cc:2263, covers only negative offsets; the marker sits behind an `inRange` test that window cannot pass). **A zero PROVED, not observed.** The port was half a subsystem; the missing half (RangeList, local/param ranges, `MapState`+`buildVariableName` guards, `ScopeInternal` fallback) is now in. **Those 1516 names were mosura's UNBOUND STACK PARAMETERS — the __watcall gap — now legible in the output instead of dressed as recovered locals.**
**⭐⭐ THE PUREST MASKED-ABSENCE INSTANCE YET — make this the canonical example: mosura declared the SAME stack slot TWICE at two widths, and it stayed LEGAL C ONLY BECAUSE THE TWO SYNTHESIZERS DISAGREED ABOUT THE STEM.** Unifying the name collapsed them and wcc386 rejected the redefinition. **The adaptation wasn't just hiding a gap — it DEPENDED ON A SECOND BUG to stay syntactically valid.** Fixed at the root (`restructure` builds a disjoint cover ⇒ one declaration per address carrying the Symbol's type), not by re-splitting names to make it compile.
**⭐ FOURTH INSTANCE OF "MACHINERY PRESENT, WIRING WRONG": planned step 4 (port `TypeSpacebase::getSubType`) was UNNECESSARY — the resolution already exists at `ptrarith.rs:232` over `recover_scope`; it was asking for `-24` while symbols were keyed by the unextended `0xffffffe8`. ONE SIGN-EXTENSION BUG PRODUCED BOTH SUB-CLASS (a) AND (b).** ⇒ **DETECTION HEURISTIC: before porting a resolver, GREP FOR AN EXISTING ONE AND CHECK WHAT KEY IT IS ASKING WITH.**
**Sub-class (c) framing stays FALSIFIED** (re-confirmed by decomposition at Ghidra's `max_basetype_size`=10): int6/uint6/int10/uint10 are FAITHFUL — Ghidra's own WAR2 output contains them — and their COMPILE_FAIL is a PRELUDE gap; only the >10 widths are ours, and their cause is heritage refinement, not naming.
**⭐ THE DEFERRED QUESTION, ANSWERED WITH A STATED DEFINITION (nearest-byte-exact = same-length MISMATCH, 23 fns): 6 of the top 12 by comparable-match, and 12 of all 23, contain an unknown-callee call PASSING ARGUMENTS ⇒ task #7 is on the byte-clean growth path for ~HALF the candidates — materially more than the 72.9% zero-arg site rate implied, but it does NOT gate the milestone as a whole. #7 stays deferred but is no longer speculative; the nearest single candidate (FUN_00063c06, 95%, first diff @+31) HAS one and is #7's natural specimen when it resumes.**
**NEXT PICK APPROVED: retire `printc.rs:58 type_prefix` → `Datatype::print_name_base`** — a ~3-line swap whose faithful replacement is already in the tree, against Ghidra's puVar ×3484 / pcVar ×1045 / piVar ×984 over WAR2 where mosura can only ever emit `pVar`. **Largest render-fidelity delta for the smallest change on the board.** Conditions: **Rule Zero on a specimen first**, and **RE-MEASURE THE NEAR-MISS SET AFTERWARDS** — naming changes character counts, and same-length MISMATCH is exactly where a naming fix can close a gap or move the goalposts; if the ranking shifts, say so. Then the `uint6` PRELUDE gap (fix the GENERATOR, never the generated file).

## ✅ `8aa73c3` type_prefix→printNameBase + `a777076` prelude/emitter LANDED — plus a CORRECTION to B2 (2026-07-30)
**⚠️ B2's "undeclared stack locals 32 → 0" WAS WRONG: the true figure is 32 → 4.** `war2-stacksym-scan.py`'s identifier pattern required an underscore, and **B2 itself introduced the no-underscore `ScopeInternal` form `xStack0000000c`** — so the predicate could not see the four functions it left behind (0005a824, 0005fbb0, 00063be5, 00072bf5). **B2 TRADED five undeclared `iStack_ffffffXX` for four undeclared `xStack<hex>`; it did not eliminate the class.** `a777076` closes them and extends the scan.
**⭐⭐ THE RULE IT BUYS: WHEN A CHANGE ADDS AN OUTPUT SHAPE, EXTEND THE SCAN IN THE SAME COMMIT** — a predicate cannot see a rendering that postdates it, and it reports the class CLOSED. **THREE separate defects in that one scan, ALL BIASED THE SAME WAY (toward reporting less wrong than there was):** `return xStack_38;` parsed as a declaration · the identifier pattern required `_` · the declaration pattern required an indent, so file-scope synthesis was invisible and four already-closed files kept reporting undeclared. **A FOURTH appeared only when the third was relaxed** (collapsing file and block scope ⇒ 91 files of phantom duplicates, since a block declaration legally shadows a file one) — the counterweight proving the loosening wasn't just "until it goes quiet". **⇒ STANDARD: every class a scan reports is CROSS-CHECKED AGAINST wcc386. A scan that agrees with an independent oracle is a MEASUREMENT; one that doesn't is a CLAIM.**
**Condition 1 (Rule Zero) satisfied** on x86-64 `partialsplit` via `oracle/capture --c` (same program, no pruning caveat): Ghidra `char *pcVar2`/`uint2 *puVar3`; mosura `pVar2`/`pVar4` → `piVar2`/`puVar4`. **Stated at the right granularity: this makes the stem's SHAPE faithful; the LETTER still differs wherever the pointee's type NAME does** (`puVar4` exact; `piVar2` shape-only) — so 24.6% must never be read as 24.6% *correct*. **Condition 2 satisfied with a REASON, not an assumption:** near-miss population 23 → 23, identical set, ranking, and per-function scores — names are erased by the compiler, and the emitter's TU synthesis (which DOES key on the prefix) was unaffected because no `Ram` identifier gained a stem longer than its recognizer accepts.
**NUMBERS:** stem recursion **0% → 24.6%** of 28582 local-name occurrences (Ghidra 27.0%); occurrences whose exact stem is in Ghidra's vocabulary **67.6% → 81.9%**; **COMPILE_FAIL 61 → 49** across both commits, EVERY transition COMPILE_FAIL→MISMATCH; wrong-code gates green, deficit set identical, corpus per-fixture identical across all 60 (expected — ccompare erases names, which is itself the confirmation the change was name-only). **BYTE-CLEAN UNCHANGED AT 15 — still no multi-block byte-exact function.** Suite 567/0, clippy 0.
**TWO JUDGEMENT CALLS WORTH KEEPING:** (a) **deliberately NOT typedef-ing the above-`max_basetype_size` widths (`uint12`/`uint20`/`xunknown12`) because "giving them a typedef would MAKE OUR DEFECT COMPILE"** — `getBase` array-izes above 10, so those are our own unported heritage refinement and they stay COMPILE_FAIL so they stay VISIBLE (the exact inverse of the label-fallback temptation); (b) an infidelity INTRODUCED AND CAUGHT inside the same commit — recursing through `Datatype::Spacebase` produced `psVar6`, but Ghidra's `TypeSpacebase` is built with NO NAME and `printNameBase`'s base case is guarded by `if (!name.empty())` ⇒ a pointer to it is a bare `pVar`; **proven by diffing the emits rather than assumed, saving a 25-minute ladder run.**
**NEXT PICK APPROVED — CORE-TYPE NAMING, the largest remaining stem gap:** mosura's `x` stem (Unknown → `xunknown<N>`, the fallback set at sleigh_arch.cc:229) covers **2211 `xVar` + 2952 `pxVar`** where Ghidra emits `u`/`undefined<N>` (pu=3484). **It is a RETIREMENT, not a rename: Ghidra's `buildCoreTypes` PREFERS the `<coretypes>` document and falls back to `xunknown1..8` only when there is none — we are carrying Ghidra's FALLBACK where Ghidra loads the real document.** Prelude-compatible (the prelude already typedefs undefined1/2/4/8 and undefined6). **CONDITIONS: its own gate (blast radius = every type name in every declaration and cast) · Rule Zero on a specimen first · RE-MEASURE THE NEAR-MISS SET — this one changes TYPE names, which appear in CASTS, and casts are NOT erased by the compiler the way identifiers are. DO NOT assume this one is name-only.** Small follow-ons: `PrintC::pushUnnamedLocation` (closes the 2 residual `spacebase` renderings) and the `iRam{:016x}` hardcoded width.
**HOUSEKEEPING: master is 18 commits behind; advance it AFTER core-type naming lands** (a coherent group, A3 through the naming retirements; clean fast-forward) — between items, never mid-measurement.

## ⛔ CORE-TYPE PICK OVERRULED BY THE AGENT, ON EVIDENCE, BEFORE ANY WORK (2026-07-30) — see [[measurement-determinism-first]] RULE ZERO-B
**The approved "core-type naming" retirement would have CREATED an invention while labelled a retirement.** `xunknown<N>` IS the faithful C++ port; `undefined<N>` is the Java layer's coretypes document, which mosura (C++ port, no program database) would have to INVENT. The 2211 `xVar` + 2952 `pxVar` figure is real as a difference from **analyzeHeadless** but is not a fidelity gap against the port target. **⇒ PICK WITHDRAWN.**
**⭐ THE REAL ITEM THE SAME CENSUS EXPOSED — and it is the residual the agent itself flagged when 8aa73c3 landed: the corpus oracle emits `char` ×24 and `int1` ZERO times; mosura emits `int1` and never `char`.** The C++ fallback set DOES contain it — `setCoreType("char",1,TYPE_INT,true)`, the flag making it a `TypeChar` — and `cacheCoreTypes` (type.cc:3225-3230) comments **"Char is preferred over other int types"** and installs it as `typecache[1][TYPE_INT]` ⇒ **`getBase(1,TYPE_INT)` returns `char`; `int1` is `type_nochar`, reachable only via `getBaseNoChar`. mosura has the default 1-byte signed integer WRONG.** Closing it also closes the known `piVar2` vs oracle `pcVar2` gap on `partialsplit` — a like-for-like Rule Zero fixture already on record.
**LEAD CAUTION (sharpening the agent's own cast point): `(char)x` and `(int1)x` compile identically ONLY BECAUSE OUR PRELUDE MAPS `int1` TO `signed char` — a coincidence of our prelude, not a property of the change. Check BOTH directions: no emitted cast changes WIDTH, and none changes SIGNEDNESS (char's signedness is implementation-defined in C and wcc386's default may not match `signed char`). If the prelude is doing load-bearing work to make them equivalent, SAY SO rather than letting it pass as "casts unaffected."**

## ⭐⭐⭐ FIRST MULTI-BLOCK BYTE-EXACT FUNCTION — `d3c3bfb` (2026-07-30); master fast-forwarded to it
**`FUN_00034668` is RELOC_EXACT with FIVE CFG basic blocks, all 5 reached, a compound `&&` condition and a nested `if`. Every previously byte-clean function was SINGLE-BLOCK. Byte-clean 15 → 16.** THE CAMPAIGN'S HEADLINE RESULT TO DATE, above any count — **the first evidence the structural work reaches the GOAL rather than merely tidying output.** Master fast-forwarded (verified true ancestor, 18 commits, no rewrite).
**⭐ THE MECHANISM — the lead's "this one is not name-only" caution was right for a reason NEITHER side predicted, and it cut in our favour: the survey's TU synthesis keys a declaration's C type off the identifier's FIRST LETTER**, so the function's two 1-byte globals named `iRam…` (stem `i`, from `int1`) were declared **4-byte `int`** and the recompile emitted a **32-bit load** where the original does a **byte load**. Renamed `cRam…` ⇒ `ctype_for('c')` declares 1-byte `char` ⇒ the byte load the original uses. **THE NAMING CHANGE CORRECTED A GENUINE *WIDTH* ERROR IN THE RECOMPILED OUTPUT.** 454 of 1303 files changed for exactly ONE byte-level move — a large render delta with a tiny byte delta, the correct signature for a naming change. Verified by direct before/after, not inferred.
**⭐ REGISTER ENTRY (stronger than reported): A VARIANT MATCH IS NOT A METATYPE TEST — and every such site is a LATENT TRIP-WIRE that only arms when a new variant appears.** mosura's predicates matched the enum *variant* where Ghidra's originals test `getMetatype()` — identical only while one variant per metatype existed. Adding `Char` broke it; first casualty `isSubpieceCast` (cast.cc:416/420) stopped recognising a 1-byte truncation and printed `SUB81(x,0)` where Ghidra prints `(char)x`. Fixed via new `Datatype::is_int_meta`, routing every predicate whose Ghidra original tests TYPE_INT: **isSubpieceCast (both arms + pointer case) · TypeOpPiece::getOutputToken · RangeHint::isConstAbsorbable/attemptJoin · the signed-compare relay · the int/uint meet. ⇒ ANY FUTURE `Datatype` VARIANT RE-OPENS ALL OF THEM — treat that list as a CHECKLIST, not a changelog line.**
**BATTERY:** exactly ONE status transition in 1303 (MISMATCH → RELOC_EXACT), none lost · COMPILE_FAIL unchanged 49 · source scans unchanged · reached==cfg 0/1303 · undefined-label 0 · falls-off-end 0 · deficit 1 fn/4 calls, EMITTER 0, set identical · corpus per-fixture identical across all 60 · suite 567/0 + a new unit test · clippy 0.
**NEXT APPROVED — `-j` AS ITS OWN ISOLATED CHANGE (agent-identified, deliberately not bundled): wcc386 runs WITHOUT `-j`, so plain `char` is UNSIGNED while Ghidra's `TypeChar` is TYPE_INT i.e. SIGNED ⇒ the harness is compiling our correct type with the WRONG SIGNEDNESS.** Cheap (recompile+compare, no re-emit) and it plausibly moves more functions the same way this one moved. **CONDITIONS: (1) state explicitly which side of the faithful/compilable line it sits on — we are correcting the HARNESS to match semantics the decompiler already emits faithfully, NOT adjusting the decompiler to suit the compiler (the reverse would be the prelude-typedef mistake we refused); (2) re-measure near-miss AND byte-clean and report movement in EITHER direction — a signedness flip can gain some functions and lose others, and "COMPILE_FAIL unchanged" will not catch a MISMATCH that got worse.** Then `pushUnnamedLocation` and the `iRam{:016x}` width.

## `-j` UPHELD + CAST AUDIT (2026-07-30, task #10) — and `getBaseNoChar` surfaces as a subsystem-lands-whole item
**WIDTH: CLEAN.** Full cast census over all 1303 emitted files — the only cast strings whose counts moved are **1 byte wide on both sides**: `(int1)` 226→0 · `(int1 *)` 459→0 · `(char)` 0→434 · `(char *)` 213→674 · `(uint1)` 229→361. **No cast changed width.** But **not a pure rename**: 342 more casts net (10002 → 10344), because `Char` and `Int(1)` have different sub_metatypes (19 vs 17) so `cast_standard` now finds a difference where the types were previously equal.
**SIGNEDNESS: `-j` KEPT on faithfulness** (see [[measurement-determinism-first]] — identical totals, 149/1303 functions with different bytes; gauge a wash ⇒ faithfulness decides). `wcc_flags=` now stamped; pre-2026-07-30 result files are pre-`-j` and must not be differenced across the boundary.
**⭐ `getBaseNoChar` — Rule Zero on FUN_00078c20 shows Ghidra emitting ONE cast where mosura emitted THREE** (`bVar1 | (char)uVar4 << (-(-cVar3 - 8U) & 0x1f)` vs our `(uint1)((char)param_3 << ((uint1)-((char)(uVar9 + -8)) & 0x1f))`). The extras are on the **SHIFT AMOUNT** — the `getBaseNoChar` opt-out (`TypeOpIntLeft/IntRight/IntSright::getInputLocal` slot 1, typeop.cc:1514/1539/1604) that keeps a 1-byte shift count as `int1` rather than `char`. **UNPORTED AND INERT BEFORE `d3c3bfb`; the char port made it ACTIVE — same shape as the heritage-completes-first cluster: a dormant gap that only arms when the thing it depends on lands.** Ported, builds, lib 500/0, re-emit running. **The residual `(uint1)` wrappers are NOT explained and correctly NOT folded in** (an unexplained residual is an un-run measurement — its own investigation, not a ride on this commit).
**LAND CONDITION: the cast census is now a proven instrument — RE-RUN IT AFTER `getBaseNoChar` and state the delta, since fewer casts IS the point of that port (its success measure, not a side check). Plus per-function byte movement, not just totals.**

## `4dc2897` LANDED (master advanced) — and the agent FALSIFIED ITS OWN PORT rather than keep the win
**`getBaseNoChar` for shift amounts was built on a hypothesis that DIED: re-emitting all 1303 with and without it changes ZERO files and the cast census is IDENTICAL — nothing consumes the seed, because `cast.rs` has no cast rule for a shift's slot 1.** Agent reported the falsification instead of quietly banking the commit.
**⭐⭐ THE PRECEDENT (register entry): FAITHFUL-BUT-INERT IS A LEGITIMATE LANDED STATE — IT JUST HAS TO BE LABELLED, WITH ITS MEASUREMENT ATTACHED.** The port was KEPT (it is what Ghidra does; the prior behaviour was wrong there) and **relabelled in-tree as faithful-and-measured-inert with a certificate saying WHAT IT WAS MEASURED AGAINST** — the death-certificate rule applied to a LIVE port rather than a dead hypothesis. If a future change adds a cast rule that consumes the seed, the next reader knows instantly that the inertness was CONDITIONAL on there being no consumer, not intrinsic.
**⭐ THE REPLACEMENT DIAGNOSIS IS SHARPER THAN THE ONE IT KILLED: Ghidra's `cVar3` is DECLARED `char`, so negating it needs no cast; mosura types the same value `uint1`, so `INT_2COMP`'s required `getBase(1,TYPE_INT)` FORCES one. ⇒ THE DIVERGENCE IS UPSTREAM TYPE INFERENCE landing `uint` where Ghidra lands `char` — a different subsystem, and it means the unexplained `(uint1)` wrappers are THE SAME THREAD, not a separate residual.**
**NEXT (approved — the thread with byte-level consequences; the two small follow-ons queue behind it): the uint-vs-char inference divergence.** Candidates to instrument, not assume: `op_meta`'s (metaout, metain) table · `type_order`'s uint-before-int preference. Specimen FUN_00078c20. **LEAD CAUTIONS: (a) Rule Zero AND its second clause both apply — you are comparing a TYPE NAME IN A DECLARATION between engines whose core-type vocabularies DIFFER BY ORACLE, so confirm which oracle produced the declaration and that it is a like-for-like program (78c20 has calls ⇒ forced callee storage if anything structural is in play); we were one unqualified "Ghidra emits X" from a wrong retirement hours ago and the same care applies to "Ghidra INFERS X". (b) If the divergence turns out to be a PREFERENCE ORDERING rather than a missing rule, FLAG IT EARLY — an ordering change has the widest possible blast radius on type names and needs the cast census PLUS per-function byte movement as gates, not totals.**

## ⛔ `-j` REVERTED `900adfd` (master advanced) — a LEAD RULING overturned by measurement, second time in two days
Agent recommended `-j`, lead upheld it on "gauge is a wash ⇒ faithfulness decides", and **both were wrong**: see [[measurement-determinism-first]] for the two rules it bought — **FAITHFULNESS GOVERNS WHAT WE EMIT; THE BINARY GOVERNS HOW WE COMPILE IT** (justifying a wcc386 flag from our type model is a CATEGORY ERROR — the flag set exists to reproduce BLIZZARD'S build and only the bytes are admissible evidence), and **"the metric is indecisive" must never be asserted from an AGGREGATE.** Per-function: length-equal-to-original **25 → 22**, 92 functions FURTHER vs 56 closer, five exact-length matches went short, near-miss 25 → 22 ⇒ **`-j` moved the corpus AWAY from the binary.** `d3c3bfb` (TypeChar itself) STANDS — that is a decompiler fact and remains correct. `wcc_flags=` stamp kept; `results.tsv` restored to the live config so no boundary is straddled.
**`getBaseNoChar` census confirmed +0 (10344 → 10344), zero of 1303 files differ — kept and labelled faithful-and-inert with what it was measured against, rather than letting the falsified explanation stand beside a green result.**

## TYPE-INFERENCE THREAD: GROUNDING ONLY, nothing implemented (task #11, @900adfd)
**⚠️ THE AGENT'S OWN WAR2 EVIDENCE IS INADMISSIBLE — caught by Rule Zero's second clause, self-flagged before building on it.** The `char cVar3` vs `uint1` comparison on FUN_00078c20 came from the per-function oracle **WITHOUT forced callee storage**, and that function HAS CALLS ⇒ absent callees default to no-params, dead-code can delete work, inference can land elsewhere. **Must be re-run with `DecompileWithForcedParams.java` before it carries any weight. LEAD RULING: run that re-run IN PARALLEL with the trace, not after — one command, and if sub-class (B) EVAPORATES under forced storage that is a result worth having EARLY (it would mean the WAR2 half was an oracle artifact). Report confirmed / evaporated / changed shape.**
**ORACLE QUESTION SETTLED CLEANLY:** `oracle/capture` uses the SAME core-type vocabulary as mosura (census-verified: `char`/`int1` = TYPE_INT, `uint1` = TYPE_UINT) ⇒ corpus comparisons are like-for-like, no translation; `analyzeHeadless` differs in NAMES but maps cleanly on METATYPES. So a signed-vs-unsigned claim is meaningful against either — once the second clause is satisfied.
**⭐ CORPUS BLINDNESS IS A FINDING, NOT A FOOTNOTE: only 2 of ~40 fixtures exercise 1-byte typing; 1 diverges.** See [[measurement-determinism-first]] — recorded with the general form (ask what fraction of fixtures could express the defect before reading "corpus unchanged").
**THE ONE CLEAN LIKE-FOR-LIKE SPECIMEN IS `orcompare`, AND IT IS NOT THE PREDICTED DIVERGENCE — it is WIDTH, not signedness:** oracle `char func(...)` / `char cVar1` / constants as char literals `'\x01'`,`'\x02'` vs mosura `int4 func(...)` / `int4 iVar1` / constants `1`,`2`. **Ghidra types the whole value chain as a 1-byte `char`; we WIDEN to `int4`.** ⇒ **TWO SUB-CLASSES until proven otherwise — (A) WIDTH, visible on the corpus now; (B) SIGNEDNESS/METATYPE, the WAR2 one, currently inadmissible. HOLD THAT LINE: unifying before measuring has cost a pass every time this campaign.**
**NEXT: `trace-diff.sh` on `orcompare` FIRST — firing evidence names the mechanism, then source explains what it named; the two candidates stay UNREAD until then. ⚠️ The `type_order` reading (SUB_UINT_PLAIN 16 < SUB_INT_PLAIN 17 < SUB_INT_CHAR 19 ⇒ a uint seed wins any meet) is a plausible mechanism for BOTH sub-classes, WHICH IS EXACTLY WHY IT MUST NOT BE ADOPTED BEFORE THE TRACE — a hypothesis that explains everything is the one most likely to be wrong here.** If it IS an ordering change: flag before writing, and gate on the cast census + per-function byte movement, never totals.

## ⚠️ TRACE NAMED A MECHANISM — AND THE AGENT REFUSED TO ACT, BECAUSE THE INSTRUMENT MAY BE LYING (task #11, @900adfd, nothing implemented)
**THE APPARENT HIT:** `trace-diff.sh orcompare` lists `prototypetypes` and `returnrecovery` under "rules Ghidra fires but mosura never does" — **suspiciously perfect**, since orcompare's divergence IS a return type and `ActionReturnRecovery::buildReturnOutput` is exactly what builds the return output from `ParamActive`, with `ActionPrototypeTypes` setting the evaluation model and output lock.
**⛔ WHY IT STOPPED: THE DIFF COMPARES ACTION NAMES AS STRINGS, AND MOSURA RENAMES.** mosura registers `activereturn` and `protorecovery_a` and references both Ghidra classes by name in comments across pipeline.rs/deadcode.rs/printc.rs. **DECISIVE: the same "never fires" list contains `collect_terms` (Ghidra) while the mosura list contains `collectterms` — a PROVEN PURE-NAMING PAIR SITTING IN THE SAME OUTPUT.** ⇒ direct evidence the column contains artifacts ⇒ the standing rule fires: **a recurring artifact in a diff instrument is a DEFECT IN THE INSTRUMENT until proven cosmetic.** Concluding "missing action" from this list is precisely the mistake that cost four instrument passes inside a rule Ghidra never fires.
**HONEST STATE: MECHANISM NAMED, NOT CONFIRMED.** Next step is NOT to port anything — settle, per name, whether mosura has the mechanism under another name and whether it RUNS on this fixture. **Fixing the trace's NAME MAPPING is probably cheapest and makes the instrument trustworthy for every future use — worth more than this one answer.**

## ⭐ THE TRACE-DIFF REPAIR PAID IMMEDIATELY: it named THREE uncatalogued inventions — and `RuleMultMult` is GONE (GO given 2026-07-31)
Fixing the instrument (name mapping: Ghidra↔mosura, unmapped names made loud instead of silently landing in "never fires" — artifacts 11 → 2) **immediately surfaced three mosura-only rules with no Ghidra class named in the port.** Second time this week that REPAIRING A MEASUREMENT TOOL FOUND DEFECTS rather than merely enabling a search.
**`RuleMultMult` DELETED, not patched — and the reasoning generalizes.** The invention built its constant with a bare `wrapping_mul` at u64 width, so `(x*0xff)*0xff` in a 1-byte INT_MULT became `x * 0xfe01` — a constant varnode that cannot fit its own size. **A masked version would have been A CORRECT INVENTION — still an invention.** Ghidra's real fold `RuleAddMultCollapse` (ruleaction.cc:4093, product via `OpBehaviorIntMult::evaluateBinary` = `(in1*in2) & calc_mask(sizeout)`, opbehavior.cc:495) **was ALREADY ported faithfully WITH the mask (rules.rs:5266+) and registered in the SAME pool** ⇒ nothing to add, only the unmasked duplicate to remove. **⭐ REGISTER ENTRY: when an invention needs a fix, FIRST CHECK WHETHER THE FAITHFUL MECHANISM IS ALREADY IN THE TREE — the fix may be a DELETION.** (Twice now; the emission loop was the inverse case.)
**⭐ PRE-FILED PREDICTION = THE MODEL FOR RETIREMENT COMPLETENESS.** Filed BEFORE measuring: *"136 → near 0; if not, a second unmasked site exists."* Measured (`MOSURA_CONSTCHECK=1`, full WAR2 emit): **136 of 1303 fns → 0; 2518 events → 0.** No second unmasked constant-fold site is reachable on WAR2. **State in advance what the number MUST be and what a miss WOULD MEAN — that is how a retirement is proven COMPLETE rather than merely effective.**
**THE GATED DELTA, and why GO: exactly ONE corpus fixture moves — `modulo` 0.965 → 0.997, UP — and it is the fixture RuleMultMult's own doc comment named as its justification. The invention was introduced FOR that fixture and was WORSE AT IT than Ghidra's real rule.** Everything else byte-identical: suite 568/0 · clippy 0 · reached==cfg 0/1303 · undefined-label/falls-off-end/empty-switch/empty-while 0 · call gauge 1 fn/4 calls, EMITTER 0, deficit set {00079130} before AND after · byte-clean 16 · COMPILE_FAIL 49 · DECOMPILE_FAIL 0 · cast census 10108 → 10060 (−48) · **0 functions changed status; per-function ir_calls/blocks_cfg/blocks_reached/status/coverage: 0 differ — the change is purely in rendered expressions.** Only 5 of 1303 fns changed C, every change the same defect vanishing (e.g. `puVar3 + uVar10 * 0xfffffffe00000001` → `puVar3 + uVar10`; and 78c20's `uRam..89144 = ... * 0xfe01 + 0x21` → `uVar8 + 0x18`, **now identical in form to Ghidra's `DAT_00089144 = DAT_00089144 + 0x18;`**).
**⭐ AGENT'S HONESTY ON THE PROXY, kept in the record: length moves TOWARD the original in both moved functions but match% moves +1 on one and −1 on the other, and the agent explicitly REFUSES to claim it as improvement — "match% is a noisy byte-alignment proxy at these levels". The defensible claims are the INVARIANT (136→0), the CORPUS FIXTURE, and THE RENDERED FORM NOW MATCHING GHIDRA'S.** Residual left alone deliberately: FUN_0007baf0's dead `uRam..89144 = 0x21;` (separate defect; its COMPILE_FAIL is `E1063` before AND after, unrelated).
**NEXT: `RuleIdempotent` (rules.rs:2316) and `RuleRangeAnd` (rules.rs:5185) — the last two rules with no Ghidra class named.** Same method: name-map audit names the candidate → an INVARIANT instrument measures reach per function → a PRE-FILED PREDICTION decides completeness → if the faithful mechanism is already ported, DELETE rather than patch. **One at a time, separate commits, so each delta stays attributable (the `-j` lesson).**
**STILL OPEN, needs its own decision when #11 resumes: the trace is STRUCTURALLY BLIND to type inference — it can never name that mechanism.**

## ✅ ALL THREE RULE-POOL INVENTIONS RETIRED — master advanced to `a144a43` (2026-07-31)
`5c9afe2` RuleMultMult (Ghidra's RuleAddMultCollapse already did it, WITH the mask) · `9c4bd10` RuleIdempotent (RuleTrivialArith folds all of it but INT_SUB) · `147adaf` RuleRangeAnd (RuleRangeMeld already merges it) · `8bb1106` trace-diff keys firings on the GHIDRA CLASS, not the trace name · `a144a43` the self-correction. **`trace-names.py` ADAPTATION list is now EMPTY — 148/148 mosura classes map to a Ghidra class.** All three are net line REMOVALS.
**BATTERIES VERIFIED BY THE LEAD (the agent kept idling at turn boundaries, so the lead measured rather than blocked): corpus 0.9539 / 58-60 — IDENTICAL to the MultMult landing, so both subsequent retirements were CORPUS-NEUTRAL and squarely in the self-approve lane; suite 500/0; clippy 0.** Master fast-forwarded (clean FF, 5 commits, verified ancestor, no rewrite).
**⭐ `a144a43` IS THE WEEK'S BEST COMMIT — see [[measurement-determinism-first]]: "removing it changed no output" ≠ "it never ran".** Corrected additively, with an observer probe, and with a BETTER replacement reason than the one it retracted.
**⚠️ SCOPE THE "ADAPTATION LIST NOW EMPTY" CLAIM: it is empty BY THE RULE-POOL AUDIT'S DEFINITION (148/148 rule classes map to a Ghidra class). The risk register still carries the alias clone-probe · the C-cluster (20 `structure()` call sites, no persistent HighVariable) · B-cluster remnants · the D-cluster · F1/F2 · phi slot ordering · the minors. "Empty" must never be read as the whole board.**
**NEXT: (1) qualify that claim in the register with an honest remaining count; (2) THE TYPE-INFERENCE BLINDNESS RULING — Ghidra's OPACTION_DEBUG is a p-code-OP MUTATION log, so a type decision that never mutates an op is INVISIBLE BY CONSTRUCTION; the trace now declares its own blind spot on every run (right first move), and the next step is to PROPOSE AND BUILD the instrument that CAN name a type-inference mechanism (per-varnode type-decision log? meet-operation trace?) BEFORE hypothesising — building the right instrument has paid every time this week; guessing has cost passes; (3) `pushUnnamedLocation` and the `iRam{:016x}` width.**

## ⭐ `e1d020b` THE TYPE-SIDE CHANNEL — and `type_order` is EXONERATED BY MEASUREMENT (2026-07-31)
Built the instrument BEFORE hypothesising, and it killed the widest-blast-radius hypothesis on the board. **FIRST RESULT on `orcompare`: Ghidra's chain is 1 BYTE throughout (char ×6, xunknown1 ×4); mosura's is 4 BYTES and assigns NO 1-byte integer type at all (its only 1-byte decisions are Bool). Both engines propagate CORRECTLY over the varnodes they have — they are running the SAME ALGORITHM ON DIFFERENTLY-SHAPED IR.** ⇒ **`type_order` EXONERATED on a STRUCTURAL argument, not a vote: `getBase(4, TYPE_INT)` is `int4` BY CONSTRUCTION, so no preference ordering could EVER yield `char` from a 4-byte varnode.** (This was the hypothesis the lead quarantined as "explains both sub-classes, therefore most likely wrong" — dead, by measurement.)
⇒ **THE DIVERGENCE IS UPSTREAM, IN SUB-VARIABLE NARROWING — and TWO INDEPENDENTLY BUILT INSTRUMENTS NOW NAME THE SAME SUBSYSTEM** (the typeprop channel, and the op trace's SHARED section: subvar_zext 6/10, subzext 8/5, subvar_and mosura-only ×2). **That convergence is the strongest signal the campaign has.**
See [[measurement-determinism-first]] for the four rules this bought (silence≠agreement · grep-before-rebuild · an instrument that says "I cannot see this" beats one that guesses · read the SHARED section).
**NEXT: SubVariableFlow**, with the week's method — instrument first (both channels now exist) · Rule Zero + second clause on the specimen · hold the two sub-classes (width / signedness) apart until measurement unifies them · pre-file the prediction that proves completeness. `pushUnnamedLocation` and the `iRam{:016x}` width stay queued.

## `ffac38c` — SubVariableFlow probe: the fork is SETTLED, and the abort op is NAMED (2026-07-31)
**Hypothesis (a) RETURN-WIDTH is DEAD with Ghidra's own trace as the death certificate:** `return(#0x0) RAX(...),RDX(...)` — `guardReturns` registers **RAX:8 and RDX:8, identical to mosura**. The 1-byte AL is produced **BY `tryReturnPull` (subflow.cc:238) at the END of a successful 1-byte trace.** Return recovery pins NOTHING — nobody goes back into guardReturns for this.
**(b) CONFIRMED — mosura attempts the EXACT SEED GHIDRA USES** (Ghidra DEBUG 183 `subvar_zext` on `ECX = ZEXT14(ZF)`, mask 0xff, flowsize 1), traces backward cleanly, **then ABORTS in `trace_forward` on the descendant `INT_MULT #0x2` that RuleCollectTerms built from `x + x`** (an earlier attempt aborts on the same op while still `INT_ADD`). **`subvarflow.rs::trace_forward` has NO INT_MULT and NO INT_ADD arm** (Ghidra subflow.cc:436, 456) — they hit the `default` abort. **Fixture census: 119 seeds — 12 OK, 77 NO-PULL, 30 ABORT.** Suite 568/0, clippy 0; probe inert when the env var is unset.
**NEXT (approved): port the missing arms faithfully from subflow.cc:373-866** — `traceForward`: INT_MULT/ADD/DIV/REM, INT_LESS/LESSEQUAL, BOOL_*, CBRANCH, BRANCHIND/trySwitchPull, FLOAT_INT2FLOAT/tryInt2FloatPull; `traceBackward`: INT_ADD/SRIGHT/MULT/DIV/REM, CALL/CALLIND `tryCallReturnPush`. **These are declared Stage-4 DEFERRALS in our own doc comments, not design ⇒ "subsystems land WHOLE" applies.**
**LEAD CONDITIONS: (1) land the full arm set as ONE coherent change — if any arm needs machinery we lack, REPORT rather than stub it; a partially-ported tracer that aborts in a DIFFERENT place is harder to diagnose than one that aborts predictably (the Stage B carve-out is the cautionary example). (2) EXPECT AN APPARENT REGRESSION and treat it as the masked-absence signal — these arms make flows SUCCEED that currently abort, changing IR shape widely; PRE-FILE what the census (119/12/77/30) should become and what a miss would mean — that is the completeness proof, not the corpus. (3) ⚠️ WATCH THE NO-PULL BUCKET: 77 of 119 is the LARGEST category and is NOT what you are fixing — if porting the arms converts ABORTs into NO-PULLs rather than OKs, the flow is reaching the end and DECLINING TO COMMIT, a different mechanism (`tryReturnPull`/`trySwitchPull`/`tryCallReturnPush` and their guards), and that RELOCATES the thread rather than closing it.** Gates: per-function byte movement not totals; byte-clean re-measured (16) with an explicit call-out if it grows — **this is the first change in the thread with a plausible path to a SECOND multi-block byte-exact function, since it directly narrows the chains that were widening to `int4`.**

## ✅✅ `36fbaae` — THE 1-BYTE WIDTH DEFECT IS SOLVED (master advanced; 2026-07-31)
**It was a HOLE IN SubvariableFlow's OPCODE SWITCH** — not the type model, not return recovery. mosura attempted **the exact seed Ghidra uses** and fell off `trace_forward`'s `default` arm at the `INT_MULT #0x2` RuleCollectTerms built from `x + x`; Ghidra carries that seed through the MULT → MULTIEQUAL → ZEXT48 → `tryReturnPull` → `return AL`. **Three hypotheses, three deaths by measurement (type_order · return width · then the switch hole), one real cause.**
**LANDED:** every remaining arm of both switches faithful to subflow.cc — traceForward INT_MULT/DIV/REM/ADD, INT_LESS/LESSEQUAL, BRANCHIND, BOOL_*, FLOAT_INT2FLOAT, CBRANCH; traceBackward INT_ADD/SRIGHT/MULT/DIV/REM, CALL/CALLIND; plus `addBooleanPatch`, `trySwitchPull`, `tryInt2FloatPull`, `tryCallReturnPush`, `Varnode::isZeroExtended`. **No deferred half remains in those switches.** Two deferrals remain and are **provably unreachable** (the sext tracers — only `RuleSubvarSext` reaches them and it is not registered; the `aggressive`/`isPtrFlow` argument — no `RulePtrFlow`) ⇒ **record in-tree as faithful-and-unreachable WITH THE REACHABILITY ARGUMENT ATTACHED**, like the `getBaseNoChar` certificate.
**PRE-FILED PREDICTION SCORED HONESTLY: (a)(b)(d) ✅ EXACTLY** — typeprop-diff now shows mosura deciding `#0x1:1 Char` / `#0x2:1 Char` with all five constant rows agreeing with Ghidra, where before **mosura assigned no 1-byte integer type at all**. **(c) partial, (e) failed — and they are ONE residual, PRINT-side, held apart as the prediction required:** mosura `cVar1 = (char)(iRam.. == 0) * 2;` vs Ghidra `cVar1 = (iRam.. == 0) * '\x02';`.
**BATTERY:** suite **569/0** · clippy 0 · corpus 0.9539 → **0.9537, 58/60**, only two fixtures move — **`modulo` 0.997 → 1.000 UP**, `orcompare` down **and correctly diagnosed as ccompare BLINDNESS, not regression** (the change fixed the return type, the variable type AND the value width; the comparator erases all three and scored the one added cast token) · WAR2 83/1303 functions change C · reached==cfg 0/1303 · all BLOCKING scans 0, none grew · call gauge 1 fn/4 calls, decompiler=4 EMITTER=0, **deficit set identical AND the surplus set identical** · **byte-clean 16 with an IDENTICAL SET** (none of the 83 changed functions was byte-clean) · **COMPILE_FAIL 49 → 46** (3 fns, all COMPILE_FAIL→MISMATCH, all `E1079`, every other class identical so first-error masking is not hiding it) · cast census −106 across 56 fns (new canonical definition).
**NEXT APPROVED: the PRINT-side residual — `PrintC::push_constant` should render a 1-byte TYPE_INT constant as `'\x01'`, not `1`.** Same class as the WAR2 `(uint1)`/`(char)` cast noise. **CONDITIONS: Rule Zero on the specimen first; treat it as POTENTIALLY BYTE-AFFECTING rather than cosmetic — character and integer literals are the same bytes to a compiler, but the surrounding CAST decisions may not be, and the `type_prefix` episode proved a "name-only" change can move real bytes through the harness.** Per-function byte movement; byte-clean called out explicitly either way.

## ⭐ `c8f1283` — ALL FIVE PRE-FILED PREDICTIONS LAND; `orcompare` is ONE TOKEN from Ghidra (master advanced, 2026-07-31)
```
mosura  cVar1 = (char)(iRam0000000000101000 == 0) * '\x02';
ghidra  cVar1 =       (iRam0000000000101000 == 0) * '\x02';
```
**LANDED:** `PrintC::pushCharConstant` (printc.cc:1606) + `printUnicode` (:1426), `unicodeNeedsEscape` (printlanguage.cc:411), `printCharHexEscape` (:1512); `pushConstant` tests `isCharPrint()` before falling through to `push_integer` — mosura's `render_var` already had the parallel FLOAT arm and simply had none for `char`. **A unit test pins the whole escape table — one wrong entry is invisible in any fixture lacking that byte.** Ghidra's Symbol/HighVariable `displayFormat` branches, the equate path and the wide-char `L` prefix are **unreachable rather than dropped, and the code says why.**
**⭐ PREDICTION (e) FLIPPED FROM FAIL TO PASS ON ITS OWN — the strongest vindication yet of HOLDING A RESIDUAL APART instead of chasing it.** `orcompare` 0.912 → **0.982** (0.929 before the arc); corpus **0.9549**, ABOVE the 0.9539 the branch started from. **The `36fbaae` dip WAS ccompare blindness and it resolved when the downstream piece landed — nobody touched the comparator or argued with the score.** Cite this the next time a fixture moves the wrong way under a faithful change.
**⭐ BEST-DESIGNED CHECK IN THE REPORT: `cast census 9375 → 9375 (+0) across 0 functions`** — a constant-rendering change SHOULD touch only constants, and the instrument chosen would have screamed if it hadn't. Paired with **393 of 1303 functions changing their C and NOT ONE changing its recompile classification** — a large render delta with zero classification movement is exactly this change's correct signature, and BOTH halves were measured.
**CONFIRMED ON REAL WAR2 CODE, not just the fixture:** `FUN_00034668` emits `cRam000000000008127a == '\0'` vs Ghidra's `DAT_0008127a == '\0'` — same rendering, differing only by the pre-existing global-symbol naming item; **it is RELOC_EXACT and stayed RELOC_EXACT.** Do this on every render change.
**BATTERY:** suite 570/0 · clippy 0 · byte-clean **16, set identical** · COMPILE_FAIL 46 · DECOMPILE_FAIL 0 · reached==cfg 0/1303 · all four BLOCKING classes 0, none grew · call gauge 1 fn/4 calls, decompiler=4 EMITTER=0, deficit `{00079130}`, 0 newly deficient · baseline sanity 0 status disagreements among the 910 unchanged functions.
**CUMULATIVE e1d020b → c8f1283** (`ffac38c` instrument · `36fbaae` flow port · `c8f1283` char literals): suite 568→570/0 · corpus 0.9539 → **0.9549** · **COMPILE_FAIL 49 → 46** · byte-clean 16 with the set identical at EVERY step · deficit AND surplus sets identical throughout · cast census 9481 → 9375. Baselines on disk: `war2-survey/results.prev-e1d020b.tsv`, `war2-survey/results.36fbaae.tsv`.
**NEXT: the `(char)` cast — the last token to Ghidra on `orcompare`, same class as WAR2's `(uint1)`/`(char)` noise. It is ActionSetCasts Brick 2, recorded as GATED behind retiring print-time type re-inference. LEAD RULING: GROUND IT READ-ONLY FIRST and TREAT THE GATE ITSELF AS A CLAIM TO VERIFY, NOT A CONSTRAINT TO OBEY** — that gate was set by a session whose reasoning cannot be re-examined, and **THREE type-layer changes have landed since (TypeChar, the SubvariableFlow arms, char literals), any of which could have moved the ground under it.** Establish (i) whether the blocker is still real at `c8f1283`, and (ii) if real, what specifically re-inference does that the cast decision depends on. **Report the cost BEFORE proposing a build; smaller than filed ⇒ say so plainly; a genuine foundation ⇒ say that and it becomes a scope decision rather than something slid into.** Sext tracers stay queued (unreachable dead code waits for a live thread to finish). Task #11 closed as superseded; #13 holds the record — **phrase the three Ghidra asymmetries as "looks like a bug, is faithful, here is why", the form that survives a hurried reader.**

## ✅ `f2d81d1` — SubVariableFlow LANDS WHOLE: the sign-extension half is ported (master advanced, 2026-07-31)
**The agent OVERRULED a lead-approved "next" for the right reason and said so up front:** the `(char)` cast is ActionSetCasts Brick 2 (gated behind retiring print-time type re-inference) while the sext half was the deferred piece of the subsystem it had just touched — **task #1's "subsystems land whole, no deferred half is grandfathered" made the ordering.**
**WHY IT IS NOT THE ZEXT MODE WITH A DIFFERENT SEED:** assuming a SIGN extension changes which ops preserve the value — `INT_SRIGHT` survives as itself with its shift amount (subflow.cc:991), signed AND unsigned comparisons work at both widths (:919), and an `INT_ZEXT` from a SMALLER size still acts as a sign extension so it becomes a PUSH rather than an ABORT (:979).
**⭐ `aggressive` READ FROM THE CSPEC, NOT HARDCODED — the class PREVENTED rather than retired. See [[hardcoded-x86-64-vs-cspec-class]].**
**REACH MEASURED, NOT ASSUMED:** `subvar_sext` fires 6× on corpus `modulo` and changes the C of 19 WAR2 functions ⇒ not inert ⇒ full battery ran. **BATTERY:** suite 571/0 · clippy 0 · **corpus 0.9549, 58/60, NO fixture moves either way** · **19/1303 WAR2 functions change C, NOT ONE changes recompile classification** · byte-clean 16, set identical, none of the 19 was byte-clean · COMPILE_FAIL 46 · DECOMPILE_FAIL 0 · reached==cfg 0/1303 · all four BLOCKING classes 0, none grew · call gauge 1 fn/4 calls, decompiler=4 EMITTER=0, deficit `{00079130}`, 0 newly deficient · cast census 9375 → **9333 (−42)** across 17 fns · baseline sanity 0 disagreements among 1284 unchanged.
**⭐ DIRECTION SPOT-CHECKED, NOT INFERRED FROM THE CENSUS SIGN** (a −42 delta is consistent with improvement OR differently-shaped wrongness): `FUN_0001163c` `if ((int4)(char)((uint1)(xStack_13 >> 0x18) & 1) != 0)` → `if (((uint1)(xStack_13 >> 0x18) & 1) != 0)` — **the pair that disappeared is exactly the `(int4)(char)` noise this arc has chased** — while noting Ghidra's shape is STILL different there, keeping the residual visible instead of letting the win absorb it.
**⚠️ TRAP THAT BIT TWO TEST SUITES: `set_replacement` refuses to assume a function INPUT is sign extended (subflow.cc:97) and refuses any input whose mask excludes bit 0 (:112) ⇒ TRACER TESTS MUST BUILD THEIR OPERANDS AS COPY OUTPUTS, or they decline for a reason unrelated to the arm under test** (the test passes while testing nothing). Recorded in both commits and task #13.
**CUMULATIVE e1d020b → f2d81d1 (four commits):** suite 568 → **571/0** · corpus 0.9539 → **0.9549** · **COMPILE_FAIL 49 → 46** · DECOMPILE_FAIL 0 · **byte-clean 16 with the set IDENTICAL at every step** · deficit AND surplus sets identical throughout · cast census 9481 → **9333 (−148)** · **baseline sanity run at EVERY step (1220 / 910 / 1284 unchanged, 0 disagreements each) — the habit that makes the rest of the numbers believable.**
**NEXT: `RulePtrFlow` + `Varnode::isPtrFlow` — the LAST deferred piece; on landing, SubvariableFlow becomes the FIRST subsystem in this campaign that is WHOLE with nothing deferred (say so in the commit; that state is rare and worth pointing at). THEN the gated `(char)` cast under the standing ruling: treat the gate as a CLAIM TO VERIFY, not a constraint to obey — four type-layer changes will have landed since it was filed.**

## ✅✅ `40602be` — SubVariableFlow is **COMPLETE, nothing deferred** (master advanced; 2026-07-31)
**⭐ THE AGENT OVERRULED ITS OWN STATED PLAN — for the second time in two increments, both on grounding done BEFORE writing code.** It had told the lead RulePtrFlow was "the last deferred piece" and would be ported next; reading it read-only killed that plan, and it reported the reversal rather than quietly changing course.
**WHY IT IS AN EQUIVALENCE, NOT A GAP:** `RuleSubvarZext` passes `Varnode::isPtrFlow()` as SubvariableFlow's `aggressive`. That flag is set **only** by `RulePtrFlow` (ruleaction.cc:9038), whose `getOpList` **returns EMPTY unless the default data space `isTruncated()`** (Ghidra's own comment at :9047: *"Only stick ourselves into pool if aggresiveness is turned on"*). A space is truncated only via `<truncate_space>` in a processor `.ldefs`, and in the pinned tree that element exists for exactly **AARCH64, PowerPC and MIPS — the same three families that carry `<aggressivetrim>`, for the same reason (32-bit ABIs on 64-bit registers). NO x86 `.ldefs` truncates a space.** ⇒ on x86:LE:64 and x86:LE:32 the rule never enters a pool, the flag is never set, and Ghidra's computed argument is `false` — **which is what mosura already passes.**
**⭐⭐ THE SHARPEST DISTINCTION OF THE CAMPAIGN: "no adaptation is grandfathered" DOES NOT MEAN WRITING DEAD CODE TO SATISFY A SHAPE.** Porting it today = a rule with an empty oplist plus a Varnode flag nothing sets — ceremony, not fidelity. **What makes the certificate legitimate is its REVIVAL CONDITION: the first target mosura builds whose `.ldefs` carries `<truncate_space>`, at which point the constant becomes WRONG and RulePtrFlow must land with it. A CERTIFIED CONSTANT WITH A FALSIFIABLE TRIGGER IS A DIFFERENT OBJECT FROM AN UNEXAMINED HARDCODE** — and the campaign now has a worked example on BOTH sides of that line, deliberately placed side by side in `rules.rs`: `RuleSubvarSext` READS `<aggressivetrim>` because the attribute exists today; `RuleSubvarZext` carries a certified constant because the mechanism behind its flag is structurally absent. ***"The difference is whether the source exists on our targets, not whether we felt like reading it"*** — the sentence that stops a future reader tidying one into the other.
**Comment-only: suite 571/0, clippy 0** (lead re-verified at the tip: suite 503/0 lib, corpus 0.9549/58).
**ARC SUMMARY `e1d020b` → `40602be`, FIVE commits:** suite 568 → **571/0** · clippy 0 · corpus 0.9539 → **0.9549** · **byte-clean 16 with the set IDENTICAL at every step** · **COMPILE_FAIL 49 → 46** · DECOMPILE_FAIL 0 · reached==cfg 0/1303 · all four BLOCKING classes 0 throughout · **call-gauge deficit `{00079130}` AND the surplus set identical at every step** · cast census 9481 → **9333 (−148)** · baseline sanity run EVERY time · **all five pre-filed predictions land; `orcompare` is one token from Ghidra.**
**OPEN (neither is SubvariableFlow's): the extra `(char)` cast — ActionSetCasts Brick 2, gated behind retiring print-time type re-inference — and global-symbol naming (`cRam...` vs `DAT_...`, pre-existing).** Lead ruling stands: **GROUND IT READ-ONLY; THE GATE IS A CLAIM TO VERIFY, NOT A CONSTRAINT TO OBEY** — five type-layer changes have landed since it was filed (TypeChar · tracer arms · char literals · sext half · this certificate). Establish whether the blocker is still real at `40602be` and, if so, what print-time re-inference does that the cast decision depends on. **Report the cost BEFORE proposing a build** — smaller than filed ⇒ say so; a genuine foundation ⇒ say that, and it becomes a scope decision made WITH the lead, not slid into.

## ⭐⭐ `e517104` — `orcompare` is TOKEN-IDENTICAL TO GHIDRA (0.929 → 1.000); master advanced (2026-07-31)
Same types, widths, literals and casts — **no residual. The fixture the whole arc was driven on is FINISHED, not merely improved.**
**THE FINDING OUTRANKS THE FIX: the "gate" was not real.** The `(char)` cast was reported to the lead as ActionSetCasts Brick 2 gated behind retiring print-time type re-inference — **and the lead ratified that framing.** Actual cause: `cast_standard` matched the `Int` VARIANT where Ghidra's `castStandard` switches on `getMetatype()` (cast.cc:300, TYPE_INT arm returns no-cast for TYPE_INT/TYPE_BOOL at :372), so `Datatype::Char` — which IS `TypeBase(1,TYPE_INT,…)` (type.hh:356) — hit the catch-all and cast unconditionally. **`Datatype::is_int_meta` ALREADY EXISTED for exactly this class** (added with `char` in `d3c3bfb`, its doc comment naming `isSubpieceCast` as "the first casualty") **and was simply never applied in `cast.rs`. An unfinished sweep from a shipped commit, not a foundation blocker — found ONLY because the agent grounded the specific token read-only instead of accepting the inherited framing.** See [[measurement-determinism-first]] for the two rules this bought.
**BATTERY:** suite 571/0 (lead re-verified 504/0 lib at the tip) · clippy 0 · corpus 0.9549 → **0.9552**, exactly one fixture moves (`orcompare` 0.982 → **1.000**) · **65/1303 WAR2 functions change C, NOT ONE changes recompile classification** · byte-clean 16, set identical · COMPILE_FAIL 46 · reached==cfg 0/1303 · all four BLOCKING classes 0 · call gauge 1 fn/4 calls, decompiler=4 EMITTER=0, deficit `{00079130}`, 0 newly deficient · **cast census 9333 → 9031 (−302) across 64 fns — the largest single drop of the arc** · baseline sanity 0 disagreements among 1238 unchanged.
**SPOT-CHECKED ON REAL WAR2 CODE:** `FUN_0001201c` `cRam...8127a = (char)0;` → `= 0;` vs Ghidra's `DAT_0008127a = 0;`. **Third WAR2 function this arc has driven to Ghidra's EXACT expression — and all three now differ from Ghidra ONLY in global-symbol naming.**
**ARC TOTAL `e1d020b` → `e517104`, SIX commits:** suite 568 → 571/0 · corpus 0.9539 → **0.9552** · byte-clean **16, set identical at every step** · **COMPILE_FAIL 49 → 46** · reached==cfg 0/1303 throughout · deficit AND surplus sets identical at every step · **cast census 9481 → 9031 (−450)** · baseline sanity at all four measured steps · all five pre-filed predictions land · SubvariableFlow complete with nothing deferred.
**NEXT (approved, agent-proposed): SWEEP THE TREE FOR THE `is_int_meta` CLASS** — other `matches!(.., Int(_))` predicates whose Ghidra original tests `getMetatype()` may still be dropping `Char`. **Must END WITH A WRITTEN LIST OF EVERY SITE CHECKED, not only those changed.** THEN global-symbol naming (`cRam...` vs `DAT_...`) — now the most visible thing left on three WAR2 functions — queued behind the sweep since the sweep may touch the same rendering paths.

## ✅ TASK #13 CLOSED — the `is_int_meta` sweep is a SMALL result, and that is the good outcome (2026-07-31)
**The agent corrected its own framing unprompted: it had implied a campaign; the sweep found `cast_standard` was the ONLY unswept site.** Verified read-only across the tree: `is_int_meta` already applied at **8 call sites** (varmap.rs ×5, cast.rs, printc.rs, infertypes.rs ×2, types.rs ×2); the only other `Datatype::Int(_)` predicates are printc.rs:70 and :76 and **both list `Datatype::Char` explicitly**; `Char` is handled at every per-variant site in types.rs (size, metatype, submeta SUB_INT_CHAR 19, name, is_int_meta); the two remaining `_`-catch-all matches (printc.rs:2279 declaration printing, infertypes.rs:255 an Option match) are **not metatype predicates**. ⇒ **`d3c3bfb`'s sweep was substantially complete and missed exactly ONE function — which happened to be high-traffic. The class is CLOSED, not open.** **⭐ The file:line evidence for the sites that are ALREADY CORRECT is what stops this being re-opened as a hunt — the enumerated-checklist discipline delivered on a NEGATIVE result, which is when it is hardest to bother.**
**ARC CLOSED `e1d020b` → `e517104` (6 commits, all on master):** the 1-byte typing divergence was **neither the type model** (task #11 exonerated `type_order`) **nor return recovery** (Ghidra's own trace showed `guardReturns` registering RAX:8 exactly as mosura does) — **it was holes in SubvariableFlow's opcode switch, and `MOSURA_SUBVAR` named the abort op on its FIRST run.** SubvariableFlow lands whole including the sext half, with a certificate for the one value that legitimately stays constant. `orcompare` **token-identical**; all five pre-filed predictions land; three WAR2 functions now differ from Ghidra ONLY in global-symbol naming. Cumulative: corpus 0.9539 → **0.9552** · byte-clean **16, set identical every step** · **COMPILE_FAIL 49 → 46** · reached==cfg 0/1303 throughout · deficit AND surplus sets identical every step · **cast census 9481 → 9031 (−450)** · baseline sanity at all four measured steps.
**⚠️ PROCESS NOTE (lead's): the agent believed six commits were sitting ungated; master had in fact been advanced at every increment with independent verification. ADVANCING IS THE LEAD'S JOB AND THE LEAD SHOULD SAY SO EXPLICITLY EACH TIME, not only inside the ratification.**
**NEXT APPROVED: GLOBAL-SYMBOL NAMING (`cRam0000000000081 27a` vs Ghidra's `DAT_0008127a`)** — now the ONLY thing between three WAR2 functions and Ghidra's exact text, and those three are the closest we have ever been on real binary code; no filed gate to inherit. **CONDITIONS: (1) Rule Zero + second clause, and be careful HERE SPECIFICALLY — a global's NAME is exactly what the isolated-import recipe can change (no surrounding context ⇒ no cross-references to inform naming), so forced callee storage and a like-for-like program are NOT optional. (2) Ground read-only first; any inherited framing is a claim with an author and a date. (3) ⚠️ TREAT AS POTENTIALLY BYTE-AFFECTING, NOT COSMETIC — the `type_prefix` episode proved a name change moves real bytes through the harness because the survey's declaration synthesis keys off the IDENTIFIER'S FIRST LETTER, and `cRam...` → `DAT_...` CHANGES THAT LETTER. Check the synthesis path explicitly before assuming render-only; report per-function byte movement either way. (4) Byte-clean re-measured with an explicit call-out — three functions differing from Ghidra by one naming convention is the closest thing to a SECOND multi-block byte-exact function in view.**

## ⭐ `7fcf9b9` GATE AUDIT (docs-only) — TWO GATES VOID, ONE STALE; the cast work is UNBLOCKED (master advanced)
Ordered by the lead after two gates fell in two days; it found more than the code sweep did. **Full detail in [[measurement-determinism-first]]** — the general form: ***an "exhausted" verdict is a claim about a search, and it decays every time the ground moves under it.***
**⇒ BOARD CHANGE: the `(char)`-class cast work and the ActionSetCasts remainder are UNBLOCKED. The only genuine foundation left in that area is the COMPOSITE/UNION LATTICE (PTRADD/PTRSUB refit, `checkPointerIssues`, `insertPtrsubZero`, union resolution). That is a real result from a DOCUMENTATION-ONLY commit.**
**✅ THE SWEEP IS ENUMERATED IN-TREE on `is_int_meta` itself — where the next variant-adder will look** — every site CHECKED not just the one changed (cast.rs 5, varmap.rs 5, printc.rs 3, infertypes.rs 2, types.rs 3), plus the per-variant answers `Char` owes (size, metatype, submeta SUB_INT_CHAR 19, name) and the two `_` arms verified NOT to be metatype predicates, ending with the instruction to walk and extend the list. **The lead's reading of "the FIRST casualty" is recorded as the tell: the word implied a SET, nobody wrote the set down, and that is how the second casualty survived four type-layer commits.**
**📌 `cast-census.py` marked CANONICAL, earlier absolutes VOID (recipe lost) — anchor 9031 @`e517104`. 📌 The baseline-validation proof (unchanged functions must show 0 status disagreements) is now the documented standard in the remeasure doc with runnable commands and four worked examples.**
**NEXT: GLOBAL-SYMBOL NAMING — agent's own recommendation, lead concurring rather than substituting judgement.** Note the lead's framing when the agent offered to defer the pick after being wrong about a gate: **it was wrong about a gate it INHERITED, and then it was the one who FOUND it — the failure mode being guarded against is UNEXAMINED claims, not MISTAKEN ones.** Conditions unchanged (Rule Zero + second clause, naming being exactly what a context-free import can change; check the declaration-synthesis path since `cRam...`→`DAT_...` changes the first letter the harness keys its C type off; per-function byte movement; byte-clean called out).

## `14a5307` — the ActionSetCasts remainder SPLIT (docs-only; master advanced, 2026-07-31)
Applied the rule the gate audit bought. **One gate note was covering FOUR different answers** — see [[measurement-determinism-first]] for the general rule (*one claim per item, or the note is unfalsifiable in practice*). Dispositions: **`resolveUnion` genuinely gated** (no union type in mosura's lattice) · **`checkPointerIssues` NOT gated but certified inert** (warns only, never mutates; **ZERO of either warning across all 1286 WAR2 functions in Ghidra's own output** — a measurement, not a judgement; revival condition attached) · **PTRADD/PTRSUB refits UNMEASURED, measurement named rather than a gate asserted** (they DO mutate IR, so reach is nonzero by construction).
**NEXT (approved, agent-recommended): the PTRADD/PTRSUB REACH COUNT** — env-gated counter + one WAR2 emit; cheap, the only unmeasured claim left in the area, and it either unlocks a real port or produces the campaign's third certificate. **THEN the `(char)`-class cast work, which needs a SPECIMEN not a gate:** `orcompare` is token-identical and the WAR2 cast census sits at 9031 with the metatype fix landed, so find a WAR2 function where mosura's casts still differ from Ghidra's, ground it **through `oracle/capture --c` per the routing rule**, and let it name itself — the same method that turned "the gated cast" into a metatype mis-port. **⚠️ Watch the naming trap: a cast difference that is really a TYPE-NAME difference routes to the Java-layer question, not the cast logic.**

## `bf04845` — the PTRADD refit is LIVE (59 ops), NOT gated, NOT inert; master advanced (2026-07-31)
Third inherited gate falsified in three days — full detail and the four rules it bought in [[measurement-determinism-first]]. Suite 572/0, clippy 0, emitted C unchanged (probe prints only, env-gated).
**⇒ GO GIVEN on porting `opUndoPtradd`** — best-evidenced item on the board, no foundation in the way, cost reported before the build as the rule requires.
**⚠️ LEAD RULING, MADE EXPLICIT BECAUSE IT IS THE GATE, NOT A FOOTNOTE: 59 FIRINGS IS REACH, NOT BENEFIT.** Undoing a PTRADD turns pointer arithmetic into plain arithmetic — a rendering change with BYTE consequences through the harness. **Full per-function battery · explicit byte-clean call-out · PRE-FILE the expectation before measuring. If the recompile says it moves functions AWAY from the original, that is a RESULT TO REPORT, not a fix to defend** — the `-j` episode is the precedent, where the faithful-looking option was the wrong one for the binary. **FAITHFULNESS governs whether we PORT it; the MEASUREMENT governs what we then SAY about it.**
**AFTER IT LANDS: the cast specimen hunt** — ground a WAR2 function whose casts still differ from Ghidra's through `oracle/capture --c` per the routing rule, with the naming-trap watch (a cast difference that is really a TYPE-NAME difference routes to the Java-layer question, not the cast logic).

## ⏸ PAUSED CLEAN at user request (2026-07-31) — tip `9e0c169`, tree clean, no stashes, master == heritage-spacebase-land, suite 504/0
**Nothing was in flight: `opUndoPtradd` was APPROVED BUT NEVER STARTED** (the GO and the pause crossed). Last commits: `14a5307` split gate note · `bf04845` MOSURA_PTRFIT probe · `9e0c169` the gate rules in AGENT.md. All merged; nothing awaiting a merge.
**RESUME POINTS, both grounded and unstarted:**
1. **`threedim` cast specimen (lead's preference).** ⭐ **THE WAR2 CAST COMPARISON IS ORACLE-CONFOUNDED AND THE CONFOUND CANNOT BE REMOVED THERE:** `ghidra-all.txt` is analyzeHeadless, which carries a **Java-layer TYPE model**, and **casts are a function of types** — so a cast-count difference against it may be a TYPE difference rather than a cast-logic one, and counts cannot separate them; there is no C++-only oracle for WAR2 because the DOS/4GW loader problem is exactly why that file exists. ⇒ **third row for the oracle-routing rule: for cast/type questions on WAR2 there is NO clean oracle — ground on the CORPUS and carry the WAR2 numbers as UNATTRIBUTABLE.** Against the right oracle the picture INVERTS: **`oracle/capture --c` vs mosura is IDENTICAL on 10 of 12 corpus fixtures**, the only two differing in OPPOSITE directions — **`threedim` mosura 8 vs 6, `pointerrel` mosura 1 vs 2.** Opposite signs matter: **a single-sign specimen invites a "fix" that merely moves the count.** METHOD (task #15): diff the two outputs → let the differing cast NAME ITS OP AND SLOT → *then* read `cast.rs`'s arm for that op against Ghidra's `getInputCast`/`castOutput`. **Not starting from the cast rules is the whole lesson of the last one.**
2. **`opUndoPtradd`** — GO given, never started. **⚠️ THE AGENT CORRECTED ITS OWN "best-evidenced item" CLAIM and the lead ADOPTED the correction: with cast parity established on 10/12, the 59 firings are REACH, not a known measured deficit. Port it because Ghidra does it; do NOT describe it as closing a gap we have not measured.** Reach-not-benefit ruling is in both `bf04845`'s message and task #15, so the caution arrives WITH the number rather than after it.

## `ab6ea9c` — the `_` arm was an INVERTED DEFAULT, not a missing feature (master; 2026-07-31)
**HALF THE PREMISE WAS AN INSTRUMENT ARTIFACT** (`threedim` — see [[measurement-determinism-first]]). **The other half was real and named its op and slot: `pointerrel` = FLOAT_ADD slot 0, req `float4`, cur `int4`.** Root cause: `cast.rs`'s **`_ => None` ("everything else is transparent") is the OPPOSITE of Ghidra's assertion** — a `TypeOp` uses the base `getInputCast` (typeop.cc:295) UNLESS it declares an override, and of the 25 that do, only `TypeOpCpoolref`/`TypeOpNew` say "never needs casting" (typeop.hh:867/878). A placeholder from `79f5406`, the first cast commit, which wired only the comparisons and never decided about the rest. **⇒ AN INVERTED DEFAULT IS INVISIBLE PRECISELY BECAUSE IT LOOKS LIKE A DECISION.**
**INSTRUMENTED BEFORE HYPOTHESISING:** a read-only probe bounded the change before a line was written — **3 firings over the 79 datatests, 693 over WAR2's 1303 functions, all ONE shape** (a pointer operand consumed by integer arithmetic, needing `(int4)`). Rule Zero on all three corpus specimens vs `oracle/capture --c`: **Ghidra emits every one**; `pointerrel` and `stackstring` now render CHARACTER-IDENTICAL to Ghidra.
**BATTERY:** corpus **0.9552 → 0.9561**, 58/60, **exactly the 3 probed fixtures moved** (pointerrel .952→.959, stackstring .868→.921, **partialsplit .950→.947 — owned, not defended: the faithful rule on a differently-shaped IR, residual named as upstream array recovery**) · cast census 9031 → 9815, 113 fns changed C — **+91 over the probe's 693 explained as a CASCADE with Ghidra's own reason** (`ActionSetCasts` does inputs first "as output may depend on input", coreaction.cc:2757) · all four wrong-code scans 0, none grew · reached==cfg 0/1303 · **call gauge IDENTICAL in every metric, deficit `{00079130}`, EMITTER=0, surplus set also unchanged** · COMPILE_FAIL **46 → 45** · **byte-clean 16 UNMOVED with an IDENTICAL SET** · baseline sanity: exactly one WAR2 status change (`00062260` CFAIL→MISMATCH) **and its C changed**, so every unchanged-C function shows 0 disagreements.
**SCOPED, NOT BLANKET — two hold-back arms keep ops OFF the base rather than inventing behaviour:** the unported `getInputCast` overrides (COPY/LOAD/STORE/ZEXT/INT2FLOAT/PIECE/SUBPIECE/PTRADD/PTRSUB/SEGMENTOP), and the ops that use the base but also override `getInputLocal` which `input_type_local` does not model (CBRANCH/CALLIND/CALLOTHER/RETURN/INDIRECT/INSERT/EXTRACT — inert today, **listed so a future `op_meta` fix cannot switch them on silently**). One line, one revival condition each. CALL deliberately not held.
**⚠️ TASK STORE WAS EMPTY this session — the agent re-filed from the brief rather than assuming. Third time state survived only because it was written somewhere durable.**
**NEXT: `opUndoPtradd`** — pre-filed expectation, full per-function battery, explicit byte-clean call-out, **a move AWAY from the original reported as a RESULT rather than defended. Reach, not benefit.**

## ⭐⭐ `8d9e42c` — `opUndoPtradd`: A RENDERING THAT WAS ARITHMETICALLY FALSE WHILE EVERY GATE READ GREEN (master; 2026-07-31)
**THE NEW WRONG-CODE CLASS:** `FUN_00072220` emitted `*((xunknown4 *)(xVar5 + 3))` where the PTRADD meant **+3 ELEMENTS of 4 bytes** — but `xVar5` is `Unknown(4)`, **not a pointer**, so `+ 3` is integer arithmetic adding **3 BYTES. The emitted offset was wrong by a factor of 4.** Now `+ 0xc`. Across the 59 misfitting PTRADDs: **56 have a pointer base whose pointee size disagrees with the element size, 3 have a non-pointer base — both mis-scalings.** ⚠️ **INVISIBLE TO THE WRONG-CODE SCANS (labels/fall-off-end/empty constructs), INVISIBLE TO THE RECOMPILE (all 14 functions MISMATCH before AND after), AND INVISIBLE TO THE CORPUS. Found ONLY by porting the guard Ghidra wrote.** ⇒ **THE STRONGEST SINGLE ARGUMENT THE CAMPAIGN HAS PRODUCED FOR THE USER'S PIVOT: some defects are reachable ONLY by faithfulness, because no gauge we own can see them.**
**⭐ THE AGENT STATED THE HARDER HALF UNASKED: there is NO oracle-checkable specimen for this port and there CANNOT be one** — the guard fires **zero** times across all 79 datatests and only on WAR2, where `capture --c` cannot run and `ghidra-all.txt` is analyzeHeadless with a Java TYPE model **while the guard is itself a type test** (identical confound structure to the cast question). **So the port rests on faithfulness + the battery — and the mis-scaling finding is INDEPENDENT OF ANY ORACLE, because it is ARITHMETIC.** That distinction does real work: *"we cannot verify this"* vs *"this particular claim needs no oracle."*
**ALL SEVEN PRE-FILED PREDICTIONS CONFIRMED** (filed before a line was coded): corpus **zero** movement (0.9561, no per-fixture score moved) · **exactly 14** functions change C, precisely those the probe named, no cascade · **byte-clean 16 unmoved, SET identical** · call gauge identical in every metric incl. surplus · wrong-code gates 0, reached==cfg 0/1303 · **census +90 against a predicted ~+59 — MECHANISM RIGHT, MAGNITUDE WRONG, reported as such rather than rounded into a success** (a real interaction with `ab6ea9c`: undoing a PTRADD makes the pointer an INT_ADD operand, and INT_ADD's base input cast went live one commit earlier) · recompile statuses: **no prediction made, answer no movement**, zero status disagreements anywhere in WAR2.
**⭐ THE PRE-CHECK THAT DECIDED IT COULDN'T REGRESS A BYTE-EXACT FUNCTION: the 59 were INTERSECTED WITH THE BYTE-CLEAN 16 BEFORE CODING (empty) — which required fixing `MOSURA_PTRFIT` to carry FUNCTION NAMES. ⇒ REGISTER RULE: "a bare count cannot answer the only question a gate asks."**
**NOT BUNDLED, each its own item:** the two `ruleaction.cc:6925/7115` `finalize=false` call sites (RulePtraddUndo/RulePtrsubUndo), and the **PTRSUB refit** — 536 PTRSUBs reach ActionSetCasts and `MOSURA_PTRFIT` still measures its guard, but `isPtrsubMatching`'s sub-field walk genuinely needs the composite lattice, **so that gate note SURVIVES examination — the only one in the area that has.**
**CUMULATIVE `9e0c169` → `8d9e42c`:** corpus 0.9552 → **0.9561** · cast census 9031 → **9905** · COMPILE_FAIL 46 → **45** · **byte-clean 16, set unchanged throughout** · call gauge untouched · all wrong-code gates 0 · suite 504 → **505**/0 · clippy 0.
**NEXT (autonomous, approved): the PTRSUB refit's NON-LATTICE half — MEASURE how many of the 536 fail the pointer-metatype test BEFORE claiming separability, rather than inheriting either verdict.**

## PTRSUB REFIT: CERTIFIED INERT — and the inherited reason was WRONG (read-only, HEAD `8d9e42c`, 2026-07-31)
**VERDICT: the ActionSetCasts PTRSUB refit would fire ZERO times today ⇒ porting it is a no-op.** Mechanism end to end: (1) all **536** PTRSUBs reaching ActionSetCasts have a **SPACEBASE** pointee — 536/536 across 197 functions, measured at HEAD; **none points to a composite**; (2) the guard is `isPtrsubMatching(off, 0, 0)` — extra and multiplier both **0** at that call site; (3) `TypePointer::isPtrsubMatching` (type.cc:1123) therefore takes its SPACEBASE branch (fail iff `getSubType` null or residual offset nonzero, then fail iff `extra >= subType->getSize()`); (4) **mosura's `Datatype::get_subtype` (types.rs:184) returns `Some((Unknown(1), 0))` for EVERY Spacebase** — the always-present stand-in, because `Datatype` has no `glb` back-pointer and symbol resolution is deferred to print time (`printc::render_ptrsub` over `varmap::recover_scope`); (5) ⇒ never null, residual always 0, `0 >= 1` false ⇒ **true for all 536.**
**⭐ FOURTH ITEM IN THAT BUNDLE TO BE RE-DESCRIBED: the note deferred it "with the composite/union lattice they concern" — IT IS NOT THE COMPOSITE LATTICE. The live branch is SPACEBASE and what gates it is the ScopeLocal SYMBOL QUERY.**
**⭐⭐ THE CONTRAST THE AGENT FLAGGED RATHER THAN GENERALISING (it nearly generalised from the PTRADD result and says so): THE TWO REFITS ARE NOT THE SAME KIND OF THING AND MUST NOT BE QUOTED IN ONE SENTENCE AGAIN.** The PTRADD guard is a **SIZE COMPARISON, fully meaningful for primitives** — which is why it fired 59× and caught a real mis-scaling. **Every PTRSUB branch that could accept requires a spacebase symbol lookup we do not do in the type layer.**
**REVIVAL CONDITION (written into the task so it cannot rot): this certificate DIES when `get_subtype` stops returning the permissive Spacebase stand-in — i.e. when the ScopeLocal symbol query moves OUT of print time INTO the type layer.** At that moment a PTRSUB whose offset does not land exactly on a mapped stack symbol starts returning false, the refit goes live, and **it must be ported IN THE SAME COMMIT as that change** — otherwise mosura renders a PTRSUB into a frame slot no symbol covers. **Whoever does the ScopeLocal-in-types work must read it.**
**SCOPE LIMIT STATED RATHER THAN HIDDEN: measured on WAR2's 1303 functions at `8d9e42c`; the guard's reach on the 79 datatests is UNMEASURED and this certificate does not cover it.**
**SESSION SUMMARY `9e0c169` → `8d9e42c`:** 2 commits · corpus 0.9552 → **0.9561** (58/60) · cast census 9031 → **9905** · COMPILE_FAIL 46 → **45** · **byte-clean 16, identical member set throughout** · call gauge **untouched** · all wrong-code gates 0 · reached==cfg 0/1303 · suite 505/0 · clippy 0. **Two premises falsified (`threedim`'s cast difference — an instrument artifact; the PTRSUB gate's stated reason) and one SILENT wrong-code class fixed.**

## `493698c` + `9f1c577` + `edf58a1` — census caveat · RulePtraddUndo · the ORIGIN audit (master; 2026-07-31)
**`493698c` — the bias now lives IN the instrument:** `cast-census.py` prints it on EVERY run (stderr, so piped stdout parsing is unaffected) with the worked example and a two-line re-check recipe; **number untouched, 9905 both sides.** Two register entries added to AGENT.md — *"a bare count cannot answer the only question a gate asks"* and *"an instrument's known bias belongs in the instrument"* — plus the bundling postscript: **four items, four answers, none of them the one asserted.**
**`9f1c577` — RulePtraddUndo ported, AND A PREDICTION WRONG IN A USEFUL WAY.** Reach measured first with a **rule-shaped probe in Ghidra's exact actprop slot** (see [[measurement-determinism-first]]): **0 firings on the corpus, 32 in 4 WAR2 functions** (20 not-a-pointer, 12 size-mismatch, ZERO constant-zero-index), all 4 a strict subset of the 14 the refit rewrites. Pre-filed: *"at most 4 functions, POSSIBLY ZERO, since the dead-last refit already repairs these before printing."* **The parenthetical was WRONG — all 4 changed**, and `FUN_000642c0` shows why it matters: **constant offsets rescaled by element size (`+1`→`+2`, `+2`→`+4` for a 2-byte element) at sites the refit did NOT reach ⇒ the two mechanisms are NOT redundant.** Why they differ is **filed open, not guessed** (candidates: the type channel `getTypeReadFacing` vs `getHighTypeReadFacing`, or timing) — a plausible story here would have been the fourth retraction of the campaign. All 8 other predictions confirmed **including the blocking one — TERMINATION: EMIT 270s vs 264/269s baseline, no RulePtrArith oscillation.**
**`edf58a1` — the ORIGIN audit found more than the shape:** the direct sibling sits in the bundle's OWN commit, and **one premise was FALSE WHEN WRITTEN** (see [[measurement-determinism-first]] — the struct-metatype claim, a month after `Struct` landed). **`findTruncation` stays DEFERRED with only its false premise retired — retiring the premise is NOT retiring the item, and inventing a verdict to close a note would repeat the exact failure under audit** (task #5 item A, with the method: a reach census).
**⭐ `ptrarith.rs`'s header (`86bd58f`) NAMED AS THE LAST UNEXAMINED SIBLING WITH NO VERDICT OFFERED** — four items, one shared reason, already questionable for `nearestArrayedComponent` given `Struct` exists. **Flagging it as unaudited rather than half-judging it is what stops the next bundle forming.** Task #5 item B — approved as next.
**CUMULATIVE `9e0c169` → `edf58a1` (5 commits):** corpus **0.9561** 58/60 · cast census 9031 → **9907** · COMPILE_FAIL 46 → **45** · **byte-clean 16, set unchanged across ALL FIVE** · **call gauge NEVER moved** · wrong-code gates 0 · reached==cfg 0/1303 · suite 504 → **505**/0 · clippy 0.


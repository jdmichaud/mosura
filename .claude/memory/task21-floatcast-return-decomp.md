---
name: task21-floatcast-return-decomp
description: floatcast 0.791 gap decomposed — RuleSubExtComm partial-port (bounded
metadata: 
  node_type: memory
  type: project
  originSessionId: 9beadf25-4682-4e85-94fa-f326d85ed777
---

Task #21 floatcast return-decomposition residual, decomposed READ-ONLY @`0f37185` (owner float1). floatcast = **0.791** (post-pullsub, confirmed off report @sha header). corpus scores chunks[0]=`prec_conditions`; oracle IGNORES the fixture prototype so Ghidra target is `xunknown8 func` with `return CONCAT44((int4)((uint8)(fVar1-fVar2)>>0x20),(float4)(fVar1-fVar2))`. Two INDEPENDENT gaps; **both** needed for ~0.95:

**(a) RETURN residual — mosura `(xunknown8)CONCAT124(...)` (16-byte) vs Ghidra `CONCAT44(...)` (8-byte). BOUNDED = the #21 scope.**
Root: mosura's **RuleSubExtComm is a PARTIAL PORT** (`rules.rs:2059-2072`). It declines the case Ghidra handles — a SUBPIECE straddling the ZEXT/SEXT boundary at nonzero offset (`out_size+subcut>in_size && subcut!=0`), with the comment "Ghidra splits it; leave those alone". Ghidra `RuleSubExtComm` (`ruleaction.cc:4423-4439`) SPLITS it: `SUB(ZEXT(V),c) → ZEXT(SUB(V,c))` (new inner SUBPIECE size `in_size-subcut`; guard is `subcut>=in_size` not `subcut!=0`). Ghidra's return collapse chain (trace DEBUG 89→90→91): subextcomm `SUB1612(ZEXT816(diff),4)→ZEXT412(SUB84(diff,4))` → **concatzext** `CONCAT124(ZEXT412(x),y)→ZEXT816(CONCAT44(x,y))` → subextcomm `SUB168(ZEXT816(concat8),0)→concat8`. mosura stalls at the un-split SUBPIECE#4; cleanup-pool **RuleSubRight** (DEBUG 140) then rewrites the leftover 16-byte SUB#4 into the `>>0x20` shift → CONCAT124. FIX: change `if subcut!=0 {return 0}` → `if subcut>=in_size {return 0}` + the subcut!=0 inner-SUBPIECE split. mosura ALREADY has RuleConcatZext (`rules.rs:6079`) + the offset-0 branch wired — they just have nothing to match until the split runs; then the chain auto-collapses to Ghidra's exact 8-byte CONCAT44. Pool order verified: SubExtComm+ConcatZext in main pool (repeated, first), SubRight in cleanup_pool (after) → collapse wins. **LaneDivide-independent** (Ghidra lanedivide fires DEBUG 126 on the param-setup `SUB164(XMM0(i),0)`, AFTER the return collapse). MOVER — watch other SUBPIECE-of-ZEXT/SEXT-straddle fixtures (concatsplit/partialsplit/longdouble/revisit/switchmulti).

**(b) = TASK #26. fRam-global-vs-fVar-local naming. SEPARATE, NOT #22. MECHANISM CORRECTED (SCOPED float1 2026-07-11, read-only probes).**
NOT `mergeAddrTied→snipReads/eliminateIntersect` (I AND the task mis-filed it there first). That mergesnip.rs machinery is FAITHFUL and correctly DECLINES: probed — the addrtied ram group forms {vn214 self-copy-out@b4, vn217 input}, but vn217's phi-read cover is block-0-only (a MULTIEQUAL input reads at the predecessor-block end) while vn214's def is block-4, so NO cover-intersection exists geometrically. The real snip is Ghidra **`Merge::mergeMarker→mergeOp→trimOpInput`** (merge.cc:889/719/692; trace DEBUG 143): mergeOp force-merges each MULTIEQUAL input with the output and, on a cover-conflict, TRIMS the input (inserts COPY `u0x1000001e=r0x100080(i)` at the predecessor-block end + rewires) — that trim COPY renders as the `fVar1 = fRam80;` init line and keeps the phi output a distinct local. mosura's `merge(f:&Funcdata)->HighVariables` is READ-ONLY (can't insert the trim COPY); `merge_markers` (merge.rs:117) unions the phi output with each input gated ONLY on `merge_test_required`, NOT cover-conflict → unions XMM0 with the addrtied global → XMM0 named fRam80. VERDICT: complete FAITHFUL fix = DEEP-ish graph-mutating pass (port mergeOp/trimOpInput into a mergesnip-style action; materializes the init lines). A read-only merge_markers cover-gate is NET-POSITIVE but INCOMPLETE + non-faithful ADAPTATION: narrow "addrtied-input + cover-conflict → non-union" gives floatcast **0.791→0.842** (+0.051), SURGICAL (only floatcast moves, avg +0.0009, NO regressions, switchloop UNCHANGED = no #23 entanglement) but MISSES the init lines. Full ~0.95 also needs (a)/#21. This is the divergence "RulePullsubMulti #4 exposed".

**(a) BUILT + MEASURED then REVERTED (parked, gated on #23; lead call 2026-07-11).** Tree back to clean `0f37185`. Turnkey re-apply AFTER #23 lands: in `RuleSubExtComm::apply_op` (rules.rs ~2068) replace `if subcut != 0 { return 0 }` + the offset-0 rewrite with Ghidra's `if subcut>=in_size {return 0}` then `let newvn = if subcut!=0 { new_op_before_sized(op, Subpiece, [invn, new_const(cut_v.size, subcut)], (in_size-subcut) as u32).output } else { invn }; op_remove_input(op,1); op_set_opcode(op,ec); op_set_input(op,0,newvn)` (ruleaction.cc:4423-4439) + the `sub_ext_comm_splits_straddling_piece` unit test. Measured @0f37185: FAITHFUL — floatcast return now `CONCAT44((xunknown4)(fVar1>>0x20),(float4)fVar1)` (8-byte, matches Ghidra structure; `(xunknown4)` vs `(int4)` = task #13 weak-type, `fVar1` vs `(fVar1-fVar2)` = #26). But NET-NEGATIVE MOVER: floatcast 0.791→**0.800** (+0.009 only — #26/(b) dominates the rest), switchloop 0.820→**0.779** (−0.041), avg 0.9222→**0.9217**. BREAKS `jumptable_recovery::switchloop_recovers_nine_targets` (9→**7** targets, diverges from Ghidra). Root = the pre-existing **#23 gap (ActionSwitchNorm)**: switchloop already fires 1763 vs Ghidra 715 rules (`switchnorm` 2x Ghidra / 0x mosura); my +4 faithful straddle-splits (subextcomm 18→22) perturb the fragile dual-width index enough to drop 2 case targets. Ghidra fires straddle-subextcomm on the same index AND still recovers 9 (via ActionSwitchNorm) → the fix is #23, the rule is correct. Lead ruling: NOT a faithful-ports-land case — switchloop 9→7 is a CORRECTNESS regression (fewer targets than Ghidra = worse output, hard-test break), not a gauge artifact; suite-green-every-commit wins; #23 is (a)'s genuine prereq. (a) parked under #21, gated on #23. Also (a) is low-value until #26 lands (only +0.009). NEXT (lead-directed): scope #26 premise-first.

**(b)/#26 BUILT FAITHFUL NO-GUARD (float2, 2026-07-12, gated MOVER, uncommitted @0f37185).** The addrtied guard float1 added was RULED AGAINST by the lead (X) — it patches around wrong upstream IR (mosura's coarse register SSA), the [[faithful-type-of-wrong-ir]] anti-pattern. float2 rebuilt the FAITHFUL trim-any-conflict version, NO guard. Ported Ghidra `Merge::mergeMarker→mergeOp→trimOpInput` (merge.cc:889/719/692) as a GRAPH-MUTATING pipeline pass `merge_marker_trim`/`ActionMergeMarkerTrim` (merge.rs), wired right after `super::mergesnip::ActionMergeRequired` (+its deadcode) in pipeline.rs; added `op_insert_end` (funcdata.rs, Ghidra `opInsertEnd` = insert at block end, before a trailing flow-break) + unit test `marker_trim_snips_a_cover_conflicting_addrtied_phi_input`. Algorithm (fixpoint): recompute `all_covers`+`merge_addrtied`-only highs (= Ghidra's mergeMarker-ENTRY state: addrtied merged, markers not yet); for each MULTIEQUAL find the FIRST input where `rep_in!=rep_out && merge_test_required(out,in) && classes_interfere(members_of(out),members_of(in))`; `trim_op_input` = new COPY(in) at pred-block stop-addr via `op_insert_end` + `op_set_input(phi,slot,copyout)`; repeat until no trim. INSTRUMENTED FIRST (MOSURA_TRIMDBG, removed): floatcast phi op85 out=XMM reg (rep233, NOT tied) vs in[0]=ram global (tied) → conflict, trims; in[1]=unique → no conflict. Why it detects despite merge_addrtied: the phi OUTPUT is a REGISTER (not addrtied) so it's a DIFFERENT high from the addrtied global group (`intersection(a,b)` returns false only when a==b). Result matches Ghidra: `fVar1 = fRam80;`/`fVar2 = fRam88;` init + locals. **MEASURED full corpus @0f37185: floatcast 0.791→0.871 (+0.080), varcross 0.905→0.851 (−0.054) — ONLY these two move (verified by full per-fixture diff; all other 58 byte-identical), avg 0.9222→0.9227, 56/60, suite 407/0, switch 8/8 — NO HARD-TEST break (varcross is a gauge SCORE dip, not a red test).** varcross −0.054 = the DIAGNOSTIC naming **task #27** (mosura's coarse register SSA: it keeps one broad register version where Ghidra SSA-splits into narrow single-use versions that never conflict → the faithful trim over-fires on the register phi input). Per [[faithful-ports-land-not-held]] this LANDS: floatcast +0.080 is the win, varcross −0.054 names #27. Files: merge.rs, funcdata.rs, pipeline.rs, docs/coverage.md (all 4 in `git status`, no stray). Residual floatcast→~0.95 = (a)/#21 CONCAT44 (gated on #23) + a minor extra `fVar3` temp. Reported gated-mover to lead, WAITING for land-go.

Tooling: `scripts/trace-diff.sh floatcast` (KEEP=1) names both rules; `cargo run -q --example dumpc -- floatcast [--raw]` = mosura C/IR; `oracle/capture <ghidra> <datatests>/floatcast.xml --c|--ir -` = Ghidra. See [[task6-lanedivide-plan]] (floatcast was mis-attributed to LaneDivide — the RETURN lever is RuleSubExtComm).

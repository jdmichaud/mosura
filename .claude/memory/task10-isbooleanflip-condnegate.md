---
name: task10-isbooleanflip-condnegate
description: "Task #10 grounded scope: port Ghidra's isBooleanFlip branch-negation subsystem + RuleCondNegate. It's an ARCHITECTURE-level port — mosura deliberately does branch negation at PRINT time; Ghidra does it IR-level. Unblocks task #4 (comparison normalization)."
metadata:
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #10/#1 (in_progress, owner flip1) = port Ghidra's `isBooleanFlip` branch-negation subsystem + `RuleCondNegate`. Base master `0acaa93`, corpus 0.8865. It is the UNBLOCKER for task #4 (proven coupling — see [[task4-comparison-normalization]]) and a consumer of RuleIntLessEqual. See [[printc-structuring-adaptation-conflicts]], [[direction-faithful-port]].

PROGRESS (owner flip1):
- **S0 DONE `a211fff`** (byte-IDENTICAL 62/62 exit 0, suite 336/0, self-approved per gate policy). Added `PcodeOp::boolean_flip`/`fallthru_true` flags (op.rs flags 0x80/0x100 — mosura's own compact bits, not Ghidra's literal values) + `is_boolean_flip`/`is_fallthru_true` accessors; `Funcdata::op_flip_condition` (funcdata.hh:489); `RuleCondNegate` (rules.rs, ruleaction.cc:5474 — uses existing `op_bool_negate`+`op_set_input`+`op_flip_condition`) with 2 unit tests constructing the flag directly. HELD UNWIRED (nothing sets the flag → inert). Coverage row RuleCondNegate→HELD. Pipeline slot documented right before RuleBoolNegate (Ghidra coreaction.cc:5607 is BEFORE RuleBoolNegate 5608 — memory's "after RuleLessNotEqual" was WRONG; source is authoritative).
- **S1 INSTRUMENT DONE (2026-07-08, flip1) — MEMORY'S S1 FRAMING IS NOT VIABLE AS WRITTEN; the plan needs an added pipeline STAGE, not a flag-swap.** Grounded findings:
  - mosura's structuring is a READ-ONLY PRINT-TIME pass: `structure(f: &Funcdata) -> Structured` (structure.rs:450), called from printc.rs, does NOT mutate IR. universal_action (pipeline.rs:333) has NO structuring action at all (heritage→3×[rules+deadcode]→types→ptrarith→cleanup→deadcode). So all rule pools run BEFORE structuring → setting boolean_flip during structuring gives RuleCondNegate NO chance to fire. "structurer sets boolean_flip instead of Structured.negated" cannot materialize the negation on its own.
  - mosura folds TWO distinct Ghidra IR mechanisms into print-time: (A) **opFlipInPlaceExecute / ActionNormalizeBranches** (blockaction.cc:2117, runs at pipeline END after structuring; the NORMAL-FORM flip `a<=b`→`b<a`/`a!=c`→`a==c` when `opFlipInPlaceTest==0`) = mosura's `flip: Vec<bool>` (structure.rs:462, per-BB opFlipInPlaceTest==0) + render_negated's ==/<=/< handling. (B) **RuleCondNegate / boolean_flip** (structuring's BlockBasic::negateCondition block.cc:2355 sets boolean_flip+fallthru_true+flips edge order; RuleCondNegate materializes BOOL_NEGATE) = mosura's `Structured.negated` (set `i==0` = body-on-false-edge at collapse sites structure.rs:250/373/411/425/441) + render_negated leading-!/De Morgan. The two are ENTANGLED in render_negated (`!(a<=b)=>b<a` does both at once) — can't coexist with IR negation (double-negation), so it's all-or-nothing.
  - Ghidra ORDER (coreaction.cc): actmainloop repeats {…actprop(RuleCondNegate@5607)… ActionBlockStructure@5659 (negateCondition sets flag)… actprop2@5671…} then final act: ActionStructureTransform@5715, ActionNormalizeBranches@5716. So structuring sets boolean_flip mid-loop, the REPEAT lets actprop's RuleCondNegate materialize it; ActionNormalizeBranches does opFlipInPlaceExecute at the very end.
  - BLAST-RADIUS de-risk: switch/determinedbranch consumers (jumpbasic.rs:753 `toswitchval=indpath==1 "no isBooleanFlip"`, determinedbranch.rs:151) run at BUILD/mid-pipeline BEFORE any proposed late stage → the edge-flip won't reach them. mosura CFG = `out_edges: Vec<BlockId>` `[false,true]` (block.rs:21).
  - TURNKEY BUILD-PIECES INVENTORY (grounded read-only, ready on green-light): the subsystem is mostly assembly of existing parts. HAVE: `get_booleanflip` (opcode.rs:157 → `Some((Copy,false))` for BOOL_NEGATE, `None` for AND/OR — matches Ghidra COPY/MAX sentinels), `replace_lessequal`, read-only `op_flip_in_place_test` (structure.rs:36 — needs promoting to a Funcdata method that BUILDS a fliplist like opFlipInPlaceTest funcdata_op.cc:1221), RuleCondNegate (S0 `a211fff`), op mutators op_swap_input/op_destroy/total_replace/op_set_input/op_remove_input. NEW to write: (1) `Funcdata::opFlipInPlaceExecute` (funcdata_op.cc:1280 — per fliplist op: get_booleanflip→ if Copy remove BOOL_NEGATE propagating in(0) to lone descendant + op_destroy; if None(=AND/OR) swap AND↔OR; else op_set_opcode + op_swap_input if flipyes + replace_lessequal if result LESSEQUAL); (2) `BlockBasic::negateCondition` equiv (block.cc:2355 — flip boolean_flip + fallthru_true on last CBRANCH + reverse the block's out_edges order) and `flipInPlaceExecute` (block.cc:2378 — flip fallthru_true + edge order only, NO boolean_flip); (3) the two IR actions (orientation + normalizebranches) + post-stage rule pool + printc rework. get_booleanflip returns Option; in opFlipInPlaceExecute a `None` return is guaranteed BOOL_AND/OR (only non-comparison opFlipInPlaceTest pushes).
  - **REVISED PLAN (reported to lead, awaiting gate):** add Ghidra's late structuring/normalize IR stage, scoped to what RuleCondNegate needs: (S1a) IR orientation action — reuse structure()'s per-BB `negated` decision, set boolean_flip+fallthru_true on body-on-false CBRANCHes + flip their CFG out-edge order (BlockBasic::negateCondition); (S1a') port opFlipInPlaceExecute + ActionNormalizeBranches for the normal-form flips; (S1b) a post-orientation rule pool with RuleCondNegate+RuleBoolNegate+RuleIntLessEqual → materialize+normalize; (S2) printc rework — read isBooleanFlip off the CBRANCH, drop render_negated's ==/<=/< flips + incr_in_width, let print-time structure() re-derive negated=false from the flipped edges. Placement: pipeline END (after types), mirroring Ghidra's final act group. GATED — do not build until lead confirms decomposition/scope.

WHY IT'S THE UNBLOCKER (the task#4 coupling proof, ifswitch): Ghidra renders the guard `99 < param_1` because it materializes the branch negation IN THE IR; mosura keeps `param_1 <= 99` (SLESSEQUAL) and negates at PRINT (`render_negated`, `!(a<=b)=>b<a` → `99 < param_1`), which is CORRECT — until RuleIntLessEqual turns SLESSEQUAL→SLESS, after which the print-negation emits `100 <= param_1` (WRONG). The faithful chain needs the negation in the IR so RuleBoolNegate+RuleIntLessEqual normalize it there.

THE FAITHFUL GHIDRA CHAIN (what to reproduce):
1. Structuring's `BlockBasic::negateCondition` (block.cc:2351) flips `PcodeOp::boolean_flip` + `fallthru_true` on the last CBRANCH + flips outgoing-edge order — when it decides the fall-through should be the false branch.
2. `RuleCondNegate` (ruleaction.cc:5474, registered coreaction.cc:5607 — immediately BEFORE RuleBoolNegate@5608, NOT after RuleLessNotEqual; source wins over this note): `if(!op->isBooleanFlip())return 0;` → build `BOOL_NEGATE(cond)`, point CBRANCH at it, `opFlipCondition` (clears the flag). Materializes the flip in the IR.
3. `RuleBoolNegate` (mosura HAS it): `BOOL_NEGATE(INT_SLESS(a,b)) => INT_SLESSEQUAL(b,a)` etc. — flips the comparison.
4. `RuleIntLessEqual` (mosura HAS, HELD@10): `c <= V => c-1 < V` / `V <= c => V < c+1` → the strict form `99 < param_1`.
5. `PrintC::opCbranch` (printc.cc:542) reads `isBooleanFlip()`/`isFallthruTrue()` to print `cond` vs `!(cond)` — but after RuleCondNegate the flag is cleared and the condition is already positive in the IR, so printc renders it directly (NO print-time negation).

WHAT MOSURA HAS vs NEEDS:
- HAS: PcodeOp `flags: u32` (op.rs:39) — add `BOOLEAN_FLIP`/`FALLTHRU_TRUE` bits. RuleBoolNegate. RuleIntLessEqual (HELD). structure.rs `op_flip_in_place_test` (the `opFlipInPlaceTest` port) + `Structured.negated` (structure.rs:108).
- ADAPTATION TO CANCEL (structure.rs:33-34 documents it explicitly): "Ghidra applies it at the structurer + ActionNormalizeBranches (blockaction.cc:2117); mosura applies it at the structurer + PRINT-TIME negation (printc::render_negated, the get_booleanflip/replaceLessequal port)". So mosura NEGATES AT PRINT via `render_negated` + `Structured.negated` — that whole path is the adaptation RuleCondNegate replaces.
- NEEDS: (a) boolean_flip/fallthru_true flags on PcodeOp; (b) the structurer to SET them (where it currently sets `Structured.negated` — reconcile: instead of a print-time negated-bool, flip the CBRANCH flag so RuleCondNegate materializes it); (c) RuleCondNegate + opFlipCondition; (d) wire RuleIntLessEqual@10 + delete printc::incr_in_width; (e) REMOVE/rework render_negated's print-time negation so printc reads the (now positive) IR condition. ActionNormalizeBranches (blockaction.cc:2117) is where Ghidra runs the flip — scope to what RuleCondNegate needs, per lead (NOT the full ActionNormalizeBranches).

SCOPE VERDICT: ARCHITECTURE-LEVEL port (replaces mosura's deliberate print-time-negation design across structure.rs + printc.rs + adds op-flag machinery + RuleCondNegate + reconciles the structurer). High blast radius (every if/while/switch guard). CONSUMERS beyond printing: jumptable.cc reads isBooleanFlip heavily (jumptable.rs port DEFERRED it — jumpbasic.rs:753); coreaction/rangeutil too — so the flag must be correct for the switch path (regression risk on the switch fixtures). Instrument-first per fixture; full suite + switch tests + corpus per step. RECOMMEND a dedicated fresh full-budget run (lead offered respawn) — this session (rock5) has the keystone + task#4 coupling proof context but is deep; the grounded turnkey plan above is the handoff. STAGING (proposed): S0 add flags + RuleCondNegate (HELD, unit-test) → S1 structurer sets boolean_flip instead of Structured.negated (the big reconcile) → S2 wire RuleIntLessEqual + delete incr_in_width + rework render_negated → S3 iterate switch + comparison fixtures to net-positive. Corpus target: net-positive vs 0.8865, recover ifswitch/forloop_withskip/condmulti/elseif/deindirect/loopcomment.

## S1a+S1b DONE (2026-07-08, flip1) — awaiting merge gate
Worktree `/home/jd/projects/mosura/mosura-condnegate`, branch `task1-condnegate` off a211fff, HEAD `e78aa99`. Suite 336/0, switch canary GREEN, corpus 0.8865→0.8882 (54/60 unchanged), delta ALL-POSITIVE (loopcomment +0.009, noforloop_globcall +0.047, pointerrel +0.021, switchloop +0.005; zero regressions; ifswitch C byte-identical since baseline print-negation already gave `99<param_1`, now materialized in IR).

**KEY PIVOT — Ghidra's edge-reversal is UNSAFE in mosura, use the flag instead.** Ghidra BlockBasic::negateCondition reverses CFG out-edges; mosura RE-DERIVES the block tree at print (structure() is read-only, called from printc), and reversed edges make the re-collapse DIVERGE/HANG on loop/short-circuit-entangled guards (rule_short_circuit re-installs CondAnd unboundedly; loopcomment hung, s.blocks→2000+). FIX: `block_negate_condition` sets boolean_flip+fallthru_true, does NOT reverse edges. Orientation lives in the persistent `fallthru_true` flag (Ghidra printc reads it too, printc.cc:542); structure() XORs it into `negated` (`is_oriented`, per-BB precompute from fallthru_true). structure() collapses the ORIGINAL topology (no corruption), only `negated` output flips → prints the positive materialized IR.

PIPELINE (after types): ActionOrientBranches → condnegate_pool {RuleCondNegate, RuleBoolNegate, RuleIntLessEqual} → deadcode. RuleIntLessEqual lives in THIS post-orientation pool (NOT main pool @10); printc::incr_in_width + render_negated UNCHANGED (compound/mechanism-A conditions still print-negate — correct, they're deferred). The atomic swap is orientation+pool+XOR; NO printc rework needed.

GATES in branch_negations (collect_negations walks FINAL tree FROM ROOT — skips stale intermediate composites, e.g. a guard temporarily an If then folded into a switch): skip compound &&/|| (defer BlockCondition::negateCondition block.cc:3023 + mechanism A); `condition_folds_cleanly` (only comparisons RuleBoolNegate folds — BOOL_AND/OR need deferred opFlipInPlaceExecute); `near_switch` (skip switch range-guards; detect via cached `f.jumptables` op_addr — robust after BRANCHIND folds to a plain terminator); `table_recovery_probe` flag on the build.rs:224 partial skips orientation during jump-table recovery (else under-recovers in-code tables — ifswitch). pointerrel test renamed→`pointerrel_negated_condition_normalizes`, asserts `fRam < fStack_18`.

DEFERRED (S3, only if a fixture needs): mechanism A opFlipInPlaceExecute/ActionNormalizeBranches (normal-form flip); compound BlockCondition::negateCondition (short-circuit distribution). orcompare confirmed a mechanism-A case (Ghidra IR = BOOL_OR(==,==) body-on-true, NOT a B-negation) — correctly left to print-time De Morgan.

## S1 COMPLETE — lead approved squash-merge (2026-07-08, flip1)
near_switch FAITHFULNESS VERIFIED: Ghidra `JumpBasic::foldInOneGuard` (jumptable.cc:1373, ends `guard.clear()`) FOLDS the switch range-guard INTO the switch (pushBranch / setDefaultBlock), it does NOT render it as an oriented `if`. ifswitch oracle confirms: outer `if (99 < param_1)` IS an oriented normal branch (mosura orients it — the win), the switch guard is folded (no separate if). So near_switch skipping the switch guard matches Ghidra's output — NOT dropping a case Ghidra keeps. (Ghidra's fold is even boolean_flip-aware, foldInOneGuard:1402, confirming the guard is switch-owned.)

LEAD RULING (merge-approved): the flag-XOR is a FAITHFUL TRANSLATION of Ghidra's edge-reversal into mosura's re-deriving structurer (Ghidra reverses edges to keep its persistent mutated tree consistent with boolean_flip; mosura re-derives so there's nothing to keep consistent — XOR is the equivalent). NOT a grandfathered adaptation. Documented in coverage.md (§1 ActionNormalizeBranches row = the divergence tied to backlog #8; RuleCondNegate row = the 4 known-limit gates with follow-ups; RuleIntLessEqual row = post-orientation-pool-not-@10 + incr_in_width-still-active = Task #2 unification).

REQUIRED-BEFORE-MERGE (all done): documented flag-XOR divergence→#8, 4 scope-gates as limits, RuleIntLessEqual/incr_in_width→#2. Squash-merge to master (drop WIP 834b1da edge-reversal), verify master 0.8882/336-0/switch-green, `git worktree remove` + delete branch. Task #1 stays in_progress (S3/mechanism-A may follow — lead re-scopes).

FOLLOW-UPS (open): mechanism-A opFlipInPlaceExecute (S3/#4), compound BlockCondition::negateCondition (block.cc:3023), RuleIntLessEqual@10-unification (#2), and the "Ghidra mutating mainloop structurer" rock (backlog #8) that would replace the XOR with real edge-reversal.

## MERGED to master `e0f2d96` (2026-07-08)
Squash-merge landed (S0 a211fff already on master; S1 = e0f2d96). Master VERIFIED: suite 336/0, switch canary green, corpus 0.8882/54-60 (reproduced). Worktree mosura-condnegate removed, branch task1-condnegate deleted. Task #1 stays in_progress (mechanism-A/S3 may follow — lead re-scopes). The 6 merged files: build.rs, funcdata.rs, pipeline.rs, structure.rs (bulk), tests/decompile_corpus.rs (pointerrel test), docs/coverage.md.

## RE-INSTRUMENT on master `0b04da8` (2026-07-11, condneg1) — keystone CORE landed; residue minor/entangled
Verified current state (NOT the 0.8882 the memory above records): suite 386/0, corpus **0.9195/56**. Keystone S0+S1 is fully on master and live — `ActionOrientBranches → condnegate_pool()` after types (pipeline.rs:420-421); condnegate_pool = {RuleCondNegate, RuleBoolNegate, RuleIntLessEqual} (pipeline.rs:452-454); structure.rs `ActionOrientBranches`/`block_negate_condition`/`collect_negations`. RuleIntLessEqual STILL HELD-unwired at slot-10 main pool (pipeline.rs:119) — only in condnegate_pool. So the brief's "land RuleCondNegate/RuleBoolNegate" = ALREADY DONE.

Instrument (oracle `--c`/`--ir -` vs `cargo run --example dumpc -- <stem>`) on the 3 deferred fixtures — **print-negation is no longer the dominant divergence:**
- **deindirect (0.857):** condition `if (param_1 < 10)` is byte-IDENTICAL to Ghidra already (mechanism-A does NOT diverge here). Gap = spurious explicit `uVar1 = param_3+5` + extra 3rd call arg (`uVar1, uVar1`) — merge/explicitness, unrelated to negation.
- **loopcomment (0.736) / elseif (0.904):** dominated by P7 (else-if flattening, for/while-loop recovery, `&&` split into nested ifs), spacebase stack-vars, spurious call args, casts.
- ONLY real negation residue = loopcomment compound `&&` leaf `0x65 <= param_2` vs Ghidra `100 < param_2`. IR-traced: Ghidra materializes the leaf negation (IR `#0x64 < ESI`); mosura `collect_negations` SKIPS compound `&&/||` leaves (structure.rs:227) → leaf un-oriented → global RuleIntLessEqual (condnegate_pool) converts raw `param_2<=100`→`param_2<101` → render_negated print-flips to `101<=param_2`. Fix = compound `BlockCondition::negateCondition` (block.cc:3023).

VERDICT: keystone IR-materialization core = DONE. Remaining pieces (mechanism-A opFlipInPlaceExecute @ structure.rs:202, compound @ structure.rs:227) are S1-scale architectural movers, corpus-neutral-to-minor + entangled with P7/spacebase (the actual fixture levers). Retiring render_negated/incr_in_width needs BOTH. Reported to lead 2026-07-11 with steer request.

★ STEER RESOLVED (lead+USER, 2026-07-11): **FINISH the faithful de-fuse** — port mechanism-A then compound per the TURNKEY PLAN below (lines 71-87), retiring render_negated/incr_in_width, completing the printc arc. User chose this over pivoting (corpus-neutral is fine — it's faithful cleanup that retires a non-faithful print-time adaptation + unblocks #9 + thins the print layer for a future Zig backend; the real corpus levers P7/spacebase come AFTER). condneg1 was REAPED mid-hold by the session limit (resets 10:40am Paris) — it never touched mechanism-A; grounding is warm here. FRESH RESET-SPAWN from the turnkey plan: mechanism-A FIRST (deindirect, self-contained) → gate per-fixture delta → then compound → then delete incr_in_width/render_negated comparison-flip. ★ CORPUS BASELINE TO BEAT = **0.9195 / 56-of-60** at master `0b04da8` (NOT the memory's older 0.8882). Same no-edge-reversal/flag discipline (edge reversal HANGS the re-deriving structurer). Gate = FAITHFULNESS (a faithful port lands regardless of corpus direction, per [[faithful-ports-land-not-held]]).

## ✅✅✅ PRINTC DE-FUSE ARC COMPLETE (defuse2, 2026-07-11) — 3 commits on master
Full faithful de-fuse LANDED (all lead-approved, corpus 0.9195→**0.9196**/56, suite 391/0, switch green):
1. **`bcf7b39` mechanism A** — `ActionPreferComplement` (if/else-scoped `BlockIf::preferComplement`) materializes the if/else normal-form flip in the IR. BYTE-IDENTICAL. (grounding correction: NOT global ActionNormalizeBranches — that would regress ifswitch).
2. **`aabfff7` compound** — `collect_negations`/`compound_leaves` orient every short-circuit leaf of a negated compound (Ghidra `BlockCondition::negateCondition`, all-or-nothing gate). loopcomment 0.736→0.745 (`0x65<=param_2`→`100<param_2`); all others byte-identical (nan gated out — nested BOOL_OR/NAN leaf not foldable).
3. **`c9f3321` final deletion** — retired `incr_in_width` + render_negated's `<=`/`<` reorder arms (audit proved DEAD post-compound; mosura-only reorder/increment shortcuts Ghidra never used). BYTE-IDENTICAL. KEPT: faithful `==`↔`!=` token flip (Ghidra negatetoken) + compound De Morgan + `!(...)` fallback.
Branch-negation now lives in the IR (mechanism B + A + compound all materialize). FOLLOW-UPS (coverage.md): (a) global `ActionNormalizeBranches` still deferred/near-inert; (b) compound negateCondition for the nested-BOOL_OR/NAN leaf (retires the all-or-nothing gate + print De Morgan) = nan's case; (c) RuleIntLessEqual @10-main-pool = Task #2/#9 (still blocked on the concat/condconst/condmulti regression). Task #9 unblock delivered — clear to reassess.

## ✅ MECHANISM A LANDED byte-identical — master `bcf7b39` (defuse2, 2026-07-11)
`ActionPreferComplement` (structure.rs) materializes the if/else normal-form flip in the IR, per the grounding correction below. New Funcdata methods: `op_flip_in_place_test`→(i32,fliplist), `op_flip_in_place_execute`, `flip_in_place_execute` (fallthru_true XOR only); `replace_lessequal` promoted pub(crate). rule_if_else now reads `is_oriented` (fallthru_true) → arm-swap + negated=false; removed print-time `op_flip_in_place_test`+`flip` vec+`if_else_flip`. Wired after condnegate_pool+deadcode (pipeline.rs). **BYTE-IDENTICAL: all 60 corpus fixtures unchanged (0.9195/56), suite 390/0 (+4 unit tests), switch canary green.** IR verified: deindirect block-0 now `INT_SLESS param_1 #0xa` (was `9<param_1`+print-flip). Self-approved (byte-identical lane).
## ✅ COMPOUND STAGED (defuse2, 2026-07-11) — awaiting lead go (MOVER); FINAL-DELETION now unblocked
COMPOUND port STAGED (NOT committed, base `bcf7b39`; files structure.rs+printc.rs+coverage.md, NOT git-added). Faithful `BlockCondition::negateCondition` (block.cc:3023): `collect_negations`/`compound_leaves` recurse a negated compound + orient EVERY short-circuit leaf CBRANCH ALL-OR-NOTHING (only if every leaf is condition_folds_cleanly + !near_switch — this gates nan out: its nested BOOL_OR/NAN leaf isn't foldable); `is_oriented` returns false for CondAnd/CondOr blocks (so the enclosing if's De Morgan `negated` isn't perturbed by leaf orientation); printc `operand_oriented` XORs each leaf's fallthru_true so a materialized leaf prints positive, connective re-derived by De Morgan. DELTA: **loopcomment 0.736→0.745** (leaf `0x65<=param_2`→`100<param_2`, materialized IR `INT_SLESS #0x64 param_2` = Ghidra `#0x64 < ESI` ✓); all 59 others byte-identical (elseif already matched; nan gated out). Corpus 0.9195→**0.9196**, 56/60, suite 390/0, switch green, **NO HANG** (flag-only). Reported to lead, WAITING for go.
★ FINAL DELETION now UNBLOCKED + BYTE-IDENTICAL: post-compound audit (DBG_RN probe) — render_negated's `<=`/`<` arms (IntLessequal/IntSlessequal/IntLess/IntSless) + incr_in_width are **DEAD** (no corpus fixture hits them). Only the `==`/`!=` arm stays live (orcompare, retspecial — body-on-TRUE compounds, correct De Morgan `!=`; Ghidra negatetoken flips `==`↔`!=` too — faithful). So the arc's FINAL step = delete incr_in_width + the `<=`/`<` render_negated arms = byte-identical dead-code removal (its own gated piece AFTER compound lands). Keep the `==`/`!=` arm + De Morgan + `!(...)` fallback. The global `ActionNormalizeBranches` stays deferred/near-inert.

COMPOUND read-only grounding (defuse2, 2026-07-11, held pending lead go): loopcomment leaf = `BlockCondition::negateCondition` (block.cc:3023: `getBlock(0/1)->negateCondition(false)` distributes NOT to each side + swaps AND↔OR + edge order; the compound normal-form flip is the separate `BlockCondition::flipInPlaceExecute` block.cc:3008). Ghidra `if (((param_1<10) && (100<param_2)) && (param_3==1000))`; mosura `if (((int4)param_1<10) && (0x65<=param_2)) { if (param_3==1000)` — TWO gaps: (a) the leaf FORM `0x65<=param_2` vs `100<param_2` (the negation IS the compound case — leaf body-on-false in the short-circuit, mosura defers to print-time De Morgan/render_negated, Ghidra distributes via negateCondition→RuleCondNegate materializes→`100<param_2`); (b) mosura recovers a 2-way `&&`+NESTED `if` where Ghidra FLATTENS a 3-way `&&` — a P7/short-circuit-recovery structuring gap, SEPARATE from negation and NOT this task. So compound negateCondition fixes only (a) on the recovered part; loopcomment stays dominated by P7 (a). Compound plan: extend `collect_negations` to also orient CondAnd/CondOr leaf CBRANCHes (the `collect_condition_cbranches` recursion flip1 built+removed at S1), set boolean_flip on each leaf (NO edge reversal), let RuleCondNegate+RuleIntLessEqual in condnegate_pool materialize+normalize, structure() re-derives the connective. Hang-guard: flag-only, watch s.blocks explosion on loopcomment.

## ★ GROUNDING CORRECTION (defuse2, 2026-07-11) — mechanism A = preferComplement (IfElse-scoped), NOT global NormalizeBranches
Read-only premise verification on master `0b04da8` (brief required it; the plan is from 2026-07-08). Instrumented deindirect/ifswitch/elseif via `oracle/capture --ir -` + `--c` + `dumpc` + DBG_FLIP eprintln. Findings that CORRECT the turnkey plan below:
1. **The IfElse normal-form flip is `BlockIf::preferComplement` (block.cc:3093), which is `getSize()!=3 → return false` = IFELSE-ONLY.** It does `split->flipInPlaceTest` → if 0: `split->flipInPlaceExecute()` + `data.opFlipInPlaceExecute(fliplist)` + `swapBlocks(1,2)`. `ActionPreferComplement` (blockaction.cc:2140) walks the structured tree calling it. This — NOT the global per-basic-block `ActionNormalizeBranches` (blockaction.cc:2117) — is what mosura's `if_else_flip`+arm-swap+render_negated already models. Porting the GLOBAL NormalizeBranches would flip ifswitch's `99<param_1` guard (op_flip_in_place_test==0) which Ghidra RENDERS unflipped → REGRESSION. So port **preferComplement scoped to FlowKind::IfElse**, mirroring the existing IfElse-only `flip` path. (NormalizeBranches is near-inert on the corpus — leave as a coverage row / follow-up.)
2. **`replaceLessequal` (funcdata_op.cc:1029) IS the IR-level `c+1` transform** (the faithful analogue of printc `incr_in_width`), called inside opFlipInPlaceExecute when a flip yields `<=` (SLESSEQUAL→SLESS with c+1, with sign/unsigned overflow guards). So opFlipInPlaceExecute alone materializes the final normal form — NO separate RuleIntLessEqual pass needed for the IfElse case. deindirect: mosura IR = `9 < param_1` (SLESS const-left) → opFlipInPlaceExecute → SLESSEQUAL(param_1,9) → replaceLessequal → `param_1 < 10` = Ghidra's IR, rendered directly.
3. Ghidra's PRINT-time negate (printc.cc:558 negatetoken → printlanguage.cc:549 `tok=tok->negate`) is a PURE TOKEN FLIP: `<`↔`>=`, `<=`↔`>`, `==`↔`!=` (printc.cc:129-134), NO operand-reorder, NO increment. mosura's render_negated REORDERS + increments — a non-faithful shortcut that happens to match Ghidra's *materialized* output. Ghidra avoids `>=`/`>` by materializing in IR, so negatetoken rarely fires.
4. **if_else_flip is LIVE (DBG_FLIP) on: deindirect, elseif(×11 blocks), forloop_varused, indproto, retstruct, union_datatype.** incr_in_width live on: deindirect, elseif, loopcomment, retstruct. render_negated `<=` path live on: elseif only. → **incr_in_width CANNOT be deleted in the mechanism-A stage** — loopcomment's incr fires in a COMPOUND/`&&`-leaf (non-IfElse) context, so it needs the compound port first. KEEP render_negated + incr_in_width through mechanism-A (IfElse routes negated=false → doesn't touch render_negated anyway); delete them only AFTER compound.

CONCRETE PLAN (grounded, ready): (a) `Funcdata::op_flip_in_place_test(op)->(i32,Vec<OpId>)` promote from structure.rs:36 building fliplist; (b) `Funcdata::replace_lessequal(op)->bool` port funcdata_op.cc:1029; (c) `Funcdata::op_flip_in_place_execute(&[OpId])` port funcdata_op.cc:1280 (get_booleanflip Some(Copy)=BOOL_NEGATE-removal via out.loneDescend / None=BOOL_AND↔OR / else set_opcode+swap_input+replace_lessequal); (d) `Funcdata::flip_in_place_execute(bid)` = XOR fallthru_true flag ONLY (no edge reversal); (e) `Structured::if_else_splits(f)->Vec<BlockId>` walk tree collecting IfElse split cbranch blocks; (f) `ActionPreferComplement` (structure.rs) — structure(data).if_else_splits → per split: op_flip_in_place_test, if 0 → op_flip_in_place_execute + flip_in_place_execute; guard `table_recovery_probe`; (g) RECONCILE rule_if_else: `let (then_c,else_c)= if is_oriented(b){(fc,tc)}else{(tc,fc)}; negated=false` — drop if_else_flip/`flip` vec; (h) wire after condnegate_pool+deadcode, add a deadcode after. EXPECT BYTE-IDENTICAL (materialization reproduces the print-flip output; verified by hand for deindirect: arms + `param_1<10` match). Verify the 6 if_else_flip fixtures + switch canary. Byte-identical → self-approve.

## MECHANISM-A + COMPOUND port (task #2 unblocker) — TURNKEY PLAN (flip1, 2026-07-08) [SUPERSEDED re: mechanism A — see GROUNDING CORRECTION above]
Retiring incr_in_width = porting the two deferred pieces. Both are S1-scale movers; use the SAME no-edge-reversal/flag discipline S1 established (edge reversal hangs the re-deriving structurer).

MECHANISM A (opFlipInPlaceExecute normal-form flip — unblocks deindirect + if_else/normal-form fixtures):
- Ghidra: ActionNormalizeBranches (blockaction.cc:2117) per 2-out CBRANCH block: if `opFlipInPlaceTest(cbranch)==0` → `opFlipInPlaceExecute(fliplist)` (funcdata_op.cc:1280) + `bb->flipInPlaceExecute()` (flip fallthru_true + edge order, NO boolean_flip).
- opFlipInPlaceTest (funcdata_op.cc:1221) BUILDS a fliplist (the ops to flip); mosura's `op_flip_in_place_test` (structure.rs:36) only returns 0/1/2 — promote to a Funcdata method returning (result, fliplist).
- opFlipInPlaceExecute: per fliplist op, get_booleanflip(opcode) [EXISTS opcode.rs:157] → Some(Copy)=remove BOOL_NEGATE (propagate in(0) to lone descendant + op_destroy); None=BOOL_AND↔BOOL_OR swap; else set opcode + op_swap_input if flipyes + replace_lessequal [EXISTS] if result LESSEQUAL. All op mutators EXIST.
- flipInPlaceExecute equivalent: flip fallthru_true (flag), NO edge reversal (S1 discipline).
- RECONCILE structure.rs if_else: rule_if_else currently sets `negated = flip` (if_else_flip = opFlipInPlaceTest==0) and swaps arms; render_condition then print-negates via render_negated (+ incr_in_width). After materializing: the condition is positive in the IR, so if_else must read the fallthru_true flag (like mechanism-B is_oriented) instead of recomputing `flip`, set negated=false, and keep the arm-swap. Then DELETE render_negated's ==/<=/< comparison-flip + incr_in_width (now materialized in IR). This is the printc rework S1 avoided — mechanism A is where it actually lands.
- PLACEMENT: a new ActionNormalizeBranches after ActionOrientBranches in the late stage (Ghidra runs NormalizeBranches at the very end). Watch: it also flips SIMPLE non-negated `c<x` guards to normal form — verify against oracle per fixture.

COMPOUND (BlockCondition::negateCondition, block.cc:3023 — unblocks loopcomment/elseif's `&&`-leaf `<`): distribute the NOT to each short-circuit leaf (negate each leaf's CBRANCH) + flip the connective. In mosura terms: orient each CondAnd/CondOr leaf (set boolean_flip via the S1 branch_negations recursion I built then REMOVED — `collect_condition_cbranches`, recover it) so RuleCondNegate materializes each leaf, and structure() re-derives the flipped connective. Then RuleIntLessEqual normalizes the leaf `<=`. Risk: the S1 hang was short-circuit edge-reversal; with the flag approach (no edge reversal) re-verify loopcomment doesn't hang.

AFTER BOTH: delete incr_in_width + render_negated's comparison-flip; the 3 fixtures (deindirect/loopcomment/elseif) render `< c+1` from the IR = Ghidra. Gate: mover, worktree, per-fixture delta (expect deindirect/loopcomment/elseif neutral-or-up, watch for new normal-form flips on other fixtures). Corpus baseline to beat: 0.8882.

SESSION NOTE (flip1): reached this boundary DEEP (S0+S1 build/hang-marathon/merge + #2 instrument). Mechanism A is S1-scale with structuring reconciliation — a marathon-risk to start deep. Recommended a fresh run from this turnkey plan (lead's call). If continuing same-agent, do mechanism A FIRST (deindirect, self-contained), gate, then compound.

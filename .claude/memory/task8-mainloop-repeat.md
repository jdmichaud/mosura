---
name: task8-mainloop-repeat
description: "Task #8 — port Ghidra's iterating mainloop. A+B+C0+C1+D+consume+C2+refn1-Stage1 `c4d0820`+F `554620e` ALL LANDED. **rds1 2026-07-17 paydown ✅ COMPLETE (★★★★★★★ section): RuleDoubleLoad+RuleDoubleStore LANDED `1bde3dd` byte-identical; the DoubleStore-produces-concatsplit premise FALSIFIED — real mechanism = LaneDivide over the unique-rooted reload web (lanedMap matches by SIZE ONLY, all spaces); the faithful collect fix LANDED `568d73c` (0.9326→0.9355/57, concatsplit→0.978 + stackstring→0.933 both Ghidra-convergent, stackstring cadence dip healed).** E park-eligible."
metadata:
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

## ★★ CAMPAIGN MAP (ml8a grounding @`2993771`, 2026-07-16 — USER-APPROVED campaign; gate one brick at a time)
**Corrected framing: NOT "build one big loop" — the loop nucleus + re-entry correctness ALREADY LANDED (S8-1 `d97bd90`, S8-2 `32648d3`/`b1c0dfa`/`25cb50b`, growth `390effc`). The S8-2 refinement-on-re-entry lesson is a SOLVED arc.** Ghidra structure (verify at coreaction.cc:5462-5690): `universal`=ActionRestartGroup(maxrestarts=1) ⊃ `actfullloop`(repeatapply) ⊃ `actmainloop`(repeatapply, :5490-5676, 28 members — full audited table in ml8a's report). Fixpoint = the `count` member (apply return = 0/breakpoint only; perform loops while lcount<count, action.cc:298/506). **⭐ Load-bearing two-phase: fullloop round 1 runs the mainloop TYPELESS (InferTypes/ConstantPtr/actprop2 gated on `hasTypeRecoveryStarted`); `ActionStartTypes` (:5687, hh:74-86) flips the flag once (count+=1) → round 2 re-runs everything TYPED.** mosura's 3-unrolled-sweeps + late reheritage restart = a hand-unrolled approximation of exactly this.
**Return-convention audit (ALL impl Action sites): FOUR more return-1 mis-ports** — ActionResolveCalls (pipeline.rs:100; Ghidra = trial-pass counts :1748/1756/1933/1951), ActionSwitchNorm (:115, would infinite-loop actfullloop), ActionMarkAddrTied (:305 → 0, analysis convention), ActionActiveReturn (:337). ActionConsume (:452) OWNED by the parked consume patch — don't double-fix. **TRAP: ActionHeritage returns 1-per-pass DELIBERATELY** (stand-in for the missing ParamActive maxpass=maxdelay driver, fspec.cc:5331-5338) — do NOT fix to Ghidra's 0 before the trial protocol exists or stack heritage stops being driven. mosura's ActionGroup lacks `rule_onceperfunc` (Ghidra action.cc:349-357) — LaneDivide needs it before joining a loop.
**BRICKS (land order):**
- **A** — the 4 return-convention fixes + `rule_onceperfunc` machinery (dormant) + unit test. Byte-identical → self-approve lane.
- **B** — tail join: append DeterminedBranch → CondConst to the reheritage restart (Ghidra :5672-5676; both audited monotone/idempotent-safe; mloop3 precedent). Gated, expected byte-identical-to-small. Unlocks #22-B ordering.
- **C — THE cadence brick (riskiest, gated mover, every fixture re-flows):** port `hasTypeRecoveryStarted` (funcdata.hh:150/176) + `ActionStartTypes` + gates; replace the 3 unrolled sweeps with ONE mainloop-restart body {spacebase→nzmask→consume→infertypes→oppool1→deadcode→ptrarith(actprop2)→heritage→deadcode→determinedbranch→unreachable→condconst} run typeless→StartTypes→typed. **Probe transiently BEFORE landing: apply brick1-consume-default.patch → piecestruct_folds_shifts_to_concat green? = the unpark test.** Park-risk real.
- **Payback (not a brick): land the consume patch** on the C-advanced base (piecestruct FIRST) → then re-ground switchloop residual C.
- **D** — #22-B Brick 2 (cancel recover_stack + per-iteration alias recompute; faithful shape = ActionRestructureVarnode `aliasyes=numpass!=0` coreaction.cc:2278) — ~20-fixture atomic mover, activates dormant `41ab722`.
- **E** — heritage/trials interleave (ParamActive pass protocol, P6-coupled) — PARK-ELIGIBLE, no in-scope fixture demands it.
- **F** — LaneDivide → stackstall slot (post-oppool1 in-loop, needs A's onceperfunc; :5652) — gated on D; payback concatsplit/stackstring.
**Known-omissions filed NOT scheduled:** restart_pending/clearAnalysis + bumpDeadcodeDelay (heritage.cc:2571; tripwire = heritage-after-dead-removal corruption); refinement partition on stack ranges (piecestruct refineInput hypothesis FALSIFIED — see [[consume-model-misport]]); ~20 absent mainloop members (each its own port task; RestructureVarnode = #22-B's alias shape, StackPtrFlow = recover_stack's faithful replacement, Deindirect/ConstantPtr/NodeJoin/etc.). **Validation every brick:** transient probe first, per-fixture name-keyed join (`> f 2>&1`, NOT `2>&1 > f`), full suite summed, perf wall, movers gated.

## ★ BRICKS A+B LANDED + C-PREMISE INVENTORY (ml8a 2026-07-16, @`323c647`, tree clean — the C builder STARTS HERE COLD, fresh full-budget run)
**A ✅ LANDED `8c08e48`** (byte-identical, self-approved per GO): the 4 return-convention fixes — ActionResolveCalls → faithful trial-protocol counts (resolve_return/resolve_call_args now return Ghidra's coreaction.cc:1933/1951 + :1748/1756 counts; resolve_call_args doc flags the Brick-E prereq: Ghidra's `isInputActive` never re-inits a cleared container, mosura's setup re-creates → loop-join needs that gate); ActionSwitchNorm → +1 per folded table gated on `jt.normalized` (= Ghidra `!jt->isLabelled()` :4551-4557; normalize_one → bool); ActionActiveReturn → +1 per committed call output (:1788, `output.is_some()` skip = cleared isOutputActive); ActionMarkAddrTied → 0 (analysis convention, hh:300). Plus **`OncePerFunc<A>` wrapper in action.rs** (rule_onceperfunc, action.cc:349-357, DORMANT — LaneDivide's loop-join key) + 2 unit tests. Suite 418→420/0, corpus per-fixture identical, clippy 79==79.
**B ✅ LANDED `323c647`** (lead gate-confirmed): `determinedbranch → condconst` INSIDE the reheritage restart **between ptrarith_pool and ActionHeritage** = Ghidra's actmainloop cycle rotated to mosura's spacebase entry (actprop2 :5666-5669 → :5672 → :5673-inlined-in-mosura's-determinedbranch → :5676 → wrap :5492/:5503); early once-instances kept (= iteration 1); NodeJoin/CondExe join when ported. Convergence (transient env-probe, reverted): 72 group-runs = 26×1/40×2/6×3 iters, 0 cap-hits. Corpus per-fixture BYTE-IDENTICAL (0.9286/56 @323c647), suite 420/0. **= the #22-B ordering enabler — load-bearing when Brick D cancels recover_stack.**
**Baselines @`323c647`:** avg 0.9286, 56/60, suite 420/0; switchloop 0.837, piecestruct 1.000, concatsplit 0.863, floatcast 0.880, varcross 0.915, revisit 0.677, condmulti 0.806, elseif 0.915, switchmulti 0.586, stackstring 0.899. brick1-consume-default.patch apply-check was CLEAN @2993771 (pre-A/B — recheck, see conflict warning below).

## ★★★★ BRICK D ✅✅ LANDED — D0 `a76d4fb` + D1 `47d082f` (ml8d 2026-07-17, lead GO; full detail in [[task22-typespacebase-campaign]] Brick-D section). Post-land: 0.9345/57 @47d082f, suite 420/0, jumptable 6/6, tree clean.
D0 (addrtied-at-creation funcdata_varnode.cc:162/database.cc:1263 + sync-side mark_addrtied + alias PTRSUB/PTRADD/deref arms) → 0.9294→0.9336, 2 movers both Ghidra-convergent (revisit +0.170 = the C1 P6 void-return residual FIXED, noforloop_iterused +0.085). D1 (cancel recover_stack general LOAD/STORE, keep retaddr; ActionRestructureVarnode{aliasyes=numpass!=0} coreaction.cc:2274 as mainloop first member) → 0.9345, suite 420/0, jumptable 6/6, perf flat, converges. Probe's 20-mover blast COLLAPSED (forloop family holds 1.000, no wrong code). varcross→0.936 oracle-exact (`*pVar1=0`/+1/[6] — C1's 3-lines-away REVERSED; RulePtraddUndo follow-on may be moot). Sole regression wayoffarray −0.106 = faithful-exposes: missing isUnmappedUnaliased param-region carve-out (varmap.cc:494, #22-C/P6 family) — filed.
**CONSUME BRICK 1 ✅ LANDED `608d7d3`** (piecestruct-first protocol: hard test green FIRST; suite 420/0 with the 24 test hunks; corpus **0.9347/57 @608d7d3**, ONE mover switchloop 0.837→**0.851** = the case-7 wrong-code class CLOSED — the standing landmine is retired). **NEXT WAS Brick F + C2 — BOTH BUILT by ml8f 2026-07-17, see the ★★★★★ section (F recommended-park on wrong-code; C2 byte-identical, held for GO); E park-eligible. Also filed: isUnmappedUnaliased param-region carve-out (wayoffarray payback, varmap.cc:494) + switchloop residual C re-ground ([[switchloop-dup-propagatecopy]] merge/naming class, now unblocked).**

## ★★★★★★★ RDS1 DOUBLESTORE PAYDOWN ✅ COMPLETE (2026-07-17, base `554620e`): PREMISE FALSIFIED → RE-FRAMED; RULES LANDED `1bde3dd` (byte-identical, self-approved); THE REAL concatsplit FIX **LANDED `568d73c` (lead GO)** — post-land verified: corpus 0.9355/57 @568d73c self-dated, per-fixture == gated measurement, suite 428/0, jumptable 6/6, must-holds hold, tree clean, patch deleted. stackstring's E-adjacent cadence classification is MOOT (healed).
**PREMISE FALSIFIED (trace-proven):** RuleDoubleStore/RuleDoubleLoad do NOT produce Ghidra's concatsplit render — capture_trace over ALL 79 datatests: ZERO firings of either rule anywhere; of the double family only RuleDoubleOut fires (revisit ×1: `CONCAT22(r76:2(i),r74:2(i))`→whole-input combine via combineInputVarnodes; doublemove = MIPS, out of corpus). The refn1/F-unpark notes' "two 8B stores come from RuleDoubleStore" was WRONG-DIRECTION: RuleDoubleStore COMBINES 2 stores→1 (gated isPrecisLo/isPrecisHi, double.cc:3520) — it would produce mosura's old 16B render, not Ghidra's split.
**THE REAL MECHANISM (trace DEBUG 162):** Ghidra's two 8B stores come from ActionLaneDivide splitting the RELOAD web — rooted at a *unique* (`u0xd700:16 = LOAD; STORE` — the XMM copies fold before the stackstall slot). Reachable because `Architecture::getLanedRegister(loc,size)` matches by SIZE ONLY (loc unused, architecture.cc:290) and `checkForLanedRegister` runs on EVERY varnode creation incl. newUnique (funcdata_varnode.cc:91/113/137/160; newConstant excluded) → the lanedMap holds unique/ram/stack storages too. mosura's `collect_laned_accesses` filtered to REGISTER SPACE only = the mis-port.
**LANDED `1bde3dd` (byte-identical, self-approve lane):** RuleDoubleLoad+RuleDoubleStore faithful port (new double.rs: double.cc:3370-3660 + adjacentOffsets/testContiguousPointers :713/:755), oppool1 tail slot (coreaction.cc:5643-5644). DoubleStore dormant until a PRECISLO/PRECISHI marker port (ActionParamDouble/SplitVarnode/splitPieces — unported). Per-fixture join 60/60 identical, dump_all byte-diff empty, suite 427/0, jumptable 6/6, clippy 79==79.
**RuleDoubleIn/RuleDoubleOut FOLLOW-ON — PREMISE DONE (rds1, turnkey for a fresh builder):** (a) RuleDoubleIn (double.cc:3204-3280) = attemptMarking (truncate-exactly-half precis marking) + phase-2 `SplitVarnode::applyRuleIn` = the ~2000-line double-precision op battery — ITS OWN LARGE PORT; fires ZERO times corpus-wide (even marking), fully dormant. (b) RuleDoubleOut (double.cc:3286-3355) = bounded (~250 lines): attemptMarking (symbol-entry equality + arithmetic/FP-whole-reader test) + isAddrTiedContiguous + `Funcdata::combineInputVarnodes` (funcdata_varnode.cc:381-455 — read, bounded; needs op_unset_input + an arithmetic/FP OpCode classifier + set_precis_lo/hi setters; total_replace/op_insert_begin/set_input_varnode exist). Fires 1× on revisit in Ghidra (`PIECE(r76:2(i),r74:2(i))`→whole-input combine) — **but mosura NEVER forms that shape (probe: `r0x100076` = 0 hits in mosura's whole revisit trace; mosura's cadence reads r74 whole from the start) → the port is corpus-DORMANT, byte-identical expected, self-approve lane; no revisit payback** (revisit residual = P6 void-return + P4 cast, unrelated). Value = faithfulness completeness + it lands the precis setters + combineInputVarnodes that RuleDoubleIn/applyRuleIn and live-DoubleStore need.
**Piece A LANDED `568d73c` (lead GO):** collect_laned_accesses → ALL spaces (constants excluded), dedup+iterate in storage order (the map's key order); lanedivide.rs only, +59/−10 incl. 1 unit test (unique-rooted LOAD/STORE web). MEASURED (gated @1bde3dd-dirty == post-land @568d73c): avg 0.9326→**0.9355**, 57/60, 62/62; movers BOTH UP, BOTH Ghidra-convergent, dump_all diff = exactly these 2: **concatsplit 0.881→0.978** (two 8B CONCAT44 stores at +0x14/+0x1c = oracle-exact statements; residual 0.022 = P6 unused-param_2 + P4 `xunknown8*`-vs-`uint8*` cast) + **stackstring 0.857→0.933** (the F-landing's score-only dip HEALED: two 8B quad locals `xStack_28/xStack_20`, `&xStack_28`/`&xStack_20+4` args = Ghidra's exact shape; residual = statement order + one `(int8)` cast). Must-holds ALL hold (floatprint/piecestruct 1.000, floatcast 0.880, switchloop 0.851, varcross 0.936, revisit 0.894, wayoffarray 0.894). Suite 428/0, jumptable 6/6, perf flat (2.35-2.41s), clippy 79==79.

## ★★★★★★ REFN1 (2026-07-17, base `1a405b0`): STAGE-1 REFINEMENT BRICK + STAGE-2 F — BOTH BUILT+MEASURED, HELD FOR GATE (nothing committed)
**STAGE 1 — the re-entry refinement partition (the F-unpark brick + a live wrong-code fix) — UNCOMMITTED IN THE WORKING TREE (heritage.rs +465/−4 + coverage.md row):**
- `refine_ranges(f, dom, pass)` (heritage.rs, before refine_overlaps): faithful `Heritage::refinement` (heritage.cc:1890 from placeMultiequals :2608-2616, gate `size>4 && max_write<size`, ≤1024) — buildRefinement boundary array over free reads + non-narrow-marker writes → partition → remove13Refinement → refineRead (CONCAT chain into a unique, refine_overlaps' pattern) / refineWrite (unique temp + per-piece SUBPIECEs + totalReplace descend-rewire) / input-like reads (no dominating write) kept whole (the landed refineInput realization, mixfloatint-class). Hooked FIRST in heritage_pass's re-entry block (Ghidra order refine :2611 → markers :2627 → normalize :2629); ranges = the shared widening_ranges merged map; narrow-marker writes excluded per collect :329-333 (removevars domain). No disjoint/globaldisjoint surgery needed — refinement precedes gather_candidates, only piece locations recorded. 3 unit tests ([8,8]-of-16 concatsplit shape + initial-pass dormancy; max_write==size skip; [4,1,3]→[4,4] remove13).
- **⚠ SPEC CORRECTION (probe-proven): scope = RE-ENTRY (`pass > space delay`), NOT the widened set.** The F-UNPARK note said "widening-scoped", but the transient probe showed concatsplit's mixed-width range materializes FRESH at pass 2 (prior=None — the merged (s-0x10, 8) range with 2×4B writes + 1×8B read never overlaps prior heritage), so `widened` never fires on it. `pass > delay` keeps pass 0 (registers+batch) and pass 1 (stack/ram initial) untouched → byte-identical outside mid-mainloop materializations.
- MEASURED @1a405b0-dirty: avg **0.9347→0.9355**, 57/60 held; per-fixture join: **concatsplit 0.930→0.978 the ONLY mover**; lane-aware dump_all byte-diff across all 62 = concatsplit only. **The baseline wrong code is HEALED**: before = `xStack_10` read-never-written (`*(param_1+0x1c) = xStack_10`, param_5/param_6 gone from the signature — CONCAT44(param_6,param_5) lost); after = `*(param_1+0x1c) = CONCAT44(param_6,param_5)`, oracle-exact statement lines. Residual 0.022 = pre-existing P6 (Ghidra's unused `param_2` in the signature) + P4 (`xunknown8*` vs `uint8*` cast). Suite **423/0** (420 + the 3 new tests; piecestruct hard test green), jumptable 6/6, clippy 79==79, corpus wall flat (1.97-2.35s vs 2.03-2.33s), 62/62 decompile.
**STAGE 2 — F re-probe: the original patch was STILL wrong-code on the Stage-1 base → root TRACED → patch REGENERATED with Ghidra's actstackstall group → wrong-code chain CUT:**
- Original flat-member patch on Stage-1: concatsplit 0.978→0.825, `CONCAT88(xStack_10, CONCAT44(param_4,param_3))` — the HIGH lane still read-never-written. TRACE ROOT: LaneDivide's buildStore mints new pointer arithmetic (base+8) AFTER oppool already ran that iteration → ptrarith converts only the low lane's STORE (+ the 16B LOAD) → heritage pass 2 refines a HALF-materialized range ([8,8] from one write + the read; high piece → input xStack_10) → the high STORE's pointer folds NEXT iteration (collectterms/addmultcollapse) and converts one heritage pass too late (trace DEBUG 209) → its write is intersect-2/no-free, never links, dead-coded. **The missing faithful piece: Ghidra's `actstackstall` is a rule_repeatapply GROUP (coreaction.cc:5509) {oppool1 :5651, ActionLaneDivide :5652, +MultiCse/ShadowVar/Deindirect/StackPtrFlow :5653-5656 unported} — after LaneDivide splits, the group REPEATS and oppool1 folds the new arithmetic BEFORE quiescence, so actprop2 converts ALL lane stores in one sweep and heritage sees the complete access set.**
- FIX (in the regenerated `brickF-lanedivide-stackstall.patch`, pipeline.rs only, applies on the Stage-1 tree — roundtrip-verified): `restart("stackstall"){ default_rule_pool, OncePerFunc(ActionLaneDivide) }` replacing the two flat members. concatsplit → `*(param_1+0x14) = CONCAT88(CONCAT44(param_6,param_5),CONCAT44(param_4,param_3))` — CORRECT code, one 16-byte store. (The "Ghidra's two 8B stores come from RuleDoubleStore/RuleDoubleLoad" attribution here was **FALSIFIED by rds1 2026-07-17** — see the ★★★★★★★ section: the split comes from LaneDivide's unique-storage lanedMap reach, not the double rules.)
- MEASURED @1a405b0-dirty (Stage-1 + regenerated F): avg **0.9355→0.9326**, 57/60 held; movers vs Stage-1: **concatsplit 0.978→0.881 (correct code, score = the DoubleStore render gap)**, **stackstring 0.933→0.857 (score-only, ml8f's Brick-E-adjacent heritage-cadence class re-confirmed: mosura's one 16B `xRam..250:16` copy == Ghidra's two 8B quads byte-for-byte; render = `axStack_28[0] = xRam..250` + array-indexed args)**. Must-holds ALL hold: floatprint/piecestruct 1.000, floatcast 0.880, switchloop 0.851, varcross 0.936, revisit 0.894, wayoffarray 0.894. dump_all byte-diff = the 2 movers only. Suite 423/0, jumptable 6/6, clippy 79==79, wall flat, 62/62.
**LAND ORDER (on lead GO): commit Stage-1 (tree as-is) → apply+commit the F patch.** Tree state at report: Stage-1 uncommitted (heritage.rs + coverage.md), F parked as the regenerated patch.

## ★★★★★ BRICKS F + C2 (ml8f 2026-07-17): **C2 ✅ LANDED `1a405b0` (lead GO)** — post-land verified: corpus 0.9347/57 @1a405b0 per-fixture == baseline, suite 420/0, jumptable 6/6, tree clean. **F was PARKED TURNKEY here; superseded by the ★★★★★★ refn1 section above (the parked patch's flat-member wiring was itself the wrong-code root — the regenerated patch carries the faithful stackstall group).** brickC2-fullloop-tail.patch now redundant (the commit is the record).

### F-UNPARK GROUNDING (ml8f read-only unit, Ghidra heritage.cc:1704-1948 + 2599-2645 vs heritage.rs — VERDICT: BOUNDED, helpers already ported, the gap is SCOPE not machinery)
- **What Ghidra's refinement does on concatsplit's range:** `placeMultiequals` per merged range, EVERY pass: collect → gate `size>4 && max_write<size` (:2610) → `refinement` (:1890): boundary array from ALL reads+writes+inputs (`buildRefinement` :1704), boundaries→partition sizes, `remove13Refinement` (:1857, 1-3/3-1→4), then **`refineRead`** (:1772 — split each FREE read into pieces + `concatPieces` into a unique, re-point the lone descend; deletes the wide read), **`refineWrite`** (:1806 — def output→unique + SUBPIECE-defined pieces + totalReplace), **`refineInput`** (:1836 — pieces + setWriteMask), and REWRITES disjoint+globaldisjoint (erase wide, insert pieces at same pass) → re-collect → removeRevisitedMarkers (:2627) → guard/normalize (:2629). On concatsplit's s-0x18 range {writes 8+8, read 16} the partition = [8,8]: the 16B re-load becomes `CONCAT(s-0x10:8, s-0x18:8)`, each piece heritages against its matching lane write → linked → forwarding; the wrong-code chain is cut at its root.
- **mosura inventory (heritage.rs): the partition machinery IS ALREADY PORTED but triple-restricted.** `split_by_refinement` (:671) + `remove13_refinement` (:645) = faithful helpers; `refine_overlaps` (:794) implements buildRefinement/boundary→sizes/refineRead-CONCAT (+ the refineInput keep-whole semantics via a dominating-write test) + a write-SUBPIECE arm — but gated `sp == reg` (register space ONLY — stack never considered) + `is_laned(base)` (XMM offsets only) + pass-0-only call site (heritage_pass :1302). Its own comment names the withheld scope: "the broad GP partition is what explodes the rule pool." `normalize_ranges`/`remove_revisited_markers` SKIP refine-domain ranges (`is_refine_range`, :478/:576) = the S8-1 deliberate no-op that concatsplit's chain falls into.
- **THE PORT (the F-unpark brick): a `refine_ranges(f, pass)` on the WIDENING path** (beside remove_revisited_markers/normalize_ranges, sharing `widening_ranges` — Ghidra order: refine FIRST :2611, then markers :2627, then normalize :2629), all-space (stack included), for widened refine-domain ranges: partition + refineRead/refineWrite/refineInput arms (reuse refine_overlaps' CONCAT pattern + normalize_write_size's PIECE building + new_unique). After partitioning, the pieces are no longer refine-domain → the existing is_refine_range skips never fire on them (keep as >1024/failed-refine guard). Rewriting the wide accesses BEFORE gather records them keeps globaldisjoint clean (mosura analog of Ghidra's disjoint rewrite). **~150-250 lines + tests = the S8-1/S8-2 brick size class, NOT deep.**
- **Scope options mirror S8-1 A/B: (B) re-entry/widening-scoped SUFFICES for F-unpark** — concatsplit's mixed-width range only materializes at the mid-mainloop re-heritage (in-pool RuleLoad/StoreVarnode conversions), pass 0 untouched → contained mover. (A) = also retire refine_overlaps' laned-only restriction + pass-0 batch → the named rule-pool-explosion risk, its own later gated brick.
- **⭐ It also covers the PRE-EXISTING baseline one-lane wrong-code** (`xStack_10` read-never-written = CONCAT44(param_6,param_5) lost): same class, same re-entry pass, arises at the CURRENT pre-pool LaneDivide slot too → the refinement brick is a GATED MOVER ON ITS OWN (expect concatsplit 0.930→up + the baseline wrong-code healed). **LAND ORDER: refinement brick first (fixes live wrong code on master), THEN re-probe/re-land F.**
- Residual named port for oracle-exactness: after refinement, concatsplit stores ONE 16-byte `CONCAT88(...)` through param_1 (correct code). (The RuleDoubleStore attribution for Ghidra's two 8B stores was **FALSIFIED by rds1 2026-07-17**, ★★★★★★★ section — real mechanism = LaneDivide over the unique-rooted reload web via the size-only lanedMap match; the double rules landed anyway `1bde3dd`, byte-identical.)
- Interaction check: composes cleanly with removeRevisitedMarkers/normalize_ranges (Ghidra sequences them in the same placeMultiequals body; the shared widening_ranges extraction is the natural hook); no overlap with the landed D1 alias machinery (the alias-clear was only ever the executioner of the unlinked lanes — with linked pieces there is nothing unaliased-dead to clear).

--- pre-land gate record (as reported) ---
**C2 = the fullloop tail join — `brickC2-fullloop-tail.patch` (58 lines, pipeline.rs ONLY). BYTE-IDENTICAL → self-approve-lane quality, held only because the brief said lead-confirms.**
- The wire: fullloop = `restart("fullloop"){ mainloop, DeadCode(:5682), SwitchNorm(:5684), StartTypes(:5687), ActiveReturn(:5688) }` (Ghidra actfullloop tail coreaction.cc:5678-5689, mosura-present members at Ghidra's order); RETIRED the linear post-fullloop ActiveReturn/SwitchNorm/DeadCode (SwitchNorm's cleanup sweep is subsumed: a fold counts a change → the fullloop repeats → the next round's dead-code members clean the orphaned address code). Absent-member ports noted in the wire comment: LikelyTrash :5679, DirectWrite×2 :5680-5681, DoNothing :5683, ReturnSplit :5685, UnjustifiedParams :5686 — each joins at its slot with its own port.
- Premise verified in source: `normalize_one` sets `jt.normalized` (jumpbasic.rs:1242) → +1 at most once per table (failures return 0, don't drive the loop); `resolve_call_output` skips `output.is_some()` (recover.rs:845); StartTypes true-once.
- Convergence (transient MOSURA_LOOPDBG cap-200 probe in ActionGroup::apply, REVERTED): **0 cap-hits**; fullloop 44×2/28×3 rounds (C1-era was 46×2/26×3 — two functions take one extra round since tail members now count inside); mainloop 72×1/41×2/57×3/2×4.
- Verified: **lane-aware dump_all byte-diff vs baseline EMPTY (all 62)**; per-fixture join 0 movers, avg 0.9347/57 @608d7d3-dirty == baseline; suite 420/0; jumptable 6/6; perf flat (2.27-2.43s vs 2.31-2.53s); clippy 108==108. LAND: apply patch → commit pipeline.rs (msg ≈ "decompile: join the actfullloop tail — DeadCode/SwitchNorm/StartTypes/ActiveReturn into the fullloop (task #8 C2)").
**F = LaneDivide → stackstall slot — `brickF-lanedivide-stackstall.patch` (52 lines, pipeline.rs ONLY: pre-pool once-slot → `OncePerFunc<ActionLaneDivide>` in the mainloop body directly after default_rule_pool = Ghidra actstackstall coreaction.cc:5651-5652). RECOMMENDED PARK (wrong-code exception) — the port is FAITHFUL and the premise CONFIRMED, but it widens a wrong-code class:**
- PREMISE CONFIRMED both sides (transient MOSURA_LANEDBG probe + `oracle/capture --ir lanedivide`): old pre-pool slot on concatsplit = spurious live 4-byte float4 call-arg SUBPIECEs → XMM1/2/3 candidates [4,8]/[4] split at 4, XMM0 splits 4 in mode-1; new slot = Ghidra's EXACT shape — one live laned varnode (XMM0:16=CONCAT88), candidates=[8], splits 8. Ghidra's own slot IR: XMM0-3_Qa all pre-narrowed by oppool1, zero 4-byte SUBPIECEs survive.
- MEASURE @608d7d3-dirty: avg 0.9347→0.9300 (−0.0047), 57/60 held, suite 420/0 (no hard-test break). Movers: **concatsplit 0.930→0.722**, **stackstring 0.933→0.857**; floatprint/piecestruct HOLD 1.000, floatcast 0.880, switchloop 0.851 hold.
- **⚠ WRONG-CODE (the park reason):** concatsplit final IR = `STORE(param_1+0x14, s-0x18:16)` with NO writes to s-0x18 — both 8-byte lane stores DIE → uninitialized stack stored through param_1. Chain: faithful split-at-8 → buildStore 2×8B STOREs → RuleStoreVarnode stack writes s-0x18:8/s-0x10:8 → the 16-byte re-load converts to a 16-byte stack read → mixed widths = Ghidra's REFINEMENT domain (`placeMultiequals` size>4 && max_write<size, heritage.cc:2610) which mosura keeps a deliberate no-op (the filed known-omission "refinement partition on stack ranges") → read never links to the writes → D1's per-iteration alias recompute classifies the write-only lanes unaliased → addrtied cleared → dead-coded. NOTE the class PRE-EXISTS at baseline: old-slot concatsplit ALREADY loses one lane (`xStack_10` read-never-written = CONCAT44(param_6,param_5)); the move widens it to both lanes. **UNPARK GATE = port the stack-range refinement partition (heritage.cc:2608-2629), then re-probe F.**
- stackstring −0.076 = the SECOND exposed adaptation: mosura's up-front `restart("heritage")` completes ALL passes pre-loop → the ram read r0x100250:16 is heritage-known at the slot → RulePropagateCopy folds XMM0 into the STORE → nothing left to split. Ghidra's per-iteration heritage hasn't run the ram pass when lanedivide (once, iter 1) fires — its slot IR shows `XMM0 = r0x00100250:10(free)` — so XMM0 stays live and splits. Exposed = the heritage-pass cadence (pre-loop completion vs in-loop passes; Brick-E-adjacent). Score-only (not wrong code) → does NOT block re-land once the refinement piece clears the concatsplit chain.
- TOOLING (kept, untracked): `examples/dump_all.rs` was lane-BLIND (`Spec::from_sla` — a byte-diff through it MISSES LaneDivide-dependent movement); switched to `speccache::get`. `examples/dump.rs` is still lane-blind (probe reverted) — use dumpc for lane-live dumps.

## ★★★ BRICK C ✅✅ LANDED (ml8c 2026-07-16): C0 `52cac08` (byte-identical, self-approved) + **C1 `8a5fdc3` (lead GATE-CONFIRMED GO)**. Post-land verified @8a5fdc3: suite 420/0, jumptable 6/6, corpus 0.9294/57 (per-fixture == gated measurement), tree clean. brickC1-fullloop.patch DELETED (redundant — the landed commit is the record).
**C0 `52cac08`** — the StartTypes machinery, semantics-preserving: `Funcdata::start_type_recovery` (faithful funcdata.cc:182-188, returns true exactly once; RETIRED the `set_type_recovery_started` adaptation — `infer_types` no longer self-sets the flag), `ActionStartTypes` (coreaction.hh:74-86; apply = `u32::from(data.start_type_recovery())`), ActionInferTypes::apply gains Ghidra's coreaction.cc:5378 gate (RulePushPtr/RulePtrArith gates pre-existed = the inventory's exactly-3 sites), StartTypes wired immediately before the once-InferTypes slot. Per-fixture identical (join 60/60), suite 420/0, clippy 79==79.
**C1 (uncommitted, pipeline.rs ONLY, 76+/102−)** — the restructure: deleted the 3 nzmask→consume→pool→deadcode sweeps + once-determinedbranch/condconst/StartTypes+InferTypes; `restart("fullloop"){ restart("mainloop"){ spacebase→nzmask→consume→infertypes→oppool→deadcode→ptrarith→determinedbranch→condconst→heritage→deadcode }, ActionStartTypes }`. The early once-ActionSpacebase dropped (redundant: mainloop's first member runs at the identical graph state; Ghidra has no pre-fullloop spacebase). ActionActiveReturn stays linear AFTER the fullloop (Ghidra slot = fullloop tail :5688 directly after StartTypes; C2 = join it + DeadCode :5682 + SwitchNorm :5684 into the tail). **ActionConsume return 1→0 INCLUDED** (physics: unconditional 1 hangs the restart; faithful analysis-half convention) + consume slot after nzmask — i.e. C1 subsumes 2 of the parked consume patch's 3 semantic edits; the patch's remaining delta = funcdata.rs `consume: !0u64` + 24 test updates.
**C1 measure @52cac08-dirty:** avg 0.9286→**0.9294**, 57/60 (revisit crossed 0.70). Scored movers: **revisit 0.677→0.724 (+0.047)** — Ghidra-CONVERGENT: the spurious `uVar1 = func();` output capture is gone → bare call + `extraout_RAX`, Ghidra's exact shape (residual = P6 void-return `return CONCAT62` vs `return;`); cause = ActiveReturn now post-loop. **Byte-level dump_all diff (all 62): ONLY revisit + varcross moved.** varcross score-flat 0.915 but 3 lines moved AWAY from Ghidra (`xunknown4 *`→`xunknown1 *`, `axStack_18[6]`→`[24]`, `+1`→`+4`): rule firings IDENTICAL; IR shows the PTRADD now forms EARLY (uniq 44 vs 134) with the weak first-pass type → `PTRADD(ptr,#4,elem 1)` vs `PTRADD(ptr,#1,elem 4)` (single InferTypes instance in-loop = Ghidra's structure; old flow gave ptrarith two infer passes first). **= faithful-exposes, named next-port: RulePtraddUndo (ruleaction.cc:6912) + RulePtrsubUndo (:7131), oppool1 slot coreaction.cc:5638-5639 — Ghidra's exact repair ("incorrect data-types assigned in the middle of simplification… converts back to INT_ADD when the mistake is discovered").** No mis-port found. Suite 420/0 (incl. piecestruct hard test green under C1 alone), jumptable 6/6, clippy 79==79. Convergence (transient env-probe, REVERTED): 0 cap-hits; fullloop 46×2/26×3 rounds, mainloop 72×1/84×2/14×3, heritage 72×3. Perf: corpus wall C0 2.35-2.42s vs C1 2.39-2.47s (this machine) → ratio ~1.02, flat.
**PAYBACK PROBE (transient, REVERTED, revert join-verified identical):** consume default `!0u64` on top of C1 → hard test `piecestruct_folds_shifts_to_concat` **RED**; piecestruct 1.000→0.873 (same as pre-C1), switchloop 0.837→**0.851 (+0.014, the case-7 `x^0x87` semantic fix fires)**. The failing shape = the SECOND struct's stack-word packing (`((p6<<8|p5&0xff)<<8|p4&0xff)<<8|p3` unfolded; first struct's CONCAT22 folds) = the memo's alternate root (ii) stack-slot-RMW/recover_stack placement class. **Root (i) mainloop-cadence is now PROVIDED by C1 and did NOT rescue it → per decision tree: C1 gates on its own merits, consume patch stays PARKED, next probe after Brick D (#22-B).**
**LANDED `8a5fdc3` per lead GO** (the section above was the gate report; kept for the record). NEXT = Brick D (#22-B Brick 2), the consume-patch unpark gate. The ActiveReturn fullloop-join + DeadCode/SwitchNorm tail = C2 (deferred); RulePtraddUndo/RulePtrsubUndo (oppool1, coreaction.cc:5638-5639) = the filed varcross follow-on port.

### C-PREMISE INVENTORY (read-only, verified @323c647)
**(1) The `has_type_recovery_started` gate inventory.** Ghidra gates EXACTLY these (grep-verified): ActionInferTypes (coreaction.cc:5378), ActionConstantPtr (:1170, NOT ported), ActionDeindirect type-arm (:1258, NOT ported), RulePushPtr (ruleaction.cc:6851), RuleStructOffset0 (:6680, NOT ported), RulePtrArith (:6642), RulePtraddUndo (:6912, NOT ported), RulePtrsubUndo (:7131, NOT ported), ScopeLocal::annotateRawStackPtr (varmap.cc:389, NOT ported). → **mosura gates = exactly 3 sites: ActionInferTypes::apply (pipeline.rs) + RulePushPtr + RulePtrArith (ptrarith.rs).** ⭐ **RuleLoadVarnode/RuleStoreVarnode are UNGATED in Ghidra** — they fire TYPELESS (keyed on isSpacebase, marked by per-iteration ActionSpacebase) — do NOT gate them; stack/ram resolution runs in phase 1 too. NO mosura oppool1 rule reads datatypes (rules.rs grep clean). Unported rules ship their gate with their own port.
**(2) Flag + ActionStartTypes.** Flag: `type_recovery_started: bool` on Funcdata, co-located with `is/set_type_recovery_exceeded` (funcdata.rs:130-134); only `started` is needed (Ghidra's typerecovery_on "will run" is trivially true in mosura; funcdata.hh:150/176, startTypeRecovery returns true exactly once). ActionStartTypes = `if data.start_type_recovery() { 1 } else { 0 }` (coreaction.hh:82-85, flags 0), slot = fullloop tail :5687. Structure: `restart("fullloop"){ <mainloop restart group>, ActionStartTypes }` — StartTypes' one-time count forces exactly round 2 = the typed phase. **Minimal C = those two members only**; the faithful fullloop tail (DeadCode :5682, SwitchNorm :5684 — convergent post-Brick-A, ActiveReturn :5688) = optional C2 follow-on, keep linear initially.
**(3) pipeline.rs blocks that move/die (@323c647 lines).** DIE: sweep 1 (:500-503 nzmask/consume/pool/deadcode), once-determinedbranch (:507), once-condconst (:517), sweep 2 (:518-521), sweep 3 (:528-531), once-ActionInferTypes (:533). The reheritage restart (:560-589) **already IS the mainloop body** {spacebase→nzmask→infertypes→oppool1→deadcode→ptrarith→determinedbranch→condconst→heritage→deadcode} — C renames it, adds the 3 gates, wraps in fullloop+StartTypes; body gains the consume slot after nzmask (the parked patch's `.then(ActionConsume)` position). STAY: heritage restart (:477) + ActionResolveCalls (Brick E scope), MarkAddrTied, LaneDivide (Brick F scope), ActionActiveReturn (:532 — linear for minimal C, fullloop for C2), the :601+ tail (switchnorm onward) unchanged. **Sub-brick staging (strongly recommended): C0 = flag + gates + StartTypes machinery wired semantics-preserving** (start types at the current once-ActionInferTypes position :533 → every gated site behaves identically) → byte-identical, self-approve lane; **C1 = the restructure** (delete sweeps/once-instances, wrap fullloop) → THE gated mover, every fixture re-flows. ⚠️ **CONFLICT WARNING: brick1-consume-default.patch (diffed @94eef0f) touches the pipeline.rs regions C1 rewrites — expect `git apply --check` FAILURE post-C1; hand-apply its 3 semantic edits** (funcdata.rs:255 `consume: 0`→`!0u64`; ActionConsume return 1→0; consume-in-loop slot) + 24 test updates per [[consume-model-misport]].
**(4) Consume-patch probe protocol (transient, BEFORE landing C1).** 1) Build C1 uncommitted → measure C1's OWN delta first (suite full-sum, corpus per-fixture join; movers gated). 2) Apply/hand-apply the consume patch on top → run `piecestruct_folds_shifts_to_concat` (decompile_corpus.rs:221) FIRST, then switchloop (expect ≥0.840, the case-7 wrong-code fix), then full suite+corpus. 3) DECISION TREE: piecestruct GREEN → land C1 (gated), then consume Brick 1 as its own commit, then re-ground switchloop residual C ([[consume-model-misport]]). | piecestruct RED but C1-alone green → C1 lands on faithful merit, patch stays PARKED, next probe after Brick D (memo's alternate root = recover_stack placement class). | C1-alone breaks a hard test → park C1 turnkey (patch+memo), land C0 only, report. 4) Always revert probes + verify per-fixture identical after each revert. GOTCHA: capture with `> f 2>&1` NOT `2>&1 > f`.

## ⭐ S8-2 ENABLER REFRAMED — it's PRINTC, not heritage re-free (mloop4, 2026-07-11, base `b573e78`, all probes REVERTED, tree clean). Premise VERIFIED end-to-end. Report sent lead, WAIT for gate.
The lead framed the restart's revisit residual as "the SSA re-normalization-on-re-entry gap → whole-widened-range re-free on heritage re-entry." **That framing is OVERTURNED.** Measured the scoped restart `{ptrarith→ActionHeritage→deadcode}` on CURRENT HEAD (post guardCalls-ram `e73a1c0` + task#7 `0b04da8`):
- **revisit 0.679→0.609 (−0.070)**, longdouble 0.909→**0.952 (+0.043 clean win)**, switchmulti 0.628→0.586 (−0.042), corpus 0.9209→0.9197. All else identical. (Baseline revisit is 0.679 now — guardCalls-ram lifted it from the mloop3-era 0.633; the lead's "0.477/0.618" numbers are STALE pre-guardCalls-ram.)
- **The heritage re-free ALREADY WORKS.** mosura's restart IR matches Ghidra's oracle IR structurally: `+10` reads the whole `r0x100074:4`, guardCalls-ram threads it across the call (`r74:4 = INDIRECT u0x10018`), post-call `+100` reads `r74:2 = SUB42(r74:4,#0)`, write reassembles via PIECE/CONCAT22. removeRevisitedMarkers brick (`32648d3`) + normalize_ranges do their job — the SUB42/CONCAT22 shape is correct.
- **The residual = a PRINTC gap.** The restart's revisit regression is TWO `r0x100074:2 = SUBPIECE(r0x100074:4,#0)` markers rendering as standalone `iRam74 = (int2)iRam74;` (one dead pre-call, one read-by-+100 post-call). Ghidra suppresses these via **`ActionMarkExplicit::baseExplicit`'s addr-tied SUBPIECE-of-addrtied sub-case (coreaction.cc:3023-3029: `SUBPIECE(addrtied, off)` w/ matching overlap → "explicit, will be a copymarker and not printed") + markInternalCopies**. mosura's `is_explicit` (printc.rs:232) forces ALL addr-tied varnodes explicit; its OWN comment (printc.rs:230-231) EXPLICITLY notes it omits these SUBPIECE sub-cases ("need VariablePiece, the P4/P8 debt") — but the scalar SUBPIECE-of-addrtied-whole case does NOT need VariablePiece.
- **PREMISE PROBE (throwaway, reverted):** (a) is_explicit inlines addr-tied `SUBPIECE(same-addrtied-whole, off)` w/ overlap-match (baseExplicit); (b) extend printc's same-high `hidden` copymarker (printc.rs:1264) from COPY-only to SUBPIECE. Result: the two self-assignments COLLAPSE → revisit matches Ghidra's `iVar2=(int2)iRam74+100; iRam74=CONCAT22(...,iVar2)`. **restart+enabler: corpus 0.9209 (=baseline), revisit 0.609→0.677, longdouble +0.043 preserved, switchmulti −0.042 the only regression, all else identical.** **enabler ALONE (no restart): BYTE-IDENTICAL per-fixture → DORMANT brick (self-approve).**
- **REMAINING restart residuals (after enabler, all INDEPENDENT of mainloop):** revisit −0.002 = P6 void-return (`uint8 func`/`return CONCAT62` vs `void`/`return;`) + P4 type residue (`(xunknown2)(iRam74>>0x10)` vs `iRam74._2_2_`); switchmulti −0.042 = dual-width switch selector ([[switchloop-dup-propagatecopy]], task#4); longdouble +0.043 = clean win.
- **STAGING (sent lead):** **S8-2a ENABLER = faithful baseExplicit SUBPIECE-of-addrtied sub-case (coreaction.cc:3023-3029) + markInternalCopies SUBPIECE arm** in printc — DORMANT/byte-identical → self-approve. **S8-2b RESTART wire** = the mover (longdouble +0.043, switchmulti −0.042); per [[faithful-ports-land-not-held]] a faithful mainloop port LANDS, switchmulti is the diagnostic. **CORE ENABLER is PRINTC, not heritage** — this is the mis-framing caught.

## ✅ S8-2a ENABLER LANDED master `b1c0dfa` 2026-07-11 (mloop4, lead GO'd self-approve). Byte-identical brick, suite 396/0, switch 6/6, corpus 0.9209/56 per-fixture IDENTICAL, clippy no-new.
printc.rs: (1) `is_explicit` — addrtied Varnode defined by `SUBPIECE(vin, off)` with `vin` addrtied + same space + `vn.offset == vin.offset + off` + contained (baseExplicit overlapJoin==offset, coreaction.cc:3023-3029) → return false (inline as `(int2)glob`, an internal copymarker, not a store). (2) statement emitter `hidden` — extended same-high copymarker from COPY to `COPY|SUBPIECE` (markInternalCopies opMarkNonPrinting merge.cc:1461/1508; mosura `high_of` identity = faithful stand-in for VariablePiece group+offset). coverage.md MarkExplicit row updated (scalar SUBPIECE now modeled; PIECE arm + VariablePiece-needing SUBPIECE stay P4/P8-deferred). NO synthetic printc unit test (is_explicit needs the full merge pass → fragile; precedent = B-iii markInternalCopies COPY arm verified via corpus, not a printc unit test; functional proof = S8-2b restart revisit-collapse). NEXT = S8-2b restart wire (measure + report for gate, WAIT — do NOT self-approve, it's a mover).

## ⭐ S8-2b RESTART MEASURED @b1c0dfa (mloop4, 2026-07-11, wired UNCOMMITTED, reported for gate, WAIT). Corpus-NEUTRAL; switchmulti = GAUGE ARTIFACT not mis-port.
Wired `ActionGroup::restart("reheritage"){ ptrarith_pool → ActionHeritage → deadcode }` at pipeline.rs:416 (before cleanup_pool). CAPTURE-BUG CAUGHT + FIXED: `cargo test … 2>&1 > f` sends the eprintln report to the TERMINAL not the file (stderr dup'd to old stdout BEFORE the `>`); use `> f 2>&1`. Re-verified all captures with the correct redirect (name-keyed join, 60 fixtures each).
**S8-2a re-verified GENUINELY byte-identical** (b1c0dfa vs baseline b573e78, join → zero differing fixtures; the earlier "byte-identical" was a false-positive comparing two report-LESS files — conclusion held but the check was broken).
**S8-2b delta (vs S8-2a b1c0dfa, correct capture):** avg 0.9209→0.9209 (NEUTRAL). Movers: **longdouble 0.909→0.952 (+0.043 clean win)**, **revisit 0.679→0.677 (−0.002)** [the two `iRam74=(int2)iRam74;` markers now COLLAPSE via S8-2a; residual = P6 void-return + P4 (xunknown2) type], **switchmulti 0.628→0.586 (−0.042)**. All 57 others byte-identical. Suite 396/0, switch 6/6, converges (mloop3: <=2 passes, 0 cap-hits, perf-flat).
**switchmulti −0.042 = GAUGE ARTIFACT, NOT a mis-port (proven vs oracle):** the ONLY C diff is the switch selector — baseline `iVar1 = iRam1000c0; …(int8)iVar1…`, S8-2b inlines `…(int8)iRam1000c0…`. **Ghidra's oracle ALSO inlines it** (`xVar1 = (*(code*)((int8)iRam1000c0 + 0x1000c0))(0x65)`), so S8-2b's selector is MORE faithful. The score drops because the similarity metric is token-LCS with names normalized (ccompare.rs:103 `2·LCS/(|a|+|b|)`): baseline's spurious `iVar1 = iRam1000c0;` (skeleton `IDENT = IDENT ;`) COINCIDENTALLY LCS-matched Ghidra's mosura-MISSING param-spill `xStack_20 = param_1;` (same skeleton); removing the spurious copy loses that accidental match. switchmulti's real gaps (missing param-spills = P6, void-return = P6, jumptable "Too many branches") are PRE-EXISTING + unchanged. So the restart is a faithful mainloop re-heritage that even IMPROVES switchmulti's IR — per [[faithful-ports-land-not-held]] it LANDS. Reported for gate.

## ✅✅ S8-2b LANDED master `25cb50b` 2026-07-11 (mloop4, lead GATE-CONFIRMED — THE mainloop milestone). Tree clean, suite 396/0, switch 6/6, corpus avg 0.9209 / 56-of-60 @25cb50b.
The iterating mainloop re-heritage is WIRED: `ActionGroup::restart("reheritage"){ ptrarith_pool → ActionHeritage → deadcode }` before cleanup_pool (pipeline.rs:416; Ghidra actmainloop coreaction.cc:5492). Composes with S8-2a (`b1c0dfa` printc baseExplicit copymarker) + the removeRevisitedMarkers brick (`32648d3`) + normalize_ranges: a LOAD/STORE freed by RuleLoadVarnode/StoreVarnode in ptrarith re-enters heritage → range widens → re-free reconstructs whole-range SSA → printc inlines the re-freed pieces. Converges ≤2 passes, perf-flat. **longdouble +0.043** (clean win, global read/modify/write across a call re-versions in-place), revisit re-versioning recovered (markers collapse), **switchmulti −0.042 = GAUGE ARTIFACT** (documented coverage.md §1 intro: Ghidra ALSO inlines the selector → IR more faithful; token-LCS lost a coincidental spurious-copy↔missing-param-spill match; NOT task#4, NOT a mis-port). coverage.md §1 intro documents the whole S8-2 landing + flipped the removeRevisitedMarkers/normalizeReadSize/MarkExplicit "dormant" rows to ACTIVE.
**STILL SCOPED** (only ptrarith re-enters heritage). S8-3 = broaden to the full actmainloop body (oppool1 + condconst in the loop) — mloop3 measured oppool1-in-loop is byte-identical/converges (no corpus fixture needs iterative condconst yet). **Downstream (lead's list) — floatcast PREMISE RE-CHECKED + FALSIFIED for S8-2b (mloop4, reactivation probe on 25cb50b, reverted):** reactivating LaneDivide gives floatcast **0.776→0.774 flat**, BYTE-FOR-BYTE identical to p4infer1's b573e78 measurement-A (floatprint −.150, concatsplit −.018, avg 0.9180). So the SCOPED S8-2b restart does NOT unblock floatcast — it re-heritages ptrarith-freed globals but runs AFTER (and does not re-run) ActionLaneDivide (pipeline.rs:381, pre-pool, once) + return-recovery, which is exactly floatcast's need (PIECE 2 = the RETURN-value 16-byte XMM decomposition ordering: narrow XMM0→Qa BEFORE the return CONCAT is built; [[task6-lanedivide-plan]] lines 209-215). **floatcast needs S8-3 = broaden the restart to include LaneDivide + return-recovery in the loop (Ghidra's stackstall slot iterating), NOT the scoped ptrarith re-heritage.** Other downstream: spacebase-S2 (task#7, re-versioning now provided — untested), iterative condconst (S8-3, no corpus fixture needs it yet). GOTCHA for future measures: capture with `> f 2>&1` NOT `2>&1 > f` (the latter drops the eprintln report to the terminal — false byte-identical checks; = task #16).

## ✅ PHASE-1 GROUNDING DONE 2026-07-10 (sbs2, base `cf14470`, READ-ONLY, tree clean). Report sent lead, WAIT for build gate (big investment — lead brings it to a gate).
Task #8 = port Ghidra's iterating mainloop. The lead recorded it after task #7 S2 Phase-1 proved the whole spacebase stack track is gated on it (naive re-heritage regresses revisit — see [[task-sb-spacebase-placeholder]]). Blocks: S2-stack (parts 1/2/3), iterative condconst, revisit re-versioning, all the "once-pass approximation" comments in pipeline.rs.

**GHIDRA STRUCTURE (coreaction.cc:5462 `ActionDatabase::universalAction`, line-cited):**
- `universal` = `ActionRestartGroup(rule_onceperfunc,"universal",1)` (:5474) — top, maxrestarts=1.
- `actfullloop = ActionGroup(rule_repeatapply,"fullloop")` (:5487) wraps
- `actmainloop = ActionGroup(rule_repeatapply,"mainloop")` (:5489). Members IN ORDER (:5490-5676):
  ActionUnreachable, ActionVarnodeProps, **ActionHeritage** (:5492 — re-runs EVERY iteration = the re-versioning mechanism), ActionParamDouble, ActionSegmentize, ActionInternalStorage, ActionForceGoto, ActionDirectWrite×2, ActionActiveParam, ActionReturnRecovery, ActionRestrictLocal, ActionDeadCode, ActionDynamicMapping, ActionRestructureVarnode, **ActionSpacebase** (:5506 — marks RSP `Varnode::spacebase`, = S2 part-2 registration, `Funcdata::spacebase()` funcdata.cc:230), ActionNonzeroMask, ActionInferTypes, `actstackstall`(repeatapply; rule pools+LaneDivide), ActionRedundBranch, ActionBlockStructure, ActionConstantPtr, `actprop2`(**RuleLoadVarnode/RuleStoreVarnode** :5666-9), ActionDeterminedBranch, ActionUnreachable, ActionNodeJoin, ActionConditionalExe, ActionConditionalConst.

**FIXPOINT MECHANISM (action.cc:298 `Action::perform` + :506 `ActionGroup::apply`):** a `rule_repeatapply` group re-runs its whole child sequence while the last pass increased the change-count: `do{ apply }while((lcount<count)&&(flags&rule_repeatapply))`. `ActionGroup::apply` iterates children once, `count += child->perform`. So the mainloop repeats until ONE full pass makes ZERO changes = fixpoint. ActionHeritage returns its heritaged-location count; at fixpoint (no free reads) it returns 0 → loop terminates. When RuleLoadVarnode converts LOAD→COPY(free varnode), the NEXT mainloop iteration's ActionHeritage re-collects+re-versions it (that's what mosura lacks).

**★ KEY DE-RISKING FINDING: mosura ALREADY HAS the fixpoint primitive.** `ActionGroup::restart` (action.rs:136/159) = faithful `rule_repeatapply`: `loop{ round=Σ child.apply; total+=round; if !restart||round==0 break }`. Verified by its unit test `restart_group_runs_to_fixpoint`. So task #8 is NOT building the loop machinery — it's (a) wrapping the mainloop BODY in `restart` and (b) making every action CONVERGENT so the fixpoint terminates without corruption. Today `universal_action()` (pipeline.rs:354) is `ActionGroup::once("decompile")` — a FLAT `.then()` chain, hand-unrolled (3 pool sweeps, condconst once, etc.); only the inner `heritage` (:359) is a restart group.

**BLAST RADIUS — action-by-action convergence audit (the real cost):**
- Rule pools (default_rule_pool, ptrarith_pool, cleanup_pool, ActionPool) = ALREADY fixpoint/idempotent. Safe under repeat.
- ActionDeadCode, ActionNonzeroMask, ActionConsume, ActionMarkAddrTied = recompute-from-scratch/flag-set = idempotent. Safe.
- **ActionHeritage re-entry = the proven blocker.** revisit probe (task #7, reverted): re-heritage (a) DOUBLE-fires guard_calls — the widened 4-byte range re-enters `new_addrs` at `globaldisjoint.add` intersect=1 (partial overlap w/ the earlier 2-byte range) → re-guards the already-guarded overlap → spurious INDIRECT save/restore; (b) does NOT unify mixed 2/4-byte accesses (refine_overlaps/normalize_read_size are pass-0-ONLY, heritage_pass:946) → no canonical var + SUB42. Ghidra's mainloop runs refinement every heritage → guards see refined non-overlapping ranges. So mosura's incremental re-heritage needs refinement-on-re-entry + guard-idempotency to be correct.
- Once-pass actions needing convergence work: ActionConditionalConst (comment: runs once, iterative = this rock), recover.rs two-phase return/param (resolve pre/post-heritage ordering — re-running heritage disrupts), ActionActiveReturn (resolve_call_output), ActionMergeRequired (merge-snip — double-snip risk), ActionOrientBranches/condnegate (boolean_flip), ActionLaneDivide (re-divide guard), ActionInferTypes (re-infer). Each must be verified convergent.
- PERF: repeat = N× pipeline cost; the 377/0 suite + corpus timing = tripwire.

**MINIMAL FAITHFUL STEP vs FULL REWRITE:**
- MINIMAL (conservative, bounded to heritage module): fix mosura's INCREMENTAL re-heritage — (i) run refinement/normalize on re-entry (unify mixed widths), (ii) guard idempotency (don't re-add INDIRECTs for the already-guarded overlap of a widened range). This is DORMANT today (no re-heritage runs in the real pipeline — heritage restart group has no rules between passes, so no reads get freed mid-heritage; the pools that free reads run AFTER heritage completes and nothing re-invokes it) → should be byte-IDENTICAL on the current corpus = landable as neutral groundwork + unit tests. THEN a SCOPED `restart{ re-heritage + pool + deadcode }` after ptrarith becomes safe → recovers revisit (S2 part-1). Does NOT make condconst iterative.
- FULL: wrap the whole mainloop body in `restart` + converge every action. Unblocks iterative condconst + all once-pass approximations. Highest blast radius in the codebase; every fixture re-flows.

**RECOMMENDED STAGING (sent lead, gated):**
- S8-1 = the MINIMAL incremental-re-heritage correctness fix (guard idempotency + refinement-on-re-entry) as an isolated heritage change + unit tests; verify byte-identical/377-green (dormant until wired). NEUTRAL groundwork.
- S8-2 = wire a scoped re-heritage-after-ptrarith restart group; GATED mover (recovers revisit; watch regressions). = S2 part-1 unblocked.
- S8-3+ = broaden toward the full mainloop (iterative condconst, etc.) incrementally, each its own gated stage. Convergence-audit each action first.
RISK MAP: guard double-fire (PROVEN, S8-1 target); refinement-on-re-entry correctness; recover.rs two-phase ordering; merge-snip double-snip; condconst/determinedbranch convergence; perf (N× cost); 377/0 + corpus = tripwire on every stage.

## ⭐ S8-1 PREMISE REFINED 2026-07-10 (sbs2, lead GO'd S8-1, deeper Ghidra grounding BEFORE code → corrected scope, sent lead, WAIT for (i)/(ii) pick). Tree clean cf14470, no code.
Ground of Ghidra re-entrant heritage (heritage.cc:2663 `heritage()` + :2599 `placeMultiequals` + :416 normalizeWriteSize + guard() :1156) CORRECTED the rougher S8-1 hypothesis:
- **"guard idempotency" bug does NOT exist.** mosura intersect==2 (contained re-heritage) ALREADY faithful (→cover, NOT new_addrs → guards don't refire). intersect==1 (widened) is marked new in BOTH mosura AND Ghidra (`disjoint.add(..., old_addresses|new_addresses)`, heritage.cc:2718) → both guard it. The probe's "spurious call save/restore" = faithful guards not yet absorbed (probe didn't finish a mainloop iteration), NOT a mosura double-fire. No dormant guard fix to make.
- **Refinement is NOT the lever** — gated `size>4 && max_write<size` (placeMultiequals :2610); revisit's range is 4. (Probe B already confirmed.)
- **REAL re-entry gap = WIDTH NORMALIZATION.** Ghidra `guard()`→normalizeReadSize (narrow read→SUB of range varnode) + normalizeWriteSize (:416, narrow write→CONCAT/PIECE) run PER heritaged RANGE, EVERY pass, keyed on the merged-range width. So heritaging the widened 4-byte 0x100074 makes the 2-byte read→SUB42 and the 2-byte write→CONCAT22 = exactly revisit's oracle IR. mosura's normalization is a PASS-0 BATCH of TWO ADAPTATIONS that BOTH miss ram re-entry: (a) `normalize_read_size` (heritage.rs:228) keyed on SINGLE-write-width uniqueness — revisit 0x100074 has TWO write widths (4-byte S1 STORE→COPY + 2-byte direct) → bails even if re-run; (b) `refine_overlaps` (:457) is REGISTER/XMM-ONLY, never touches ram.
- **FAITHFUL FIX (no-grandfathering) = port Ghidra range-driven normalizeReadSize/normalizeWriteSize into per-pass guard() (heritage_spaces step, beside guard_calls/guard_stores), retiring the pass-0 batch heuristics.** This CHANGES initial-heritage normalization → NOT a priori dormant/byte-identical → measure-then-GATE MOVER (might be byte-identical since mosura already matches Ghidra IR, but the batch heuristics were over-collapse guards — refine_overlaps comment: "broad guard() would otherwise over-collapse" — so it may move). 
- **TWO S8-1 OPTIONS sent lead:** (i) build the faithful range-driven restructure + measure full corpus (self-approve iff byte-identical, else gated mover — bigger than "first brick", real corpus risk); (ii) [my lean] add a RE-ENTRY-SCOPED range-driven normalize (fires only on a re-heritage pass = never today → dormant/byte-identical + synthetic unit tests), keep pass-0 batch, defer batch-retirement to a later gated stage (documented 2-path duplication). WAITING on the pick.

## ⭐ S8-1 BUILD STARTED 2026-07-10 (mloop1, base cf14470, lead GO'd decision (i) FAITHFUL-FIRST). Premise CONFIRMED from source+oracle.
- **Ghidra source CONFIRMED**: `guard()` (heritage.cc:1172-1182) runs `normalizeReadSize` on each read + `normalizeWriteSize` on each write where `vn->getSize() < size`, PER merged range, EVERY pass. `size` = merged-range size from `placeMultiequals` (heritage.cc:2617-2618 `memrange.size`), keyed on cumulative `globaldisjoint`. Refinement (partition) is a SEPARATE gate `size>4 && max_write<size` (heritage.cc:2610); normalize fires for ANY narrow access regardless of max_write (NOT gated on max_write==size — that's mosura's refine_overlaps adaptation).
- **revisit ORACLE IR (anchor for unit test)** — RAM range r0x00100074:4 (proves ALL-SPACE, not register-only):
  `r74:2 = SUB42(r74:4, #0)` · `r76:2 = SUB42(r74:4, #2)` · `r74:4 = CONCAT22(r76:2, AX)` (AX 2-byte write at base: overlap0, mostsig2 → PIECE(high-from-old, AX)).
- **DESIGN (low-SSA-risk, keeps proven cover/scan/phi/rename)**: new `normalize_ranges(f, pass)` at TOP of heritage_pass EVERY pass, BEFORE gather_candidates (so cover stays exact-loc == merged widths, as the batch produces today). Merged ranges via a CLONE of globaldisjoint (faithful cumulative merge incl left-overlap quirk) unioned w/ this pass's free-access footprints. Per range: free reads (`!is_heritage_known`) narrower than merged size → SUBPIECE (reuse read-normalize pattern); writes narrower → `normalize_write_size` (already faithful). RETIRE `normalize_read_size` + refine_overlaps' Normalize mode (keep its laned Refine partition, pass-0). Reusable → fallback (B) = gate `normalize_ranges` to re-entry passes only.
- LIKELY MOVER: all-space + narrow<size gate (vs register-only max_write==size) normalizes ram/stack partial writes the batch skipped. Measure → if moves, report delta+cause, WAIT for gate (A land / B scope-to-reentry).

## ⭐ S8-1 BUILT + MEASURED 2026-07-10 (mloop1, base cf14470, UNCOMMITTED faithful mover, suite 380/0). GATED — sent lead, WAIT for A/B.
CODE (heritage.rs): `normalize_ranges(f, pass)` at top of heritage_pass EVERY pass (replaces the pass-0 batch); `LocationMap::merged_range`; module `XMM_BASE`/`is_laned_register`; refine_overlaps' Normalize narrowed to laned-only; `normalize_read_size` DELETED; 3 synthetic unit tests (revisit mixed-width → SUB42+CONCAT22, single-width no-op, refinement-range skip). **CRUCIAL GATE ADDED**: normalize_ranges SKIPS `size>4 && max_write<size` ranges — Ghidra's *refinement* domain (heritage.cc:2610), which mosura keeps a deliberate non-laned no-op. Without it, I over-fired normalizeWriteSize on stack/input pieces → impliedfield/stackreturn regressed; the gate reverted both to baseline.
MEASURE: corpus 0.9168→**0.9151 (−0.0017)**, 56/60, suite 380/0. Movers: **deindirect2 −0.116** (0.936→0.820) + loopcomment +0.010 (unique-renumber noise). deindirect2 ROOT (confirmed vs Ghidra IR) = NOT a normalize bug: mosura's CALLIND output lands in `r0x0:8` (RAX) while Ghidra's is a `unique` (u0x10000009) — so mosura's RAX range merges the 8-byte call output with the 2-byte AX=0 return, which faithful normalizeWriteSize correctly PIECEs; Ghidra never merges (no RAX in range). = pre-existing **call-output-in-RAX** modeling debt (P6/resolve_call_output), a faithful-port-exposes-non-Ghidra finding. impliedfield's SUB84(RDI) confirmed FAITHFUL (matches Ghidra IR).
RECO: (A) land faithful mover, file deindirect2 call-output finding as P6 follow-up (CLAUDE.md: fix exposed code, don't revert port). (B) ready-cheap fallback = scope normalize_ranges to re-entry-only passes (byte-identical, batch stays). WAIT for lead pick before commit.

## ✅ S8-1 LANDED (B) master `d97bd90` 2026-07-10 (mloop1). Byte-identical brick. Suite 381/0, corpus 0.9168 (56/60, per-fixture IDENTICAL), all 62 x86:LE:64 decompile.
Lead picked (B): the brick's value is re-entry correctness (S8-2); batch-retirement (A) regresses deindirect2 only by exposing call-output-in-RAX (task #6) which S8-2 needs fixed ANYWAY → clean brick now → task#6 → S8-2+retirement compose net-neutral.
LANDED (heritage.rs): faithful `normalize_ranges` (guard() normalizeReadSize/WriteSize per merged range, `LocationMap::merged_range`, is_refine_range gate) PRESERVED but wired **WIDENING-re-entry-ONLY**; `normalize_read_size` RESTORED (interim batch); refine_overlaps back to functional-original (XMM_BASE hoisted only). 4 unit tests. coverage.md §5 updated.
★ KEY LESSON (why the first re-entry gate failed): `find_pass(sp,off)!=-1` is TOO BROAD — at pass 1 EVERY pass-0-heritaged register (RAX...) has find_pass=0, so any free reg access at pass 1 (deindirect2's AX-in-RAX) counts as re-entry and STILL fired (not byte-identical). The correct byte-identical scope is **WIDENING**: `merged.merged_range(off).size > f.globaldisjoint.merged_range(off).size` (the range grew vs its prior-pass heritage). revisit widens 2→4 (fires, correct); deindirect2 is same-width AX-in-8byte-RAX (does NOT widen → skip → batch handles → byte-identical). No range widens without the mainloop restart → dormant today.
DEINDIRECT2 = the concrete call-output-in-RAX diagnostic (task #6): mosura CALLIND out → r0x0:8 (RAX), Ghidra → unique u...9; so RAX wrongly merges w/ 2-byte AX=0 return → faithful normalizeWriteSize PIECEs a merge Ghidra never makes. Confirmed vs Ghidra IR. Fix = call output to a unique (resolve_call_output).

## S8-2 PLAN (next, blocked on task #6): scoped re-heritage-after-pool restart → recovers revisit re-versioning, unblocks S2-stack.
- WHERE: wrap `{ptrarith_pool → heritage_pass → deadcode}` (or extend the existing heritage restart, pipeline.rs:359) in ActionGroup::restart so a read freed by RuleStoreVarnode/RuleLoadVarnode (S1 `cf14470`) re-enters heritage. Ghidra runs ActionHeritage every mainloop iter (coreaction.cc:5492).
- MECHANISM: RuleStoreVarnode STORE→COPY(free ram vn) → heritage_complete()=false → re-heritage_pass re-collects, range WIDENS (globaldisjoint.add intersect=1) → `normalize_ranges` (LANDED, widening-scoped) re-normalizes now-narrow accesses → SUB/CONCAT → correct re-version (Ghidra `iRam=iRam+10` vs mosura snapshot `iVar1=iRam;iRam=iVar1+10`, the revisit −.021 cited in coverage §3 RuleLoadVarnode).
- PREREQ task #6: re-heritage re-processes RAX + eventual batch-retirement both surface the RAX+AX merge → fix call-output-in-RAX first so S8-2 composes net-neutral.
- RISKS: guard re-fire on widened range (intersect==1 → new_addrs → re-guards overlap; FAITHFUL — Ghidra does too, absorbed only when the restart runs to fixpoint, so the restart must iterate); recover.rs two-phase ordering; perf N×. GATE: revisit recovers (+), watch corpus (mover→gate).

## ⭐ S8-2 INSTRUMENTED (restart-only) 2026-07-10 (mloop2, base `2be93b4`, READ-ONLY measure, tree reverted byte-identical). HELD — premise partially FAILS; sent lead, WAIT.
WIRING measured: wrapped `restart{ ptrarith_pool → ActionHeritage → deadcode }` at pipeline.rs:411 (before cleanup_pool). Env-gated dbg in `normalize_ranges`/`heritage_pass`. Reverted after (git diff empty, corpus 0.9168 restored).
**CONVERGENCE = CLEAN.** No infinite loop; the restart adds exactly ONE re-heritage pass (pass 2) then `heritage_complete`→true; corpus wall 1.75s vs 1.61s baseline (negligible). ptrarith(0 at fixpoint)/ActionHeritage(0 at complete)/deadcode(0 idempotent) all bottom out. So the restart PRIMITIVE + guard-absorption concern are NOT the blocker.
**MECHANISM FIRES as grounded, but re-versioning is INCOMPLETE.** revisit trace: pass 1 (early heritage) heritages the DIRECT-ram `0x100074:2` read (`mov ax,[rip+..]` decodes as a direct ram COPY, free early) as its own size-2 SSA var; the `0x100074:4` access is `LOAD` through rcx, freed ONLY by RuleLoadVarnode in the restart's ptrarith → pass 2 re-heritage: `0x100074` widens 2→4, `widened={ram:0x100074}` NON-empty, `normalize_ranges` fires. BUT it only rewrites the freshly-FREED 4-byte read; the 2-byte reads committed at pass 1 are `is_heritage_known()` → normalize SKIPS them (heritage.rs:465) → they stay separate `r0x100074:2` vars, and the widened writes emit spurious `CONCAT22`/double-writes. Result IR is a HYBRID, worse than the snapshot.
**MEASURE (MOVER, regresses):** corpus 0.9168→**0.9143 (−0.0025)**, 56/60. Per-fixture: **revisit 0.633→0.477 (−0.156)** [the TARGET regressed], switchmulti 0.628→0.586 (−0.042), longdouble 0.909→**0.952 (+0.043)** [a global that widens+recovers CLEANLY with just the restart — restart is directionally right]. No other movement.
**ROOT (confirmed vs Ghidra oracle IR):** Ghidra target = `r74:2 = SUB42(r74:4(0x1c),#0)`, `AX = r74:2 + #0x64`, `r74:4(0x2b) = CONCAT22(SUB42(r74:4,#2), AX)` — the WHOLE `0x100074:4` range re-normalizes ALL its accesses (incl. the already-seen 2-byte reads) to SUB42 every widening pass. mosura's INCREMENTAL per-location SSA CANNOT retroactively re-normalize accesses it already committed to a narrower SSA version → the exact **refinement-on-re-entry / mixed-width unification** gap in this memory's risk map + S8-1 grounding ("refine_overlaps/normalize_read_size pass-0-ONLY; re-entry does NOT unify mixed 2/4-byte accesses").
**CONCLUSION:** the scoped-restart wiring is NECESSARY, converges, and even nets a longdouble win — but is NOT SUFFICIENT: revisit needs re-heritage to RE-FREE + re-normalize the ENTIRE widened range (not just the newly-freed read). That is a heritage-INTERNAL change (make `normalize_ranges`/the pass-2 cover re-free the already-versioned narrower reads in a widened range, matching Ghidra's whole-range guard()-per-pass), NOT restart placement. = real S8-2 core; the restart is its enabler. NEXT (gated, premise-first): design the widened-range re-free in heritage, THEN re-wire the restart; measure revisit recovers + switchmulti + longpath. deindirect2 unaffected (0.936 held — the call-output-in-RAX #6 fix composed; no 4-byte-XMM regression).

## ⭐ S8-2 REAL MECHANISM GROUNDED 2026-07-10 (mloop2, READ-ONLY, Ghidra source cited). The missing port = `removeRevisitedMarkers` (NOT more normalize_ranges).
Ghidra's widening re-entry is TWO steps in `placeMultiequals` (heritage.cc:2608-2629), per merged range, BEFORE guard()/normalize:
1. **`collect()` marker-detection (heritage.cc:327-338):** when iterating varnodes in the range, a WRITTEN vn whose def `isMarker()` (MULTIEQUAL|INDIRECT) or `isReturnCopy()` = "evidence of previous heritage in this range". If that vn is NARROWER than the merged range → `remove.push_back(vn)` (schedule for removal); else `clearProperty(new_addresses)` (prior pass covered all). Free reads (`!isHeritageKnown && !hasNoDescend`) → `read`; inputs → `input`. maxsize tracked for the refinement gate.
2. **`removeRevisitedMarkers(removevars, addr, size)` (heritage.cc:244-297, called placeMultiequals:2627 when removevars non-empty):** for each scheduled narrower marker vn (def = MULTIEQUAL or INDIRECT): `opUninsert` the def, make `big = newVarnode(size, addr)` a FRESH FREE whole-range varnode (`setActiveHeritage`), rewrite the SAME op in place as `vn = SUBPIECE(big, #offset)` (offset = `vn->overlap(addr,size)`), re-insert after the markers, then `vn->setWriteMask()` (exclude from re-collection). Return-form COPY → just `opUnlink`. The fresh `big` free varnodes are then collected as free reads THIS pass → guard()/normalizeReadSize links them into the whole-range SSA → the narrower access is now `SUB42(whole,off)` = revisit's exact oracle form.
So Ghidra lets `0x100074:2` heritage early (pass 1, its own MULTIEQUAL/INDIRECT markers), then on the pass-2 widening REWRITES those markers as SUBPIECE of a fresh free `0x100074:4` whole, which re-heritages. mosura's `normalize_ranges` ports ONLY step-(guard normalizeReadSize of already-FREE reads); it has NO marker-detection + NO `removeRevisitedMarkers` → the pass-1 markers survive → hybrid IR.
**mosura gap check:** has `Op::is_marker()` (op.rs:91, MULTIEQUAL|INDIRECT) + varnode `is_indirect_creation`, but NO `removeRevisitedMarkers`, NO write_mask flag, and `collect()`-equiv (`gather_candidates`/`normalize_ranges` footprint scan) does NOT detect prior-heritage markers. STRUCTURAL twist: mosura heritages each exact (space,off,size) as ITS OWN SSA location (Ghidra merges into one range), so `0x100074:2`'s markers belong to a SEPARATE location from `0x100074:4` — the port must bridge them (rewrite the :2 location's markers as SUBPIECE of the :4 whole on widening).
**FAITHFUL S8-2 PLAN (staged, gated, premise-first):** (a) add varnode `write_mask` flag (Varnode::setWriteMask/isWriteMask); (b) port `removeRevisitedMarkers` (INDIRECT/MULTIEQUAL→SUBPIECE(big,off), COPY→unlink, mask); (c) in the widened-range re-entry (top of heritage_pass beside normalize_ranges, or fold in), detect prior-heritage markers narrower than the widened merged range → removeRevisitedMarkers → the fresh whole free reads flow into normalize_ranges/cover as today; land (a)+(b)+(c) DORMANT/byte-identical (fires only on a widening re-entry = never today, exactly like S8-1's normalize_ranges) + synthetic unit tests. (d) SEPARATE gated stage: wire the scoped restart (pipeline.rs:411) = the mover. PREDICT: revisit's :2 INDIRECT/MULTIEQUAL markers → SUBPIECE(r74:4) → `r74:2=SUB42(r74:4,#0)`, write→CONCAT22 = oracle form → recovers; longdouble +0.043 preserved (free-read widening path untouched); deindirect2 held (RAX doesn't widen). RISK: per-location vs merged-range bridge; refinement gate (size>4&&max<size — revisit is size 4 so N/A, but longdouble/switchmulti may hit it); the `bumpDeadcodeDelay`/deadremoved warning path (heritage.cc:248,2714) mosura omits — check no infinite deadcode-delay loop; write_mask interplay with gather_candidates/heritage_complete termination.

## ✅ S8-2 BRICK (b)+(c) LANDED master `32648d3` 2026-07-11 (mloop3). DORMANT/byte-identical. Suite 386/0, corpus 0.9168 (56/60, per-fixture IDENTICAL vs 8f18c22), clippy clean, perf flat.
Faithful `removeRevisitedMarkers` (heritage.cc:244-297) + collect() marker-detection (heritage.cc:327-338):
- `remove_revisited_markers(f,pass)` (heritage.rs): on a WIDENING re-entry, a written non-write-masked varnode in the widened non-refine range whose def is MULTIEQUAL/INDIRECT or is_return_copy AND narrower than the range → rewrite the op IN PLACE as `narrow=SUBPIECE(big,#off)` (`big`=fresh FREE `new_varnode(size,base)`), set_write_mask on the narrow output; INDIRECT also clear_addr_force + placed after guarded_op (its causing op); MULTIEQUAL placed via op_insert_begin (after leading multiequals); return-COPY → op_uninsert+op_destroy (unlink). Runs BEFORE normalize_ranges each pass (Ghidra order).
- `widening_ranges(f,pass) -> (merged, widened-base-set, max_write)`: EXTRACTED + SHARED by normalize_ranges AND remove_revisited_markers so both act on EXACTLY the same widening non-refine ranges (divergence = hybrid IR). Skips write-masked (Ghidra collect heritage.cc:326).
- heritage_pass gates BOTH behind ONE widening probe (`!widening_ranges(f,pass).1.is_empty()`) → dormant common case = 1 footprint scan (perf flat, measured 1.43-1.57s vs baseline 1.53-1.76s).
- gather_candidates + normalize_ranges write-side skip write-masked. `Varnode::clear_addr_force` added.
- 3 synthetic unit tests (MULTIEQUAL→SUBPIECE+writemask+fresh-free-read+not-recollected; INDIRECT→SUBPIECE+addrforce-cleared+after-call; return-COPY→unlinked/dead). coverage.md rows updated (removeRevisitedMarkers + collect write-mask skip).
- OMITTED (documented): deadremoved warning + bumpDeadcodeDelay (heritage.cc:248, unreachable — brick removes no dead code); full-width-marker clearProperty(new_addresses) (heritage.cc:334 — subsumed by mosura's intersect==2 cover logic).
- GOTCHA fixed en-route: the pre-`8f18c22` write_mask flag groundwork is the enabler; big is FREE (mosura needs no setActiveHeritage — new_varnode yields a free varnode gather_candidates collects).
## ⭐ S8-2 (d) RESTART MEASURED + HELD 2026-07-11 (mloop3, restart wiring UNCOMMITTED then REVERTED byte-identical; tree clean at brick `32648d3`, corpus 0.9168). Brick WORKS; restart not yet SUFFICIENT (residual = missing oppool1-in-loop = S8-3).
Wired `ActionGroup::restart("reheritage"){ ptrarith_pool → ActionHeritage → deadcode }` at pipeline.rs:411 (before cleanup_pool), measured full corpus, reverted.
**MEASURE (MOVER):** corpus 0.9168→**0.9166 (−0.0002)**, 56/60. Per-fixture (ONLY movers): **longdouble 0.909→0.952 (+0.043)** [clean free-read-widening win, as S8-1 predicted]; **revisit 0.633→0.618 (−0.015)**; **switchmulti 0.628→0.586 (−0.042)**. deindirect2 HELD 0.936 (call-output-in-RAX #6 fix composed; no regress).
**BRICK VERIFIED WORKING (revisit):** restart-only (mloop2) was revisit 0.477; brick+restart = **0.618 (+0.141 recovery)**. The baseline SNAPSHOT reads (`iVar1=iRam74; iVar2=iRam74` — two unlinked pre-value copies) are ELIMINATED — reads now LINK to the whole-range SSA (`uVar1 = iRam74 + 10; iRam74 = uVar1`), matching Ghidra's linked `iRam74 = iRam74 + 10`. removeRevisitedMarkers did its job.
**REVISIT RESIDUAL (−0.015 despite structural win) = REDUNDANT NARROW WRITE.** IR at pc 0x10001a has TWO writes: widened `r0x100074:4 = COPY u0x10018:4` (op :17) PLUS a redundant `r0x100074:2 = SUBPIECE(u0x10018:4,#0)` (op :77) → prints the spurious extra `iRam74 = (int2)uVar1;`. Ghidra collapses this to ONE 4-byte write because its mainloop RE-RUNS oppool1 (RulePropagateCopy/DumptyHump/deadcode) AFTER re-heritage. mosura's scoped restart runs ONLY {ptrarith→heritage→deadcode} — NO full oppool1 in the loop — so the widened-write pieces don't collapse. The extra statement costs more on the text metric than the read-linking gains → net −0.015. Also pre-existing P6 void-return caps it (`uint8 func`+`return CONCAT62` vs oracle `void func`+`return;`) — unrelated.
**SWITCHMULTI −0.042 = NOT brick-caused** (IDENTICAL to mloop2 restart-only −0.042; the brick doesn't touch it). Restart re-heritage perturbs switchmulti's dual-width switch selector (cf. [[switchloop-dup-propagatecopy]]); its C diff is dominated by PRE-EXISTING gaps (jumptable "Too many branches", param stack-spills, void-return) → a separate restart-interaction, an S8-3 item.
**VERDICT: HELD (per gate policy — didn't cleanly recover revisit + switchmulti regressed).** The brick is CORRECT and lands DORMANT-clean (`32648d3`). The restart is NECESSARY + directionally right (longdouble +0.043, revisit +0.141 vs restart-only) but NOT SUFFICIENT alone. NEXT S8-3 = put oppool1 in the loop — lead GO'd instrument-only. (measured below).

## ⭐ S8-3 FULL-MAINLOOP MEASURED + HELD 2026-07-11 (mloop3, oppool1-in-loop UNCOMMITTED, action.rs convergence instrumentation, REVERTED byte-identical; tree clean `32648d3`, corpus 0.9168). HYPOTHESIS DISPROVEN — residual is guardCalls-for-ram, NOT oppool1.
Wired `restart("reheritage"){ ptrarith_pool → ActionHeritage → default_rule_pool(oppool1) → deadcode }` @pipeline.rs:411 + env-gated (MOSURA_MAINLOOP_DBG) iter-counter + 200-cap in ActionGroup::apply. Measured, reverted.
**CONVERGENCE = CLEAN + PERF FLAT.** 74 functions: reheritage iters 44×1 / 30×2 (max 2 = ONE productive pass + confirm), **0 cap-hits (no oscillation/non-termination)**. Wall 1.68s = baseline (oppool1-in-loop does NOT blow perf — most functions converge in 1). The full-mainloop restart PRIMITIVE is sound + safe.
**CORPUS = BYTE-IDENTICAL to restart-only (0.9166, same 3 movers longdouble+0.043/revisit−0.015/switchmulti−0.042). oppool1-in-loop folds NOTHING extra + introduces NO new regressions** (no other fixture moved — oppool1 re-flow is stable). My S8-2 hypothesis ("Ghidra folds the redundant :2 write via oppool1-repeat") is DISPROVEN.
**REAL ROOT CAUSE (traced Ghidra IR vs mosura IR, revisit):** mosura does NOT guard RAM globals across CALLs. `guard_calls` (heritage.rs:1017) handles reg (sysv effect) + aliased-stack (unknown) ONLY — a RAM range hits the `else { return }` → NO passthrough INDIRECT. So the +10 STORE can't thread through a call INDIRECT: mosura materializes `r0x100074:4 = COPY u0x10018` (WRITE1) + write-masked `r0x100074:2 = SUBPIECE(u0x10018,#0)` (WRITE2, = the brick's rewritten :2 marker after oppool1 copy-propagated big_4→u0x10018), and the post-call `+100` (op :24) reads WRITE2 = the PRE-call 2-byte value → prints spurious `iRam74 = (int2)uVar1;`. **Ghidra IR:** `r0x100074:4(:5c) = EAX [] iop(call)` (guardCalls unknown_effect passthrough INDIRECT threads the +10 result across the call) → post-call `r0x100074:2 = SUB42(r0x100074:4(:5c),#0)` reads the POST-call whole → unified `(int2)iRam74`, NO separate write. So the residual is the MISSING guardCalls-for-ram-globals (heritage guard gap), which the widening SURFACES — NOT oppool1, NOT the brick (brick correctly recovered the +10 INPUT read = the structural win).
**switchmulti −0.042** = independent restart re-heritage perturbation of the dual-width switch selector (cf [[switchloop-dup-propagatecopy]]), unchanged by oppool1.
**VERDICT: HELD.** Mainloop primitive WORKS + is safe (clean converge, flat perf, no new regressions) but net-NEGATIVE to land now (−0.0002; revisit capped by guardCalls-ram gap, switchmulti regressed). **UNBLOCK for revisit = guardCalls for ram globals across calls** (port the ram `unknown_effect` passthrough INDIRECT branch of guardCalls, heritage.cc:1443) — a HERITAGE-GUARD deliverable, not mainloop. Prereqs to landing the mainloop: (a) guardCalls-for-ram, (b) switchmulti dual-width selector. RECOMMEND: harvest independent rocks; take the full-mainloop as its own deliberate rock gated behind (a)+(b). WAITING on lead.

## ⛔ S8-3 PREMISE FALSIFIED 2026-07-11 (mloop5, base `e86b7d4`, READ-ONLY Ghidra trace + staged-IR, tree clean, NO code). floatcast's return CONCAT44 is a **SplitFlow/Piece2Zext/Pullsub** deliverable, NOT LaneDivide/return-recovery iteration. Report sent lead, STOP.
Task #17 S8-3 = "broaden the mainloop restart to iterate ActionLaneDivide + return-recovery so XMM0 narrows before the return decomposition (floatcast lever)." **The premise does NOT hold — this is re-frame #6, caught by Ghidra's own trace (the authority).**
- **floatcast oracle target** (`return CONCAT44((int4)((uint8)(fVar1-fVar2)>>0x20),(float4)(fVar1-fVar2))`, avg 0.776 @e86b7d4): Ghidra ALSO uses the global `fRam..80` (so PIECE-4 global-vs-local is NOT a corpus divergence — p4infer1's "global-vs-local" was a consequence of the 16-byte width, not a real gap).
- **RAW LIFT IDENTICAL** mosura==Ghidra: `MOVSD XMM0,[glob]` → two 8-byte writes `r0x1200:8=COPY glob` + `r0x1208:8=COPY #0x0` (XMM0_Qa + XMM0_Qb=0). Both heritages then normalize to the 16-byte form `XMM0=CONCAT88(#0x0,Qa)` (mosura: `r0x1200:16 = PIECE #0x0:8 value`, verified `--ir returnrecovery` shows Ghidra's identical CONCAT88 post-heritage). So NO raw/heritage divergence — same 16-byte starting form BOTH sides.
- **GHIDRA NARROWS VIA oppool1 SubVariableFlow-family RULES, all firing EARLY (first stackstall pass, DEBUG 17-38, BEFORE lanedivide=126 + re-heritage=129):** `piece2zext` (RulePiece2Zext, coreaction.cc:5614 — `CONCAT88(0,Qa)→ZEXT816(Qa)`, 4×), **`splitflow`** (RuleSplitFlow/SplitFlow, subflow.cc:2039/1754, coreaction.cc:5623 — the KEY: splits the 16-byte XMM0 MULTIEQUAL/CONCAT88/SUB168 dataflow into 8-byte Qa/Qb lanes; Qb=0 lane dies via earlyremoval), `pullsub_multi` (RulePullsubMulti — pushes SUB168 through the phi). The return **CONCAT44 is built on the 8-byte Qa BEFORE lanedivide runs** (confirmed `--ir lanedivide` dump already has `CONCAT44(SUB84(XMM0_Qa,#4), FLOAT2FLOAT(XMM0_Qa))`).
- **lanedivide fires EXACTLY ONCE (DEBUG 126)** and ONLY rewrites the input-param extract `XMM2_Da=SUB164(XMM0(i),#0)→XMM0_Da(i)` (the MOVAPS/CVTSS2SD param path). It does NOT touch the return. So iterating LaneDivide would NOT narrow the return; return-recovery is likewise not the lever.
- **mosura INVENTORY (grep-confirmed):** `RulePiece2Zext` = PORTED (rules.rs:3906) + unit-tested but **UNWIRED** (pipeline.rs:227 comment "HELD until re-measured after RuleSubvarZext narrows returns"); **`RuleSplitFlow`/`SplitFlow` = NOT ported** (only the shared TransformManager framework transform.rs exists — the same base LaneDivide uses; SplitFlow ≈ 319 lines subflow.cc:1754-2100); **`RulePullsubMulti` = NOT ported**; `RulePushMulti` (coreaction.cc:5518) also absent.
- **CONCLUSION:** floatcast belongs to the SubVariableFlow/SplitFlow track (task #18-adjacent), NOT mainloop S8-3. The narrowing is a first-oppool-pass job (all pre-iteration) → likely does NOT even need the iterating mainloop. p4infer1's/mloop4's flat LaneDivide-reactivation measures (0.776→0.774) are EXPLAINED: LaneDivide isn't the return's mechanism. NEITHER piece-1 (XMM0:4 retire) NOR piece-4 (global-forwarding) is the lever. **RECOMMEND: STOP S8-3 as framed; redirect me to port RuleSplitFlow + RulePullsubMulti + wire RulePiece2Zext (bounded faithful port on the existing framework, gated mover), OR pick a mainloop-relevant target.** See [[task6-lanedivide-plan]], [[task9-subvariableflow-plan]].

## ✅ LEAD ACCEPTED REDIRECT → task #17 reframed = SplitFlow port (mainloop-independent). GROUNDED + staging, mloop5 2026-07-11 @e86b7d4.
Lead rewrote task #17 = "SplitFlow XMM 16→8 narrowing (RuleSplitFlow + RulePullsubMulti + wire RulePiece2Zext) — TRUE floatcast lever, mainloop-independent." Read-only grounding of the port:
- **RuleSplitFlow (subflow.cc:2039 applyOp / :1754 SplitFlow class, ~330 lines):** triggers on a SUBPIECE that takes the HIGH part (`loSize=in1.offset != 0`, `out.size + loSize == in0.size`) of a value written by a PIECE reached directly or through INDIRECT/MULTIEQUAL, with `concatOp.in(1).size == loSize`. Builds `SplitFlow(data, vn, loSize)` = a 2-lane (low/high) TransformManager; doTrace pushes the split fwd/back through COPY/MULTIEQUAL/INDIRECT/AND/OR/XOR/SUBPIECE/INT_LEFT/SRIGHT/RIGHT (fwd) + COPY/MULTIEQUAL/AND/OR/XOR/INDIRECT/PIECE/INT_ZEXT/INT_LEFT (back); apply(). **INDEPENDENT of laned-register metadata (Spec.laned) — fires on ANY SUBPIECE-of-PIECE, so floatcast needs NO LaneDivide reactivation.**
- **transform.rs framework READY (grep-confirmed):** every primitive SplitFlow needs — new_split/get_split/new_op_replace/new_preexisting_op/new_op/new_unique/new_constant/new_iop/inherit_indirect/op_set_input/op_set_output/apply/clear_varnode_marks/get_preexisting_varnode/LaneDescription(lane_size/whole_size) — already exists + is exercised by lanedivide.rs. SplitFlow is a clean mechanical port, structurally == lanedivide.rs.
- **RulePiece2Zext = PORTED (rules.rs:3906) UNWIRED.** Hold reason (rules.rs:3899 doc, likely STALE): floatconv over-fire (mosura 2× vs Ghidra 1×) = "same SubVariableFlow gap as RuleSubZext, wire once that lands (Task #9)". RuleSubvarZext IS now wired (pipeline.rs:239) → the over-fire may be resolved; re-measure at wire. Impl: PIECE with const-0 high → drop input0, set opcode IntZext (trivial).
- **RulePullsubMulti (ruleaction.cc:683-977, ~290 lines):** pulls SUBPIECE down through a MULTIEQUAL. In Ghidra's floatcast trace it handled the PARAM side (XMM2_Qa=SUB168(phi,#0)); mosura ALREADY recovers `(float8)param_1` (baseline C), so **PullsubMulti is likely DEFERRABLE for floatcast** — confirm at S2 measure.
- **floatcast fix = SplitFlow + wire Piece2Zext.** Baseline C already has `(float8)param_1`; the two gaps (`CONCAT88(0,x)` wrapping + return `(xunknown8)CONCAT124`) are BOTH the un-narrowed 16-byte XMM0 → SplitFlow fixes both.
- **FLAGGED RISK (S1 firing-check):** mosura's FINAL IR uses an `INT_RIGHT #0x40 + SUBPIECE #0` idiom for high-extract where Ghidra triggers on `SUBPIECE #8`. Raw+heritage shapes are identical (both `PIECE #0x0:8` + `MULTIEQUAL`), so the trigger likely presents at the oppool stage, but VERIFY RuleSplitFlow fires on floatcast's mid-pipeline shape as S1's first check.
- **STAGING (sent lead):** S1 = port SplitFlow class + RuleSplitFlow, UNWIRED + unit test → byte-identical, self-approve (+ firing-check on floatcast). S2 = wire RuleSplitFlow + RulePiece2Zext into oppool → gated MOVER, full corpus delta @sha (expect floatcast/concatsplit/stackstring narrow; watch floatconv) → report + WAIT. S3 (conditional) = RulePullsubMulti if a residual needs it, separate gated.

## ✅✅ S1 + S2 BOTH LANDED master (mloop5, 2026-07-11, lead GATE-CONFIRMED path (a) two commits). S1 `d171301` (byte-identical brick), S2 `f4d511b` (floatcast mover). Corpus avg **0.9220 @f4d511b / 56**, suite 397/0, switch 6/6, tree clean, clippy clean.
S2 (`f4d511b`): wired RulePiece2Zext @RuleFloatRange (coreaction.cc:5614) + RuleSplitFlow @RuleSubvarSubpiece (coreaction.cc:5623) with faithful doc comments; coverage.md RuleSplitFlow→PORTED/WIRED + RulePiece2Zext→PORTED/WIRED (hold resolved). **floatcast 0.776→0.845 (+0.069), the ONLY mover, ZERO regressions across all 60.** floatconv unchanged 0.653 (the Piece2Zext hold's over-fire was the wide-return divergence, cleared by the now-wired RuleSubvarZext — empirically confirmed, not a guess). Residual (task #21) noted in coverage.md + pipeline.rs doc.

--- pre-land record ---
## ✅ S1 BUILT + S2 MEASURED (mloop5, base e86b7d4, wire REVERTED → tree = S1-only: splitflow.rs new + mod.rs M; pipeline.rs clean). Report sent lead, WAIT for gate.
- **S1 = `crates/mosura/src/decompile/splitflow.rs`** (new, ~430 lines): faithful port of Ghidra `SplitFlow` (subflow.cc:1754-2037) + `RuleSplitFlow` (subflow.cc:2039-2088) on the existing transform.rs TransformManager. 2-lane (lo,hi) split via `LaneDescription::two`; setReplacement/addOp/traceForward/traceBackward(COPY/MULTIEQUAL/INDIRECT/AND/OR/XOR/SUBPIECE/PIECE/ZEXT/INT_LEFT/SRIGHT/RIGHT)/processNextWork/doTrace; INDIRECT via guarded_op (mosura 1-input, like lanedivide.build_indirect); typelock→reject (mosura has no TypePartialStruct). +1 unit test (joined value read by a high SUBPIECE splits into lanes). Registered `pub mod splitflow` (mod.rs). **BYTE-IDENTICAL unwired: suite 397/0, corpus avg 0.9209/56 per-fixture identical.**
- **FIRING-CHECK RESOLVED (the flagged INT_RIGHT#0x40-vs-SUBPIECE#8 risk):** RuleSplitFlow DOES fire on floatcast — the MULTIEQUAL-join trigger presents at oppool stage. It narrows the operand join-paths (`CONCAT88(0,xRam..80)`→direct `fRam..80` 8-byte).
- **S2 MEASURED (wire RulePiece2Zext @RuleFloatRange coreaction.cc:5614 + RuleSplitFlow @RuleSubvarSubpiece coreaction.cc:5623, Ghidra-faithful slots):** **floatcast 0.776→0.845 (+0.069), avg 0.9209→0.9220, the ONLY mover, ZERO regressions across all 60** (full name-keyed per-fixture join). Suite 397/0. **floatconv UNCHANGED 0.653** — the stale RulePiece2Zext hold reason (floatconv over-fire "wire once SubVariableFlow lands") is RESOLVED (RuleSubvarZext is now wired, pipeline.rs:239). Both operands narrow (fRam..80, uVar1=uRam..88 vs baseline CONCAT88(0,x)).
- **RESIDUAL (why 0.845 not ~0.95): the RETURN decomposition stays `(xunknown8)CONCAT124((xunknown12)(diff>>0x20),(float4)diff)` vs Ghidra `CONCAT44((int4)(diff>>0x20),(float4)diff)`.** Root: the FLOAT_SUB result → CVTSD2SS → return chain is a STRAIGHT-LINE PIECE→SUBPIECE#0 (no MULTIEQUAL-join high-SUBPIECE trigger), so RuleSplitFlow (correctly, faithfully) does NOT fire on it (Ghidra's `vn->getDef()!=multiOp` guard rejects a direct PIECE→SUBPIECE). Piece2Zext turns the return's `PIECE #0:8 diff`→`ZEXT816(diff)`, re-widening the 8-byte diff into the 16-byte XMM0 → the `INT_RIGHT #0x20`/`SUBPIECE:12` decomposition runs 16-byte. = the SAME XMM0 heritage-16-byte-merge root; a deeper follow-on (the FLOAT_SUB→return chain never gets narrowed to XMM0_Qa because mosura's return reads the reassembled 16-byte XMM0). DEFERRED.
- **RulePullsubMulti NOT needed** for this win (the param side already recovers). Deferred.
- **NEXT = lead gate on S2 (the mover).** Then finalize: doc comments on both wires (cite coreaction.cc:5614/5623) + coverage.md SplitFlow/Piece2Zext rows + commit. Placement re-measured identical at faithful slots.

**SCOPE VERDICT (mloop2, sent lead 2026-07-10): BOUNDED** (not the DEEP incremental-SSA→whole-space restructure). Decisive: removeRevisitedMarkers = the SAME `new_varnode(size,base)`+`SUBPIECE(whole,off-base)` transform normalize_ranges ALREADY does for free reads (heritage.rs:472-477), applied at the marker DEF site; mosura's globaldisjoint already merges ranges Ghidra-faithfully so the incremental per-location model is PRESERVED. Ghidra confirmed NOT whole-space-per-pass — same cumulative-merged-range incremental model, just with the re-free step mosura lacks. **STATUS: grounded, verdict sent, NO CODE — waiting on lead's gate to build (a)+(b)+(c) dormant brick, then (d) gated restart.**

GOTCHAS (inherited): dump_all hangs non-x86-64 (filter x86:LE:64); firing-grep masks panics (use exit codes + C diff + green suite); oracle final IR = `oracle/capture ../ghidra <fixture.xml> --ir -` / `--c`; mosura IR dump = `cargo run -q --example dump -- <stem> --ir`.

---
name: task4-heritage-guard-collect-plan
description: "Task #4 turnkey plan — port Ghidra's faithful heritage guard/collect layer, replacing recover.rs INDIRECTs + refine_overlaps laned adaptations"
metadata: 
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #4 (APPROVED by user 2026-07-02 under "decompiler restructures" + no-grandfathering): port Ghidra's faithful heritage guard/collect layer into mosura, REPLACING the non-faithful adaptations standing in for it. Base HEAD `786f91e`. Owner=ruleport. Large, high-churn, multi-commit; lead gates EACH commit + corpus delta (a faithful dip doesn't block — fix the remaining non-Ghidra cause). See [[direction-faithful-port]] [[port-all-faithful-rules]].

GHIDRA STRUCTURE (grounded):
- `Heritage::heritage()` (heritage.cc:2663): per pass, iterate infolist by delay group; build the `disjoint` cover from free varnodes (globaldisjoint LocationMap, address-ordered); then `placeMultiequals()`; then `rename()`.
- `Heritage::placeMultiequals()` (heritage.cc:2599): FOR each range in address-ordered `disjoint`: `collect(range, read,write,input,remove)` -> if size>4 && max_write<size: `refinement()` -> `guardInput(addr,size,input)` -> `guard(addr,size,newAddrs, read,write,input)` -> `calcMultiequals(write)` -> create MULTIEQUAL per merge block. mosura's phi creation already matches address order (Task #21 `786f91e`).
- `Heritage::guard()` (heritage.cc:1156): normalize each read < size via `normalizeReadSize` + each write < size via `normalizeWriteSize` (setActiveHeritage); then if addIndirects: queryProperties, `guardCalls(fl,addr,size,write)`, `guardReturns(...)`, and if `highPtrPossible(addr,size)`: `guardStores(...)` + `guardLoads(...)`.
- `guardCalls` (1443): for each CALL/CALLIND/CALLOTHER between defs that may modify range, insert INDIRECT (+ guardCallOverlappingInput 1210 for over-large param). `guardStores` (1538): INDIRECT around STOREs. `guardReturns` (1652). `guardInput` (1952): fill input-range holes with setInputVarnode pieces + `concatPieces` into one input.

MOSURA CURRENT (what replaces what):
- `heritage.rs::heritage_spaces` (~832): semi-pruned Cytron (globals + defblocks -> phis at DF + rename). NO collect/guard/disjoint-driven placeMultiequals.
- `heritage.rs::refine_overlaps` (~457) + `normalize_read_size`/`normalize_write_size` (228/375): per-range read/write normalization, LANED-scoped (the adaptation to REPLACE with the faithful uniform guard()). normalize_read_size/write_size are FAITHFUL ports already — reuse them.
- `recover.rs`: call INDIRECT insertion (killedbycall, passthrough) as a SEPARATE pre-heritage phase — REPLACE with guardCalls inside guard().
- refineInput logic in refine_overlaps ~595 -> REPLACE with guardInput.
- MISSING entirely: guardStores/guardLoads/guardReturns, the disjoint/collect structure, highPtrPossible.

PROPOSED GATED COMMITS (each: green + corpus reported; refine decomposition during exec):
1. collect() scaffolding: per disjoint range gather read/write/input varnode vectors (Ghidra collect) — inert groundwork.
2. Route read/write normalization through guard() over collected vectors, UNSCOPED (retire refine_overlaps laned-scoping; reuse normalize_read_size/write_size). Corpus WILL move — measure, fix non-Ghidra fallout.
3. guardInput (fill holes + concatPieces) replacing refineInput.
4. guardCalls into guard(), retire recover.rs INDIRECT insertion.
5. guardStores/guardLoads/guardReturns (new) + highPtrPossible.
6. Drive placeMultiequals from the disjoint cover (collect->guard->calcMultiequals) if not already.

RISK: HIGHEST blast radius in the codebase (every fixture re-flows). Prior laned-scoping was BECAUSE broad application regressed GP/scalar — expect corpus dips; per no-grandfathering they're symptoms of OTHER non-Ghidra code to fix (SubVariableFlow #9 dissolves byte-packing; may be entangled). Ground each commit's IR with the (working) dump. STOP+report if a commit needs a subsystem mosura lacks.

KEY GROUNDING FINDING (2026-07-02, reshapes the plan): mosura's INDIRECT is a SIMPLIFIED 1-input model (before-value only). Ghidra's `Funcdata::newIndirectOp` (funcdata_op.cc:683) creates a 2-INPUT INDIRECT: in(0)=newVarnode(sz,addr) [before-value], in(1)=newVarnodeIop(indeffect) [an "iop" annotation referencing the causing CALL/STORE]. mosura has NO iop space / newVarnodeIop (grep clean); consume.rs:113 explicitly documents OMITTING "Ghidra's IOP branch (setIndirectSource) ... a dead-code-removal detail." recover.rs builds INDIRECTs as `new_op(Indirect, [pre])` (1 input). CONSEQUENCE: guardStores/guardCalls's `newIndirectOp` either (a) uses mosura's established 1-input INDIRECT form (CONSISTENT with recover.rs, introduces NO new divergence, but guards are behavior-CHANGING not neutral) or (b) requires porting the iop 2-input model first = a big REPLACE across printc/consume/merge/deadcode. There is NO meaningful behavior-NEUTRAL scaffolding commit (Ghidra guard structure alongside mosura's semi-pruned Cytron = dead code). So the lead's "additive behavior-neutral scaffolding first" doesn't map cleanly; the genuinely-absent guardStores is behavior-changing.

RECOMMENDED RESUME START: COMMIT 1 = guardStores using mosura's EXISTING 1-input INDIRECT form (like recover.rs) — additive (removes nothing), behavior-changing (gated: report corpus), consistent with mosura's INDIRECT model, avoids the huge iop-model change. Hook: heritage_pass per heritaged range, insert an Indirect for each STORE that may alias the range (same space / spacebase container, cf. guardStores heritage.cc:1538), add its output to the range's writes so phi placement accounts for it. Defer the iop 2-input model as a separate faithfulness question (mosura omits iop EVERYWHERE — it's a pre-existing simplification, not #4's job).

STATUS 2026-07-03: guardCalls (M2) LANDED `7e06aa2` — lead-APPROVED (faithful-authoritative GO; lead re-verified 207/0). Ported Ghidra `Heritage::guardCalls` (heritage.cc:1443) as per-range `guard_calls` in heritage_spaces step 0 (before guard_stores, Ghidra guard() order), driven by lookup_effect/hasEffect: killedbycall→indirect-CREATION (`#0` const in, set_indirect_creation out), unknown_effect/return_address→passthrough newIndirectOp (INCLUDING the R10/R11/flags branch old recover_call_effects WRONGLY omitted — now faithful, corpus-safe/DCE'd), unaffected→nothing. INDIRECTs spliced AFTER the call (op_insert_after) to match placement resolve_call_output consumes. DELETED recover_call_effects (recover.rs) + its pipeline pre-heritage call. Threaded 2 Funcdata fields: `alias_boundary: Option<i64>` (stack gate) + `call_guards_active: bool` (master gate — probe clone leaves false so AliasChecker boundary computed WITHOUT call INDIRECTs; pipeline sets both true before real heritage). +1 unit test guard_calls_models_call_effects. Corpus 0.8659→0.8655 (−0.0004), 54/60 unchanged; 4 fns moved (deindirect2/switchhide gauge+, loopcomment/stackstring gauge−) but oracle-grounded 3-of-4 TOWARD Ghidra (stackstring correctly void; loopcomment/switchhide params int4 not over-widened xunknown8; loopcomment `if(param_3==1000)` matches). KEY MECHANISM: old width-8 killedbycall creations ran PRE-normalize_read_size, over-widening caller-clobbered sub-regs to 8 (non-Ghidra); faithful guardCalls runs AFTER normalize (Ghidra order) so that over-widening is shed. Lone casualty loopcomment while-operand = #15-adjacent (un-dissolved CONCAT71 bit-packing SubVariableFlow dissolves). Output/input TRIAL branches (characterizeAsOutput/InputParam) = P6 gap. Task #1 TRACTABLE migrations (guardStores+guardCalls) DONE; remainder gated: guardInput unification=follow-up, M1 partition→#15, M3 guardReturns→P6. Next per standing-continuation: Task #2 (P4 Types).
PRIOR STATUS 2026-07-03: COMMIT 1b (guardStores) LANDED `aa5edef` — lead-APPROVED, faithful, byte-neutral, 206 green. Ported Ghidra `Heritage::guardStores` (heritage.cc:1538) as `guard_stores` in heritage.rs, hooked step 0 of `heritage_spaces` per newly-heritaged range. GROUNDED: STORE in(0) = const encoding SpaceId index (build.rs `new_const(8, sid.0)`); decode `SpaceId(f.vn(in0).loc.offset as u32)`. Fires on `spc == storeSpace` disjunct. The `container==storeSpace && usesSpacebasePtr()` disjunct is a DEFINITIONAL no-op — `usesSpacebasePtr` set ONLY by generateLoadGuard/generateStoreGuard (heritage.cc:915/932) = LoadGuard/discoverIndexedStackPointers subsystem = Task #19 (mosura never marks the flag → always false). highPtrPossible = "not unique space" (x86-64 cspec has NO <nohighptr>). addIndirects=newAddresses() modeled by threading `new_addrs` (intersect != 2) into heritage_spaces so re-heritage (intersect2+freed) doesn't re-add. Corpus BYTE-IDENTICAL 0.8659/54-of-60 zero movement; guard_stores CALLED 12434x, FIRES 20x on fixed-addr ram globals (INDIRECTs DCE'd where alias immaterial). +1 direct unit test `guard_stores_indirects_aliasing_stores`. Tree GREEN at aa5edef.
NEXT: migrations (each own GATED corpus-mover commit). MIGRATION TRACTABILITY MAP (grounded 2026-07-03, read-only):
- Migration 1 (refine_overlaps laned-scoping → uniform guard() normalization): the guard() NORMALIZATION half is ALREADY faithful+landed — refine_overlaps applies normalizeReadSize/normalizeWriteSize UNIFORMLY to every GP range a single write covers (Mode::Normalize, heritage.rs:564). ONLY the *partition* refine (CONCAT/SUBPIECE split of ranges NO single write covers) is laned/XMM-scoped. Broadening that partition to GP is GATED ON TASK #9 (SubVariableFlow) — code comment heritage.rs:537 + plan RISK note: broad GP partition "explodes the rule pool"; #9 dissolves that byte-packing. NOT independently landable.
- Migration 3 (recover_return heritage.rs recover.rs:82 → guardReturns heritage.cc:1652): NEEDS (a) `characterizeAsOutput` — ABSENT in mosura (grep-clean); (b) `active_output` set up BEFORE heritage — currently set POST-heritage (recover.rs:151, two-phase DEFERRED param recovery); (c) markReturnCopy + collect isReturnCopy + persist→return-copy (persist/set_addr_force primitives EXIST, varnode.rs:126/134). Entangled with param-recovery ordering = P6 Prototypes (task list #4). mosura's recover_return is a hardcoded RAX@8/XMM0@8/XMM0@4 pre-heritage append — a slice of guardReturns part-1 only. Not self-contained.
- Migration 2 (recover.rs call-INDIRECTs → guardCalls heritage.cc:1443): MOST TRACTABLE. recover_call_effects (recover.rs:255) is ALREADY a partial guardCalls port (cites it; uses EffectRecord/sysv_effect_list/lookup_effect + alias_boundary + active_inputs ParamActive — all present fspec.rs). Migration = relocate the INDIRECT insertion from the pre-heritage whole-fn phase INTO guard() per-range during heritage. Corpus-mover, real blast radius, but subsystem pieces EXIST.
DEEPER GROUNDING 2026-07-03 (all 3 migrations, read-only + instrumented). "synthetic-entry alignment" DROPPED by lead (planning shorthand, no Ghidra basis; fold input-handling into guardInput). Lead recorded #1-completion depends on #15 (SubVariableFlow 3-4). Standing land policy: NEUTRAL faithful lands self-approve+report-after; CORPUS-MOVERS gate (report delta+non-Ghidra-cause BEFORE, wait for go).
- guardInput: INSTRUMENTED — genuinely diverges (120 corpus overlap events: XMM0:4-vs-:8 float params [86], GP :4-vs-:8 params, a stack slot). mosura's lazy current_def→new_input (heritage.rs:987) keeps overlapping input reads INDEPENDENT; Ghidra guardInput (heritage.cc:1952) unifies to one whole-range input + narrow reads as SUBPIECE via concatPieces. REAL migration but INTRICATE (multi-pass input collection/concatPieces); corpus effect unmeasured; refineInput comment (heritage.rs:595) says mosura kept inputs whole to avoid un-rejoined pieces = #9-adjacent risk. = MEASURED FOLLOW-UP, not a quick land.
- guardCalls: recover_call_effects (recover.rs:255) runs PRE-heritage over a WHOLE-FUNCTION varnode set — per CALL inserts killedbycall reg indirect-CREATIONS (8-byte, lookup_effect KILLEDBYCALL) + aliased-stack passthrough INDIRECTs (offset>=alias_boundary, set_addr_force). Ghidra guardCalls (heritage.cc:1443) is PER-RANGE during guard(), driven by fc->hasEffect(transAddr,size): killedbycall→newIndirectCreation, unknown/return_address→newIndirectOp. Coherent faithful relocation = new `guard_calls` in heritage_spaces per new range (like guard_stores), driven by lookup_effect; DELETE recover_call_effects from pipeline.rs pre-heritage phase; reconcile alias_boundary (probe-heritage) into per-range stack guarding. guardCalls' output/input TRIAL branches (characterizeAsOutput/InputParam) = P6/absent = documented gap (like guardStores' usesSpacebasePtr #19 gap). This is a LARGE pipeline-restructure corpus-mover (NOT a mechanical relocation) — its OWN warm-budget commit.
- guardReturns: BLOCKED — needs characterizeAsOutput (absent) + active_output pre-heritage (currently post). = P6.
STATUS: STOPPED at green boundary aa5edef after grounding (budget). guardStores DONE. All 3 remaining migrations are substantial + partially subsystem-gated (#9/#15 + P6). guardCalls is the most-coherent faithful one = next, as its own warm commit. Turnkey plan above. Alt per standing-continuation: Task #2 (P4 Types) is independently tractable while #1 migrations wait on #15/P6.
PRIOR STATUS 2026-07-02: COMMIT 1a LANDED `70b46b8` — `Funcdata::new_indirect_op` primitive (1-input INDIRECT form, funcdata_op.cc:683), additive + behavior-neutral (not wired), +1 unit test, 205 green.
PRIOR: NO #4 code committed. Tree GREEN at 786f91e. Stopped at green boundary per lead's HARD INVARIANT (never half-land; don't start a commit you can't finish on a low budget) after grounding revealed the iop-model entanglement. Turnkey for resume.

CONTEXT NOTE: flagged to lead that this multi-commit restructure is best executed on a fresh/warm budget; this plan is turnkey for a cheap resume.

---
name: actionsetcasts-campaign
description: "ActionSetCasts port (retire print-time-cast adaptation) — Brick 1 landed; Brick 2 (castInput) byte-identical is architecturally gated behind retiring mosura's print-time type RE-inference."
metadata: 
  node_type: memory
  type: project
  originSessionId: 99103a08-9d06-430b-8446-4ca5e3757106
  modified: 2026-07-19T20:50:24.992Z
---

Lead's B-class faithfulness campaign: port Ghidra `ActionSetCasts` (coreaction.cc, group "casts", TERMINAL — added at coreaction.cc:5735, AFTER cleanup/merge/NameVars) to retire mosura's print-time cast adaptation (printc `get_input_cast`/`cast_operand` + `cast.rs::cast_standard`). Corpus-NEUTRAL (does NOT fix impliedfield — casts are terminal). Staged, gated, byte-identical goal.

**Brick 1 ✅ LANDED `3579ac3`** (cssa1 2026-07-19): printc renders a `CAST` op as `(dt)x` where `dt = type_of(output)` — faithful `PrintC::opTypeCast` (printc.cc:448, `getOut()->getHighTypeDefFacing()`), prec 14 like IntSext/FloatInt2float. Byte-NEUTRAL (SLEIGH never emits CAST, no rule makes one — zero CAST ops reach the printer). Was the catch-all `other => "{NAME}(...)"`. Gates: corpus 0.9528/57 identical, suite 509/0, clippy 0, jumptable 6/6.

**castInput GROUNDED (Ghidra, decisive):** `ActionSetCasts::castInput` (coreaction.cc:2655): `ct = getInputCast(op,slot)`; ct==null → markExplicitUnsigned/markExplicitLongSize (constant U-suffix; = mosura `mark_explicit_unsigned`, stays print-time); constant input → `updateType(ct)` NO cast op; else insert `vnout(implied unique, size=in, type=ct) = CAST(vnin)`, rewire slot. `apply` (2722): blocks in dominance order, ops in block order, skip CAST/notPrinted, per input slot resolveUnion+castInput, then castOutput. castOutput (2532, Brick 3) = output-token-vs-outvn-type mismatch. Struct-offset0→PTRSUB (insertPtrsubZero 2630) + union (resolveUnion 2490) are aggregate, deferred.

**★ THE BYTE-IDENTICAL BLOCKER (cssa1 grounding, decisive — the framing is incomplete):** Ghidra runs ActionInferTypes in the MAINLOOP, then ActionSetCasts TERMINAL, then prints reading HighVariable types — **infer NEVER sees the inserted CASTs.** mosura instead **re-infers types AT PRINT time** (printc.rs:1823 `infer(f,&locks)` builds `self.types`; `type_of`/`get_input_cast` read it), which is AFTER where a terminal ActionSetCasts would insert. And CAST is faithfully **OPAQUE** to inference — Ghidra `TypeOpCast::propagateType` is NOT overridden → base `TypeOp::propagateType` returns null (mosura matches: no `Cast` arm in `infertypes.rs::propagate_type`, ~line 334). So a faithful opaque CAST inserted on a **compare** operand BLOCKS that compare's input↔input type propagation (`propagate_type` relays type between slots 0↔1 for IntSless/IntLess/IntEqual, infertypes.rs:349-362) when printc re-infers on the CAST-containing graph → the OTHER operand's inferred/rendered type can shift → NOT byte-identical, and the change is NOT confined to the cast site. Corpus compare-adjacent input-casts confirmed: **forloop_varused** `(int4)uVar1 < param_1` (param_1 = a variable → genuine risk), switchloop `(int4)param_1 < 0x96` + loopcomment (const/uint1 operands → likely benign). Most corpus casts are on OPAQUE ops (modulo `(int4)uVar1 >> 0x1f` = IntSright; threedim/twodim/dupptr `(int8)` = IntSext OWN-render, not castInput) → those are safe.

**VERDICT: faithful byte-identical Brick 2 = ActionSetCasts (opaque CAST) + RETIRE mosura's print-time type re-inference (make printc read IR-resident `vn.ty` committed by the pipeline's ActionInferTypes, instead of recomputing `infer` at print).** The re-inference retirement is a FOUNDATION refactor with its own byte-diff risk (printc recomputes for a reason — HighVariable-representative resolution + constant handling in infer(), infertypes.rs:682-737, that per-varnode `vn.ty` writeback may not reproduce identically). Making CAST transparent in propagate_type would be a NON-faithful hack (Ghidra's is opaque) — rejected. So Brick 2 is NOT a small brick; it is gated behind a foundation change, for ZERO gauge gain (corpus-neutral, off the real levers per the Live "pivot off cast rules" verdict). Recommend: hold the cast campaign at Brick 1 (landed, faithful, harmless); if pursued, sequence "printc reads IR-resident types" as its own gated foundation brick FIRST, then castInput lands byte-identical (nothing re-infers). See [[task27-coarse-register-ssa-splituses]] (revisit is in this same cast-adjacent family).

---
name: heritage-core-campaign
description: "THE campaign (task #6): heritage core Stage A BUILT — merged cover + placeMultiequals + guard(); gauge 100.9%, deficit 28→4 fns, mixfloatint healed, corpus UP. Commit gated on strict-subset audit + NEW surplus-side check."
metadata:
  type: project
---

**Stage A of the heritage core is BUILT AND MEASURED (2026-07-29 evening, @7596459+worktree).** Plan was written READ-ONLY into task #6 before any code. Quadruple justification in [[guardreturns-port-held]].

**WHAT LANDED (all heritage.cc):** `LocationMap::add` returns the MERGED (base,size,intersect) (:33/66-69) — **the merged extent was ALREADY COMPUTED and then DISCARDED; that single fact is why AL and EAX were two SSA variables.** MemRange+TaskList (heritage.hh:60/78); `disjoint` built from merged ranges with intersect 0/1/2 (:2699-2732); `place_multiequals` as ONE loop in Ghidra's body order, rename once after (:2749); `guard()` (:1156) with NO widening condition; normalizeRead/WriteSize in Ghidra's real shape (narrow varnode stays at its address, write-MASKED, :396/:493) replacing the retarget-to-unique adaptation; guardInput+concatPieces (:1952/:507). **Retired BY DISUSE** (never removed-then-reconstructed): normalize_read_size batch, normalize_ranges+gate, refine_overlaps Normalize mode, reaching_phi_input.

**NUMBERS:** corpus 0.9426→**0.9530, 58/60** · suite 564/0 (−1 = retired pin test, to confirm) · clippy 0 · ir_parity 9/9 · **ABSOLUTE GAUGE 100.9% of Ghidra; deficit 28 fns/60 calls → 4 fns/9 calls** (00079130 −4, 00068902 −3, 00051298 −1, 0006529f −1 — ALL old members, no new) · **specimens: FUN_0001bd30 = 4 calls (Ghidra 4), FUN_0003dd60 = 31 (Ghidra 31)** · **mixfloatint 0.235→0.967 HEALED.**

**⭐ THE BUG NOT TO RE-BUY: Ghidra marks the MULTIEQUAL OUTPUT `setActiveHeritage()` (heritage.cc:2635). Without it rename never pushes the phi and every loop-carried value dies** — 24 fixtures regressed, pointercmp rendered a non-progressing while-loop. One faithful line: 24→3. "Instrumenting the ranges found it; source-reading would not have."

**HELD (correctly, half-a-subsystem rule):** the refinement carve-out (:2610) — FAITHFUL (Ghidra's own `capture --ir` shows CONCAT44(XMM0_Db,XMM0_Da) — Ghidra splits XMM0 identically) and it fixes stackreturn→1.000, but its DOWNSTREAM CONSUMER is unported: param recovery re-joining adjacent input trials in one ParamEntry (`ParamListStandard::fillinMap`; mosura lacks check_input_join/do_input_join/joinTrial) ⇒ hard test mixfloatint_float_param_stays_whole FAILS ⇒ held at <subject-survey>/held-patches/heritage-core-with-refinement.patch.
**FILED:** `revisit` −0.063 = REAL print-time defect — non-idempotent extra `._0_2_ = (int2)x+100` before a CONCAT rejoin (IR single-assignment and correct; explicitness gap on addr-tied narrow writes; class pre-exists on master as harmless self-assign).

**⚖️ LEAD RULINGS PENDING BEFORE COMMIT:** (1) **commit GATED on the completed mosura-vs-mosura strict-subset audit** — aggregate triumph + incomplete per-fn audit IS the stackptr configuration; gauge-vs-Ghidra cannot see a fn that lost a call while staying ≥ Ghidra. (2) **NEW SURPLUS-SIDE CHECK (first time >100%):** list fns where mosura > Ghidra; settle top ones against flow-following disassembly — surplus = Ghidra under-emitting (oracle-ledger news) OR mosura spurious calls (defect inside a win). (3) confirm suite −1 is the retired pin test.

**DURABILITY:** everything future sessions need is in **/home/jd/projects/mosura/<subject-survey>/held-patches/** with README: heritage-core-with-refinement.patch, call-args-twin.patch, B1, stackpointer-fix, corpus-baseline-7596459.txt (the per-fixture baseline all task-#6 deltas quote against). Convention: blocking-check inputs NEVER in session scratchpads.

**NEXT (approved order):** audit + surplus check → commit Stage A → Stage B cover filter (heritage.cc:2704 — input + unaffected varnodes with no descendants; mosura's op-derived LocSet cannot see them) → call-args twin lands on top → stackptr re-test → input-join consumer (un-holds the carve-out). Perf improvement observed (master emit far slower) but NOT claimed — unmeasured.

## ✅ COMMIT GATE PASSED (2026-07-29f) — all three rulings answered
**(1) AUDIT:** mosura-vs-mosura A/B, base side PROVEN (reproduces 99.6%/28/60 exactly): **0 functions lost calls · 0 dropped · 24 gained (+51) · strict subset YES (24 fixed / 4 remaining / 0 new; survivors 00051298 0006529f 00068902 00079130, all old).**
**(2) SURPLUS = THE ORACLE UNDER-EMITTING, NOT US.** 18 fns / 45 calls above Ghidra; checker decodes 5 bytes at each named return-address site (NOT a linear sweep) → **66/66 real `e8` CALLs, 0 failures — with a NEGATIVE CONTROL** (fires on a non-call byte and on a wrong expected target). Cause, stated by Ghidra itself: FUN_00066da8 carries TEN "Removing unreachable block" warnings then emits 2 calls where mosura's 9 are all byte-verified — **an artifact of OUR per-function oracle recipe** (isolated raw import → unresolvable callees → folded guards → pruned live blocks). ⇒ **"% of Ghidra" numbers inherit this caveat; 100.9% never quoted as quality — deficit quoted, surplus stated separately with cause.** FILED (not built): whole-image oracle import (fixup-applied image, one raw block, 1303 seeded entries) should close most of the 18. Open caveat at gate time: 2 of 4 FUN_0007baf0 sites past the 512-byte orig_hex cap still reading from the loaded image (2 already `e8 → 0x78c20`); any non-CALL ⇒ blocker.
**(3) THE −1 IS ACCOUNTED:** heritage tests 15→14 = 4 removed / 3 added, all named: 2 replaced by guard_* equivalents; the dormancy pin died WITH the gate it pinned (by design); the refinement-skip pin's concern moved to the HELD carve-out **which now owes a test on landing (recorded in held-patches README)**; net-new `merged_cover_unifies_subregister_and_containing_read` pins the headline property. `#[ignore]` counts byte-identical.

## ✅ STAGE A COMMITTED `e840e56` (parent 7596459) — 2026-07-29g
Commit quotes DEFICIT (28/60→4/9) + strict-subset audit + A/B (0 lost / 0 dropped); **no "% of Ghidra" quoted, per the surplus ruling.** Surplus CLOSED **70/70** — the 4 past-cap sites read from the loaded image all `e8 → 0x78c20`; checker negative-controlled. Oracle under-emission ledgered.

**COMPILE_FAIL +49 CLASSIFIED: 47 E1052 + 1 E1081 + 1 E1011 (`switchD` emitter symbol leak).** Agent adjudicated the 47 as the in-repo VERIFIED-FAITHFUL construct (docs/decompiler-nonbug-e1052-void-indirect-call-faithful.md — Ghidra emits the identical uncompilable `iVar = (*(code *)p)();` under FULL-analysis analyzeHeadless). **⚠️ LEAD RECONCILIATION OPEN: the arithmetic doesn't close — today's prelude `code`→int fix makes exactly that construct COMPILE (E1052 47→0 in the intcode run). Either (a) the 78-fail run used a STALE prelude.h (the stale-artifact class), or (b) the construct differs (void-declared NAMED extern `iVar = FUN_x();` — callee's unrecovered return, possibly actionable — or an inline `void(*)()` cast bypassing `code`).** Settle by prelude verification + character-level construct diff; **stamp prelude hash into the sentinel/TSV mechanization.** Note: "do not fix" binds the DECOMPILER, not the harness — a prelude/extern-decl cure stays legitimate. Pre-existing 29 untouched (10 E1079, 9 E1018 undefined-label, 4 E1010); queued: E1011 + E1018 class.

**STAGE B PLAN — corrects Stage A's own assumption, by source read:** `ActionFuncLink::funcLinkInput` (coreaction.cc:1474) appends NO argument-register inputs for an UNLOCKED prototype — only initActiveInput() + stack placeholder ⇒ **Ghidra discovers arg registers inside guardCalls; no chicken-and-egg** (the arg register is WRITTEN before the call ⇒ already in the cover via the op-scan). **heritage.cc:2704 is NOT the twin's blocker** (stays filed as a smaller gap needing a Funcdata varnode bank). **LIKELY blocker (hypothesis, falsifier written): the twin appends the CALL input WITHOUT `setActiveHeritage()` (Ghidra heritage.cc:1503 has it)** — invisible under master's Loc-set rename, decisive under Stage A's flag-based rename ⇒ unmarked input never renamed, stays free at an arg register = the 7-hard-test break. Falsifier: twin + that one line → 7 hard tests; still failing ⇒ STOP and instrument.

## STAGE B: FALSIFIER FIRED → TWO REAL BLOCKERS INSTRUMENTED (2026-07-29h, master 719d46f)
setActiveHeritage (+:1226 sibling) was NOT the blocker — hypothesis dead (predecessor's, lead-endorsed). Agent stopped at the falsifier and instrumented. **Both blockers are faithful-port gaps:**
- **B-1 SCHEDULING: Ghidra's ActionActiveParam is INSIDE actmainloop (coreaction.cc:5499, rule_repeatapply); mosura's ActionResolveCalls runs ONCE outside the fullloop.** Invisible while max_pass=0; the twin faithfully ports setMaxPass(3) (fspec.cc:5335) ⇒ one invocation can never reach fully_checked ⇒ build_input_from_trials NEVER CALLED (instrumented: every call at passes=1/3 forever; probing max_pass→0 makes it run).
- **B-2 CONSUMER: mosura's build_input_from_trials is a REDUCTION** ("keep leading run of active trials in op_slot order") whose precondition was the OLD hardcoded RDI..R9 argument-order appender. Heritage delivers ADDRESS-order trials (RDX 0x10 before RDI 0x38) ⇒ first slot inactive ⇒ ALL arguments dropped. Ghidra: resolveModel + deriveInputMap (→ParamListStandard::fillinMap) + buildInputFromTrials iterating SORTED trials keeping isUsed(). **mosura already HAS fillin_map/build_trial_map/force_no_use/force_inactive_chain/sort_trials — unwired on the call side.** ⇒ **queue item 6 (input-join consumer) is a PREREQUISITE of the twin, not a follow-on. Resequenced.**
- **Lead addition: check the RETURN-side sibling while instrumenting** — RETURN_MAXPASS=0 const with a "mainloop flip should raise it" comment = the same dormant shape (Ghidra initActiveOutput maxpass from getMaxOutputDelay, funcdata_varnode.cc:589-593). Port in the same change or file with a measured reason.
Base snapshots durable: <subject-survey>/src.stageA-e840e56 + manifest.stageA-e840e56.tsv; pre-Stage-A at src.base-7596459. WIP at held-patches/call-args-twin-stageB-wip.patch.

## GAUGE TRAPS 3+4 FOUND — SURPLUS STORY CORRECTED 18→4 (same increment)
- **TRAP 3:** `\b(?:FUN_|func_0x)` cannot match `thunk_FUN_...` (no word boundary before `_`) — 30 Ghidra sites invisible ⇒ manufactured surplus. **TRAP 4:** Ghidra names a thunk after its TARGET (`FUN_00051c2c` renders as `void thunk_FUN_00067d45(void)`) ⇒ own-va definition filter scored the DEFINITION as a call. Fixing only trap 3 ⇒ phantom 37-fn base deficit; **both fixed ⇒ the DEFICIT was bit-for-bit unchanged all along (base 28/60, Stage A 4/9, same fns, membership-checked) — the campaign gate was never wrong; only surplus (18/45 → 4/17) and "% of Ghidra" totals were.** 14 of 18 "Ghidra prunes live code" fns were artifact; the 4 real ones carry Ghidra's own warnings. Gauge now: --selftest with negative controls, definition-line keying, drop-reporting (instantly caught 9 wrapped Ghidra definition lines).
- **⭐ LESSON: verifying YOUR side of a comparison against ground truth does not validate the COMPARISON — the other side can carry the counting bug.** (The 70/70 e8 verification was sound; the attribution wasn't.) The deficit surviving two opposite-direction bug fixes bit-for-bit = the strongest audit the gate has had.

## E1052 RESOLVED: verdict (a) STALE PRELUDE — mechanism: A HAND-FIX TO A GENERATED FILE
prelude.h is GENERATED by corpus_emit.rs's PRELUDE constant on every EMIT; the `code`→int fix was applied to the OUTPUT and committed as docs (26db108) while the generator still said void ⇒ next EMIT reverted it (disk prelude byte-identical to the .voidcode.bak; WCCOUT carries 74 E1052 lines). Specimen construct char-for-char the documented one — **adjudication right about the CONSTRUCT, wrong about the CONCLUSION** ("do not fix" binds the decompiler; the identical output compiles under int-code). **47 fns RETURN TO THE ACTIONABLE COMPILE_FAIL SET.** Fixed at the generator; `corpus_emit --prelude-only`; prelude_sha stamped into .compile-complete + results header; compare refuses on mismatch. **⭐ STANDING RULE: NEVER FIX A GENERATED FILE — FIX ITS GENERATOR.**

## ⛔ STAGE B HELD ON WRONG CODE (2026-07-29i, master d046fc0; decompiler unchanged since e840e56)
Three blockers FIXED (B-1 ActionResolveCalls → Ghidra's actmainloop slot; B-2 derive_input_map/fillin_map wired + faithful buildInputFromTrials; **B-3 the alias probe ran with call_guards_active=false — load-bearing to flip true (control: off loses all six piecestruct params); the in-repo comment claiming Ghidra keeps guardCalls out of the probe is BACKWARDS** — AliasChecker::gather runs from ScopeLocal::restructureVarnode on the real guarded graph). Twin then: 496/496 lib, switchind byte-identical, recovers_jump_table + emits_switch_statement fixed. **BUT: FUN_00077dcb 1→0 calls (loop body gone, gotos to UNDEFINED labels — E1018 is decompiler-reachable) and piecestruct hard test breaks (R9 heritaged 4-wide, PIECE/CONCAT chain → INT_AND #0xff).** One root cause suspected, not two: **a WIDTH RATCHET** — with ActionResolveCalls repeating, iteration N's call candidate widens the merged range iteration N+1 candidates at.
**LEAD HYPOTHESIS (to adjudicate by instrument): the twin sizes trials/inputs at ENTRY width; Ghidra's guardCalls (heritage.cc:1494-1509) registers the trial AND creates the CALL input at the guarded RANGE's own (addr,size) — born from real code writes — never entry width.** Coherent single story: entry-width trials ⇒ join machinery has nothing to join (matches B-2's suspicious ease) AND each pass unions a new 4-byte extent = the ratchet. Ghidra's stopper: guard-created varnodes never introduce an extent the code didn't have. Falsifier: trace where the first 4-byte R9 enters the cover. (guardCallOverlappingInput :1210 for partial overlap.)
**Dispositions:** backwards comment corrected in the B-3 commit · COMPILE_FAIL ladder STALE under the fixed prelude — re-measure joins the next battery, no counts quoted until then · FUN_00077dcb's undefined-label emission = its own wrong-code defect if the ratchet fix doesn't dissolve it.
Artifacts: held-patches/stageB-consumer-scheduling-probe.patch (526 lines) + src.stageB-wip; proven bases src.stageA-e840e56, src.base-7596459.

## B-4 INVERTED (2026-07-29j, master 4e09315): the ratchet was BACKWARDS — the twin NARROWS; base was RIGHT FOR THE WRONG REASON
Instrumented at guard() on piecestruct: base range 0x88 **size=8** (from recover_call_args' spurious R9:8!) vs twin **size=4**; neither side has a 1-byte read at guard time. **Base's correct 1-byte param_6 existed ONLY because the spurious R9:8 widened the range and guardInput's piece split minted a 1-byte input that outcompeted the 4-byte one.** Lead's entry-width hypothesis: dead (that's 0-for-8).
**⭐ CLASS NAMED: AN ADAPTATION CAN MASK ITS OWN ABSENCE — minting correct results through a wrong mechanism, so retiring it breaks things that were right for the wrong reason.** Third instance: XMM0:8→mixfloatint merged cover · RAX:8→narrow-switch recovery · R9:8→piecestruct's 1-byte param. ⇒ "the corpus didn't move" can NEVER certify an adaptation harmless.
**ORACLE VERDICT (asked, not assumed): the hard test is CORRECT** — `oracle/capture --c` emits `xunknown1 param_6` + the full CONCAT31/21/11 chain; Ghidra genuinely reaches a 1-byte R9 input by a real mechanism. **SHARP QUESTION, UPSTREAM of heritage: mosura's R9 read is ALREADY 4 bytes when collect() sees it, on both sides.** Next instrument: raw lifted p-code of the R9-reading instruction vs the oracle's raw p-code → (i) lift difference [same .sla ⇒ would be a SLEIGH-layer port bug, own specimen+test, NOT fixed in passing] · (ii) an earlier widener still live · (iii) Ghidra's guardInput split path minting the narrow input (heritage.cc:1952-2013). No heritage changes before the dump decides. FUN_00077dcb (3→4 params, same flavour) = same root cause until proven otherwise.
**Also closed this increment:** generated-file rule + gauge lesson landed in task #3 · corrected oracle ledger committed (4 fns/17 calls: 00066da8+7, 0007baf0+6 short-slice, 0006581c+3, 00066ea8+1; byte-check proves mosura's side ONLY) · **return-side maxpass: premise FALSE, nothing to port** — no RETURN_MAXPASS exists; init_active_output ports the cap verbatim; the dormant const was CALL_MAXPASS (input side), replaced by the twin; asymmetry is the CONVENTIONS' (input list has the delay-1 stack entry ⇒ maxpass 3; output register-only delay 0 ⇒ 0). Patch re-saved clean: held-patches/stageB-consumer-scheduling-probe.patch (523 lines, applies to 4e09315).

## B-4 SETTLED BY ORACLE (2026-07-29k): it is MISSING SUB-VARIABLE NARROWING ON A FUNCTION INPUT — both hypotheses falsified
- Lead's entry-width hypothesis: **falsified by measurement** (R9's ParamEntry is 8 wide; observed varnode is 4 ⇒ the twin already sizes to the guarded range per heritage.cc:1494-1509). Agent's ratchet: **falsified** (twin narrows, no ratchet exists).
- **THE MECHANISM (from bytes + Ghidra's own IR):** raw p-code has ONLY a 4-byte R9 read (`MOV R8D,R9D`) — Ghidra has NO 1-byte read either. Ghidra's final IR: `u0x10000065:2 = CONCAT11(R9B(i), R8B(i))` — **it MATERIALIZES a 1-byte INPUT varnode via SubVariableFlow** (used lane is one byte). The 1-byte param was never a heritage product. Base reached the same shape BY ACCIDENT (spurious R9:8 → piece split minted it): removing the accident revealed the real mechanism NEVER FIRED in mosura.
- Twin's render `(param_6 & 0xff) << 8` is exactly the un-narrowed form = **RuleSubvarAnd's own input shape (rules.rs:4535 ↔ subflow.cc:1553). mosura HAS the rule + the Subvar family ⇒ a rule DECLINING TO FIRE, not a missing port.** Three instruments took B-4 from "heritage restructure" → "one rule's gate declining" — two orders of scope shrink, zero code written.
- **NEXT (last instrument before code): print which gate in RuleSubvarAnd::apply_op returns 0** (constant input / consume==constant / consume&1 / cmask walk / descend / SubvariableFlow::do_trace). **Prime suspect: do_trace declining on a FUNCTION INPUT — Ghidra's replacement MINTS A NEW NARROW INPUT (`R9B(i)`) and the prototype follows; mosura's stage 3 may not (cross-check [[task9-subvariableflow-plan]]/task9-stage3-blocker resolved 4fa456c).** If the print says elsewhere, follow the print — predictions 0-for-9, instruments undefeated.

## B-4 TRACE DEEPENS (2026-07-29l): consume-mask abort proven for 7 of 15 — SECOND DECLINE SITE STILL OPEN
Branches (i)/(iii) CLOSED by data: both sides lift `MOV R8D,R9D` identically (no SLEIGH defect); Ghidra's 1-byte input is a SubVariableFlow product, not guardInput. RuleSubvarAnd::apply_op: **every gate passes; all 15 declines are inside SubvariableFlow::do_trace.** `set_replacement` on `0x88:4 input=true bitsize=8 flowsize=1`: **7/15 abort at `(consume & !mask) != 0` (consume=0xffffffff not 0xff); 8/15 have consume=0xff, PASS, and still decline deeper in the walk.** Agent explicitly NOT attributing the whole failure to consume before the second site is named — instrument saved durably at <subject-survey>/held-patches/b4-subvar-instrument-results.txt, indexed in README.
**LEAD ANALYTICS:** (1) **order the 15 declines by pipeline pass and weight the LAST — quiescence decides the render**; if final attempts are all in the 8×0xff group, consume is early-iteration noise. (2) **DECOUPLE the slot question from the consume story**: agent's ordering lead = Ghidra runs ActionActiveParam at actmainloop slot 10, BEFORE NonzeroMask(18)/InferTypes(19)/pool; mosura's cycle is rotated (matches rel. Heritage/DirectWrite, NOT rel. Consume/pool). **Where the slot sits is a FAITHFULNESS question answered by coreaction.cc's universe construction — compare absolutely; deviation ⇒ move it regardless of B-4; match ⇒ no slot moves on a hunch.** (3) second-site hunt: trace_backward/trace_forward/create_link over the whole flow (R8 0x80 + shift/OR chain); Ghidra mints R9B(i) AND R8B(i) — if create_link declines input-origin replacement, the 8-group should cluster there.

## B-4 = ONE GATE (2026-07-29m): every decline is setReplacement's consume guard — subvarflow.rs:232
Gate print: RuleSubvarAnd gates all pass; **all 15 declines inside do_trace, and the split is FULLY accounted: 7 never build a seed (seed 0x88:4 input consume=0xffffffff vs mask 0xff) + 8 fail process_next_work → trace_forward INT_LEFT arm (faithful) → create_link → set_replacement — SAME GATE** (`if !aggressive && (vn.get_consume() & !mask) != 0`; Ghidra's "don't consider multiple variables packed into one location"). Top declines: 98× vn 0x0:4 (EAX) mask 0xff · 8× 0x23d00:4 (shift result) mask 0xff00 · 7× 0x88:4 (seed). **B-4 in one sentence: CONSUME IS WIDER THAN THE LOGICAL MASK, everywhere Ghidra's is narrow.** Stage-3 input-minting NEVER REACHED — untested, untouched. Slot lead SELF-ANSWERED: the rotated cycle preserves Ghidra's relative order across iterations + mainloop restarts recompute consume ⇒ B-1 slot NOT established wrong, not moving it.
**NEXT INSTRUMENT: per-mainloop-iteration dump of get_consume() for the three varnodes + (lead addition) THE DESCENDANTS of each wide varnode — name the consumer.** Lead hypothesis #10 (0-for-9 disclaimer): the wide consumers are UNPRUNED TRIAL INPUTS — CALL/RETURN candidates consume fully until pruned; Ghidra prunes at slot 10 BEFORE Consume recomputes, so its pool always sees narrow consume; with maxpass=3 ours stay unpruned 3 iterations, and if the pool quiesces before the pass-4 prune (or build_input_from_trials doesn't REMOVE unused inputs like Ghidra's buildInputFromTrials, or the restart group doesn't re-poke the pool after the commit) the rule never sees narrow consume. If the reader is a trial input ⇒ fix = TRIAL-LIFECYCLE SEMANTICS, not ActionConsume math, not slots. If not ⇒ #10 dies like its predecessors; investigate ActionConsume's computation. The 7-vs-8 mask split should fall out free (post-prune iterations coherent or not).

## B-4 MOVES UPSTREAM AGAIN (2026-07-29n): consume DEAD as cause; the gate is RIGHT; the WIDTH is the question
**Ordered-by-attempt killed the consume story:** seq 1-7 wide-consume declines = early-pass noise; **seq 8 the R9 seed PASSES and stays passed to quiescence; the deciding, stable-through-final decline is `0x23d00:4` (a unique temp) mask 0xff00 consume 0xffff — and the gate is CORRECT to refuse it: that varnode genuinely holds TWO packed variables** (param_6 high byte, param_5 low). Hypothesis #10 (trial-input consumers) dead — **four hypotheses dead by instrument on B-4 (two lead, two agent), each death shrank the problem.** Lead 0-for-10.
**Slot question SETTLED ABSOLUTELY from coreaction.cc's universe construction:** mosura's cycle = Ghidra's actmainloop rotated to start at RestructureVarnode; ActionResolveCalls sits immediately after DirectWrite(b), before DeadCode — **Ghidra's exact slot; repeat-until-quiescence makes the rotation equivalent. FAITHFUL, closed permanently.**
**THE FINDING: Ghidra never reaches the gate.** subflow.cc traceForward INT_LEFT has an extension-patch branch before createLink: `(rvn->mask&1) && (sa+bitsize == 8*outvn->getSize()) && (outvn->getConsume() & ~newmask)` → addExtensionPatch. mosura's port is faithful; it doesn't fire on the SIZE term: sa+bitsize=16 but out is **4 bytes in mosura vs 2 in Ghidra** (`u0x10000065:2 = CONCAT11(R9B(i),R8B(i))`). **⇒ B-4 = why is the bit-packing chain 4 bytes wide in mosura where Ghidra's is 2?** (A) lift/normalization width vs (B) CONCAT11 from the piece/shift rule family (Subvar a red herring) — **with the nested version: CONCAT11 could be DOWNSTREAM of already-narrow inputs; then the real question is what minted R9B:1 FIRST (possibly Subvar seeded from a different op with clean consume).**
**NEXT: `scripts/trace-diff.sh piecestruct` / `oracle/capture_trace --trace` — NAMES the firing rule; read for the SEQUENCE (which rule touches R9 first, at what width), not just the set.** Agent explicitly refusing to patch the size condition — that would be an invented gate (the 7b6d36c failure mode). Third instrument pass appended to held-patches/b4-subvar-instrument-results.txt.

## B-4 REDIRECTED BY THE DESIGNATED INSTRUMENT (2026-07-29o): **`subvar_and` FIRES ZERO TIMES IN GHIDRA on piecestruct**
#10 falsified first with the descendants dump (NO call/return reads either wide varnode; the deciding wide consume is the legitimate packed-bytes fact) — then **trace-diff (run for the FIRST time on this fixture, as the FIFTH instrument) invalidated the entire SubVariableFlow hunt: neither side fires subvar_and here.** Four passes clean, every conclusion true, none load-bearing. **Agent owned it: the designated instrument (trace-diff / capture_trace) ran fifth instead of first, against AGENT.md rule 5 + task #3. ⭐ SEQUENCING RULE (now with teeth, bought at BOTH ranks — the lead endorsed four source-chaining passes without asking for the trace): for any "why does X differ from Ghidra" question, TRACE-DIFF RUNS BEFORE THE FIRST HYPOTHESIS IS STATED.**
**WHAT THE TRACE NAMES (held-patches/b4-tracediff-piecestruct.txt; ghidra=599 vs mosura=1082 total firings):** `andmask` ghidra-only @ 0x10077d 0x100792 0x100798 0x1007a8 0x1007b6 0x1007c4 0x1007ce · `subzext` ghidra-only @ **THE SAME SEVEN ADDRESSES** (the bit-packing chain — one coherent site) · subvar_zext 12 vs 8 · **shiftpiece 4=4 EQUAL — the rule the hard test is NAMED after is not the missing one.** Width 4-vs-2 = probably a CONSEQUENCE of the non-firings (to verify, not assume).
**LEAD #11 (standing disclaimer; minutes-cheap falsifier): andmask and subzext plausibly share ONE gate — NZMASK** (RuleAndMask's core test is `nzmask & constmask == nzmask`; the SUBPIECE/ZEXT collapses consult it too). If mosura's nzmask at the seven sites is WIDER than Ghidra's, both rules decline for one reason — **and B-4 connects to foundation-menu item G (extended→u64 nzmask), which would acquire a price tag the way menu-A did: a measured defect with seven addresses attached.** Falsifier: print each rule's own gate terms + diff nzmask at the seven addresses. Hypothesis ledger on B-4: FIVE dead (3 agent, 2 lead) + one method lesson. Filed: trace-diff support for the subject specimens (77dcb) — reasoning from renders is buried.

## B-4 = ONE RULE, ONE OP, ONE ADDRESS (2026-07-29p) — after a RETRACTION
**⚠️ RETRACTED: the previous trace-diff was Ghidra-vs-BASE (tree at 4e09315), not vs the twin** — the andmask/subzext seven-address deltas are base-vs-Ghidra artifacts (plausibly downstream of base's spurious inputs), NOT the twin's defect. Task #3's own prove-both-sides rule, broken one increment after quoting it. (`subvar_and` never fires in Ghidra: STANDS, state-independent.) **Lead #11 (nzmask/menu-G): MOOTED, not falsified — unadjudicated, don't chase.** ⭐ MECHANIZATION ORDERED: capture_trace/trace-diff stamp producing-side git SHA+dirty into trace headers; diff refuses unstamped or suspicious-identical pairs — "wrong side" becomes mechanically impossible (sentinel/prelude-hash precedent).
**THE PROPER THREE-WAY RUN:** r0x88:1 formed: BASE 16× / TWIN 0× / Ghidra yes (as R9B(i)) · subvar_subpiece: BASE 10 / TWIN 8 / Ghidra 8. **Both base and Ghidra mint R9B:1 with THE SAME RULE — subvar_subpiece — on DIFFERENT source ops:** Ghidra on `AL = SUB41(R9D(i),#0)` (genuine 4-byte source); base on `SUBPIECE r0x88:8(i)` (the SPURIOUS recover_call_args input) — **the masked-absence class at rule-firing granularity: same rule, right answer, wrong source.**
**DECISIVE: the twin already has Ghidra's EXACT shape at the EXACT address** — twin `0x10076b: r0x0:1 = SUBPIECE r0x88:4(i) #0x0:4` ≡ ghidra `AL = SUB41(R9D(i),#0)` → R9B(i). Ghidra's RuleSubvarSubpiece fires on it; mosura's declines. **⇒ B-4: why does RuleSubvarSubpiece decline that one op? Fix is twin-compatible BY CONSTRUCTION.** Next: gate-print it — but **READ GHIDRA'S FIRING CONTEXT FIRST** (what its graph looked like when it fired — firing ORDER from the side-by-side traces); our forward walk dies at the legitimately-packed unique, so if Ghidra's walk saw a differently-shaped downstream graph, the gate print alone won't say why. Sequence first, then gate.
Method ledger this arc: trace-diff fifth-not-first + wrong-side A/B — both already-written rules ⇒ **written rules don't self-execute; mechanize.** Artifacts: 5 instrument passes + both errors in b4-subvar-instrument-results.txt; three raw traces (ghidra / mosura.BASE / mosura.TWIN) side by side.

## 🎯 B-4 BOTTOMED OUT (2026-07-29q): A COPY-PROPAGATION DEFECT **LIVE IN MASTER** — task #9, blocking #6
**#11's spirit tested against the real target: RuleSubvarSubpiece's G2 gate is `(consume & mask) != consume` — CONSUME, not nzmask ⇒ menu G untouched.** Gate print: G2=FAIL on ALL ELEVEN attempts incl. the last (nzmask also 0xffffffff but not what refuses) — for THIS rule consume genuinely blocks.
**WHY mosura's R9D is fully consumed and Ghidra's isn't — the raw p-code answers:** both sides lift `R8D = COPY R9D` then an 8-byte widening of R8D (Ghidra `INT_ZEXT(R8D)`, mosura `PIECE #0:4, r0x80:4`) — **then mosura's COPY PROPAGATION rewrites the widening op to read R9D DIRECTLY** ⇒ R9D consumed in full ⇒ G2 fails forever ⇒ R9B:1 never minted ⇒ param_6 stays 4 bytes ⇒ no CONCAT. **Ghidra runs RulePropagateCopy 161× on the fixture and does NOT do this rewrite — R8D=R9D stays live.**
**⚠️ MASTER DOES THE IDENTICAL REWRITE.** Base only answers right because the spurious r0x88:8 hands subvar_subpiece a DIFFERENT source with clean consume. **⇒ the adaptation wasn't supplying a missing input — it was ROUTING AROUND a real defect, silently, on every `MOV r32,r32` feeding a widening op. THE DEEPEST INSTANCE OF THE MASKED-ABSENCE CLASS: the adaptation hid a BUG, not just an absence.** ⇒ **B-4 is NOT a Stage B problem; fixing #9 likely unblocks the twin with B-1..B-3 untouched** — which would be the cleanest validation of the hold discipline.
**TASK #9 (filed, blocks #6):** specimen piecestruct @0x10074d victim R9D; both sides' p-code + gate print attached; **explicit: do NOT relax G2** (faithful gate, wrongly-made graph — fix where the graph is made). **LEAD's grounding additions:** (i) "same shape" may hide an OPCODE DIVERGENCE — Ghidra lifts INT_ZEXT-from-birth, mosura lifts PIECE + needs piece2zext normalization ⇒ either a propagation WINDOW before normalization (impossible in Ghidra) or a firing-ORDER difference (sequence check answers) — and if our lifter emits PIECE where Ghidra emits ZEXT for the same semantics, that divergence gets NAMED in #9 regardless (it silently shifts which rules can fire on every zero-extending move in every binary); (ii) RulePropagateCopy condition diff read only AFTER the sequence check; (iii) **blast radius = absolute gauge + targeted scan for the MOV-r32,r32-feeding-widening shape — a population, not a guess — BEFORE pricing.** Work order: #9 grounding → blast radius → faithful fix → twin battery rerun. Seven instrument passes + three raw traces durable.

## TRACE STAMPING COMMITTED `6c123d5` (2026-07-29r)
trace-diff.sh writes `# MOSURA-TRACE-STAMP sha=<short>[+DIRTY] fixture=<stem> produced=<iso>` (+GHIDRA rev); **+DIRTY from `git diff --quiet -- crates/`** — an uncommitted patch is exactly where sha alone lies. trace-diff.py REFUSES missing stamps, echoes both, warns on dirty. **Control pair: clean=4e09315/1082 firings vs 4e09315+DIRTY/1055 — same sha, different graph, the fooled-case made visible.** Pre-stamping traces kept-but-rejected (content is record; provenance untrustworthy; tooling says both). #11 parked in the instrument file as base-vs-Ghidra artifacts, unadjudicated, not-to-chase. Master `6c123d5`. NEXT = task #9 grounding (window-vs-order-vs-condition, cheapest first; name the PIECE-vs-ZEXT opcode divergence regardless; blast radius as population; G2 untouched) → twin battery rerun.

## #9 STARTING POINT MOVED ONE LEVEL HIGHER (2026-07-29s): **THE LIFT IS FAITHFUL — the PIECE is RULE-MADE**
One-command check via the disasm goldens (254/254 parity green): **goldens contain 54× INT_ZEXT, ZERO PIECE — mosura's SLEIGH lift emits INT_ZEXT identically to Ghidra.** Agent's filing corrected: the `PIECE #0x0:4` in the trace is produced by a mosura TRANSFORM after lifting; THAT transform opens the copy-propagation window. **#9 order rewritten: (0) find what converts INT_ZEXT→PIECE and whether it's faithful — candidates to CHECK not assume: guardInput/concatPieces (heritage.cc:507/1952), normalize_write_size — i.e. STAGE A'S OWN NEW CODE** → (1) sequence: was propagation even attempted there in Ghidra (window vs ordering) → (2) only then RulePropagateCopy condition diff → (3) G2 untouched.
**⚠️ LEAD GUARD ON STEP 0: a faithful PIECE-maker does NOT close #9 — `piece2zext` fires 19× IN GHIDRA, so Ghidra manufactures PIECEs too (plausibly its own normalizeWriteSize, heritage.cc:416) and normalizes them back. If both sides make PIECEs faithfully, the differentiator is the WINDOW: does propagation reach the PIECE's input before the re-zext rules do (ordering), or does Ghidra's propagation decline by condition. And if Stage A pieces a write Ghidra doesn't: COMPARE THE RANGE EACH SIDE COMPUTED at that site before comparing code — the range decides whether the guard triggers.** Blast radius before pricing; stamped traces regenerated (mechanization correctly rejecting its author's own pre-stamp artifacts). Master 6c123d5.

## #9 STEP-0 FIRST HALF: LIFT VERIFIED FAITHFUL BY DIRECT GOLDEN, NOT ABSENCE (2026-07-29t; agent stopped clean at context limit)
Upgraded from absence-inference to direct verification: **`goldens/disasm/x86_64_cov.golden` carries the EXACT shape** — `MOV EAX,EDI` → `(0x0,4)=COPY(0x38,4); (0x0,8)=INT_ZEXT(0x0,4)` — **ZEXT's input is the DESTINATION register, Ghidra's exact form.** The PIECE is a POST-LIFT mosura transform, confirmed.
**STEP 0 SECOND HALF (next session's first move): WHICH transform builds `r0x80:8 = PIECE #0x0:4 r0x80:4`** (zero-extension re-expressed as concat, BEFORE propagation swaps the low input to R9D). Prime suspect: `normalize_write_size` (heritage.rs:493 region). **SHARP SUB-QUESTION: the ZEXT already writes all 8 bytes — is what gets normalized the COPY's 4-byte output rather than the ZEXT's 8-byte one?** Then: does GHIDRA piece the same write (compare the RANGE each side computed first — the range decides whether the guard triggers; Ghidra fires piece2zext 19× so it makes PIECEs too). If Ghidra pieces too → step 1 (window/ordering: was propagation attempted there in Ghidra) → step 2 (RulePropagateCopy condition diff) → G2 untouched throughout. Blast radius as POPULATION (absolute gauge + targeted MOV-r32,r32-feeding-widening scan) before pricing any fix. Saved traces are pre-stamp and correctly refused — regenerate through the stamped path.
Agent stopped itself at its context limit rather than half-instrumenting step 0b — correct stewardship, second time this campaign.

## 🎯🎯 TASK #9 PREMISES ALL FALSIFIED — REAL ROOT CAUSE FOUND + CAUSALLY PROVEN (2026-07-29u, master c2950da)
**THE INSTRUMENT WAS HALF-BLIND AND THAT EXPLAINS SEVEN PASSES: trace-diff compared mosura RULES vs Ghidra RULES+ACTIONS** (Ghidra's OPACTION_DEBUG prints per-ACTION blocks too — Action::perform action.cc:316-322 + ActionPool::processOp :822, ONE counter; mosura had two emitters and trace-diff.sh set only MOSURA_TRACE = rule-only). Twelve Ghidra ACTION names sat in every diff as "known artifacts" — they were the signal; an action-vs-rule ORDERING difference was structurally invisible. **Fixed c2950da: one facility (Ghidra's), shared counter, MOSURA_TRACE now an alias. ⭐ RULE: recurring "artifacts" in a diff instrument's output are DEFECTS IN THE INSTRUMENT until proven cosmetic — a diff's two sides must emit the same event classes.** The action-level trace cracked the case in ONE pass.
**ALL THREE #9 PREMISES DEAD:** (1) Ghidra pieces the ZEXT TOO (its own normalizeWriteSize builds `R8 = CONCAT44(...)` at the same address — the lead's step-0 guard was right, first win in 11); (2) **Ghidra MAKES the propagation rewrite** (propagatecopy on the CONCAT44 → then piece2zext → `R8 = ZEXT48(R9D(i))`) — RulePropagateCopy EXONERATED; (3) wide consume is a state BOTH engines pass through. Title wrong too: the visible defect is NOT live in master (masked by the spurious input) — **the missing SUBSYSTEM is.**
**REAL MECHANISM = ORDERING, measured on both sides:** Ghidra block 464 activeparam COMMITS the arg list (12→1) → 465 deadcode kills the ZEXT → 474 subvar_subpiece mints R9B(i) → 536 earlyremoval drops the stack slot. Twin: earlyremoval kills the stack slot at 1019 and THE SUBPIECE at 1052 → resolvecalls commits at 1056, four blocks too late → subvar fires on a different op. **What spans Ghidra's window: SIX STACK-PARAMETER TRIALS at the call (s0x...c0:1 + siblings are call ARGUMENTS from 464, die at 536 after the prune). mosura's arg list is REGISTERS ONLY.**
**ROOT CAUSE (self-documented as deferred in our own source): mosura NEVER registers a stack input trial at ANY call site** — heritage.rs guard_calls carves spacebase ranges out of trial registration (getSpacebaseOffset unmodelled ⇒ permanently offset_unknown); rules.rs:286 defers RuleLoadVarnode's isSpacebasePlaceholder trigger (the old task #22-C deferral). **CAUSAL PROOF: max_pass(0) probe → Ghidra's EXACT CONCAT31 render (not-a-fix, not committed); failed control recorded (disabling RuleEarlyRemoval makes it WORSE — dead ops inflate consume).**
### ⚖️ AUTHORIZED (lead): THE STACK-PLACEHOLDER SUBSYSTEM PORT — campaign-shaped, flagged honestly, strategic
Citation map (= the read-only plan skeleton): createPlaceholder fspec.cc:4849 · opStackLoad coreaction.cc:1512 · spacebase_placeholder flag · RuleLoadVarnode trigger ruleaction.cc:4295-4302 → resolveSpacebaseRelative fspec.cc:4870 · abortSpacebaseRelative :4911 + housekeeping :5156-5181 · guardCalls spacebase branch heritage.cc:1461-1466 · checkInputTrialUse SPACEBASE branch fspec.cc:5614-5634 · buildInputFromTrials translation :5713. **WHY STRATEGIC: `__watcall` passes overflow args on the STACK ⇒ mosura currently cannot recover stack arguments at ANY x86-32 call site — a structural ceiling on the whole the subject binary, not a one-fixture detour. Blast radius MUST include the subject population count (call sites passing stack args).** Conditions: plan-first read-only → incremental against the battery (B1: subsystems reveal second gates only when built — surface early) → close the subsumed deferrals (#22-C, guard_calls carve-out) → twin battery rerun → Stage B lands → stackptr re-test. G2/RulePropagateCopy/max_pass untouched (faithful, innocent).

### CORRECTION (2026-07-29v): the carve-out is INSIDE STAGE B, not master — #9 and #6 are ONE piece of work
The `getSpacebaseOffset` carve-out lives in the Stage B patch (stageB-consumer-scheduling-probe.patch:89-93), NOT master. Master's `guard_calls` (heritage.rs:1250) registers NO input trials at all (its call args were the hardcoded recover_call_args). Trial-registration-from-heritage is INTRODUCED by Stage B, porting the REGISTER half and carving out the SPACEBASE half. ⇒ **Stage B is an INCOMPLETE PORT; task #9 is that omission's symptom; the stack-placeholder subsystem lands ON TOP of Stage B as its missing half** — exactly [[port-all-faithful-rules]]: no deferred piece is grandfathered; a subsystem lands whole or holds. Nothing causal changes (neither side ever registers a stack trial). The combined patch runs the battery.

## PLACEHOLDER PORT LIVE @e7cf68d (2026-07-30a) — piecestruct HEALED; held on ONE wrong-code blocker
**THE PORT WORKS, measured (MOSURA_PLACEHOLDER=1): 135 placeholders created / 135 RESOLVED (the 135 abort-UNRESOLVED are the discarded alias-probe clone — a NEGATIVE CONTROL proving the window IS the mechanism) · 85 SPACEBASE input trials vs structural ZERO before · piecestruct_folds_shifts_to_concat PASSES** (Stage B's documented breakage healed by its missing half, as predicted). 3 commits on 08ca850, each "Not for merge on its own".
**WHAT IT COST: mosura's priming heritage stopped being a RESTART GROUP** — Ghidra runs ActionHeritage at the mainloop HEAD (coreaction.cc:5492), `H [rest] H [rest]`; mosura's rotation+restart gave `H H [rest]` = no rule pool between the register and stack passes = no resolution window. **Retiring that exposed a CLUSTER resting on one unstated premise — "heritage completes before anything else touches the graph": (a) RuleEarlyRemoval's deadRemovalAllowedSeen guard (dropped IN WRITING on that premise) — ported in; (b) ActionDeadCode's pre-live seeding for un-heritaged spaces (absent) — ported in; (c) `heritage_complete`, a mosura-INVENTED graph-shape completion predicate where Ghidra has NO completion predicate — REMOVED (it was declaring heritage finished after iteration-1's pool deleted the not-yet-anchored ram accesses ⇒ the stack pass never ran, hpass pinned at 1).** ⭐ RISK-REGISTER ENTRY: adaptations resting on "heritage completes first" — three fell out of one window change; expect more; the retirement track knows the smell.
**⛔ ONE BLOCKER (wrong code, correctly held): floatcast 1.000 → `void func(void){return;}` + hard test recovers_float_return.** Localized, not guessed: ActionReturnRecovery commits after pass 0 (SysV registers-only output ⇒ maxpass 0 — **verified Ghidra commits there too**); at that point the float return's ancestor chain reaches a still-free ram global; is_realistic PASSES, `ancestor_op_use` returns false at hpass=1 / true when complete. **Ghidra's own trace: same window, same removals, still recovers XMM0 ⇒ the divergence is INSIDE ancestor_op_use/only_op_use for that graph state.** Fix = diff against funcdata_varnode.cc's actual conditions (free-ram-ancestor / refinement-PIECE-descendants case); NO invented conditions in the ancestor walk.
**Battery @e7cf68d vs 08ca850:** suite 495/1 (that test) · corpus 0.9515→0.9322 with only THREE movers (floatcast 1.000→0.151, floatconv 0.653→0.229 = the blocker; piecestruct 0.887→1.000) · wayoffarray regressed mid-cluster and RECOVERED by the fixes · clippy 0. Gauge/audit/scans correctly deferred until the blocker clears; audit base saved: <subject-survey>/src.stageB-08ca850 (+manifest; deficit there = 5 fns/10 calls).
**⚠️ COUPLING (adopted): the placeholder is INERT ON the subject until the stackpointer patch lands** (space.rs:108 hardcodes RSP register:0x20,8 — x86-32 has no spacebase register to resolve against) ⇒ **#2 + stackptr = ONE the subject deliverable; wave 1 starts IMMEDIATELY on land with the stackptr re-test; the subject stack-args population count is measurable only after BOTH** (measuring before = measuring an inert path).

## BLOCKER NARROWED (2026-07-30b, @1d216a2, 4 commits on 08ca850): walk faithful, timing faithful — hypothesis moved to PASS-0 GRAPH SHAPE (the refinement/input-join)
- **Real mis-port found+landed while tracing (1d216a2, measured NEUTRAL): ancestorOpUse's COPY arm tested the copied-FROM input's space where Ghidra tests invn's own** (the SUBPIECE arm below it was already right — the two arms disagreed). Not the blocker. **⭐ RULE (added to standing): leaving a known mis-port in place while hunting nearby is how the next reader mis-attributes the next symptom — fix it, commit separately, measure neutral.**
- **The walk is RULED OUT:** only_op_use vs Funcdata::onlyOpUse (funcdata_varnode.cc:1805) and ancestor_op_use vs ancestorOpUse verified line-by-line (slot-skip, PIECE/SUBPIECE truncation flags, INDIRECT/MULTIEQUAL/CALL/default arms all match).
- **REMAINING HYPOTHESIS (untested — 1-for-11 rule applies): the GRAPH SHAPE mosura's pass 0 builds around laned XMM0** (trial 0x1200:8 def=SUBPIECE; pass-1 descendants RETURN only, pass-2 +PIECE) = the heritage-REFINEMENT question already HELD in heritage-core-with-refinement.patch, blocked on the input-join consumer (fillinMap check_input_join/do_input_join/joinTrial). **If confirmed, the dependency INVERTS: input-join = PREREQUISITE of #2's land, not follow-on.**
- **LEAD RULING: dump Ghidra's post-pass-0 IR for floatcast and diff the 0x1200 PIECE/SUBPIECE structure FIRST — no scope decision on an untested hypothesis. If confirmed: input-join JOINS #2 (subsystems land whole; the "whole" was one piece bigger than the brief drew). If not: follow the diff.**

## ✅ BLOCKER CLEARED @91655e4 (2026-07-30c) — "guardReturnsOverlapping must setActiveHeritage its whole-range read"
**Same class as Stage A's MULTIEQUAL setActiveHeritage bug: a whole-range read must be MARKED so rename reaches it.** (Two instances now — check setActiveHeritage marking first whenever a value dies between heritage passes.)
**LEAD-MEASURED at 91655e4** (agent's turns were bouncing on 529s, so the lead ran the pending diff itself): **corpus 0.9534, 58/60 — floatcast 1.000 (was 0.151), floatconv 0.653 (baseline restored, was 0.229), piecestruct 1.000, `piecestruct_folds_shifts_to_concat` PASSES. ABOVE the 08ca850 baseline of 0.9515. All three movers resolved.**
**⛔ THE GRAPH-SHAPE / INPUT-JOIN HYPOTHESIS IS DEAD — evidence, so it is never re-opened:** Ghidra IR (`oracle/capture --ir`) vs mosura IR (`dump --ir`) for floatcast are **THE SAME SHAPE** — Ghidra `u0x1000000c = CONCAT44(u0x10000008:4, XMM0_Da(0x23))` → `XMM0_Qa(0x38) = COPY` → `return XMM0_Qa`; mosura `u0x1000c:8 = PIECE u0x10008:4 r0x1200:4` → `r0x1200:8(0x100039:38) = COPY` → `RETURN r0x1200:8`. Same operands, same addresses (0x100035 / 0x100039:38), PIECE ≡ CONCAT44. ⇒ **the refinement/input-join was NOT a prerequisite; the dependency does NOT invert; input-join remains a FOLLOW-ON.** (Untested-hypothesis ledger: 1-for-12. The diff cost ten minutes and prevented a scope error.)
**LAND BATTERY running:** suite+clippy · absolute gauge BLOCKING vs the 08ca850 base (5 fns/10 calls) · strict-subset audit vs <subject-survey>/src.stageB-08ca850 · empty-switch/loop scan 0 · byte-stability of the 12. Then the "Not for merge on its own" markers come off / are superseded by a landing commit stating the final measured state. **ON LAND → wave 1 (task #4) starts immediately with the stackptr re-test: that is when the 85 stack trials go live on x86-32 and the subject stack-args population becomes measurable.**

## ✅✅ CAMPAIGN LANDED `c0ac350` (2026-07-30) — the spacebase/stack-trial half completes Stage B; task #2 CLOSED
Series kept unsquashed (per-step attribution is the record); the landing commit supersedes all five "Not for merge on its own" markers.
**FULL BATTERY GREEN @91655e4 vs 08ca850:** suite FULLY green (496 unit + all integration; **both** previously-failing hard tests pass — piecestruct_folds_shifts_to_concat AND build::tests::recovers_float_return) · clippy 0 · **corpus 0.9515→0.9534, 58/60, ABOVE baseline, exactly ONE mover in all 60 (piecestruct 0.887→1.000)** · **absolute gauge BLOCKING PASSES by MEMBERSHIP not totals** — deficit set identical {00079130 −4, 00068902 −3, 00051298 −1, 0006529f −1, 00077dcb −1} = 5 fns/10 calls, mosura 3944 vs Ghidra 3937, `--selftest` PASS · **strict-subset over all 1303: 0 lose, 0 gain, 3974/3974** · empty switch 0 / empty while 0 / while(true) 0 (two `empty for` renders identical in baseline ⇒ pre-existing) · **byte-stability of the 12 proven by SOURCE IDENTITY (byte-identical C ⇒ objects cannot move) — a stronger claim than a compile round-trip, with no variance to argue about: REUSE THIS METHOD.**
**⭐ AGENT DISCIPLINE WORTH COPYING: its own strict-subset FIRST CUT matched only 672 of 1303 (keying regex too strict) and it THREW THE NUMBER OUT rather than quote a half-blind instrument, re-running through the gauge's own loader.**
**TWO OPEN ITEMS (flagged, not buried):**
1. **`FUN_00077dcb` did NOT recover its call** — the brief expected the spacebase half to heal Stage B's 1→0 loss; it didn't. Gauge is BLOCKING-clean (nothing lost, set unchanged) but the recovery is open. **DISPOSITION: not a wave-1 blocker, does not reopen #2; re-check it as the FIRST measurement after the stackptr patch lands** (stack-arg-shaped loss, fixing machinery inert until the hardcode falls). Recovers ⇒ expectation right but premature; doesn't ⇒ its own filed defect with a specimen.
2. **467 of 1303 the subject fns changed their emitted C** (call counts identical fn-by-fn, byte-clean 12 identical, no gate moved) = the ordering cluster's architecture-independent RENDER effect. **NOT wrong code by any measure we have, but a huge misattribution surface. PINNED: snapshot `<subject-survey>/src.landed-c0ac350` + manifest as the durable base (future deltas diff against a state that CONTAINS this), and record number+cause in `<subject-profile>/notes/function-status.md`, not only the commit.**
**⭐ RISK-REGISTER ENTRY #1 WRITTEN (task #4) — worth more than the four fixes it describes because it makes the FIFTH member findable: the class = "adaptations resting on the unstated premise that heritage completes before the pool runs"; four members with file:line both sides; DETECTION HEURISTIC = any code reading a heritage-derived property (is_written, descend, activeHeritage, a space's SSA state) WITHOUT consulting the per-space delay/pass counter is suspect (Ghidra gates exactly that in ≥4 places). The alias clone-probe is a SECOND CONSUMER of the same premise — it is why the instrument shows 135 unresolved aborts — tagged onto G1.**
**WAVE 1 NOW ACTIVE (task #4):** stackptr re-test → re-check 00077dcb → the subject stack-args population count → G1 alias probe → F1.

## ⚠️ GIT-STATE NEAR-MISS + ENDPOINT RULING (2026-07-30d)
**The ENTIRE landed series was on an UNREFERENCED DETACHED HEAD** — `git branch --contains c0ac350` returned nothing; only the reflog held it. Cause: the agent's own `git checkout 08ca850 / 62f7812` round trip for a true per-fixture baseline. Fixed ADDITIVELY (branch `heritage-spacebase-land` created at c0ac350, HEAD attached; no branch moved, no rewrite, nothing deleted) and reported plainly. **⭐ RULE: after any `git checkout <sha>` for baseline measurement, NAME THE RETURN POINT before detaching, and check `git branch --contains` before declaring anything landed — "landed" means REACHABLE FROM A REF, not "committed".** Closest this project has come to losing work.
**LEAD RULING (topology verified by the lead: c2950da IS an ancestor ⇒ clean fast-forward, no rewrite, no merge commit, trivially reversible): FAST-FORWARD master to `1ac5a6d`.** Master gains the 8-commit series as ONE unit (08ca850 Stage B → 1ac5a6d docs) — **Stage B and the spacebase half reach master TOGETHER, which is required since Stage B alone is the documented-held state and must never be master's tip.** Conditions: do it from a CLEAN tree (wave-1 stackptr work is currently uncommitted in cspec/alias/build/funcdata/heritage), and AFTER the wave-1 first numbers so a branch move and a measurement aren't juggled at once (the series is battery-green on its own merits and does NOT wait on wave 1's verdict). `stageb-spacebase-trials`@62f7812 becomes a stale mid-series pointer — leave it (additive-only) but note it so it isn't mistaken for a live head.

## 🎯 WAVE 1 FIRST EVIDENCE (partial, emit still running): THE PRICE TAG IS PAID
Stackpointer patch applied to the land ⇒ **the placeholder subsystem is LIVE ON the subject: 1902 placeholders created / 1875 RESOLVED (98.6%) · 1686 SPACEBASE INPUT TRIALS registered (vs 14062 register trials) — against a STRUCTURAL ZERO before, because hardcoded RSP=0x20 matched no register on x86:LE:32.** That is the number the whole subsystem was priced on. **PERFECT CONTROL: the x86-64 corpus is UNMOVED at 0.9534, 58/60, 7/7 green — expected, since x86-64's own cspec declares RSP and the invention was correct-by-coincidence exactly there.** Final figure + deficit/gauge + the FUN_00077dcb re-check (wave-1 step 2) pending the emit. Open-item conditions CONFIRMED DONE: `<subject-survey>/src.landed-c0ac350` + manifest is the durable post-land base; the 467-function render change is in `<subject-profile>/notes/function-status.md` (1ac5a6d) with number, cause, base-to-diff-against, the source-identity byte-stability method, and 00077dcb's non-recovery.


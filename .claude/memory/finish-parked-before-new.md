---
name: finish-parked-before-new
description: "User directive (2026-07-18): FINISHING PARKED work (built-but-unintegrated code) is the top priority — clear it before starting anything new. Do NOT park a deep/hard problem and pivot to an easier win; that's wasteful (strands working code). Take deep problems on as deliberate campaigns and finish them all the way. We finish what we start."
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 99103a08-9d06-430b-8446-4ca5e3757106
  modified: 2026-07-19T12:20:21.498Z
---

User correction (2026-07-18), after I parked the processMultiplier unlock (it hit a deep RPO/structural-CFG wall) and pivoted to the easier A1 (wire the existing cspec loader): *"I would have preferred you only list the adaptations and persist with the processMultiplier thing. It's wasteful to pivot this way. Once you are fully done with A1, come back to processMultiplier and go ALL THE WAY to finish it. Your next priority is to finish all the items you parked (all the code you've done but not integrated because it was missing something). We need to finish what we start."*

**Why:** parking-and-pivoting accumulates half-done debt. Built code has ZERO value until integrated/landed; leaving a turnkey patch parked while chasing a cleaner win is momentum/gauge-driven, not completion-driven. A deep wall is not a reason to abandon — it's the actual work; take it on as a deliberate staged campaign and finish it.

**How to apply:**
1. **Parked patches = DEBT to burn down FIRST.** Before starting NEW work, clear the parked turnkey items (`*.patch` in this memory dir; check `git apply --check`).
2. **When a problem goes deep, FINISH it** as a deliberate campaign (stage + gate like task #8) — do NOT park+pivot to something easier. Parking is only for a genuine hard-blocker where a prerequisite must land first (and then you go land the prerequisite, not pivot away).
3. Track the parked inventory; a parked item is not "done."

**PARKED INVENTORY (2026-07-18 @a2211f3) — the burn-down order the user set:**
- (in progress) **A1** — wire cspec loader, retire hardcoded SysV lists (cspec1).
- **✅ DONE 2026-07-19 (`4ffbf39` ActionDoNothing RPO-removal + `bb758a8` processMultiplier/multlist) — corpus 0.9482→0.9519 (+0.0037; longdouble→1.000, floatcast→0.990, switchloop→0.969 case-2 CORRECT, 3 movers all UP zero-collateral, zero wrong-code).** The unlock was NOT the C-cluster foundation (re-frame #20 falsified it) but a bounded faithful `ActionDoNothing` removal-ORDER fix (RPO + one-per-apply, coreaction.cc:3466) that F1 (`a749a4b`, load-bearing) fed the correct order into. Five re-frames (#18-#21), F1 banked, no wrong-code ever forced. Full journey below (historical record).** ~~processMultiplier~~ — `processmultiplier-brick.patch` (LANDED, backup deleted). Blocked by switchloop case-2 wrong-code. **RE-FRAME #18 (rpo1 Stage-1 map, 2026-07-19 — CORRECTS the prior 5-session target):** the wall is NOT "block-INDEX = Ghidra RPO" — that's a proxy. Real mechanism: phi input slot = predecessor's position in the merge block's **`intothis` (in-edge) list** (heritage.cc:2533 getOutRevIndex; mosura mirrors at heritage.rs:1885), and `intothis` order = the **`addInEdge` temporal sequence** (block.cc:73), which `findSpanningTree` (block.cc:1009) NEVER reorders — it only sets the block index (for dominators). So ordering in_edges by RPO would be a FORBIDDEN heuristic. **Corrected target = mosura's in_edge/`intothis` CONSTRUCTION ORDER.** Ghidra's switchloop loop-header (Block-1) intothis = [initial-flow edges (entry/default/case-1), then jumptable-recovered cases appended in REVERSE during ITERATIVE recoverJumpTables — case-4 at slots 9,10; case-2 at 11,12 LAST]. mosura pre-computes all switch targets in ONE pass (build.rs) then re-sorts in_edges by ascending predecessor block-index (cfg.rs:282-285), losing edge-add order → case-2 before case-4 = swapped/wrong. **C1 persistent-BlockGraph NOT a prerequisite** (fix is upstream of structuring). **VERDICT (rpo1 Brick-0 + fork-grounding, 2026-07-19): DEEP, Branch NO — NO faithful surgical fix exists (PROVEN).** All 3 of mosura's GENUINE recovery orders (edge-creation=ascending-addr; flow-decode LIFO=case4-before-case3; jt-index=ascending) were traced; NONE reproduces Ghidra's `intothis` (entry,default,case1, then case9..case5, case3, **case4-tail, case2-tail LAST**). Ghidra's case3-before-case4 + conditional-split-tails-last is a flow-STRUCTURAL artifact of two-phase recovery + `nodeSplitBlockEdge` timing — absent from every mosura order; producing it = fabricating = forbidden heuristic. (mosura's LIFO gives the right *relative* case2-after-case4 → would functionally fix the trim, but NOT intothis-exact → not faithful.) **Only faithful unlock = the F1 CFG-foundation port:** port `FlowInfo::generateBlocks/splitBasic/connectBasic` (flow.cc:824-1037) over a creation-order op bank; retire (a) build.rs BTreeMap address-sort op stream + (b) cfg.rs:282-285 block-index in_edge sort. Re-architects the LOAD-BEARING op-index=address-order assumption; MULTI-SESSION; byte-safety UNPROVEN until full-corpus+suite gate. Stages: **F1** connectBasic over creation-order op bank → **F2** two-phase recovery-edge + conditional-tail-last timing → **F3** apply patch. Payoff +0.0036 + retires 2 adaptations + faithful CFG foundation. **User decision 2026-07-19: BUILD F1+F2 byte-exact.** **F1 BUILT + GATED GREEN (rpo1, 2026-07-19): byte-IDENTICAL all 60 fixtures, suite 509/0, clippy 0, corpus 0.9482 unchanged — a clean FAITHFUL retirement (connectBasic flow.cc:1021 + PcodeOpBank deadlist op.cc:947 creation-order op-bank + fallthruOp fix flow.cc:961/970 for intra-instruction CBRANCH e.g. nan@0x1011d5 + Option-A address-stable renumber; retires the cfg.rs:282-285 block-index in_edge-sort = item-49). LANDING self-approve (byte-identical lane).** **BUT F1 does NOT unlock switchloop (payoff FAILED) — re-frame #19, reproduces explicit1 part-4:** the loop-header phi @0x100027 is BYTE-IDENTICAL HEAD-vs-F1 (case-2 still slots 9,10). Root: mosura keeps a **latch block 0x1000c4 (`R8=EAX; goto loop`, 14 ops) that Ghidra ELIMINATES** (Ghidra cases branch DIRECTLY to loop header). The 13-input loop-header phi forms from the pipeline's **latch-splice + heritage**, NOT from cfg in_edge construction → reordering cfg in_edges is byte-identical AND ineffective. **The real unlock = the LATCH LAYER (one layer below cfg):** either (a) eliminate the latch in cfg so cases → loop-header directly (then F1's connectBasic in_edges set the phi), or (b) make the latch-splice (determinedbranch/ActionRedundBranch coreaction.cc:5658) preserve connectBasic order into 0x100027. **F1 COMMITTED `a749a4b` (banked faithful win, tree clean).** **LATCH-LAYER GROUNDING VERDICT (rpo1, 2026-07-19): DEEP — C-cluster-adjacent, the 4th layer down. mosura ALREADY eliminates the latch** (ActionDoNothing→push_multiequals→block_remove_internal_preserving, determinedbranch.rs:379/480 faithful funcdata_block.cc:84/254, landed `78de54a`) — final CFG has NO 0x1000c4, cases branch directly to b1(0x100027), 13-input phi AT b1, structurally = Ghidra. **NOT a missing action.** The phi ORDER is wrong + INVARIANT to F1: simple cases 9→3 DO follow F1 connectBasic descending order, BUT the two CONDITIONAL cases (case-2/case-4, each `…;if(cond)`) re-attach LAST in ASCENDING address (case-2 0x58 before case-4 0x74) = reverse of connectBasic, AND their block boundaries SHIFT during heritage (cfg tails 0x63/0x81 → final 0x5f/0x7d = re-cut mid-pipeline). block_remove_internal_preserving appends in the latch's in-order (faithful) so the order is already set by upstream conditional-`if` restructuring + re-cut. **The determinant = conditional-case restructuring order + block-boundary alignment = persistent-BlockGraph/structural-CFG (C1/C3) territory, NOT a bounded action-port.** part-1 proved case-2-last→correct output, so flipping the conditional-case order WOULD unlock — but reaching it needs the deep restructuring/boundary alignment. A "possible bounded angle" (force conditional-case edges to preserve connectBasic descending through restructuring) exists but the mid-pipeline boundary shift = "not cleanly separable." Escalated → user chose "C-cluster foundation now" — BUT the Stage-1 map's CAUSAL-CHAIN CHECK (lead-demanded before build) FALSIFIED it. **RE-FRAME #20 (rpo1, 2026-07-19, airtight): C1/C2/C3 is ORTHOGONAL to processMultiplier.** Ghidra has TWO block graphs (funcdata.hh:93-94): `bblocks` (unstructured — the loop-header phi-slot order lives HERE, heritage.cc:2533) + `sblocks` (structured, built LATER by ActionBlockStructure). **C1/C2/C3 = ALL sblocks/print-stage, run AFTER heritage → cannot change a phi placed during heritage** (proven: Ghidra's b1 order byte-stable across ActionBlockStructure). Building C1 = a 5th layer that STILL doesn't unlock case-2. **REAL UNLOCK = bblocks conditional-case (`if`) block-split + edge-ordering, set in the MAINLOOP:** connectBasic (F1 ✓) + latch-elim (determinedbranch.rs:480 ✓) + the conditional-case edge handling that flips case-2/case-4 (mosura ascending case-2-before-case-4; Ghidra case-2-LAST; flip is between-latch-and-b1, a bblocks op NOT structuring). rpo1: "likely FAR SMALLER than a C-cluster rebuild." **LEAD REDIRECTED (user piling-on, take recommended): option (a) — read-only probe of the exact bblocks flip mechanism → bounded-vs-deep → if bounded build+gate+apply patch, self-approve if faithful+no-wrong-code. C1/C2/C3 filed SEPARATE = gauge-inert decoupled faithful-refactor, NOT now.** **RE-FRAME #21 (rpo1, 2026-07-19 — BOUNDED, F1 VALIDATED load-bearing):** per-action CFG trace pinpoints the flip to **`ActionDoNothing`** (latch/do-nothing elimination, determinedbranch.rs). F1's connectBasic produces the CORRECT case-4-before-case-2 at build_cfg + survives heritage; **ActionDoNothing flips it** (earlier "F1 no-op" read was a multistage-recovery PARTIAL-CLONE artifact — real build is correct, F1 is load-bearing). Cause: mosura iterates do-nothing candidates ascending block-index=address + removes ALL in one apply (determinedbranch.rs:581); Ghidra (coreaction.cc:3466-3486) iterates **RPO block index** + removes exactly **ONE per apply** (return after first, mainloop repeatapply re-scans). **Fix = align to RPO+one-per-apply** (reuse dominator::postorder RPO). BOUNDED, one localized function — NOT C1. rpo1: (1) read-only confirm RPO+one-per-apply → case-4-before-case-2 + verify mosura mainloop repeatapplies determinedbranch; (2) faithful port; (3) gate (b1 intothis==oracle, byte-diff, suite 509/0, jumptable 6/6, clippy); (4) apply patch → case-2 correct, longdouble→1.000, floatcast→0.990, +0.0036, zero wrong-code. Lead self-approves land if clean+faithful+no-wrong-code. processmultiplier-brick.patch turnkey. Detail: [[adaptations-inventory]] item-49 + merge-family-cluster.md.
- **✅ DONE 2026-07-19 (`ba3be18`)** — mergeIndirect: Merge::mergeIndirect + snipOutputInterference/collectInputs for addr-forced INDIRECTs (merge.cc:846-889), faithful reconstruction (old patch was a stale full-file snapshot). 0.9519→0.9527 (+0.0008; switchmulti 0.586→0.667 convergent; stackreturn 0.926→0.889 benign SSA-equivalent reorder = faithful-exposes; multiret oracle-less; zero wrong-code, suite 509/0). Two conservative under-approximations (collectInputs exact-high-only no VariablePiece-group; final-snip omits SymbolEntry type-resolution inheritance) — under-merge-only, never wrong-code, filed. **BOTH PARKED ITEMS NOW FINISHED — parked-debt slate CLEAR.**

---
name: task4-comparison-normalization
description: "Task #4 Phase-1c comparison-normalization: instrument-first findings + scope. Post-keystone the RuleIntLessEqual landscape shifted; the biggest levers (RuleCondNegate, condconst, condmulti) pull into isBooleanFlip-subsystem / P7 territory."
metadata:
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #4 (owner rock5, in_progress) = cancel the print-time `<=` adaptation (printc::incr_in_width) + wire RuleIntLessEqual + port RuleCondNegate + repair structuring's SLESSEQUAL dependency. Base = master `cd7929d` (post-keystone). See [[printc-structuring-adaptation-conflicts]], [[direction-faithful-port]]. Corpus baseline 0.8865, 54/60.

## ★ TASK #9 ROOT-CAUSED (2026-07-11, on master c9f3321, corpus 0.9196/56) — RuleIntLessEqual @10 exposes MISSING RuleRangeMeld
Wired RuleIntLessEqual @10 (pipeline.rs:119, after RuleScarry). Post-#8 the OLD broad regression NARROWED: concat/concatsplit/condconst(now 1.000)/condconst2/condconstsub all BYTE-IDENTICAL. Only 4 movers, all one class: condmulti 0.806→0.750, deindirect 0.857→0.811, elseif 0.904→0.892, loopcomment 0.745→0.687 (<0.70 → 56→55). corpus 0.9196/56→0.9168/55, suite 391/0, switch 6/6 GREEN.
**VERDICT = faithful-EXPOSES-a-gap, NOT a mis-port.** INSTRUMENTED via Ghidra `capture_trace` (condmulti) + IR diff. Ghidra's exact chain for the x86 jg/jle flag reconstruction: (1) `sborrow` → `SLESSEQUAL(6,param_2)`; (2) `intlessequal` → `SLESS(5,param_2)` (**RuleIntLessEqual fires @10 in GHIDRA too, identical op+conversion — the wire is byte-faithful here**); (3) `rangemeld` collapses `NOTEQUAL(param_2,6) && SLESS(5,param_2)` → `SLESS(6,param_2)`. mosura has 1+2 but **NO RuleRangeMeld** (Ghidra coreaction.cc:5612). mosura had been collapsing via RuleLessNotEqual (ruleaction.cc:2290, needs the SLESSEQUAL form) — the faithful @10 conversion kills SLESSEQUAL before it matches → leaf stays fused, printc De-Morgans it (condmulti `param_2<7`→`param_2==6 || !(5<param_2)`; deindirect `param_1<10`→`param_1==9 || param_1<9`). All 4 movers = the jle OR-form `(x==c)||(x<c)` + jg AND-form `(x!=c)&&(c-1<x)`, both exactly what RuleRangeMeld collapses.
RuleRangeMeld = BOUNDED port: mosura CircleRange already has pull_back/intersect/circle_union; functional_equality+is_bool_output exist; only `CircleRange::translate2Op` (rangeutil.cc:~1093, NOT yet ported — circlerange.rs:9 lists it deferred; it's a self-contained 44-line fn reading CircleRange left/right/mask/isempty/step — all fields present) missing. = translate2Op + ~81-line applyOp + wire @slot 101 (after RuleLessNotEqual pipeline.rs:217, mirroring Ghidra coreaction.cc:5612).

★ LANDED path (a) — master `0c6e0ea` (2026-07-11): lead chose LAND-now + file RuleRangeMeld as **task #11** (blockedBy #9). RuleIntLessEqual @10 wired in default_rule_pool (pipeline.rs), coverage.md RuleIntLessEqual row → DONE. Corpus 0.9196/56→0.9168/55 (the 4 movers above), suite 391/0, switch 6/6. Task #9 COMPLETE. The translate2Op return-code contract: 0=produced (opc,c,cslot); 1=always-true; 2=cannot-represent; 3=empty. RuleRangeMeld pull_back maps `range1.pull_back(data, sub, false)` (mosura omits Ghidra's &markup symbol-preservation out-param — print nicety, not correctness).

★★ TASK #11 RuleRangeMeld LANDED — master `9f9ebca` (2026-07-11, intle1): the paydown. Ported `CircleRange::translate2_op` (circlerange.rs, faithful rangeutil.cc:1093, 2 unit tests) + `RuleRangeMeld` (rules.rs, faithful ruleaction.cc:1357, 3 unit tests) + wired @101 (pipeline.rs, after RuleLessNotEqual). Recovers condmulti/deindirect/elseif/loopcomment **byte-identically** to pre-#9 baseline → corpus 0.9168/55→**0.9196/56**, zero other movers, suite 391→396/0, switch 6/6. FAITHFULNESS verified: MOSURA_TRACE fires `rangemeld` right after `intlessequal` on condmulti (matches Ghidra `sborrow→intlessequal→rangemeld`); IR leaf collapses BOOL_AND→single INT_SLESS = Ghidra. The two faithful rules (#9 @10 + #11 @101) now compose to reproduce Ghidra's exact chain. printc de-fuse arc `<=`-unification COMPLETE. NOTE: RuleLessOne (coreaction.cc:5611) still MISSING (coverage.md:213) — a separate pre-existing gap between RuleLessNotEqual and RuleRangeMeld, not exposed by anything now.

## POST-ORIENTATION RE-INSTRUMENT (2026-07-08, flip1 — Task #2 = wire RuleIntLessEqual+delete incr_in_width, on master `e0f2d96` AFTER task #1 S1 orientation landed)
RuleCondNegate/orientation (task #1 S1) already materializes the mechanism-B SIMPLE-comparison guards in the IR (condnegate_pool's RuleIntLessEqual handles them) — incr_in_width no longer touches those. incr_in_width now fires on EXACTLY 3 fixtures (measured: env-gated it off, dumped corpus, diffed), ALL of them the DEFERRED task #1 cases:
- **deindirect** = an IF-ELSE guard (`if (param_1 < 10){}else{}`) → mechanism A (opFlipInPlaceExecute normal-form flip), deferred.
- **loopcomment, elseif** = the `<`-with-const leaf inside a compound `&&` → BlockCondition::negateCondition distribution (block.cc:3023), deferred.
Ghidra (oracle --c) renders the strict `< c+1` for all 3 (deindirect `param_1 < 10`, loopcomment `(param_1 < 10) && (100 < param_2) && ...`) via IR RuleIntLessEqual AFTER materializing the negation; mosura produces the IDENTICAL `< c+1` at print via incr_in_width = the print-time stand-in for the deferred IR normalization (matches Ghidra exactly, NOT a silent crutch).
**Q2 CONFIRMED (measured, reverted): wiring RuleIntLessEqual@10 in the main pool REGRESSES all 3 into `x==c || x<c` disjunctions** (deindirect `param_1 == 9 || param_1 < 9`), WITH or WITHOUT incr_in_width — same coupling as the pre-orientation proof, because these conditions' negation is at PRINT (no IR `<=` for @10 to convert; @10 hits `SLESS(c,x)` and the print-negation/structuring makes the disjunction).
**VERDICT:** Task #2 is BLOCKED ON / subsumed by task #1's deferred pieces (mechanism A opFlipInPlaceExecute + compound BlockCondition::negateCondition). Deleting incr_in_width / wiring @10 standalone regresses. The faithful sequence: port those two (task #1 S3) → they materialize the negation → RuleIntLessEqual normalizes in IR → incr_in_width falls out cleanly. Do NOT touch incr_in_width or wire @10 before then. Reported to lead; awaiting steer (fold #2 into #1 S3, or pivot to #3/#5). See [[task10-isbooleanflip-condnegate]] (COMPLETE-S1 + deferred pieces).

INSTRUMENT-FIRST DONE (2026-07-07, rock5) — trace-diff + oracle C on the 7 brief fixtures. The scope is bigger than "wire RuleIntLessEqual"; the biggest levers are subsystem/P7:

CURRENT SCORES (master cd7929d): condmulti 0.793, condconst 0.814, concatsplit 0.881, elseif 0.899, varcross 0.913, concat 0.929, noforloop_alias 0.976 (noforloop_alias already recovered by the keystone's RuleLessNotEqual+Equal2Zero-arm).

PER-FIXTURE MECHANISM (oracle C vs mosura C):
- **condconst** (0.814): divergence is CONDITIONAL-CONSTANT PROPAGATION, not `<=`. Ghidra propagates the guard constant into the block — `if(param_2==0) *p=param_3` (mosura keeps `param_2+param_3`); `if(==10) p[2]=10` (mosura `param_2`); `if(param_4==7) p[4]=0x10` (mosura `param_4+9`). Ghidra fires a `condconst` rule 1x. = Ghidra's conditional-constant / ActionConditionalConst — a SEPARATE mechanism from comparison-normalization.
- **condmulti** (0.793): COMPOUND-`&&`-STRUCTURING. Ghidra merges guards into `if((param_1==10) && (xRam=10, param_2<7))` (comma-expr side effect); mosura emits 3 separate ifs. = P7 ruleBlockOr / short-circuit structuring. `param_2 < 7` ALREADY matches (not a `<=` issue).
- **elseif/varcross/concat/concatsplit**: dominated by `condnegate` (RuleCondNegate) + loadvarnode/storevarnode (spacebase, Task #3/#7) misses.

TRACE-DIFF (Ghidra fires, mosura NEVER) — the comparison-cluster misses:
- **condnegate (RuleCondNegate)** = the DOMINANT recurring miss: elseif 7x, condmulti 3x, condconst 2x, varcross 1x. **BLOCKER: mosura has NO isBooleanFlip flag** — it canonicalizes branch sense at CFG-build instead (jumpbasic.rs:753 `no isBooleanFlip in mosura's canonical CFG`; determinedbranch.rs:151). RuleCondNegate (ruleaction.cc:5474) reads `op->isBooleanFlip()` + calls `opFlipCondition` — needs the flag on PcodeOp + set at lift/CFG time + opFlipCondition. That is a CFG-level subsystem port, NOT bounded (same isBooleanFlip subsystem task #8 investigated + found misdirected for switches).
- **intlessequal (RuleIntLessEqual)**: condmulti 2x, elseif 2x. HELD slot 10.
- loadvarnode/storevarnode fire HEAVILY (elseif 60x/25x) = spacebase-placeholder = Task #3/#7, not #4.

RuleIntLessEqual WIRING EXPERIMENT (wired slot 10, incr_in_width STILL active, measured+reverted): corpus 0.8865→0.8860 (-0.0005), 54→53/60. **The old catastrophic regression is GONE** (RuleLessNotEqual+Equal2Zero-arm changed the landscape — no more `x==c||x<c` disjunctions). Now MIXED: condmulti 0.793→0.806, noforloop_globcall 0.810→0.857, threedim 0.985→1.000 UP; forloop_withskip 1.000→0.964, deindirect 0.636→0.609, loopcomment 0.717→0.696 (<0.70), ifswitch 0.985→0.970, elseif 0.899→0.890 DOWN. The losses are the incr_in_width/SLESSEQUAL-structuring dependency — need to CANCEL incr_in_width + repair structuring for the wiring to net-positive.

⛔ COUPLING FOUND (2026-07-07, executing (A)) — task #4 (A) is BLOCKED ON task #10 (RuleCondNegate). Did wire RuleIntLessEqual@10 + delete incr_in_width, measured (NET-NEGATIVE, reverted to clean `0acaa93`). ROOT via ifswitch IR diff (baseline vs wired):
- BASELINE: guard IR = `INT_SLESSEQUAL(param_1, 0x63)` = `param_1 <= 99`; mosura's print-time negation `!(a<=b)=>b<a` (printc.rs:964) renders `99 < param_1` = MATCHES Ghidra. mosura's SLESSEQUAL-in-IR + print-negation is INTERNALLY CONSISTENT and CORRECT.
- WIRED: RuleIntLessEqual converts `param_1<=99` → `INT_SLESS(param_1,0x64)` = `param_1<100`; print-negation of SLESS `!(a<b)=>b<=a` renders `100 <= param_1` = WRONG (Ghidra `99 < param_1`).
So the faithful `99 < param_1` requires the negation MATERIALIZED IN THE IR (RuleCondNegate → the CBRANCH condition becomes the positive `99 < param_1` in the IR, which RuleIntLessEqual already handled). Without RuleCondNegate, wiring RuleIntLessEqual is STRICTLY WORSE (breaks the correct SLESSEQUAL+print-negation). incr_in_width was a minor separate sub-case (negated SLESS with const in in(0)), NOT the main mechanism — the premise "mosura keeps SLESSEQUAL + does <= at print" was only half-right; the print-time NEGATION is the actual adaptation, and it's correct as long as SLESSEQUAL stays. CONCLUSION: sequence task #10 (RuleCondNegate/isBooleanFlip) FIRST, THEN task #4 = {wire RuleIntLessEqual + delete incr_in_width + delete the print-time negation's compensating logic}. Reported to lead. Nothing landed; master clean.

SCOPE VERDICT (per brief's re-scope-if-balloons guidance): the in-scope `<=` core (wire RuleIntLessEqual + cancel printc::incr_in_width @printc.rs:1365/975 + repair the SLESSEQUAL structuring dep) is a real coupled MIXED mover. The BIGGEST levers are OUT of the comparison-normalization cluster: RuleCondNegate = isBooleanFlip CFG-subsystem (its own task); condconst = conditional-const-prop rule; condmulti = P7 compound-`&&` structuring. REPORTED to lead for re-scope. NEXT (pending lead steer): either (a) do the bounded `<=` core alone (wire+cancel+structuring-repair, iterate to net-positive), (b) take RuleCondNegate as its own isBooleanFlip subsystem task, (c) fold condconst/condmulti into P7 (#5). incr_in_width location: printc.rs:1365 (fn) + :975 (call site).

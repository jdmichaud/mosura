---
name: guardreturns-port-held
description: "guardReturns/active-output faithful port WORKS (gauge 94.8%→98.9%, target fn renders) but is HELD on 2 wrong-code signals, both pointing at the heritage merged-cover gap (task #6) — plus the two-feeder reframe of the 246-call deficit"
metadata:
  type: project
---

**2026-07-29: the Tier-2 faithful return-recovery port is BUILT AND WORKS — and HELD, not landed, by the blocking rule.** Working tree vs master `44ea232`.

**WHAT IT IS:** `recover_return`'s hardcoded pre-heritage `[(RAX,8),(XMM0,8)]` RETIRED (instance #2 of [[hardcoded-x86-64-vs-cspec-class]]). Candidates now arise BY QUERY as in Ghidra: `Funcdata::initActiveOutput` (funcdata_varnode.cc:585) opens the trial container pre-heritage; `Heritage::guardReturns` (heritage.cc:1652) asks `characterizeAsOutput` of EVERY heritaged range, registering trial+RETURN-input on containment (truncating via `guardReturnsOverlapping`, heritage.cc:1609); commit = `deriveOutputMap` + faithful `buildReturnOutput` (coreaction.cc:1837). Also completed two partial ports it needs: `ParamTrial::setEntry`/entry-clearing in `derive_output_map`, and `sortTrials`' entry-less-sinks-last (fspec.cc:1894).

**RESULTS:** target `FUN_0005f84c` renders `xunknown4 ... { return 0x88d0c; }` ✅. **Absolute gauge 3705→3867 calls (94.8%→98.9% of Ghidra); deficit 92 fns/246 calls → 29 fns/87 calls.** Survey discovers 17 new fns (1286→1303 — extend the Ghidra sweep before trusting per-fn gauge rows for them). Corpus 0.9535→0.9426 with 3 fixtures UP (deindirect2 →1.000, switchmulti, partialsplit).

**⛔ WHY HELD — two wrong-code signals, both pointing at task #6's heritage core:**
1. **`FUN_0006af2c` 1→0 calls** (Ghidra emits 18; multi-width AL:1/EAX:4 fn). Body + trailing indirect call vanished → 3-store stub.
2. **mixfloatint 0.967→0.235, `float8 func(...)` → `void func(void)` — a REAL dropped return, NOT a mis-port: the MERGED-DISJOINT-COVER GAP.** Ghidra adds the MERGED range to its work list (`disjoint.add((*liter).first,(*liter).second.size)`, heritage.cc:2710) so XMM0's two 4-byte lanes + the 8-byte read become ONE range and ONE trial. mosura keeps the per-varnode `loc` (heritage.rs:1648, merged extent discarded) ⇒ two lanes = two trials at two RETURN slots ⇒ Ghidra's own `onlyOpUse` RETURN case (funcdata_varnode.cc:1849) rejects BOTH (verified: all 5 candidates active=false). **The old hardcoded XMM0:8 masked this** — appended pre-heritage, it was picked up by pass-0 `normalize_read_size` and rebuilt as a PIECE of the lanes. (Same masking is the lead's hypothesis for 6af2c — old RAX:8 + batch pass-0 accidentally glued widths.)

**⭐ THE TWO-FEEDER REFRAME of the 246-call deficit (lead hypothesis, agent to verify by intersecting the 29 with the 92):** ONE executioner (`determinedbranch`), TWO stale-binding feeders — (a) **missing callee returns**: a wide EAX read after a call bound PAST the call to a stale def because the callee returned nothing — **67 fns/163 calls, fixed by this port**; (b) **multi-width heritage** (AL under EAX): ~29 fns/87 calls, = task #6. The earlier "86/92 one cause" attribution captured the EXECUTIONER level, which both feeders share.

**SEQUENCING RULING (pre-authorized):** if 6af2c confirms merged-cover ⇒ preserve port as patch + side branch, **promote task #6 to ACTIVE** — merged disjoint cover (heritage.cc:2710) + placeMultiequals per-range loop = THE campaign, now **triply justified** (stackptr patch regression, sub-register class, these two signals) ⇒ land guardReturns on top ⇒ re-test stackptr on top ⇒ confirm the EXACT. If 6af2c is independent+bounded ⇒ fix directly, land guardReturns first. **Also: probe the NEW render (`xunknown4` spelling) through wcc386 NOW — if the type spelling breaks the probe that's blocker #2 of [[first-exact-lane]], name it before the campaign.**

Related: [[war2-subregister-heritage-rootcause]], [[war2-stackptr-wrong-code]], [[gate-byte-identical-only]].

## ✅ GO GIVEN — LANDED (2026-07-29, lead gating decision, auditable)

**FUN_0006af2c's "1→0" was NOT like-for-like** — `0x06af52` is one of the four dispatch sites in `docs/decompiler-bug-narrow-switch.md`, and **that filed bug is CLOSED BY THIS PORT**: the sentinel test `ground_truth_parity.rs::narrow_switch_recovery_gap` tripped in the good direction; `sw_short` recovers all 8 targets; **the doc's own pre-filed prediction confirmed exactly** (0x0513a8 0→8, 0x058afb 0→5, 0x06af52 0→8-of-10, 0x0199b7 0→4; WAR2 dispatch sites 8→12). MECHANISM (causal-tested by re-adding the read): the hardcoded pre-heritage `RAX:8` read = an 8-byte read at register offset 0 spanning EAX **and ECX** — a range no x86-32 instruction writes — forcing a spurious heritage location whose batch read-normalization severed the guard's `SUBPIECE(x,0)` from the table index's `INT_AND`/`INT_ZEXT`, exactly what JumpBasic needs. So at baseline 6af2c was ALREADY WRONG (1 call vs Ghidra 18, unrecovered switch as a loop); post-port it is differently-wrong on a TRUER CFG (switch recovered, matches Ghidra's `void FUN(int,int)` verdict); residual = downstream structuring/consume, filed.

**THE GATING PRINCIPLE APPLIED: the blocking rule stops SILENT, UNDIAGNOSED wrong-code landings (the stackptr shape). It does not make "hold" sacred when HOLDING IS NOT THE WRONG-CODE-FREE OPTION** — the RAX:8 adaptation itself corrupted IR program-wide (4 severed dispatch sites + dozens of downstream fns). Both branches contained wrong code; **landing contained measurably less**, with both residuals root-caused, causal-tested, and owned (mixfloatint → task #6; 6af2c structuring → filed follow-up). Independent pre-filed prediction + sentinel test = the strongest evidence class we have.

**Post-land order:** full survey (EXACT headline + 5f84c + the nine accessors; `xunknown4` spelling answered as COMPILE_FAIL-or-not) → if 5f84c not EXACT, that blocker first (lane rule) → 6af2c structuring follow-up → task #5 sweep. Extend the Ghidra sweep to the 17 new fns before quoting their gauge rows. Stackptr stays held until after the heritage core (#6).

## RESIDUAL MAP (2026-07-29, post-land): the deficit decomposes into THREE feeders, one executioner
Strict-subset audit: **63 fixed / 29 remaining / 0 newly deficient.** The 29 (87 calls) are TOP-HEAVY: **FUN_0006af2c (−18) + FUN_00051298 (−10) = 32% of the remainder, and BOTH are newly-recovered narrow switches** — `switchnorm` folds the BRANCHIND onto the switch variable exactly as Ghidra (traced), **but the case-body blocks are then dropped and printc renders nothing after the fold**. Baseline masked this by never recovering the tables (BRANCHIND rendered as indirect call, loop kept). ⇒ **FUN_00051298 leaves the named 4-tail (now 3): it has an owner.** So: feeder (a) missing callee returns — FIXED by guardReturns; feeder (b) multi-width AL/EAX heritage — task #6; **feeder (c) newly-recovered-switch structuring/print gap — NEXT UP** (likely also owns 0x6af52's 8-of-10 residual). Lead's grounding directive: check BLOCK LINKAGE first (are the jump table's out-edges linked into the CFG at structuring time, or are case blocks orphaned when switchnorm folds?) — orphaned-then-removed = the familiar executioner pattern; fix the linkage, not structuring/printc. Ghidra side: how `JumpTable::recoverAddresses` feeds `BlockSwitch`. Flavor check on the other 27 still open.

**compare.py reference fix IN** (backup `war2-survey/compare.py.rawref.bak`): targ = manifest `orig_hex` (fixup-applied); `orig_fixup` = read from the LE relocation table via `fixup_byte_set_in_range` (an earlier raw-vs-applied-diff INFERENCE under-reported multi-byte fixups and was corrected before any number); `cand_fixup` = wcc386 FIXUPP; mask = INTERSECTION only (both-sided); EXACT prints masked=0; no silent fallback (missing orig_hex ⇒ explicit MISMATCH reason). Distribution pending `compiled ok=`.

## CORRECTIONS (2026-07-29, agent self-caught) — "feeder (c)" DEMOTED from class to ≤2 single-function defects
1. **The 0x6af52 "8-of-10 residual" DOES NOT EXIST** — the table has 10 entries, 3 duplicates of 0x6b180; "8" was distinct-target dedup. Dropped.
2. **The switch-structuring class does NOT generalize.** Of the 4 newly-recovered-narrow-switch fns, only `FUN_0006af2c` collapses (1 block/18 ops/NO edges while `f.jumptables` holds all 10 addresses; case bodies ALIVE through heritage passes, removed AFTER — post-recovery teardown, NOT a recovery failure). `00051298` (26 blocks, −10 calls), `000198d4` (15), `00058ad4` (19) structure fine ⇒ **51298 is a DIFFERENT defect; the merge was a guess, retracted.**
**⇒ LEAD HYPOTHESIS for 6af2c (trace to adjudicate): FEEDER (b) WEARING A SWITCH COSTUME** — 6af2c is a multi-width AL:1/EAX:4 fn; if the switch SELECTOR (or dominating guard) binds a wide read past a narrow write and folds constant, the full teardown is correct-on-wrong-premise (the `FUN_0001bd30` executioner). If confirmed: file under task #6, no switch-path defect exists; take 51298 as a FRESH diagnosis (no inherited assumptions).

**WRONG-CODE LEDGER @6e1b113 = EXACTLY ONE FUNCTION.** Whole-survey collapsed-render scan (>50% non-blank lines lost, base ≥12): 4 hits, 3 of them the port WORKING (184→59, 86→34, 75→26 lines while holding Ghidra's exact call count — recovered returns letting dead scratch die); only 6af2c a real loss. 473 fns grew / 89 shrank. **STANDING SURVEY CHECK worth keeping: collapsed-render candidates cross-checked against Ghidra's call count — "shrank while holding count" = health, not damage.**

## LEDGER CORRECTED (2026-07-29b): the top-2 residual UNIFY — "EMPTY SWITCH BODY", one defect, two severities, FOUR carriers
`FUN_00051298` renders `switch(*(xunknown2*)...){ }` — jump table recovered (8 targets), **26 blocks EXIST in Funcdata**, surroundings track Ghidra — all 8 case bodies (10 of 12 calls) dropped. `FUN_0006af2c` = same defect, higher severity (CFG torn to 1 block, −18). Together **28 of the residual 87 = a third of the remainder.** ⇒ task #8, queue-jumped ahead of #5; **ground-truth Watcom source test FIRST** (narrowsw.c + non-trivial case bodies).
**SCAN CORRECTION (agent, on the record): the "exactly one function" ledger claim was a PREDICATE GAP — the empty-body scan tested `while`/`for`, never `switch`.** Correct scan: baseline 2 fns / ported 3 (new: 51298; none disappeared) ⇒ honest ledger @6e1b113 = **2 regressed + 2 PRE-EXISTING carriers** of the same defect (the pre-existing pair = the absolute-scan rule proving itself again; differential scans can't see them). Fix must cure ALL FOUR, not just the new pair.
**Stage split for the trace:** 51298 blocks exist-but-unemitted vs 6af2c blocks destroyed = different stages; 51298's LIVE selector expression argues against feeder-b there (a folded selector wouldn't render `switch(expr){}`). First question: are the jump-table out-edges present as CFG edges on the BRANCHIND block? Feeder-b stays alive only for 6af2c, only if its selector actually folds.

## ✅ TASK #8 ROOT-CAUSED (2026-07-29c): an INVENTED MULTI-EXIT HEURISTIC, deleted — NOT a structuring gap
`cfg.rs` (from `cb95592` "switch-in-loop structuring"): `if exit_targets.len() > 1 { blocks[bi].out_edges.clear(); }` — for a switch in a loop with >1 exit it CLEARED the case edges of a FULLY RECOVERED jump table; the reachability sweep below then destroyed the case bodies. **The table was never the problem; the edges were deleted after recovery succeeded.** Its self-justification misread Ghidra: "Too many branches. Treating indirect jump as call" is jumptable.cc:2629, a NO-MODEL bail in `recoverAddresses` — Ghidra has NO loop-exit count anywhere in jump-table recovery. AGENT.md rule 4 names "a multi-exit check" as the anti-pattern BY NAME. Deleted with a tombstone comment.
**RESULT:** 6af2c 1→40 blocks, 0→**19** calls (Ghidra 18 — ⚠️ settle 19-vs-18 against the BINARY per the reference hierarchy, either direction is news); 51298 26→50 blocks; renders Ghidra's `while(...){ switch(...){ case 0: ... } }` shape. **CORPUS UNCHANGED (0.9426, 57/60, not one fixture) — the heuristic was CORPUS-INERT: it only ever destroyed WAR2 code. The campaign's thesis in one line: an invented heuristic invisible to the gauge we stared at for weeks.** The guardReturns port didn't cause the defect — it EXPOSED it (pre-6e1b113 these tables never recovered, so there were no edges to clear; 2 of 3 empty switches predate the port).
**⇒ my feeder-b/"switch costume" hypothesis for 6af2c: WRONG (again — the lead's seventh dead hypothesis this campaign). The executioner was an invented edge-clear, not a stale fold.**
**Pre-commit checks (lead):** (1) 19-vs-18 vs flow-following disassembly; (2) empty-switch scan must read ZERO — all FOUR carriers incl. both pre-existing; (3) audit the REST of `cb95592` for sibling inventions (thresholds/counts not traceable to Ghidra). Gauge = blocking check before commit; then the `recover_call_args` twin.

## ✅ DELETION LANDED `7b6d36c` (2026-07-29d) — all checks clear
**Check 1: 18-18-18 three-way agreement** (flow-following capstone disassembly from the fixup-applied image, entry + 8 table roots, NOT linear). The "19" was a hand-rolled `grep -cE "func_0x|FUN_"` counting the `extern` DECLARATION line — trap 1 in the gauge's own docstring; `count_calls` exists to dodge it. **RULE: never hand-roll a metric the gauge already implements.**
**Check 2: ALL FOUR carriers cured** — baseline pair 000793e0/0007a5b0 gone too ⇒ one mechanism, fix treats it not the symptom. Post-deletion empty-switch scan: NONE.
**Check 3: lineage** — `6a59d3b` introduced the invention as "decline EVERY switch-in-loop"; `cb95592` NARROWED it to multi-exit instead of questioning it (how adaptations survive). Same misread citation both times; deletion removes the last of it. Sibling `rule_switch` defer condition (structure.rs) FILED not deleted — an ORDERING condition that destroys nothing; check against Ghidra's BlockSwitch when next in structure.rs.
**BATTERY @7b6d36c: gauge 3867→3894 = 99.6% of Ghidra · deficit 29/87 → 28 fns/60 calls · 0 functions lost a call, +27 across 2 · corpus UNCHANGED 0.9426 57/60 · suite 565/0 · clippy 0.** RECONCILED: **ONE leaver (6af2c, 18→18 exact parity), ZERO entrants; 29−1=28.** The agent's "both specimens leave" was INFERENCE on top of a verified fact — **51298 went 2→11 of Ghidra's 12: +9 calls but STAYS, one call short.** New shape = one specific missing call from an otherwise-parity fn (one mechanism, not a class) — look-when-nearby, NOT a queue jump. **RULE (task #3): a set-membership claim needs the membership check, not the delta that motivated it.** Emit 478s→708s (+48%): the price of rendering code we used to delete.

## 🏁 SESSION CLOSE (2026-07-29e): HEADLINE 12 BYTE-CLEAN @7b6d36c · TWIN HELD · HERITAGE CORE NOW **QUADRUPLE**-JUSTIFIED
**@7b6d36c sentinel-verified `ok=1225 fail=78`: 5 EXACT + 7 RELOC_EXACT = 12 byte-clean (from 1 at session start). ALL TEN task-#1 accessors byte-clean** (the three orig=8 = RELOC_EXACT with padtrim=2b — their "blocker" resolved as padding-trim, named). COMPILE_FAIL 29→78: **expected in KIND, not a regression — 7b6d36c restored case bodies, so ~49 fns that emitted stubs now emit REAL code wcc386 rejects. The COMPILE_FAIL bar moved to where the code actually is.** The 49 unclassified = natural next COMPILE_FAIL item.

**⛔ THE `recover_call_args` TWIN IS HELD** (`scratchpad/call-args-twin.patch`, 252 lines + git stash@{0}; tree clean `7596459`): faithful port (`init_active_input` fspec.cc:5330/coreaction.cc:1483 + guard_calls input branch heritage.cc:1494-1509 + `guardCallOverlappingInput` heritage.cc:1210, `tryregister=false` for spacebase) **breaks SEVEN hard tests incl. `recovers_jump_table`** (switchind loses its switch). **ROOT CAUSE = heritage candidate collection: Ghidra's cover INCLUDES INPUT VARNODES AND UNAFFECTED REGISTERS (heritage.cc:2703 `if ((!vn->isWritten()) && vn->hasNoDescend() && (!vn->isUnaffected()) && (!vn->isInput())) continue;`) — mosura's doesn't, so an arg register the caller never touches is never a candidate and the argument vanishes. `recover_call_args` papered over exactly that by unconditionally making those ranges candidates.** Twin itself NOT campaign-shaped (~250 lines, correct); it is GATED on task #6.

**⇒ FOUR independent items now blocked on the heritage core: (1) mixfloatint merged cover (heritage.cc:2710) · (2) feeder-b multi-width class, 28 fns/60 calls · (3) the held stackptr patch · (4) the call-args twin.** Agent stopped itself rather than starting the foundation on a long context — correct "report rather than start" + stewardship. **NEXT SESSION = task #6 as THE campaign with a fresh agent; the four items land/heal/retest ON TOP in that order-of-proof.**


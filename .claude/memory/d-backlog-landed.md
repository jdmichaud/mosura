---
name: d-backlog-landed
description: Full outcome record of the D2–D6 decompiler mis-port backlog (all landed/held 2026-07-22) + the menu-(E) P6 output-trial DEEP re-verdict
metadata: 
  node_type: memory
  type: project
  originSessionId: 99103a08-9d06-430b-8446-4ca5e3757106
  modified: 2026-07-23T20:51:01.899Z
---

# D-backlog outcomes (complete 2026-07-22; detail preserved from the live index)

SHA note: the D-commit SHAs below (`4e633e7`/`64524b7`/`8059f69`/`ff83afb`) are PRE-REBASE ids. Master was rebased onto analysis-port `13221ea` on 2026-07-23 (user-directed); D3/D2 now = `1d7956f`/`b0fb75b`. D5/D4 (pre-merge-base) keep their ids. Backlog spec/repros: [[decompiler-misport-backlog]].

- **D5 ✅ DONE `4e633e7` (d51):** call INDIRECTs placed BEFORE the call (newIndirectCreation/newIndirectOp opInsertBefore funcdata_op.cc:726/696) + resolve_call_output backward scan (collectOutputTrialVarnodes fspec.cc:5543) + `current_def_at_op` same-time rename rule (heritage.cc:2506-2517) — retires the after-call+forward-scan adaptation; extraout_RAX blocker GONE catalog-wide (deepchain 9/9 returns captured); 0.9528→0.9540 (switchmulti→0.698, stackreturn→0.926, both toward-oracle, zero wrong-code). **Naming half reclassified NOT-a-misport** (isolated-decompile oracle itself emits func_0x/xunknown; FUN_/undefined need the full-analysis symbol DB) → filed follow-on: thread a function-symbol resolver into printc for the full-analysis path (bounded separate feature).
- **D4 ✅ PRIMARY DONE `64524b7` (d51):** CALL_RETURN flow overrides threaded analysis→decompiler (SharedReturnAnalyzer call-typed refs → raw_funcdata_flow_image_overrides BRANCH→CALL+RETURN, faithful to overrideFlow funcdata_op.cc:997-1009 + flow.cc:416) — EMPTY-LOOP WRONG-CODE GONE (is_odd byte-identical to Ghidra); corpus byte-neutral. **Residual filed = P6 output-trial gap (DEEP):** bare-`return;` on return-of-bare-call-result — guardCalls never sets possibleout, so AncestorRealistic (funcdata_varnode.cc:2046-2050) rejects the killedbycall creation before ActiveReturn promotes it; fix = guardCalls possibleoutput/isOutputActive machinery (multi-pass). NOT same root as D3 (D3 is constant-0x100-specific per probe).
- **D3 ✅ `8059f69` (d51):** return-of-input-derived-value — is_realistic rejected ALL unwritten inputs; Ghidra rejects only a DIRECTLY-input trial (execute funcdata_varnode.cc:2205), traversed-to inputs = pop_success (:2040). dispatch all-7-cases + fallthrough now byte-match Ghidra. Corpus 0.9540→0.9513: sole mover partialunion −0.158 = NET CORRECTNESS (baseline dropped the return to `void`; fixed matches oracle signature/semantics; dip = intermediate-copy render from the HighVariable-merge gap → C-cluster follow-on filed). Zero wrong-code (void stays void).
- **D6 — DEEP, HELD (d51 grounding 2026-07-22):** Ghidra narrows the 128-bit cqo/idiv via signform→piece2sext→subcommute (trace-confirmed); mosura HAS all 3 wired (pipeline.rs:211/240/241) but they never fire for the 8-byte case — mosura's **u64-only nzmask** (nzmask.rs:10 "extended-precision branches collapse"; SUBPIECE nzmask :213 skips truncation for in_size>8) collapses the 16-byte SEXT sign-smear to `#0x0` first → ZEXT rebuild → 128-bit SDIV stays, renders `(int16)`. **Fix = extended-precision (>u64) nzmask foundation** (kin to task9 RuleSubvarSext) → foundation menu (G). Severity LOW (ABI-equivalent, recompilation-cosmetic, not wrong-code).
- **D2 ✅ `ff83afb` (lead-side after d51 limit-kill):** probe-proven root = a DEAD CALLIND (destroyed with its constant-folded-unreachable block, stale parent BlockId(2) on 1-block CFG) collected by op_ids() — mosura's flat op-Vec range includes dead ops where Ghidra's PcodeOpBank::destroy (op.cc:989) removeFromCodeList (:997) + deleteCallSpecs (funcdata.hh:128) make iteration live-only. Ported the invariant: all 7 recover.rs per-opcode collectors (3× Call/Callind + 4× Return) skip dead ops. tm_clones decompiles cleanly (recompilable `void {return;}`); corpus byte-neutral; catch_unwind net stays. **op_ids() dead-op audit DONE (d51, READ-ONLY): CLEAN — D2 class unique to recover.rs.** All 20 other consumers safe: 11 guard is_dead (jumpbasic:1213, merge:502/831/1461/2196, action:246/399, jumptable:53/89, infertypes:571, consume:262); 9 never deref parent (printc:658/981/1872, fspec:897, build:512, subvarflow:553, funcdata:1064, stackvars:81, rules:1697). **DEFENSIVE follow-on (declined now, style not mechanism):** the 9 no-deref sites are safe by what they READ, not a guard — add `!is_dead()` for consistency IF a future edit adds a parent-deref there. Not on critical path.

## ⛔ menu (E) OVERTURNED (2026-07-23, oracle-verified): the persistent-activeoutput premise was FALSE

Agent war2-remediation asked the isolated oracle directly (`oracle/capture --c`, minimal fixtures): **mosura is BYTE-IDENTICAL to isolated Ghidra on both bare-call-return (BOTH drop it) and used-call-result (BOTH capture it).** The D4-doc capture (`uVar1=FUN..(); return uVar1;`) comes from FULL analyzeHeadless cross-function prototype propagation (callee prototype recovered → call output LOCKED → funcLinkOutput isOutputLocked branch coreaction.cc:1540), NOT an isolated activeoutput trial lifecycle. Menu (E) as an isolated-path foundation is RETIRED — building it would move mosura AWAY from the oracle (beat-Ghidra). The faithful lever for the extraout_/dropped-return class = cross-function callee-prototype propagation on the analysis path (DecompilerParameterID analog). The section below is the superseded d51 theory, kept for history.

## menu (E) P6 output-trials — RE-VERDICT DEEP (d51 built+measured+REVERTED @ff83afb 2026-07-22) [SUPERSEDED — see above]

The BOUNDED 2-brick scope was WRONG — under-weighted the SECOND return-trial gate. Built Brick 1 (guard_calls possibleout flag) + Brick 2 (is_realistic input-isIndirectZero); both confirmed working via trace (is_even's tail-call RAX creation → `is_realistic=TRUE`). But **corpus BYTE-NEUTRAL, is_even UNCHANGED — bricks INERT.** Root: `return_trial_kept` = is_realistic AND `ancestor_op_use`; `ancestor_op_use` (recover.rs:419) ALSO rejects every indirect-creation, and that is FAITHFUL (Ghidra `ancestorOpUse` funcdata_varnode.cc:1936 `if(def->isIndirectCreation()) return false`). So the FUNCTION's ReturnRecovery can NEVER keep a bare call-result RAX — in Ghidra either. Ghidra captures `uVar1=FUN_..()` via the CALL's OWN ActionActiveReturn promoting the call output, which needs the PERSISTENT `FuncCallSpecs::activeoutput`/`isOutputActive` trial (registered in guardCalls possibleout, kept alive INDEPENDENT of the RETURN's use). mosura's D5 resolve_call_output is a post-heritage LOCAL scan — when ReturnRecovery prunes the RETURN's RAX (faithfully), the creation's only descendant vanishes → `descend.is_empty()` → skipped. **The missing piece = the persistent isOutputActive/ParamActive call-output lifecycle (multi-pass foundation), NOT 2 bricks.** DEEP, filed with menu (G)/D4-residual (all call/return trial-lifecycle foundations). Instrument caught it pre-land; reverted (no value in corpus-inert scaffolding).

## Superseded @3579ac3 detail (cast/type cluster)

Parked-debt slate CLEAR (processMultiplier `4ffbf39`+`bb758a8`, mergeIndirect `ba3be18`); cast lands `eea0dcc`+`f3f96c3` (floatcast→1.000, HighVariable representative retires `meet`). **Cast-cluster VERDICT: print-time cast only helps where upstream IR matches Ghidra → remaining gaps are UPSTREAM (type-inference + coarse-register-SSA), PIVOT off cast rules.** See [[actionsetcasts-campaign]] · [[bounded-levers-exhausted]] · [[task27-coarse-register-ssa-splituses]].

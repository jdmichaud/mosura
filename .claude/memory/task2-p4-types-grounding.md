---
name: task2-p4-types-grounding
description: "Task #2 (P4 Types) grounding — current state is MORE mature than TODO says; concrete next increments pinned from packstructaccess oracle diff"
metadata: 
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #2 = P4 Types (types.rs + infertypes.rs + printc.rs). Owner=p4t.

## ★ TASK #12 RE-FRAMED (2026-07-11, intle1, master 9f9ebca) — packstructaccess/impliedfield residual is a PRINTER heuristic, NOT P4 typing
Lead created task #12 ("P4 composite-field width/sign recovery: packstructaccess short-field SEXT24, impliedfield float4") from my #10 report. INSTRUMENT-FIRST re-frames it: **mosura's IR ALREADY matches Ghidra — the gap is purely in the SUBPIECE printer.**
- packstructaccess: mosura IR already = `INT_SEXT(SUBPIECE(x>>0x30,0):2)` = `SEXT24(SUBPIECE(x>>0x30,0):2)`. RuleSubNormal FIRES correctly (its popcount-extension SEXT branch, rules.rs:7171, faithful to ruleaction.cc:7714). Ghidra IR identical (RuleSubNormal→SEXT24(SUB82(x,6)); cleanup RuleSubRight re-expands to the shift form).
- impliedfield: same shape — `FLOAT2FLOAT(SUBPIECE(x>>0x20,0):4)`.
- Struct type is a RED HERRING: oracle/capture does NOT apply `tinystruct` (renders xVar1/(uint8), not tVar1.b) → gauge target is untyped `(int4)(int2)(x>>0x30)` = no composite typing needed.
NAMED DIVERGENCE: Ghidra `PrintC::opSubpiece` (printc.cc:843) → `CastStrategyC::isSubpieceCast(outtype,intype,offset)` (cast.cc) = `offset==0 && scalar in/out metatypes` → ALWAYS renders the `(type)` truncation cast (no nzmask). mosura `opSubpiece` (printc.rs:740) gates on `effective_width(in0) > out_size` (use/nzmask-based, printc.rs:174) — for `SUBPIECE(x>>0x30,0):2` the shifted input's used width already fits 2 bytes → 2>2 false → SUPPRESSES the cast → drops `(int2)`/`(float4)`.
FIX = port isSubpieceCast into opSubpiece (replace effective_width gate). BUT load-bearing corpus-wide printer heuristic (gates EVERY offset-0 SUBPIECE); risks = spurious casts where mosura types differ from Ghidra; Ghidra's non-cast branch = opFunc `SUB(x,off)` (mosura renders transparent); cast-name needs SUBPIECE-out type int2/float4.

★ MEASURED (2026-07-11, intle1, local/uncommitted on 9f9ebca) — lead GO'd the FULL faithful port (declined narrow scope: narrowing = new adaptation). Implemented: `is_subpiece_cast` (printc.rs, faithful CastStrategyC::isSubpieceCast — offset0 + scalar in/out metatypes) + opFunc branch `SUB<insize><outsize>(x,off)`; effective_width now dead (`#[allow(dead_code)]`, remove on land). Corpus **0.9196→0.9209 (+0.0013)**, 56/60, suite 396/0, switch 6/6. Movers: WINS floatconv +.057, packstructaccess +.045 `(int4)(int2)(uVar1>>0x30)`, impliedfield +.017 `(float4)(...)` (targets FIXED, correct type names — (int2) not xunknown2). DIPS floatcast −.028, revisit −.012, loopcomment −.003, switchloop −.001. BUCKETS: (iii) mis-port NONE (faithful, green, opFunc SUB() inert on corpus — RuleSubRight re-expands offset≠0); (ii) `(xunknownN)` = floatcast/revisit/switchloop wide/multi-precision CONCAT truncations where SUBPIECE out-type is Unknown (mosura float/wide inference < Ghidra float8); (i) type-width mismatch = loopcomment `(uint1)iVar1` / switchloop `(uint4)` extra-SUBPIECE. Dips = faithful cast EXPOSING upstream type gaps effective_width was MASKING (faithful-exposes-gap, guardCalls-ram→#7 pattern), NOT (ii)-dominated-garbage. Per faithful-ports-land-not-held → LANDS. ✅ LANDED master `b573e78` (lead land-go): printc.rs is_subpiece_cast + opSubpiece rewrite + effective_width REMOVED (the non-faithful adaptation gone), coverage.md opSubpiece row. Suite 396/0, switch 6/6, corpus 0.9209/56. Task #12 COMPLETE. Lead filed the 2 exposed-gap follow-ups: **#13** (float/wide-CONCAT weak type inference → (xunknownN) on floatcast/revisit/switchloop; mosura infers Unknown where Ghidra infers float8) + **#14** (extra/narrower SUBPIECE type-width divergence → loopcomment/switchloop spurious cast). Both are P4 type-inference (infertypes.rs) — the printer is now faithful; the gaps are upstream type-recovery.
 PAUSED (user, 2026-07-03) at green boundary HEAD `955574e`, 207 green, tree clean — 3 faithful commits landed this session (3085283, d7105b8, 955574e; corpus 0.8655→0.8715). The 3 self-contained printc/type increments are DONE; the remaining #4 bulk is a big subsystem (see below). Warm-resume decision pending: composite-type subsystem vs elevated P6.

EMPIRICAL RE-GROUNDING 2026-07-04 (agent, HEAD 955574e, throwaway diag test dumping mosura C vs oracle --c on all composite fixtures — deleted after, tree clean): **the composite-type subsystem is ALREADY PORTED + LANDED — the lead's "extend infertypes past the struct/array/union deferral" premise is largely overtaken.** Present + faithful: Datatype::Array/Struct in the lattice + get_subtype (types.rs); RulePtrArith + full AddTreeState creating PTRADD/PTRSUB with Array/Struct base_type handling (ptrarith.rs, 589 lines); ScopeLocal/MapState/RangeHint stack-array recovery → Array(elem,n) symbols (varmap.rs, 633 lines); printc render_mem base[i]/base->field + anchor_stack_arrays + detect_arrays. RESULT on the named fixtures: twodim/offsetarray/nestedoffset=1.000, threedim=0.985, wayoffarray=0.920, ptrtoarray=0.954, packstructaccess=0.826. Array indexing WORKS (offsetarray `auStack_98[param_1]`, nestedoffset). infertypes.rs:24-29 + ptrarith.rs:14 stale-ish "deferred" comments understate how much is done.
The residual divergences on these fixtures are NOT composite — they cluster into a small set of faithful typing gaps (leverage order): (1) **CONSTANT/SIGNEDNESS TYPING in infertypes** — pervasive `5U`/`4U` spurious unsigned + `uint4` vs Ghidra `int4`/`xunknown4` + the spurious `(int4)` casts that follow. ROOT: infertypes.rs:616/626/635 `infer()` FILTERS constants out of the returned map (`!is_constant()`), so `type_of(const)`=Unknown even though build_localtypes+propagation ALREADY computed a type for it → printc `mark_explicit_unsigned` (faithful port of cast.cc markExplicitUnsigned, which reads `vn->getHighTypeReadFacing(op)`) sees Unknown and over-fires `U`; Ghidra types the const INT via inference so bails. FIX = stop discarding constant types / give a read-facing const type. Also missing markExplicitUnsigned's isCharPrint/isEnumType guards. Touches twodim/threedim/nestedoffset/offsetarray/union_datatype/wayoffarray. Biggest P4 lever, in-file. (2) return width int8 vs int4/xunknown4 + void return = guardReturns/P6 (OUT of P4). (3) SUBPIECE/PIECE→float/composite typing (impliedfield float4-from-SUBPIECE; packstructaccess shift-extract=rule-pool #2) — deep, few fixtures. (4) pointer casts on arithmetic-derived pointers (pointercmp `(xunknown1 *)(param_1+8)`) = ActionSetCasts. (5) wayoffarray base-selection: mosura picks scalar `&xStack_a0` (-0xa0), Ghidra array `axStack_98 + -8` (-0x98 base, -8 resid) = spacebase symbol resolution (propagateSpacebaseRef, infertypes.rs:26 deferral) — ONE fixture, deep. (6) negative const hex `-16` vs `-0x10` (pointersub) = render_const, tiny.
C1 + C3 LANDED 2026-07-04 (agent ptype, base 955574e; lead GO on C1 + self-approve C3). Tree CLEAN, 207 green, corpus 0.8715→0.8720.
- C1 `928e705`: infertypes.rs `infer()` stops discarding constants — each active const → own committed temp type (not meeted into a HighVariable; a const is its own singleton high). Grounded coreaction.cc writeBack + cast.cc markExplicitUnsigned (reads getHighTypeReadFacing) + varnode.cc:665. + coherence comment updates in printc.rs (mark_explicit_unsigned) & rules.rs (RuleAddUnsigned no longer "dormant"). EFFECT: spurious `U` GONE matching Ghidra (twodim `*5U`→`*5`, threedim `0xaU`→`10`, nestedoffset `2U`→`2`); RuleAddUnsigned ACTIVATED (its designed trigger). Corpus: revisit +0.042, switchmulti +0.009 (RuleAddUnsigned), longdouble −0.017 (FAITHFUL gauge-artifact: const `0x101000` now correctly typed Pointer→float10 → render_mem drops spurious `*(float10 *)` cast; Ghidra names the global `fRam...=` so residual is global-naming P8/task#6, NOT a C1 defect).
- C3 `840ed3c`: printc.rs render_const negative branch now prints MAGNITUDE via ≤10-dec/most_natural_base (printc.cc:1288 push_integer) → pointersub `-16`→`-0x10`, small mags like `-8` stay decimal. Byte-neutral gauge, real fidelity. NOTE: render_const's `signed > -0x10000` threshold (decide WHEN to print negative by magnitude) remains a NON-faithful heuristic — Ghidra decides print_negsign by TYPE sign (TYPE_INT); faithful fix = thread the (now-available) const type-sign into render_const, DEFERRED (bigger, touches all call sites) = C2-adjacent follow-up.
DEFERRED per lead: C4 (SUBPIECE-float/pointer-arith casts, impliedfield/pointercmp), C5 (wayoffarray spacebase base-selection). NEXT: pinged lead for next BIG target (lead picks from P7/float-NAN/switch/indirect/P6 — corpus leverage is there, not P4).

LEVERAGE CAVEAT reported to lead: even gap (1) is largely ccompare-NEUTRAL (twodim/nestedoffset/offsetarray already 1.000 on the gauge) — it's FIDELITY not gauge movement. Corpus-gauge leverage lives in OTHER subsystems (worst fixtures: nan 0.378, switchmulti 0.55, revisit 0.576, floatconv 0.578, deindirect 0.636 = P7 struct/float/indirect, not P4). Plan + go/redirect choice sent to lead 2026-07-04 (gated proposal). See [[task2-tailrules-progress]].

STATE IS MORE ADVANCED THAN TODO:119-122 IMPLIES:
- Casts SUBSTANTIALLY DONE: `get_input_cast` (printc.rs:342) handles ZEXT/SEXT/SUBPIECE→(T)x, SDIV/SREM/DIV/REM signedness, EQUAL; `cast_operand` (:428) wraps operands; `cast_standard` (cast.rs) is the TypeOp::getInputCast/castStandard port; LOAD/STORE pointer casts `*(T *)(addr)` (:590).
- DECLARATIONS ALREADY EMITTED (not gated as TODO says): `p.decls` vec (printc.rs:123) collected in first-use order, printed at body top (:1632); recovered stack arrays declared `T name[N];` (:582/1634). The stale TODO "ENABLE after CSE" is OBSOLETE.
- Datatype lattice + metatype `meet` + per-HighVariable meet: DONE (infertypes.rs).

PROGRESS (2026-07-03, base 7e06aa2):
  #1 RETURN-TYPE — DONE, commit `3085283` (lead GO). The real faithful fix was BIGGER than "un-downgrade the return": DELETE `printc::symbol_type` entirely (int/uint→undefined downgrade for declared symbols). Ghidra applies NO downgrade — `FuncProto::updateOutputTypes`/`updateInputTypes` (fspec.cc:4159/4076) set return AND param types to `vn->getHigh()->getType()` (high on: ActionOutput/InputPrototype coreaction.cc:5730/5731 > ActionStartTypes:5687). `type_of` now returns the raw inferred type. The downgrade was FORCING spurious `(int8)(int4)param` casts (undefined needs the intermediate) + `xVar`/`xRam` names. Corpus 0.8655→0.8690, 14 up / 0 down (twodim→1.000), grounded on twodim/threedim oracle --c. No-grandfathering de-adaptation.
  #3 `func(void)` — DONE, commit `d7105b8` (lead GO, modulo2 tradeoff explicitly accepted). printc.rs +4/-1: empty param list → `void` (Ghidra `emitPrototypeInputs` printc.cc:2227, `numParams()==0→"void"`, unconditional). 0.8690→0.8699. GAINS 10 (displayformat/enum/convert→1.000). REGRESSIONS modulo2 1.000→0.941 + noforloop_globcall = param-recovery gap EXPOSED (mosura fails to link EDI→param_1, reads it anon `xVar1`; body still matches, only honest sig differs) — P6. NOISE: injectoverride -0.066 despite sig now EXACTLY matching Ghidra (ccompare provably lying).
  RESIDUAL return-type gaps → their subsystems (NOT printc): WIDTH xunknown8 vs int4 = heritage `guardReturns` (heritage.cc:1652 per-write-size trials+SUBPIECE; mosura recover_return appends fixed RAX:8) = Task #1; VOID over-recovery (dupptr/elseif/revisit) = AncestorRealistic/ancestorOpUse P6/#4; int-vs-uint signedness = infertypes.rs propagation.
  STILL TODO (ALL heavy subsystems — the easy printc increments are done):
   - #2 `(int4)(int2)` shift-subfield casts = a RULE-POOL item, NOT P4 (lead framing): mosura IR = raw `INT_LEFT`+`INT_SRIGHT` (`(x<<0x10) s>>0x30`); Ghidra folds to truncated sign-extract `(int2)((uint8)x>>0x20)` = `(int_{(64-c2)/8})(x u>> (c2-c1))`. RuleShiftPiece-family p-code rule (ruleaction.cc), corpus-wide firing risk, needs trace-diff. Few fixtures. → belongs to the rule-pool work, not this task.
   - `5U`/`4U` spurious unsigned = an INFERTYPES gap (lead framing): mosura `mark_explicit_unsigned` (printc.rs:368) faithfully ports `CastStrategy::markExplicitUnsigned` (cast.cc:38) BUT infertypes doesn't type constant varnodes → constant read-facing type = Unknown, so step-4 doesn't bail → over-fires `U` where Ghidra (getHighTypeReadFacing→INT) prints bare. Also missing isCharPrint/isEnumType/PARTIAL guards. ccompare-NEUTRAL but real fidelity.
   - #4 pointer/struct/array recovery — IN PROGRESS. (a) DECL ORDER — DONE, commit `955574e`: printc.rs sort of `decls` by stack offset (Ghidra `emitScopeVarDecls` printc.cc:2265 = ScopeLocal symbol-map = ascending storage-Address). Landed 0.8699→0.8715, +0.0016, 4up/0down (wayoffarray+.040 pointerrel+.025 varcross+.020 partialsplit+.007; all green). Documented gap (lead-OK, unexercised): fully-faithful sorts ALL locals by storage address incl register space; mosura sorts stack-by-offset + register temps keep first-use (no corpus fixture mixes reg+stack in one decl block). Empirically NO x86-64 fixture mixes reg+stack locals in one block → reg-vs-stack precedence unexercised (register temps keep first-use via stable sort). (b) base-relative array addressing + array `base[i]` rendering — BLOCKED on POINTER-ARITHMETIC TYPE INFERENCE. printc.rs:510-511 already documents it: mosura renders `base[i]` only when the base is Array-typed, and "Array typing comes from pointer-arithmetic inference (#2/#10) — not yet produced." Ghidra picks the ARRAY symbol as the base for a variable-indexed stack access (`axStack_98 + param_1*4 + -8`, array base + neg residual); mosura falls back to the nearest scalar (`&xStack_a0`). Needs Ghidra's PTRADD/PTRSUB typing (TypeOpPtradd / ActionSetCasts pointer typing) = a BIG subsystem, own multi-commit — the real remaining bulk of #4. (c) width uint8 vs xunknown4 = guardReturns/P6. (d) access-type uint4 vs xunknown4 = infertypes.
  So after decl-order lands, #4's remaining bulk (base selection, array[i], pointer pointees, struct recovery) = COMPOSITE TYPE inference. PRECISE BLOCKER: mosura infertypes.rs ALREADY ports pointer-arith propagation (PTRADD/PTRSUB/LOAD/STORE propagateType, propagateAddIn2Out, down_chain array-wrap — infertypes.rs:20-25, 355-385) but EXPLICITLY DEFERS composite (struct/array/union) types + TypePointerRel struct-container (infertypes.rs:26-29). That deferral is why the base is never Array-typed → printc falls back to scalar/pointer form. So #4 bulk = port Ghidra's composite type system (TypeArray/TypeStruct/TypeUnion + TypePointerRel + down_chain into arrays) + where composite symbol types come from (ScopeLocal array recovery). BIG multi-commit, on par with P6.
  DECL-ORDER LANDED: commit `955574e` (lead GO). decl-order was the one self-contained tractable #4 piece. Session commits: 3085283, d7105b8, 955574e (corpus 0.8655→0.8715). REMAINING #4 and ELEVATED P6 are both big subsystems = re-scope/checkpoint point; awaiting lead steer (composite-type plan vs P6).
   - #5 param-size = P6-gated (skip).
  CROSS-TASK / P6 ELEVATED (lead, 2026-07-03): guardReturns is CONFIRMED P6-GATED (needs characterizeAsOutput + active_output PRE-heritage = a P6 restructure, NOT a clean standalone). It's the highest-leverage gap (return-width + param-recovery EDI→param_1 + void-over-recovery), so lead ELEVATED P6 (Task #4) as the highest-leverage NEXT big target AFTER struct/array. Leave guardReturns until P6.

## ★ #13/#14 INSTRUMENT-FIRST RE-DIAGNOSIS (2026-07-11, p4infer1, HEAD b573e78 — READ-ONLY, no code) — BOTH are NOT P4 type-inference gaps; STOP+report sent to lead
The lead framed #13 ("weak float/wide-CONCAT type inference → mosura infers Unknown where Ghidra infers float8") + #14 ("extra/narrower SUBPIECE type-width divergence"). IR-diff (oracle `capture --ir -` vs `dumpc --raw`) on ALL FOUR named fixtures proves the `(xunknownN)`/`(uintN)` casts are DOWNSTREAM symptoms of already-tracked HELD/gated subsystems — NOT infertypes.rs weaknesses. Strengthening infertypes to type the synthetic wrong-width values = inventing a non-Ghidra heuristic (violates the port principle; the `(xunknownN)` is the FAITHFUL type of an IR value that shouldn't exist).
- **floatcast (#13, −.028)** = XMM 16-byte lane modeling = **task #6 LaneDivide** (held-inert). Mosura raw IR carries XMM as `r0x1200:16` (16 bytes): `r0x1200:16 = PIECE #0x0:8 r0x100080:8` (=CONCAT88(0,float8)), `u0x10124:16 = INT_RIGHT r0x1200:16 #0x40`, `SUBPIECE r0x1200:16 #0x0:4`. Ghidra operates on LANES `XMM0_Qa`(8B)/`XMM0_Da`(4B) — never builds a 16-byte value. The held-inert LaneDivide note (speccache.rs:25-45) LITERALLY describes this (`dead SUBPIECE r0x1200:16 -> :4 of XMM0`) and records "At reactivation floatcast improves +0.038" (would recover the −.028 and then some). Gated on P6 stopping spurious 4-byte XMM trials [primary] OR post-pool stack-resolve. DEEP.
- **revisit (#13-listed, −.012)** = P6 return OVER-recovery: mosura `uint8 func(void)` builds `RAX:8 = PIECE(upper-6B-of-CALL-result, 2B-add)` → `(xunknown6)(uVar2>>0x10)`; Ghidra recovers `void func(void)` (dead RAX) + `_2_2_` partial-field `CONCAT22`. = guardReturns/AncestorRealistic (P6, gated). NOT P4, NOT lane.
- **switchloop (#13 xunknown7 + #14 uint4)** = the int8-wide switch-body accumulator `iVar2` (Ghidra keeps uint4), driven by dual-width switch selector (`iVar1 + -1`, cases shifted) + propagatecopy dup (`iVar1=2;iVar1=2;`) = **task #4** (propagatecopy over-fire / [[switchloop-dup-propagatecopy]]). The `(xunknown7)`/`(uint4)` are symptoms of the 8-byte width. NOT P4.
- **loopcomment (#14, −.003)** = `(uint1)iVar1` is a merged-phi bool read (`10<iStack_28 & (uint1)iVar1`) where Ghidra recomputes `aiStack_1c[0]<100` inline; tangled with missing for-loop + array recovery. Structural, NOT a clean SUBPIECE type-width fix.
CONCLUSION: #12's faithful `is_subpiece_cast` is doing its job — the 4 dips are HONEST diagnostics naming the next fixes in #6/#4/P6, exactly per corpus-is-a-diagnostic. There is NO faithful P4 type-inference change that closes them. RECOMMEND re-attribute #13→#6(floatcast)+P6(revisit), #14→#4(switchloop)+structural(loopcomment); await lead steer (don't force a type-inference rewrite). See [[task6-lanedivide-plan]], [[task2-p6-prototypes-plan]], [[switchloop-dup-propagatecopy]].

DISCIPLINE (lead standing): byte-neutral lands self-approve+report-after; corpus-movers gate (report delta + non-Ghidra cause BEFORE landing, wait go). P4 changes are corpus-movers → gate each. Warm-resume the SAME agent (p4t) across tasks — never respawn (see [[single-agent-protocol]]). See [[task4-heritage-guard-collect-plan]].

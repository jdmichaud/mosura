---
name: task2-p6-prototypes-plan
description: "P6 Prototypes — Stage A+B-1 LANDED; Bricks 1+2 landed lead-side; Brick 3 (deindirect/elseif input tail) PIN RESOLVED 2026-07-17: root = (b) any-slot skip leak, faithful own-slot onlyOpUse PARKED TURNKEY as brick3-ownslot-onlyopuse.patch (deindirect→1.000), gated mover awaiting lead GO"
metadata:
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

## ★ TASK #5 ARC PARKED TURNKEY 2026-07-16 (p6b1, base `19f1ebb`, tree reverted CLEAN, suite 417/0 @clean) — Bricks 1+2 BUILT+VERIFIED, GATED (lead confirm pending), Brick 3 pinned
**Patches (this memory dir; both `git apply --check` clean on `19f1ebb`, sequence brick1→brick2 byte-exact vs the verified tree):**
- `brick1-xmm04-retire.patch` (181 lines: recover.rs + coverage.md ActionReturnRecovery row) — retires the overlapping XMM0:4 return candidate (recover_return, was `[(RAX,8),(XMM0,8),(XMM0,4)]`) AND `is_const_padded_piece` (both artifacts of the same non-Ghidra multi-candidate adaptation; Ghidra `guardReturns` heritage.cc:1652 registers ONE trial per heritaged range via characterizeAsOutput, and has NO const-padded narrowing — a `float` return commits at the wide trial, 8→4 narrowing is downstream subflow, same finding-class as twodim int4). Keeping is_const_padded_piece without the sibling would VOID bare-float returns (documented decision + new unit test `bare_float_return_commits_the_wide_xmm0_trial`). VERIFIED @19f1ebb: corpus avg 0.9276 **per-fixture IDENTICAL** (fresh stash-diff), full suite **418/0** (+1 test), 62/62.
- `brick2-lanedivide-reactivate.patch` (121 lines: speccache.rs populate + lang.rs load mirror + dumpc.rs→speccache + coverage.md ActionLaneDivide row) — reactivates the COMPLETE held-inert LaneDivide subsystem (ends task #6 S3b inert state). dumpc previously built its own `Spec::from_sla` and would have silently dumped lane-blind — now loads via speccache. VERIFIED composition @19f1ebb+Brick1: **avg 0.9286 (+0.0010, NET-POSITIVE)**, floatprint HOLDS 1.000, stackstring 0.818→**0.899** (+0.081 — now splits into Ghidra's exact 8-byte lane shapes `xStack_28/xStack_20`, `&xStack_20+4`; residual = missing `xStack=xRam..` assigns = spacebase ram→stack forwarding #22/#7 class), concatsplit 0.881→0.863 (−0.018, the ONLY regression: PRE-EXISTING do_trace/placement gap — genuine live 4-byte SUBPIECEs at mosura's pre-pool slot make faithful smallest-first pick 4-byte lanes; Ghidra's post-oppool1 stackstall slot sees only 8-byte reads → task #8 mainloop-cadence class, cited in coverage row). Suite 418/0, jumptable 6/6, ir_parity 9/9, proto_recovery 3/3.
**Fresh-probe falsifications vs old memory:** stackstring is NO LONGER blocked at reactivation (the #22-A/#27 spacebase+splitUses work unblocked the split's rendering — old "0.794 UNCHANGED" claims are stale); LaneDivide-live is net-POSITIVE for the first time.
**RESUME/LAND (on lead confirm):** apply brick1 → re-run corpus (expect per-fixture identical) + suite → commit (recover.rs + docs/coverage.md, msg ≈ "decompile: retire the XMM0:4 overlapping return candidate — single-trial guardReturns direction (task #5 Brick 1)", trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`) → apply brick2 → re-run corpus (expect avg 0.9286, the 2 moves above) + suite/jumptable → commit (speccache.rs + lang.rs + examples/dumpc.rs + docs/coverage.md, msg ≈ "decompile: reactivate ActionLaneDivide — populate Spec.laned (task #5 Brick 2, retires the task #6 held-inert state; concatsplit −0.018 = pre-existing do_trace/placement follow-on)").

## ★ BRICK 3 PIN RESOLVED + BUILT + GATED 2026-07-17 (p6b2, base `568d73c`, tree reverted CLEAN, patch parked) — ROOT = (b) ANY-SLOT SKIP LEAK, VERIFIED
**PIN VERDICT (probe MOSURA_P6B3 over all 60 @568d73c, reverted): root (b) alone — NOT (a).** The any-slot skip in `only_op_use` fires with own-slot≠any-slot on EXACTLY 3 fixtures: deindirect (callind@0x100769 RDX trial reaches the RSI-slot vn), elseif (calls @0x100931 + @0x10078f, same shape), impliedfield (RETURN side, XMM0-slot trial reaches the RAX-slot vn). In every CALL case the probe's would-be `check_call_double_use` verdict = **false** (real use) → Ghidra would markInactive the RDX trial → 2 args. ORACLE-SIDE EMPIRICS (capture --ir activeparam/deadcode + capture_trace): Ghidra's heritage re-links deindirect's ESI copy to read `SUB84(RDX(0x100739:20),0)` — the RDX trial's value flows through a dedicated (actionalt) COPY into the SAME call's RSI slot; Ghidra's own-slot test (funcdata_varnode.cc:1823-1825) does NOT skip that use → checkCallDoubleUse → isAlternatePathValid(actionalt)=true → reject. Fixture note: deindirect.xml DECLARES `extern void realfunc(int4,int4)` but capture ignores prototype locks — the oracle's 2-arg verdict is pure recovery (recovery-vs-recovery fair). **(a) fillinMap wiring falsified as the Brick-3 lever:** shadow `fillin_map` at every `build_input_from_trials` agrees with the reduced leading-active-run wherever trial flags agree; zero used-unref (hole-fill) trials corpus-wide at call sites; the 2 disagreements are deindirect2@0x10001d (fillinMap would hole-fill to 4 args vs oracle's 1 — mosura's RCX trial is spuriously active, an UPSTREAM state divergence that gates any future fillinMap wiring) and partialsplit@0x10004d (early-round transient, final already oracle-matching).
**BUILT (faithful, recover.rs only): `brick3-ownslot-onlyopuse.patch` (this memory dir, 144 lines, `git apply --check` clean on `568d73c`).** (i) own-slot-only skip in `only_op_use` top-of-loop + the RETURN-case own-slot test (funcdata_varnode.cc:1823-1825/1852-1854) — same-op-other-slot uses now fall through to `check_call_double_use`/the Return case; (ii) DELETED `is_return_value_use` (the any-slot accommodation — retired, no-adaptation-grandfathered); (iii) `check_input_trial_use` restored to Ghidra's SEQUENTIAL in-loop mark+free (fspec.cc:5613-5651; the batched-verdicts shape was an undeclared adaptation — later trials' checkCallDoubleUse must see earlier verdicts via the isChecked branch, funcdata_varnode.cc:1787); (iv) test `overlapping_return_candidates_keep_the_value` → `sibling_slot_use_fails_the_own_slot_test` (asserted the OLD accommodation; Ghidra's own rule rejects that shape — Stage B-1 test-update precedent).
**VERIFIED @568d73c+patch: corpus avg 0.9355→0.9379 (+0.0024), 57/60 holds. Movers: deindirect 0.857→1.000 (byte-exact vs oracle — `(*(code*)0x1006ca)(param_2+3,param_3+5)`), elseif 0.915→0.918 (spurious `param_1` tail arg GONE at the 0x1f site; residuals = missing `iStack_c=param_1` init + for-loop structuring, P7 class). ALL 58 other fixtures byte-identical** (all must-holds hold: concatsplit .978, floatprint/piecestruct/indproto/condconst 1.000, varcross .936, stackstring .933, revisit .894, floatcast .880, switchloop .851; impliedfield .938 unchanged — the RETURN-side own-slot flip is corpus-inert, RAX first-match wins either way). Suite 428/0, jumptable 6/6, proto_recovery 3/3, ir_parity 9/9.
**concatsplit param_2 declaration gap: NOT surfaced by this fix (as predicted — own-params/A6 declaration surface, not the call path). Still open, FILE SEPARATELY: mosura numbers params around the hole (param_3…param_7) but omits the `xunknown8 param_2` declaration Ghidra emits for the unref hole-fill.**
**RESUME/LAND (on lead GO): `git apply` brick3-ownslot-onlyopuse.patch → re-run corpus (expect avg 0.9379, the 2 movers above) + full suite (428/0) → commit recover.rs (msg ≈ "decompile: onlyOpUse skips only the trial's own slot — retire the any-slot accommodation (P6-B Brick 3)", trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`); consider a coverage.md ActionActiveParam-row note citing the own-slot restore.**

## BRICK 3 PARKED AT THE INSTRUMENT-FIRST PIN (input-tail: deindirect 0.857 / elseif 0.915, both = ONE spurious tail call-arg vs oracle) — RESOLVED ABOVE, kept for the record
Divergences @19f1ebb (oracle-diffed): deindirect `(*(code*)0x1006ca)(param_2+3, iVar1, iVar1)` vs Ghidra 2 args; elseif `func_0x00100590(0x100a24, param_1+0x1f, param_1)` vs 2 args. TWO candidate roots, both real gaps — do NOT build either without a log-probe pin (MOSURA_P6FAITHFUL methodology: log verdicts at the real decision point, run all 60):
- (a) **missing deriveInputMap**: Ghidra `ActionActiveParam` (coreaction.cc:1747-1754) runs resolveModel→`deriveInputMap`(=`ParamListStandard::fillinMap` fspec.cc:1285)→`buildInputFromTrials` (fspec.cc:5685 — keeps isUsed trials, creates unref hole-fill varnodes, SUBPIECE-truncates oversized). mosura `resolve_call_args` (recover.rs:633) goes straight to the REDUCED leading-active-run `build_input_from_trials` (recover.rs:758). The fillinMap machinery is ALREADY PORTED (fspec.rs:223 `fillin_map`, proven on own-params `recover_input_params` fspec.rs:833) — wiring is small.
- (b) **any-slot skip leak**: `only_op_use` (recover.rs, the `op == opmatch && is_return_value_use(...)` skip) treats a same-call use at ANY value slot as "the use we are evaluating"; Ghidra (funcdata_varnode.cc:1823-1825) skips ONLY the trial's OWN slot and routes same-op-other-slot uses through `checkCallDoubleUse` (funcdata_varnode.cc:1756), which can reject → trial inactive. deindirect's `param_3+5` feeds RSI AND RDX of the SAME call — under (b) Ghidra's RDX trial goes inactive where mosura's stays active. The accommodation exists for the RETURN multi-candidate scheme; after Brick 1 the remaining candidates (RAX:8/XMM0:8) are disjoint, so the own-slot revert may now be safe — but it is SHARED with the input path: instrument, don't assume.
- Also check at Brick 3: **concatsplit param_2 declaration gap** — Ghidra declares the unref hole-filled `xunknown8 param_2` (RSI, unused) in the prototype; mosura's param NUMBERING accounts for the hole (args named param_3…param_7 correctly) but the declaration omits it. Own-params/A6-consumption side; see if fillinMap wiring surfaces it, else file separately.
- **Brick 4 (filed, dormant)**: retire `recover_call_args`' fixed `[RDI..R9]:8` list → guardCalls-style per-heritaged-range registration (`characterizeAsInputParam`, heritage.cc:1496-1509; `guardCallOverlappingInput` for contained_by) — adds XMM/stack/sub-width arg trials. Corpus-inert today (no in-scope fixture passes float/stack args); the input-side sibling of the Brick-1 adaptation.

## ✅ STAGE B-1 LANDED 2026-07-10 (p6b1) — master `c73a074` (verified: tree clean, 374/0, corpus 0.9140/56)
Lead GO'd A (land sub-part 1), DEFERRED B (fold XMM0:4 retirement into the characterizeAsOutput backlog — the ad-hoc [(RAX,8),(XMM0,8)] is still a multi-candidate heuristic, not the faithful single-trial whole), ACCEPTED C (LaneDivide stays inert; re-route to task#1 spacebase + task#6 do_trace). Committed recover.rs + coverage.md (explicit paths). Implementation = faithful `check_input_trial_use` register branch (3-way verdict) + new `realistic_faithful` (is_realistic with unwritten-input base-case = `!is_return_address()`, matching enterNode pop_success for a traversed-to input). ONE unit test updated (`arg_through_passthrough_indirect_is_realistic`): its synthetic passthrough value was a CONSTANT — under the added aou gate a constant fails ancestorOpUse (Ghidra funcdata_varnode.cc:1922 same), so changed it to a written only-used-by-call value (a real caller value, loopcomment's Load) — NOT test-massaging, the constant case asserted the OLD is_realistic-only behavior. Corpus delta EXACTLY as measured pre-land: indproto→1.000, deindirect .636→.857 (crosses .70, 55→56), deindirect2 .889→.936, noforloop_globcall .889→.947, switchhide .955→.960, switchmulti .559→.564, elseif .912→.904 (−.008 wash, 2 residuals cited on coverage ActionActiveParam row). **P6 tractable scope now COMPLETE: return-void (Stage A `82f9293`) + input-params (Stage B-1 `c73a074`).** Remaining P6 = the characterizeAsOutput single-trial retirement (return-side overlapping XMM0:8/XMM0:4 candidates, recover.rs:480) — lead logging as its own backlog rock (byte-identical to retire, blocks LaneDivide's floatprint at reactivation). LaneDivide reactivation NOT a P6 deliverable (task#1 + task#6).

--- pre-land grounding (for the record) ---
## ✅ STAGE B PREMISE-VERIFIED 2026-07-10 (p6b1, base `82f9293`, all probes REVERTED → tree clean 82f9293) — GATED, REPORT SENT LEAD, WAIT FOR GO
Two sub-parts grounded premise-first (Stage-A methodology: log-probe at the real decision point, measure all 60, revert). Suite/corpus baseline confirmed 374/0, 0.9077/55.

### Sub-part 1 (INPUT PARAMS → indproto) — VERIFIED NET-POSITIVE MOVER, faithful, ready to implement on go.
**MECHANISM PINNED:** mosura `check_input_trial_use` (recover.rs:630) diverges from Ghidra `FuncCallSpecs::checkInputTrialUse` (fspec.cc:5638-5651, register branch) in TWO coupled ways:
(1) **Realism base case** — mosura's `is_realistic` returns false for ANY chain bottoming at an unwritten input; Ghidra's `AncestorRealistic::execute` returns false ONLY when the trial varnode ITSELF is a top-level input (execute early-return, funcdata_varnode.cc:2211), while an input reached THROUGH a COPY/SUBPIECE/PIECE/ZEXT chain → `pop_success`=realistic (enterNode:2033). indproto's else-call EDI arg is the WRITTEN chain `r0x38=ZEXT(SUBPIECE(PIECE(...SUBPIECE(EDI_input))))` (PROBE-confirmed) → Ghidra realistic, mosura not.
(2) **No `ancestorOpUse` gate + wrong disposition** — mosura marks a not-realistic trial `mark_no_use()` (FREES dataflow→const0); Ghidra: realistic→`ancestorOpUse`(Stage A's `ancestor_op_use`)→active/inactive; not-realistic-but-`vn->isInput()`→`markInactive()` (PRESERVE, fspec.cc:5645); else→markNoUse. Consequence: mosura frees EDI's only descendant → `recover_input_params` (fspec.rs:833, marks trial active only if `!descend.is_empty()`) drops param_1.
**PREMISE VERIFIED (log-then-behavioral probe, env `MOSURA_P6FAITHFUL`, all 60, reverted):** the faithful register-branch (`realistic_faithful` traversal-aware realism + `ancestor_op_use` + isInput-preserve) reproduces indproto's EXACT if/else asymmetry — else-call(65) EDI `ACTIVE`(→param_1), if-call(50) EDI `inactive(aou-no)`(dropped), direct-calls keep const arg. The asymmetry EMERGES from `ancestor_op_use` (both calls see EDI; only else passes). CORPUS (all 60): **indproto .947→1.000 (TARGET), deindirect .636→.857 (+.221, crosses .70), noforloop_globcall .889→.947, deindirect2 .889→.936, switchhide .955→.960, switchmulti .559→.564; elseif .912→.904 (−.008 wash). avg 0.9077→0.9140 (+.0063), 55→56/60, nothing newly <.70.** elseif −.008 = the gate CORRECTLY recovers its missing param_2/param_1 call args (matches Ghidra) but 2 residuals: params typed uint4-vs-int4 (2nd-order type inference) + one spurious extra `param_1` arg (build-side hole-filling gap; mosura's `build_input_from_trials` keeps leading-active-run vs Ghidra fillinMap+forceInactiveChain).
**STAGING (gated mover, one atomic commit):** port the faithful register-branch into `check_input_trial_use` (add `realistic_faithful` = is_realistic with unwritten-input base-case=true; wire `ancestor_op_use` on the input side active_output=false; 3-way verdict active/inactive(preserve)/nouse(free)). Keep build_input_from_trials leading-active-run for now (the elseif spurious-arg is a residual fillinMap gap — note in coverage, likely needs input-side fillinMap later). Full per-fixture delta already measured above; suite green; gate before land.

### Sub-part 2 (LANEDIVIDE reactivation) — PREMISE PARTIALLY FALSIFIED: P6 does NOT unblock LaneDivide.
Reactivated the inert wire locally (env `MOSURA_LANE`, speccache+dumpc populate; probes reverted). On CURRENT master, live wire NET-NEG: floatprint 1.000→0.850, concatsplit .881→.863, floatcast .804→.791, stackstring 0.794 UNCHANGED, avg 0.9077→0.9047.
**4-byte-XMM SOURCE PINNED (LANEPROBE on `process_varnode`):** the blocking spurious 4-byte SUBPIECE is `recover_return`'s **XMM0:4 overlapping candidate** (recover.rs:480, `[(RAX,8),(XMM0,8),(XMM0,4)]`) — NOT the call-output side (stackstring/floatprint have no XMM-returning call). Heritage materializes the XMM0:4 return candidate as `SUBPIECE(r0x1200:16)->:4`, DEAD after resolve_return voids it but surviving to lanedivide (pre-pool, pre-deadcode) → `collect_lane_sizes` sees `[4,8]` → picks 4 (smallest-first) → over-splits. This IS the backlog "overlapping XMM0:8/XMM0:4" item, RETURN-side (corrects lead's call-output framing).
**KEY: retiring XMM0:4 (`MOSURA_NOXMM4`, append only `[(RAX,8),(XMM0,8)]`) is BYTE-IDENTICAL on the corpus** (0.9077/55, zero deltas — the XMM0:4 candidate + is_const_padded_piece are provably INERT/dead-weight on the corpus, only creating the blocking SUBPIECE). With it retired, floatprint's lane regression VANISHES (stays 1.000).
**BUT LaneDivide STILL net-neg with XMM0:4 retired (LANE+NOXMM4): stackstring 0.794 UNCHANGED, concatsplit .881→.863, floatcast .804→.791, avg 0.9072.** Two residual blockers, NEITHER a P6 trial:
  (a) **stackstring** (the target): split now correctly 8-byte but STILL missing `xStack_28=xRam..100250` assigns — blocked on the PRE-POOL STACK ORDERING (KEY ORDERING FINDING / task #1 spacebase, speccache cause (ii)), independent of trials.
  (b) **concatsplit/floatcast**: over-split on GENUINE LIVE 4-byte SUBPIECEs (probe: all `L`ive multi-4-byte float4/CONCAT44 reads + call-output `Indirect->16` clobbers), NOT dead trials → a deadcode sweep won't help → a LaneDivide do_trace over-fire / pre-pool placement issue (task #6), NOT P6.
**CONCLUSION:** LaneDivide reactivation is NOT a P6 deliverable — keep it INERT. The XMM0:4 retirement is a separate SMALL faithful cleanup (byte-identical corpus, removes floatprint's would-be lane block; full faithful form = single-trial characterizeAsOutput per the backlog). Recommend to lead: (i) land Sub-part 1 (the real win); (ii) OPTIONALLY land XMM0:4 retirement as byte-identical cleanup (self-approve lane, but it touches return recovery so flag it); (iii) LaneDivide stays blocked on task #1 (spacebase) + task #6 (do_trace) — NOT this task.

## ✅ STAGE A LANDED 2026-07-10 (sb6) — master `82f9293` (re-verified: 374/0, corpus 0.9077/55, condconst 1.000, switchhide .955, noforloop_globcall .889, tree clean)
Lead GO'd landing. Committed recover.rs + coverage.md (explicit paths, no git add -A). is_return_value_use APPROVED by lead as a faithful representation-translation (same class as flag-XOR/guarded_op). **BACKLOG logged (coverage ActionReturnRecovery row + here): retire the overlapping XMM0:8/XMM0:4 recover_return candidates via the active-output single-output-trial ParamActive/characterizeAsOutput model — the deeper fix that removes the sibling-slot artifact at its source; likely shared with Stage B call-output side.** Task-#1 tripwires CLOSED in coverage (switchhide + noforloop_globcall, resolution anchored to recover.rs return_trial_kept). elseif −0.003 confirmed pre-existing P7/P8 (A/B-stash). NEXT = Stage B (input params/indproto via symmetric input-side ancestorOpUse/checkInputTrialUse + the LaneDivide call-output/param 4-byte-XMM trials — watch for the SAME overlapping-candidates adaptation on the call-output side) — SEPARATE stage, premise-first grounding before code.
--- build record (pre-land) ---
Lead GO'd Stage A (faithful full port + real-delta measure; lead gates landing). BUILT in recover.rs: `TRIM_RECURSE_MAX=5`, `mod traverse` (TraverseNode flags), `is_alternate_path_valid` (expression.cc:28), `check_call_double_use` (funcdata_varnode.cc:1756, mosura active_inputs model + block-pos for seqnum-order), `only_op_use` (funcdata_varnode.cc:1805, full use-classes + TraverseNode concat/lsb + persist), `ancestor_op_use` (funcdata_varnode.cc:1917, INDIRECT/MULTIEQUAL/COPY-internal/PIECE/SUBPIECE-overlap/CALL/default), `overlap_bytes`, `is_return_value_use`, `return_trial_kept` = the 2-gate (realism AND aou). Wired into BOTH check_output_trial_use + build_return_output. +7 unit tests (STORE/persist use-class, MULTIEQUAL condconst shape, overlapping-XMM accommodation, check_call_double_use active/inactive). Suite 374/0.
**REAL-FLIP DELTA (corpus 0.8976→0.9077, +0.0101, 55/60): 9 wins, 1 wash (elseif −0.003), 0 real regressions, nothing <0.70:**
condconst .862→1.000 (TARGET), pointercmp .765→.933, piecestruct .889→1.000, **noforloop_globcall .810→.889 + switchhide .918→.955 (BOTH task#1 merge tripwires CLOSED)**, heapstring .863→.898, dupptr .954→.969, noforloop_alias .976→.988, union_datatype .991→1.000 (probe "miss" fixed by real-flip deadcode). elseif −0.003 = the return correctly voids (matches oracle void) but a few structuring tokens shuffle around the now-void return = pre-existing else-if/loop P7/P8 gap, NOT the aou gate (call-arg drops are pre-existing, verified via git-stash A/B). Probe's noforloop_iterused/revisit misses UNCHANGED (diff upstream root).
**KEY BUILD FINDING (float crash → fix):** first flip crashed floatconv .596→.229, mixfloatint .967→.235, floatcast .804→.469 (float returns VOIDED). Root = mosura's non-Ghidra `recover_return` appends OVERLAPPING XMM0:8+XMM0:4 candidates; a float value feeds BOTH return slots, and faithful onlyOpUse (checking only the trial slot, like Ghidra which has ONE output trial) saw the sibling slot as an external use → false. FIX = `is_return_value_use`: a use by opmatch at ANY return-value slot (≥1) is the return use, not external — accommodates mosura's multi-candidate scheme (the faithful adaptation to the non-Ghidra candidate representation; documented). Restored all 3 floats to baseline.
NEXT: report sent lead, WAIT for landing go. On go: commit (recover.rs + coverage.md; fill the coverage.md `<sha>` tripwire cites; NEVER git add -A), close task#1 switchhide/noforloop_globcall tripwires. Stage B (input params/indproto + LaneDivide call-output/param XMM trials) separate.

## ✅ PREMISE VERIFIED 2026-07-10 (sb6, base `0b2d35e`, 367/0, corpus 0.8976, read-only, throwaway probe reverted → tree clean) — VOID LEVER = `ancestorOpUse`, GATED PLAN SENT LEAD
**MECHANISM PINNED (exact, oracle-grounded):** Ghidra's return trial is gated on TWO conditions, `ActionReturnRecovery::apply` coreaction.cc:**1930-1931**: `if (ancestorReal.execute(op,slot,&trial,false)) if (data.ancestorOpUse(maxancestor,vn,op,trial,0,0)) trial.markActive();`. Gate 1 = AncestorRealistic (mosura ports its essence as `is_realistic`, recover.rs:34). **Gate 2 = `ancestorOpUse` (funcdata_varnode.cc:1917) → `onlyOpUse` (1804): is the trial value ONLY used to feed this RETURN, vs consumed by a real use (STORE/LOAD/BRANCH/CALL/another-RETURN/persist) elsewhere. mosura OMITS gate 2 entirely** (check_output_trial_use recover.rs:159/171 checks only is_realistic + is_const_padded_piece). condconst: `r0x0(RAX) = MULTIEQUAL(PTRADD(param_1,3,4), PTRADD(param_1,4,4))` and each PTRADD output IS the ADDRESS of a `STORE #0x1 r0x0 val` (block 5/6 of mosura --ir) → onlyOpUse hits CPUI_STORE → false → not marked active → **void**. Ghidra oracle `capture --c condconst` = `void func(...)` (no return). mosura = `int4* func()...return pVar1`. Realism is NOT the lever (PTRADD=arithmetic=pop_solid=accepted, as sb5 said); **the lever is the missing USE gate.** [[task-sb-spacebase-placeholder]] merge-regressions land here too.
**PREMISE VERIFIED read-only (log-only `ancestor_op_use` probe wired into check_output_trial_use at the REAL resolve_return decision point — pipeline.rs:356, post-heritage/pre-pool/maxpass=0 — logging the would-be verdict WITHOUT changing the keep decision; ran all 60; probe reverted):**
- condconst RAX trial: realistic=true → aou=false → FLIP to void. Confirmed AT the actual decision point (not just final IR). Voiding it → PTRADD readers become {STORE only} → address inlines → oracle-identical shape (`param_1[4]=0x10` directly, no `pVar1`).
- **ZERO regressions across 60.** Every oracle-RETURN fixture keeps a `realistic+aou` slot (its real return survives). The 2 oracle-RET-but-aou=n rows (deindirect2, partialunion) are ALREADY void in mosura (pre-existing under-recovery, hasReal=n) → aou touches nothing. mixfloatint/pointerrel flip ONLY their low-priority XMM0:4 slot (slot 3); their real return (float8 slot2 / RAX slot1) stays realistic+aou → preserved.
- **11 FIXES** (oracle void, mosura RET, no slot survives aou → correctly voids): condconst, dupptr, elseif, heapstring, noforloop_alias, **noforloop_globcall**, piecestruct, pointercmp, sbyte, **switchhide** (+ union not — see misses). The 2 bolded ARE the task#1 merge-bank regressions — SAME lever, one fix. (Multi-root was WRONG: they're all the missing USE gate.)
- **3 MISSES** (oracle void, mosura RET, a slot survives aou → aou can't void): noforloop_iterused, revisit, union_datatype = the "different upstream root" cases (revisit RAX=CONCAT62 low-lane global+100 genuinely looks only-used; needs deferred-maxpass/later-simplified-graph or upstream). Unchanged by aou, NOT regressions.
- LaneDivide 4-byte-XMM (task#6) linkage: NONE. stackstring is void both sides (not over-recovering); mixfloatint/pointerrel XMM0:4 flips are harmless. aou does not reactivate LaneDivide's XMM trial.
**GATED STAGING PLAN (sent lead, WAIT for go):**
- Stage A (ONE atomic gated mover — the gate is one mechanism, all 11 move together): port `ancestorOpUse`+`onlyOpUse` (+`checkCallDoubleUse`, TraverseNode concat/lsb flags, `isAlternatePathValid`) faithfully; wire as the 2nd gate in BOTH check_output_trial_use (mark active only if realistic AND aou) AND build_return_output (keep first slot passing realistic AND aou) — consistently. Full per-fixture corpus delta BEFORE landing (trial-scan proves no return stripped, but downstream naming/deadcode effects measured on the real corpus; nothing newly <0.70). Expect big net-positive (condconst .862→~1, +the 2 merge regressions recovered, +8 more void fixtures).
- Stage B (SEPARATE, input-side, = old P6-5): params/indproto. indproto is void BOTH sides — its gap is the missing INPUT param EDI, not the return. Symmetric ancestorOpUse on CALL-input/function-input trials (check_input_trial_use). Own grounding + gate.
- CAVEAT for execution: probe approximations (checkCallDoubleUse skipped→more aggressive, COPY-internal-only, PIECE/SUBPIECE flag nuance) are ALL in the SAFE direction (can only hide a fix, never a regression); the faithful port keeps ≥ as many returns. maxpass stays 0 (single pass); this is not a mainloop-repeat item.

## ⟳ CURRENT-MASTER RE-GROUNDING 2026-07-10 (sb5, base `0b2d35e`, 367/0, corpus 0.8976) — STALE PLAN BELOW SUPERSEDED ON RETURN-WIDTH
Task #4 (=this P6, owner sb5) re-assigned by lead after the merge-phase bank (task #1 `9c1a20c`) + lane2 LaneDivide merge (`0b2d35e`). Read-only re-grounding on CURRENT master; the 2026-07-04 grounding below (base ee3579a, corpus 0.8804) is OBSOLETE on the central point:
- **RETURN-WIDTH is FIXED.** RuleSubvarZext is now WIRED (pipeline.rs:234) — it was HELD at the stale grounding, whose whole conclusion was "return-width blocks on SubVariableFlow/Task #8, DO NOT hand-roll a ZEXT narrower." That blocker is GONE. twodim = `int4 func()...return iVar1`, stackreturn = `void func(void)`, both byte-match oracle. So P6-1's characterizeAsOutput groundwork (still landed `e9efd8b`, inert) + the guardReturns ACTIVE-OUTPUT *width* branch (old P6-2/P6-3) are MOOT for width. DROP the active_output-pre-heritage restructure.
- **P6 is now purely VOID over-recovery + PARAMS.** Live consumers on master:
  · condconst (lead's "int4*-residual"): mosura `int4* func()...return pVar1` vs oracle `void`. IR: `RETURN r0x0:8` where `r0x0:8 = MULTIEQUAL(PTRADD(param_1,3,4), PTRADD(param_1,4,4))` — leftover array-element-ADDRESS arithmetic (`param_1[3]/[4]` write addr) left in RAX; mosura recovers it.
  · task#1 void regressions (noforloop_globcall, switchhide): DIFFERENT root — guard_returns persist COPY held global / stack-canary leftover in RAX. Multi-root, as prior grounding warned.
  · indproto: missing `param_1` (EDI) = input-side ParamActive trials (P6-5).
- **PREMISE CORRECTION (verified read-only, NO code): condconst void is NOT a checkConditionalExe/AncestorRealistic tightening.** AncestorRealistic::enterNode (funcdata_varnode.cc:2026): MULTIEQUAL→traverse inputs; PTRADD inputs hit `default: return pop_solid` (arithmetic = solid movement) → execute() returns pop_solid → **ACCEPTS as realistic**. So AncestorRealistic does NOT reject condconst's RAX. Void must come from the DEEPER output-trial evaluation — `checkOutputTrialUse` (is the trial value actually USED/markUsed) and/or the used-vs-realistic split in ParamActive output recovery — NOT is_realistic leniency. My own first-cut plan (port checkConditionalExe) is WRONG; same "deep ParamActive subsystem, verify-before-code" verdict as the 2026-07-04 grounding. checkConditionalExe (funcdata_varnode.cc:1997) needs the RETURN block sizeIn()==2 + hasCondExeEffect retest — a narrow gate, not the condconst lever alone.
- NEXT (read-only, before any code): pin HOW Ghidra rejects condconst's realistic-but-unused RAX trial — ground `ParamActive::checkOutputTrialUse` / `markUsed` / the guardReturns active-output branch trial-USE marking (fspec.cc + coreaction.cc ActionActiveReturn) vs mosura recover.rs check_output_trial_use (recover.rs:159). RECOMMEND fresh-budget warm-resume for the implementation (P6 realism/output-trial subsystem; session already ran full task #1). Reported to lead; awaiting start-now-vs-resume steer.

Task #2 = P6 Prototypes (owner ptype, in_progress). Lead-elevated broad-leverage target after float cluster [[task11-float-nan-plan]]. Base HEAD `a2e3ad9`, 210 green, corpus 0.8804. Plan-before-code; scoped plan sent lead 4d0928dc (awaiting go + start-now-vs-warm-resume steer). NOTE: long session already (7 commits) — P6-2 is a heritage restructure, may warrant warm-resume.

CURRENT STATE (grounded, recover.rs + pipeline.rs): `recover_return` (recover.rs:82, PRE-heritage, pipeline.rs:43) appends FIXED candidates RAX:8/XMM0:8/XMM0:4 to every RETURN. `resolve_return` (recover.rs:122, POST-heritage) prunes to first AncestorRealistic (`is_realistic`) via two-phase ParamActive/trials (setup_active_output :134, check_output_trial_use :159, build_return_output :190). RETURN_MAXPASS=0 → commits immediately (deferral machinery exists, inert). is_const_padded_piece (:99) defers wide XMM0:8 to XMM0:4 for float returns. = partial guardReturns.

FAITHFUL MECHANISM (Ghidra Heritage::guardReturns heritage.cc:1652, READ): runs DURING heritage per written range. `characterizeAsOutput(addr,size)` (FuncProto, ABSENT mosura) → contained_by ⇒ guardReturnsOverlapping (SUBPIECE trial for sub-reg); else containment ⇒ registerTrial(addr,size) + append newVarnode(size,addr) to each RETURN; persist flag ⇒ COPY+markReturnCopy. So return WIDTH = actual writes (writes EAX:4→int4 return); mosura's fixed RAX:8 OVER-WIDENS → pervasive xunknown8-vs-int4/void gaps. active_output is set up PRE-heritage in Ghidra (mosura: post, in resolve_return).

SCOPED COMMITS (gated, refine per-commit):
- P6-1: port characterizeAsOutput (FuncProto: (addr,size) match/overlap output ParamEntry RAX/RDX/XMM0; fspec.cc) — inert groundwork.
- P6-2 (BIG corpus-mover + BLAST): active_output setup PRE-heritage + hook guardReturns into heritage per-range (like guardStores/guardCalls already are, heritage.rs step 0), width-trials from actual heritaged output writes; RETIRE recover_return fixed candidates. Fixes return width xunknown8→int4. active_output-pre-heritage = the restructure lead flagged — thread through heritage carefully, gate.
- P6-3: guardReturnsOverlapping (SUBPIECE for contained_by sub-register returns).
- P6-4: VOID over-recovery (dupptr/elseif/revisit) = tighten AncestorRealistic/ancestorOpUse (is_realistic too lenient).
- P6-5: input-side PARAM recovery (EDI→param_1; modulo2/noforloop_globcall) = ParamActive input trials, symmetric.
Order P6-1→2→3 (return width core) → P6-4 (void) → P6-5 (params). param/void need more grounding at execution.

P6-1 LANDED `e9efd8b` (byte-neutral, self-approved, 211 green): fspec.rs added `Containment` enum (fspec.hh:99), `ParamEntry::contained_by`, `ParamList::characterize_as_param` (port ParamListStandard::characterizeAsParam fspec.cc:682, linear scan) + unit test. INERT (nothing calls it yet). This is what FuncProto::characterizeAsOutput (fspec.cc:4336 unlocked→model) delegates to.

P6-2 APPROACH GROUNDED (precise hook, for warm-resume execution): heritage_spaces (heritage.rs:976) step 0 (lines 985-989) iterates `guarded`(=new_addrs) calling `guard_calls(f,l)`+`guard_stores(f,l)` per new range. guardReturns hooks the SAME WAY = add `guard_returns(f, range)` there. Per range: `sysv_output(spaces).characterize_as_param(loc,size)` → ContainsJustified/Unjustified ⇒ register output trial(range) + append newVarnode(size,addr) to each RETURN (invn.set_active_heritage); ContainedBy ⇒ guardReturnsOverlapping SUBPIECE (P6-3); persist ⇒ COPY+markReturnCopy. THEN: (a) active_output must exist PRE-heritage — currently setup_active_output is in resolve_return (POST); move setup to pre-heritage (replace recover_return's fixed-candidate append at pipeline.rs:43); thread active_output so it survives to resolve_return (already a Funcdata field). (b) RETIRE recover_return fixed RAX:8/XMM0:8/XMM0:4. (c) resolve_return already evaluates trials — now they come from actual writes not fixed candidates. BLAST = every fn's return reflows (returns get width from actual output-reg writes → int4 not xunknown8; void when no output write). Gate corpus delta BEFORE landing. Guard `call_guards_active`-style: the probe/alias clone must NOT run guard_returns (like guard_calls) — check.
P6-2 ATTEMPTED + REVERTED 2026-07-04 (warm-resume, reported f10d8661). Implemented FULL faithful guardReturns (guard_returns hooked heritage_spaces step0 after guardCalls; guardReturnsOverlapping SUBPIECE for ContainedBy/XMM via biggest_contained_output; retired recover_return fixed candidates). 211 green BUT net-NEG (0.8804→0.8772: deindirect2 +0.024; floatprint 1.000→0.991, mixfloatint 0.967→0.933, partialunion 0.941→0.889, wayoffarray 0.920→0.800) AND DIDN'T fix width. REVERTED to e9efd8b (P6-1 stays).
**PREMISE-CORRECTING FINDING (durable): guardReturns is the WRONG lever for return-width.** wayoffarray return stayed uint8 not int4. ROOT: mosura's heritage ALREADY widens the 4-byte LOAD result to RAX:8 BEFORE guard_returns sees the range → characterize_as_param gets RAX:8 → appends RAX:8 → width unchanged. Ghidra's int4 comes from the VALUE's TYPE (a 4-byte load typed int4), NOT from guardReturns picking narrow storage. The plan premise "fixed RAX:8 over-widens; guardReturns derives width from writes" was WRONG. Plus regressions: guard_returns appends RAX candidates for non-return functions (partialunion spurious `return 0`; wayoffarray write-only store dropped via deadcode cascade from removing recover_return's pre-heritage candidates); realism/void check too lenient.
RE-SCOPE ACCEPTED by lead 2026-07-04: drop guardReturns (P6-2/P6-3); P6-1 stays. Re-target = return-TYPE inference (type RAX value by producer width) + P6-4 void-over-recovery. Warm-resumed to start with the cleaner lever.

P6-4 VOID GROUNDING 2026-07-04 (dumped revisit/dupptr/elseif at e9efd8b): Ghidra `void`, mosura recovers spurious return — revisit `return CONCAT62(xVar1,iVar2)`, dupptr `return 0x1009ea` (a CONSTANT), elseif `return uStack_c` (a stack LOAD). mosura's `is_realistic` (recover.rs:34) is already a substantial AncestorRealistic port (Copy/Sub/Zext/Sext trace, Multiequal any, Piece low-lane, Indirect creation/retaddr=false, else=true, **constant=true**). GAP is NOT a one-liner — 3 DIFFERENT roots + Ghidra AncestorRealistic (funcdata_varnode.cc:2026 enterNode) is deeper: pop_success/pop_fail/**pop_solid**/pop_failkill states + a THRESHOLD (must reach a solid movement) + `checkConditionalExe` (:1997, reject a value set only conditionally) + `ancestorOpUse` (:1917). ALSO mosura RETURN_MAXPASS=0 commits GREEDILY on pass 1 (Ghidra defers via getMaxInputDelay across passes → candidates rejected after more simplification; mosura's deferral machinery exists but inert w/o iterating mainloop). So P6-4 = multi-cause subsystem (AncestorRealistic threshold/solid-tracking + maybe maxpass deferral + checkConditionalExe), NOT a bounded commit. Per-fixture diagnosis needed. dupptr constant + elseif LOAD both hit mosura's `constant=true`/`_=>true` leniency where Ghidra's threshold/solid logic rejects.
P6-4 PREMISE VERIFICATION 2026-07-04 (NEW RULE: verify premise READ-ONLY before any code; new code = faithful port only; implement-then-revert = process failure. P6-2 was that failure). Dumped mosura RETURN IR for the 3 void fixtures → **PREMISE FAILS, did NOT implement** (reported 29d7d51e):
- dupptr `RETURN r0x288:8 #0x1009ea:8` = return value is a BARE CONSTANT. Ghidra AncestorRealistic::enterNode (funcdata_varnode.cc:2033-2040): constant = not-written+not-input → `return pop_success` = REALISTIC. Faithful port ACCEPTS it → does NOT make dupptr void. dupptr void = UPSTREAM mosura IR divergence (RAX=const 0x1009ea where Ghidra's isn't; mosura fold/lift artifact), NOT is_realistic leniency.
- revisit RAX=CONCAT62, low lane = global read +100 = solid value → accepted, not rejected.
- elseif RAX=uStack_c via MULTIEQUAL — ONLY one where AncestorRealistic MULTIEQUAL kill/solid threshold + checkConditionalExe *might* reject (if a MULTIEQUAL input is a call-clobber). Unverified.
CONCLUSION: AncestorRealistic void-port would NOT fix dupptr (& likely revisit) — 3 void cases = 3 DIFFERENT UPSTREAM roots (dupptr const-fold, revisit CONCAT+global, elseif MULTIEQUAL-kill), not one is_realistic subsystem. Faithful next = per-fixture upstream root-cause READ-ONLY (which is a portable Ghidra mechanism), or different P6 target. Return-TYPE angle also still premise-unverified. DO NOT write the AncestorRealistic port.

RETURN-TYPE PREMISE VERIFICATION 2026-07-04 (read-only, reported 755bbb88) — **FAILS, no code**. Traced mosura return value on twodim/wayoffarray/packstructaccess: `RAX:8 = INT_ZEXT(v:4)` → 8-byte ZEXT → int8/uint8 return. Ghidra `oracle --ir` raw p-code = `EAX = <load>; RAX = ZEXT48(EAX)` — 4-byte value in **EAX (register)**. Ghidra's int4 = recovering the narrower EAX:4 STORAGE (guardReturns EAX:4 trial, output-recovery prefers over ZEXT-padded RAX:8, like is_const_padded_piece), NOT typing an 8-byte ZEXT as int4. So premise (a) type-by-producer-width ≠ Ghidra's mechanism; (b) would mis-render (wrong width + ZEXT is unsigned not int4). REAL ROOT: mosura keeps pre-ZEXT value in a UNIQUE (u0x23d00:4) where Ghidra keeps EAX → mosura has no EAX:4 return candidate. Upstream LIFTING/simplification divergence.
**P6 NET FINDING (durable): all 3 verified P6 angles (guardReturns width, void AncestorRealistic, return-type) trace to the SAME UPSTREAM class — sub-register writes widened to RAX:8 / lifted into uniques (not EAX) / mosura-specific const-folds — NOT the P6 recovery subsystems.** P6 as framed ("guardReturns + AncestorRealistic + return-type inference") is NOT the lever; the levers are upstream heritage/lifting IR fidelity. Recommended to lead: STOP P6, either investigate the upstream unique-vs-EAX/RAX:8-widening divergence as its own task (shared root of return-width+void) OR pick a different corpus target. Holding e9efd8b.

UPSTREAM ROOT-CAUSE 2026-07-04 (task #12, read-only, warm-resume, base ee3579a). Investigated the unique-vs-EAX divergence:
- NOT a SLEIGH lift divergence: mosura's RAW p-code for the indexed load EXACTLY matches Ghidra's raw `pcode:` dump = `u=LOAD; EAX(r0x0:4)=COPY(u); RAX(r0x0:8)=ZEXT(EAX)`. (Verified: mosura raw_funcdata print_raw vs `oracle/capture twodim.xml` disasm pcode lines.)
- DOWNSTREAM: mosura FINAL IR = `RAX:8=ZEXT(u:4)` (EAX propagated away by RulePropagateCopy → dead). Ghidra --ir keeps `RAX=ZEXT48(EAX)` for the DIRECT mov (`EAX=COPY(ram_free)`; ram not heritage-known → Ghidra RulePropagateCopy gate `if(!invn->isHeritageKnown())continue` blocks it → EAX stays). mosura's RulePropagateCopy propagates the unique through EAX regardless.
- UNRESOLVED/TANGLED: for the INDEXED load (the ACTUAL return), Ghidra's `EAX=COPY(u)` has u heritage-known → Ghidra's RulePropagateCopy WOULD also propagate (kill EAX) — yet Ghidra still renders int4. So the isHeritageKnown gate explains the direct-mov EAX but NOT the indexed-load int4 return. There's an ADDITIONAL Ghidra mechanism (return recovery recovering the 4-byte value from the ZEXT, or a copy-prop timing/gate) I did NOT pin down.
CONCLUSION: return-width root = DEEP INTERACTION (copy-prop + heritage + return-recovery), NOT a single bounded divergence. Premise for ANY specific fix NOT cleanly verified → NO code (tightened rule). NEXT faithful read-only step = the mosura↔Ghidra rule-firing TRACE-DIFF (oracle/capture_trace --trace + MOSURA_TRACE=1, examples/trace.rs, scripts/trace-diff) on twodim's copy-prop/return-recovery to see EXACTLY where Ghidra keeps the 4-byte value and mosura loses it. Reported to lead. Ghidra RulePropagateCopy gate: ruleaction.cc — `if(op->isReturnCopy())return 0; if(!invn->isHeritageKnown())continue;` + marker addrtied gates.

TRACE-DIFF CORRECTION 2026-07-04 (task #12, `scripts/trace-diff.sh twodim`, read-only): CORRECTS the copy-prop hypothesis above. mosura's RulePropagateCopy (rules.rs:420) gates ≈ Ghidra's for this case — Ghidra's extra gates (isReturnCopy, marker-addrforce, marker-addrtied) are ALL marker/return-copy specific, DON'T apply to the ZEXT. So Ghidra ALSO collapses `EAX=COPY(u);RAX=ZEXT(EAX)`→`RAX=ZEXT(u)` (my "Ghidra keeps EAX" was an early --ir snapshot; final = ZEXT(u) BOTH sides). So int4 is NOT from copy-prop keeping EAX. TRACE-DIFF twodim (ghidra 202 / mosura 218 firings): Ghidra fires **returnrecovery(1) + prototypetypes(1) + setcasts(1)** that mosura doesn't (name-match); Ghidra fires **earlyremoval 75x** (mosura never); **propagatecopy DIVERGES ghidra56/mosura110** (mosura over-propagates elsewhere — separate faithful-port lead, NOT the return-width cause); held SubVariableFlow rules piece2zext/subzext/subvar_zext fire in Ghidra (Task #9). CONCLUSION: int4 return = Ghidra's OUTPUT-TYPE RECOVERY (ActionPrototypeTypes/ActionReturnRecovery) recovering `RAX:8=ZEXT(v:4)` as the 4-byte ZEXT-input width → int4. This IS P6 output-type territory (returnrecovery/prototypetypes), NOT copy-prop/lift. PREMISE STILL UNVERIFIED: HOW returnrecovery/prototypetypes recovers int4 from ZEXT — needs grounding ActionPrototypeTypes/ActionOutputPrototype (coreaction.cc) read-only. That's the next step before any code. NOTE: the earlier P6 return-type premise ("type by producer width") may be closer-to-right than I concluded IF it's returnrecovery unwrapping the ZEXT — re-verify against the source.

CHECKPOINT 2: P6-4 grounded (no clean single commit — multi-cause). Return-type angle also non-trivial (entangled LOAD/ZEXT/effective_width). Session VERY long (8 commits + P6-2 attempt/revert + P6-4 grounding). Recommended warm-resume for P6-4 AncestorRealistic-threshold implementation w/ fresh budget. Turnkey grounding above.

RE-SCOPE (recommended to lead, awaiting steer): DROP guardReturns restructure (P6-2/P6-3). Real return-width lever = (1) infertypes typing the RAX value by its producer width (LOAD:4→int4 return) = TYPE-inference not heritage; (2) void-over-recovery P6-4 (AncestorRealistic tightening) = independent, maybe cleaner. P6-1 characterizeAsOutput groundwork stays landed (inert, harmless, still the right primitive if guardReturns ever needed). CHECKPOINT: clean at e9efd8b, long session (8 commits + this neg result), natural warm-resume point.

DECISIVE GROUNDING 2026-07-04 (task #12, base ee3579a, read-only, fresh agent). Lead re-hypothesized: guardReturns registers trials for BOTH EAX:4 (COPY) + RAX:8 (ZEXT pad); AncestorRealistic/checkOutputTrialUse marks EAX:4 used, prefers over RAX:8. **VERIFIED — FALSE. NO CODE (premise-fail STOP).**
- SOURCE PROOF (single trial, no EAX:4): `Heritage::collect` (heritage.cc:307) returns `maxsize` = max WRITE size in range (line 336). twodim return region r0x0: writes are EAX:4(COPY) + RAX:8(ZEXT) → maxsize=8. `placeMultiequals` (2610) refines only if `size>4 && max<size` = 8<8 = FALSE → NO refine → ONE guarded range r0x0:8 → `guard`(2629)→`guardReturns(r0x0,8)` (1193/1652). `characterizeAsOutput(r0x0,8)` (fspec.cc:4336, unlocked→model): RAX out-entry r0x0:8 exactly contains range as low bytes → `contains_justified` (semantics: fspec.hh:862-874 — contained_by = range BIGGER than entry, NOT our case). So the `else if(!=no_containment)` branch (1663) runs → `registerTrial(r0x0,8)` + appends ONE newVarnode(8,r0x0) to RETURN. `guardReturnsOverlapping` SUBPIECE path (1609, the only sub-register-trial source) is NOT taken. `testShrink` (fspec.cc:1871) has NO live caller. ⇒ exactly ONE output trial, r0x0:8. AncestorRealistic never chooses EAX:4-vs-RAX:8 (enterNode: COPY→pop_solid, ZEXT→default pop_solid; both "realistic" anyway). Lead's mechanism + my earlier "output-type recovery unwraps the ZEXT" both DISPROVEN as stated.
- EMPIRICAL BOTH SIDES at ee3579a (oracle/capture --ir setcasts/protorecovery_a; mosura `cargo run --example dump -- twodim [--ir]`): **Ghidra final RETURN reads EAX(r0x0:4)=COPY(u0x23d00:4); RAX:8=ZEXT48(EAX) is DEAD-ELIMINATED → `int4 func(){return iVar1;}`.** **mosura final: `r0x0:8=INT_ZEXT u0x23d00:4; RETURN r0x288:8 r0x0:8` — the EAX=COPY(u) op is GONE, return reads the 8-byte ZEXT → `int8 func(){return uVar1;}`.** (This CORRECTS line 47's "final=ZEXT(u) BOTH sides" — that trace-diff over-correction was WRONG; Ghidra's final keeps EAX:4=COPY, not ZEXT(u):8. capture --ir break granularity beyond default `heritage`(raw) is unreliable — group names repeat in mainloop; only raw + near-final dumps trustworthy.)
- REAL DIVERGENCE (crisp): Ghidra NARROWS the return value from the 8-byte ZEXT down to the 4-byte sub-register — it KEEPS `EAX=COPY(u)` alive (return reads it) and dead-eliminates `RAX=ZEXT(EAX)`. mosura does the OPPOSITE: RulePropagateCopy fuses `EAX=COPY(u)` into `RAX=ZEXT(u)` (COPY dies, wide ZEXT survives) and the RETURN reads the 8-byte ZEXT; nothing narrows it. So width-4 = a RETURN-VALUE NARROWING (output-type int4 ⇒ SUBPIECE(ZEXT,0)⇒RuleSubZext across passes, keeping the narrow COPY live), NOT a guardReturns sub-register trial.
- TIES #12≡#13: the exact op that keeps EAX alive / narrows the return is entangled with **propagatecopy over-fire (mosura 110 vs ghidra 56, task #13)** — mosura over-propagates the COPY into the ZEXT-input, eating the EAX:4 Ghidra preserves as the return value. #12 and #13 are the SAME bug. Next grounded step (read-only, before any code): pin WHICH Ghidra pass narrows RAX:8-ZEXT→EAX:4 return (output-type→SUBPIECE→RuleSubZext ordering) and WHY mosura's copy-prop/deadcode collapses the COPY instead. guardReturns-done-right plan NOT written — premise falsified. Reported to lead.

RAW-LIFT vs LATER-PASS RESOLVED 2026-07-04 (task #12 re-assigned a/b/c; read-only, base ee3579a). CORRECTS the "#12≡#13 copy-prop" line above.
- (a) NOT a SLEIGH divergence. mosura's TRUE raw lift (`cargo run --example dump -- twodim --prestack`; NOTE `--ir` runs FULL pipeline first = FINAL not raw — the RAW is behind `--prestack`) is byte-identical to Ghidra's per-instruction pcode (`oracle/capture <gh> twodim.xml`, default mode): `MOV EAX,[mem]` ⇒ `u=LOAD; r0x0:4=COPY(u); r0x0:8=INT_ZEXT(r0x0:4)`. Confirmed also by goldens/disasm/x86_64_tiny.golden (`89f0`: COPY then ZEXT(reg0x0:4)). So EAX:4=COPY IS emitted at lift on BOTH sides. Divergence is a LATER pass.
- COPY-PROP IS NOT THE DIVERGENCE (corrects "#12≡#13"): Ghidra RulePropagateCopy (ruleaction.cc:3926-3954) WOULD ALSO propagate u into `ZEXT(EAX)`→`ZEXT(u)` here — gates don't block it (ZEXT is neither isReturnCopy nor a marker; `invn`=u is heritageKnown). So both sides copy-prop identically; keeping-EAX is NOT how Ghidra gets int4. mosura's propagatecopy over-fire (#13) is a SEPARATE issue, NOT the return-width root.
- (b) THE DIVERGENCE = a MISSING RETURN-VALUE NARROWING in mosura's OUTPUT RECOVERY. Ghidra's final `EAX=COPY(u)` feeding the return is a LATE-created op (seqnum `db`, high) — the 4-byte return value is RE-MATERIALIZED by output/type recovery narrowing the ZEXT-padded RAX:8 down to its extended-from width; the wide `RAX:8=ZEXT(u)` is then dead-eliminated. mosura's `recover.rs` deviates: `recover_return` (recover.rs:82,88) appends ONLY fixed candidates {RAX:8, XMM0:8, XMM0:4} — NO EAX:4/RAX:4 integer sub-register candidate — and its sole narrowing `is_const_padded_piece` (recover.rs:99) recognizes ONLY `PIECE(const_high, low)` (the float XMM0:8→:4 case), NOT `INT_ZEXT(v:4)`. So twodim's `RAX:8=ZEXT(u:4)` return: not const-padded → wide RAX:8 kept, no narrower candidate exists → `int8 func(){return uVar1;}` vs Ghidra `int4`.
- EXACT GHIDRA OP = STILL THE PRE-CODE PIN. Not `assumedOutputExtension` (fspec.cc:5263/coreaction.cc:1553 — CALL-output only, not the function's own RETURN). Not copy-prop. It's in the ActionReturnRecovery(coreaction.cc:1908)/ActionOutputPrototype(4765)/updateOutputTypes(fspec.cc:4136)/ActionInferTypes cluster — the guardReturns trial is RAX:8 (single, proven above) yet the committed return is EAX:4, so SOMETHING narrows the RAX:8-ZEXT trial/return to 4 bytes across passes (candidate: type-inference typing ZEXT48(v:4) then a SUBPIECE-narrow → RuleSubZext → COPY at EAX). `--ir` break granularity beyond default `heritage`(raw) is UNRELIABLE (group names repeat in mainloop) so mid-pass snapshots aren't trustworthy — pinning needs source-trace, not the tool.
- (c) FIX SHAPE (do NOT write until the exact Ghidra op is pinned + lead-approved): extend mosura output recovery to model the integer sub-register return — recognize `INT_ZEXT/INT_SEXT(v:4)` (and provide the EAX:4 narrower candidate) so a ZEXT-padded RAX:8 return commits as v:4 → int4, faithfully mirroring whatever Ghidra op is pinned. BLAST HIGH: every fn returning a 32-bit int zero-extended into RAX reflows int8→int4 (most int-returning fns); symmetric on params (EDI→param_1). Gated corpus-mover.

EXACT OP PINNED 2026-07-04 (task #12, `scripts/trace-diff.sh twodim` + kept ghidra.trace, read-only). **The narrowing op = `RuleSubvarZext` (Ghidra subflow.cc:1710) — SUPERSEDES the "(b)/(c) missing output-recovery narrowing / is_const_padded_piece-for-ZEXT" framing above, which would have been a NON-FAITHFUL heuristic.** Ghidra trace, the return's evolution: raw `RAX:a0 = ZEXT48(EAX)` → returnrecovery commits `return RAX:8` (trial=RAX:8, single, as proven) → propagatecopy → `RAX:b7 = ZEXT48(u0x23d00:4)` → **DEBUG 187 `subvar_zext`**: materializes `EAX:da = u0x23d00:4` (+chained `EAX:db=EAX:da`) and rewrites `return RAX(:a0)` ⇒ `return EAX(:db)` (the 4-byte read) → propagatecopy collapses `EAX:db = u0x23d00:4` → `earlyremoval` deletes the dead `RAX:8=ZEXT`. So int4 = **RuleSubvarZext narrowing the ZEXT-padded return to its sub-register**, NOT guardReturns, NOT copy-prop, NOT output-recovery. RuleSubvarZext (subflow.cc:1704 getOpList=INT_ZEXT; 1710 applyOp) seeds `SubvariableFlow(data, zext_out, mask=calc_mask(invn.size), ...)`; if `doTrace()` (value used only through the low subvariable) → `doReplacement()` shrinks it.
- mosura DEVIATION (exact): mosura has the SubvariableFlow ENGINE (`subvarflow.rs`) but the **ZEXT-driving rule is NOT wired** into the pipeline (grep pipeline.rs/action.rs = empty; rules.rs:2483/2591/2910 comments: RuleSubZext/RuleSubvarSubpiece/etc. "Wire it once SubVariableFlow lands", gated on **Task #8/#9**). trace-diff confirms mosura fires `subvar_zext` 0× (ghidra 7×), also `subzext` 0×(gh 8×), `piece2zext` 0×(gh 8×), `subvar_subpiece` 0×(gh 1×). So the ZEXT-padded RAX:8 return never narrows → int8.
- RE-SCOPE (durable): **return-width int4 is a SubVariableFlow problem (Task #8), NOT P6/output-recovery.** #12 root = mosura holds RuleSubvarZext (SubVariableFlow Stage 3-4 HELD, net-regressing). The fix is to LAND SubVariableFlow faithfully (all driving rules: SubvarZext/SubvarSext/SubvarSubpiece/Subvar + the shared engine), which is exactly the held Task #8 — return-width is a downstream BENEFIT, not a standalone commit. Not #13 (copy-prop identical both sides), not P6 guardReturns/output-recovery. Recommend: fold #12 into #8; do NOT hand-roll a ZEXT return-narrowing heuristic (non-faithful). Reported to lead ee3579a, no code.

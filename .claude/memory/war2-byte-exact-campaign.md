---
name: war2-byte-exact-campaign
description: Sustained autonomous campaign (user-directed 2026-07-24) to drive WAR2.EXE toward byte-exact recompilation via faithful Ghidra type-inference ports
metadata: 
  node_type: memory
  type: project
  originSessionId: 99103a08-9d06-430b-8446-4ca5e3757106
  modified: 2026-07-28T16:09:43.749Z
---

USER DIRECTIVE (2026-07-24): "everything must be implemented so that WAR2 function can compile to byte exact. Take executive decisions and keep working until rate limited." Delegated executive GATING authority to the main/lead agent (main can approve toward-oracle/net-correctness corpus moves without waiting for the human; hard-block only wrong-code).

LEAD deep-dive (2026-07-24, agent rate-limited): fully pinned partialsplit + landed a faithful guard. **`804d274` LANDED (byte-neutral): the missing `TypeOpIndirect::propagateType` `if(op->isIndirectCreation()) return 0` guard** (mosura relayed stale pointee types across call-clobber creations; unit test added; MAY help other WAR2 fns where INDIRECT-relay is the dominant element-8 source — re-measure on resume). **partialsplit ROOT CAUSE fully pinned (`525a0cc` checkpoint): NOT the call-clobber — it's the 8-byte STORE `*(xunknown8*)base` (faithful, present in Ghidra too) whose element-8 wins the base pointer type via an iteration FIXPOINT; Ghidra keeps the base `uint2*` (array-symbol type DOMINATES) + casts the store (ActionSetCasts). Faithful fix = array-symbol-type dominance + cast-at-mismatch, a multi-system (varmap↔infertypes↔ActionSetCasts) resolution, NOT a one-liner** — confirms the "delicate corpus-wide type-core" assessment; gather_open/type_order/preferred all re-verified faithful. Master now `525a0cc`, corpus 0.9516/57, suite 561/0, clippy 0.

**IR-CAST-MODEL REWRITE PLAN GATED (2026-07-24, docs/ir-cast-model-plan.md) — branch `ir-cast-model` off e9c0655, master stays clean, merge-gate = whole+corpus-recovered.** Stages: **0** retire print-time re-inference (HARD PREREQ, zero-gauge, LARGEST: 0a make in-pipeline ActionInferTypes+HV-resolution authoritative+persisted [DEPTH-VALVE: if needs PERSISTENT HighVariable not per-merge() resolution → STOP→user]; 0b printc type_of→committed types, delete render-time infer() printc.rs:1884; 0c reconcile churn) → **1** OpCode::Cast+TypeOpCast (small; no-propagateType BLOCKS back-relay, typeop.cc:2209) → **2** ActionSetCasts inserts CAST ops in-pipeline (coreaction.cc:2722/2655/2532; getInputCast MOVES from printc.rs:509 to IR action) → **3** printc renders CAST + remove cast-at-print. Toward-oracle value only after Stage 2+3 (all-or-nothing, funded). PROGRESS on branch: **0a ✅ `932023d` PROCEED** (persistent-HighVariable NOT required — 99.4% consistency, 371/54320, committed Varnode::ty broadcast from HV-resolved type gives stable per-fn type). **0b ✅ `7cb7e32`** (printc type_of→committed Varnode::ty, prereq for IR CAST). Branch corpus 0.9517→0.9512 (−0.0005, expected mid-rewrite; movers pointerrel/revisit = the 0a reverse-cases, committed Float(4)/Bool LESS refined than old render pass). **STAGE 0 ✅ COMPLETE `7399ca9` — CORPUS-NEUTRAL (0.9517/57 byte-identical to master, suite 495/0, clippy 0).** The zero-gauge prerequisite (retire print-time re-inference) landed NEUTRAL: 0c added a final ActionInferTypes pass at universal_action tail (after merge) that reproduces the render-time fixpoint exactly, so the 0b churn (pointerrel/revisit) fully recovered. Depth valve did NOT bite — no persistent-HighVariable needed for Stage 0. Print-time re-inference RETIRED; in-pipeline committed Varnode::ty authoritative. **STAGE 1 ✅ `f03e11a` byte-identical** (Cast op node + printc render arm + consume transparency ALREADY existed as scaffolding — a prior comment anticipated this port; agent added only propagate_type(Cast)=>None, the back-relay block). Stage 0+1 done byte-identical. **REMAINING = Stage 2 (the core, first corpus-MOVING stage): port ActionSetCasts::apply/castInput/castOutput (coreaction.cc:2722/2655/2532), MOVE getInputCast from printc.rs:509 into an in-pipeline IR action, insert CAST via op_insert_before + unique typed varnode after ActionInferTypes; + Stage 3 REMOVE half (delete print-time cast wrapping, coupled to avoid double-casts). LEAD GATE ON STAGE 2 = the ONE-CAST COHERENCE CHECK on pointercmp FIRST (does a post-merge-inserted CAST varnode get a correct committed type without a persistent HighVariable? if not → STOP→user); then broad insertion; per-fixture delta = merge-candidate evidence.**
**⚠️ LEAD CRUX FINDING `27c9b17` (2026-07-24, agent rate-limited, lead diagnosed before implementing): the tail-slot Stage-2 plan is INSUFFICIENT.** typeprobe: pointercmp's `param_1+8` INT_ADD = inputs [Int(8),Int(8)] but OUTPUT Pointer(8,Unknown(1)) — phi relays pointer backward onto the arithmetic result → already Pointer at the tail → NO divergence → setcasts casts nothing → does NOT fix E1010. Ghidra keeps it int8; Ghidra ActionSetCasts is DEAD-LAST (coreaction.cc:5735, NO re-infer after) so the CAST is RENDER-ONLY, does NOT block propagation (corrects the agent's premise). **REFRAME: Stage 0-1 IR-cast (byte-identical) is a valid RENDER improvement but the E1010 lever is the TYPE-PROPAGATION MEET — mosura lets a MULTIEQUAL pointer OVERRIDE an INT_ADD int output where Ghidra keeps int (TypeOpIntAdd::propagateType `inslot==-1 don't-propagate-ptr-output→input` + TypeOpMulti relays-free + Datatype meet/typeOrder).** Lead did NOT implement a propagation change (agent grounded this area; non-faithful override risk). Agent warm-resume: instrument mosura's meet vs Ghidra's updateType/typeOrder for ptr-vs-int-add-output, report exact divergence + faithful fix before code. Master clean e9c0655.
**STAGE 2 LANDED ON BRANCH `c11f25e` (agent war2-cast): FLAGSHIP MET — pointercmp renders EXACTLY oracle `(xunknown1*)(param_1+8)`, scored 1.000; pointerrel 0.951→0.966.** Mechanism confirmed: castOutput computes getOutputToken=arithmetic_output_standard (token from INPUT read-facing types = int8), castStandard(committed_ptr, token_int8)→CPUI_CAST — RENDERS the cast WITHOUT fixing the over-relay (my crux point 4 propagation-meet fix NOT needed). setcasts.rs = ActionSetCasts (castInput+castOutput) run last after Stage-0c ActionInferTypes; probe-gated off under table_recovery_probe (switch recovery). **BUT net corpus 0.9517→0.9507 = 4 REGRESSIONS (all valid C, ZERO wrong-code): switchloop+stackstring = classification-perturbed (the Stage-2 depth-valve HIT — mosura re-classifies explicit/implied at PRINT time after casts; Ghidra freezes ActionMarkImplied 5720 BEFORE setcasts 5735); partialmerge (int4 vs oracle xunknown8 — MUST oracle-verify: cast over-fire mis-port OR faithful?); heapstring (register-reuse merge gap mosura lacks, cast surfaces).** **GATE (B) DONE `2c32c36` (Stage 2.5 freeze — ported ActionMarkExplicit/Implied 5719-5720 as merge::ActionMarkImplied, frozen pre-setcasts; printc reads frozen flag). CORPUS 0.9517→0.9527 NET TOWARD-ORACLE (+0.0010): pointercmp 1.000 (flagship EXACT), pointerrel→0.966, heapstring→0.941, switchloop+revisit recovered. 2 residuals (partialmerge/stackstring) = CONFIRMED pre-existing type/merge divergences (correct casts, upstream global-type + stack-array-typing gaps), valid C, ZERO wrong-code. Suite 495/0, clippy 0.** DATATEST GATE PASSED. **WAR2 RE-MEASURE DONE (branch, clean obj) = PAYOFF PROVEN: COMPILE_FAIL 87 (vs results.prev.tsv 139, but that prev predates this session's E1063 fix so headline conflates — the POINTER-CLASS drop IS the rewrite: E1079 33→11, E1080 12→0, E1045 11→0, E1036 3→0, E1029 19→10). ZERO wrong-code, no new panics (1282 ok, DECOMPILE_FAIL 6→4). 54 fns newly compile. E1010 14→18 = error-CASCADE (all were already COMPILE_FAIL, not new). BLEMISH: 3 NEW COMPILE_FAILs (00246/00247/00250, all E1029) = 16-bit SEGMENTED far-pointer STOREs from the DEFERRED checkPointerIssues (LOAD/STORE cast arm).** **ISOLATED DELTA (lead drove full base re-measure, e9c0655 vs branch, both clean obj): base 108 CF → branch 87 CF = −21 TRUE cast-model delta (the −52 was confounded by base fixes). 25 WINS, 4 REGRESSIONS. FLAGSHIP: E1045 12→0 ELIMINATED; E1080 5→0; E1029 net 17→10. Zero wrong-code, no new panics, datatest 0.9527 toward-oracle.** THE 4 "regressions" = NOT cast-model, PROVEN: (1) checkPointerIssues is WARNINGS-ONLY (no cast) so (B) was a dead premise; (2) canonical decompile_function (dumpwar2) renders all 4 IDENTICALLY base==branch (32-bit compilable) — the 16-bit `segment(...)` E1029 form exists ONLY in the war2_survey --le FULL-SURVEY emit on the branch RUN = whole-program decode NON-DETERMINISM (shared discovery state, address-size mode flips run-to-run), affects base+branch surveys equally, pre-existing harness noise. Cast model is type-only → CANNOT change decode/address-size (airtight by construction; analyze_le_file unchanged on branch). ⇒ REFINED SPLIT (agent self-corrected): 3 segment E1029 = survey decode-noise (dumpwar2 base==branch); 1 (0x70f4d E1010) = REAL cast-model effect but NOT a mis-fire — a FAITHFUL (uint4) getInputCast on an unsigned compare SURFACES a pre-existing pVar13 pointer-mistyping (present in base as a latent Watcom warning; the faithful cast tips it to E1010). Same class as datatest partialmerge = faithful-cast-surfaces-pre-existing-divergence → faithful-ports-LAND (suppressing the faithful cast = non-faithful). **GATE PASSED (a) → MERGING ir-cast-model→master (--no-ff): honest numbers (108→87 CF, E1045 12→0, 0.9517→0.9527, zero wrong-code/mis-fires); 0x70f4d filed with type-core/partialmerge follow-on; 3 segments filed as war2_survey --le decode-nondeterminism harness bug. Agent merging + verifying suite/corpus 0.9527/clippy.**
**🏁 MERGED TO MASTER `6e9c6a4` (--no-ff, whole rewrite Stage 0/1/2/2.5; +docs `64c4ddf`). New setcasts.rs (ActionSetCasts, 207L) + cast.rs + merge::ActionMarkImplied freeze + printc print-time-cast REMOVED; 11 files +896/−109. Master now 64c4ddf. Agent verifying suite/corpus(0.9527)/clippy in flight. FOLLOW-ONS FILED: (i) 0x70f4d/pVar13 pointer-mistyping → type-inference-core foundation (w/ partialmerge/stackstring); (ii) war2_survey --le decode non-determinism (docs/war2-survey-decode-nondeterminism.md) → agent fixing at root next (determinism-first, see [[measurement-determinism-first]]).**
LEAD GATE = (B): agent ports checkPointerIssues (coreaction.cc:2349/2762 LOAD/STORE pointer cast) on-branch to clear the 3 E1029 + fix/confirm 00070f4d → zero-regression → THEN lead merges (Stage 0/1/2/2.5 + checkPointerIssues) to master. DEPTH-VALVE: if it needs the full composite/aggregate lattice → STOP, lead gates merge-with-3-documented (edge-case segmented, payoff decisive) vs opening aggregate foundation. Lead confirming 87 deterministically via bg waiter. Master clean e9c0655.
(historical) STAGE 2 GROUNDED + planned `b2dfaef` (branch). Coherence RESOLVED by grounding: the inserted CAST varnode gets its type EXPLICITLY at creation (new_unique_out, cast target type) — no re-merge/persistent-HV needed. Stage 2 (insert) + Stage 3 (remove print-time cast wrapping printc.rs:621) = ONE ATOMIC change (else double-casts). MERGE-GATE (lead, at Stage 2+3 boundary) = ALL THREE: (1) pointercmp renders exactly oracle `(xunknown1*)(param_1+8)` (coherence proof); (2) all 60 fixtures scanned — every mover toward-oracle by oracle-diff, ZERO wrong-code; (3) WAR2 RE-MEASURE (clean obj/) showing E1010 COMPILE_FAIL actually DROPS = the payoff proof (if corpus moves but WAR2 COMPILE_FAIL doesn't fall, rewrite didn't deliver → reassess before landing). Master untouched e9c0655 until this gate passes.**
(historical) Stage 1 (OpCode::Cast; should be neutral). REMAINING DEPTH VALVE = STAGE 2: does a freshly-inserted CAST varnode stay type-coherent through per-merge resolution without a persistent HighVariable object? One-cast coherence check on pointercmp FIRST before broad ActionSetCasts insertion; if needs persistent HV → STOP→user. Toward-oracle E1010 value appears at Stage 2+3.** Branch commits: 9e9f50f/932023d/7cb7e32/7399ca9; docs/ir-cast-model-plan.md. Master untouched e9c0655.

Agent `war2-typeinfer` RATE-LIMITED 2026-07-24 ~08:42 at committed-green 32ff095 (12 commits). Re-spawn/resume from docs/war2-typeinfer-checkpoint.md at reset. Lead did a read-only grounding advance @5780f84 (oracle target nailed + 8-byte-store shared-source subtlety — the a/b verdict needs refining: divergence is array-vs-scalar-at-0 reconcile, NOT a rushed type-core edit). #5(a) fix HELD for the agent's fresh, careful implementation (corpus-wide type-core regression risk).

Runs as the single background agent `war2-typeinfer` (single-agent-protocol). Task board:
- **#1 (PRIMARY, active): port Ghidra type-inference C-cluster** → eliminate the 137 WAR2 COMPILE_FAILs (E1052 ×34 indirect-call-return-type / E1079 ×33 pointer-in-bitwise / E1029 ×17 under-pointered-deref / rest). Compile precondition for ANY byte-exact. Faithful port of ActionSetCasts + TypeOp propagation + HighVariable type model. Prior art [[actionsetcasts-campaign]] (Brick1 CAST render landed 3579ac3; Brick2 castInput gated behind print-time re-inference retirement — verify if that retirement is itself a needed brick) + [[adaptations-inventory]] (C-cluster = no persistent BlockGraph/HighVariable = deep foundation).
- **#2 (blocked on #1): codegen/regalloc MISMATCH analysis** (797 of 1056) — the byte-exact WALL. Instrument closest MISMATCHes (FUN_0006a7d0 75%, FUN_00077f65 64%, FUN_00034668 42%), classify each divergence faithful-fixable-IR vs Watcom-regalloc-specific (NOT decompiler-reachable — enumerate honestly) vs fixup-only. Establishes the honest byte-exact ceiling.
- **#3 (blocked on #1): scoped extraout_ per-case diff** (93 residual) — faithful killedbycall vs effect-model gap.

**⚠️ PREMISE CORRECTED (2026-07-24, agent war2-typecore, instrument-proven): the "mosura over-propagates a phi pointer backward onto an INT_ADD result" root cause is WRONG.** Ghidra's `TypeOpMulti::propagateType` (typeop.cc:1951) relays input↔output FREELY with no pointer guard — mosura does the same, and its TypeOpIntAdd guard (`inslot==-1` → null) correctly stops it one step further. **pointercmp is NOT divergent (scores 1.000, structurally identical to oracle).** THE REAL ROOT (named by the ported `ActionInferTypes::propagationDebug` instrument, committed `cfe9cc2`, env-gated MOSURA_TYPEPROP): **mosura's SPECULATIVE MERGE lacks Ghidra's entire gate battery.** WAR2 0x70f4d: TEN versions of one register (incl. the FUNCTION-INPUT version) collapse into ONE mosura HighVariable; most-specific-wins then BROADCASTS `Pointer` onto members propagation correctly typed `Uint`/`Int` → pVar13 becomes a pointer → the faithful (uint4) cast tips it to E1010. Ghidra would never form that HV: `mergeTestAdjacent` (merge.cc:186) has "Make sure variables have the same type" (`high_out->getType() != high_in->getType() → false`) + NameLock/illegal-input/isolated-symbol/both-have-VariablePiece refusals; `mergeTestSpeculative` (merge.cc:224-234) additionally REFUSES isPersist/isInput/isAddrTied; `mergeByDatatype` groups by EXACT type. mosura's `merge_same_storage` (merge.rs:557) applies ONLY merge_test_basic + cover-interference — no required/adjacent/speculative/type gate — and `mergeAdjacent` (5726) isn't ported at all. **Lead VERIFIED both Ghidra gates verbatim before gating. = MIS-PORT, not a foundation.** **BRICK 1 BUILT+GATED-GO (corpus 0.9527→0.9532: dupptr 0.969→1.000 oracle-diffed token-identical, ALL others byte-identical, suite 564/0, clippy 0). KEY FINDING — the type gate is INERT until Brick 2, by construction: `merge()` is called from INSIDE `infer()` (infertypes.rs:729), so the gate reads `Varnode::ty` that infer() already OVERWROTE with the class type; first pass all-default → everything merges → broadcast → the two classes carry identical types forever (once merged, never separable). Ghidra has NO such circularity: writeBack (coreaction.cc:5043) commits ONLY the per-varnode type, HighVariable::type is a SEPARATE derived field, and merge runs ONCE after types settle (5722-5727). ⇒ BRICK 2 IS A PREREQUISITE, NOT A FOLLOW-ON (lead's ordering was wrong; agent corrected with evidence).** **BRICK 1 COMMITTED `265facb`** (corpus 0.9532/57, suite 564/0, clippy 0). WAR2 85→84 CF = NEUTRAL (agent correctly refused to claim the flattering netted 85→81); +3 E1029 = a `war2_survey.rs:286-295` HARNESS gap (declaration synthesis is TEXTUAL: recognizes `ident[...]` → pointer but not `*ident`; IR proves the varnode IS typed Pointer) → fix filed, non-blocking. **BRICK 2 PLAN GATED GO (2a first).** Ghidra's THREE channels mosura collapses into one: (1) `Varnode::type` = per-varnode writeBack (coreaction.cc:5043, never consults a HighVariable); (2) `HighVariable::type` = SEPARATE derived field via getTypeRepresentative (variable.cc:377-400, typeDirty invalidation); (3) `getHighType{Read,Def}Facing` (varnode.cc:651/665) = what printc/cast/setcasts read. SITE AUDIT: all ~18 mosura sites map to the HIGH-facing accessor → 2b is a wholesale channel move, not 18 judgement calls. **LEAD VERIFIED the ordering 2a rests on: coreaction.cc 5717-5730 = ALL merge actions (MergeRequired/MarkExplicit/MarkImplied/MergeMultiEntry/MergeCopy/DominantCopy/MarkIndirectOnly/MergeAdjacent/MergeType/HideShadow/CopyMarker), THEN NameVars 5734, THEN ActionSetCasts 5735 — merging completes BEFORE casts.** ⇒ **2a = freeze the merge at the faithful slot; printc must CONSUME it instead of recomputing merge() on the POST-cast graph (printc.rs:1824) — same defect class as the ActionMarkImplied freeze already landed at `2c32c36`, one slot over.** **BRICK 2a BUILT+GATED-GO (corpus 0.9532→0.9542): heapstring 0.941→1.000, oracle-diffed structurally exact (one pre-existing pointer-declarator spacing delta), ALL others identical, suite 564/0, clippy 0.** MECHANISM CONFIRMED by the mover itself: `param_1 = (xunknown8 *)*param_1;` is a parameter reassignment THROUGH A CAST that the post-cast merge recompute was splitting — the cast perturbed the partition, freezing before casts fixed it (2nd confirmation in 2 bricks; dupptr's `param_1 = param_1 + (int8)param_2*8` was the same shape). Implementation: `Funcdata::highs: Option<HighVariables>` set by new `merge::ActionMergeType` at Ghidra slot 5727 (after MarkImplied, before SetCasts); printc `.expect()`s it, old `merge(f)` call+import DELETED = no silent recompute fallback. Post-freeze CAST varnodes get singleton classes via `extend_to` (correct, not a convenience — a cast is NOT the same C variable as its operand; Ghidra allocates a fresh HighVariable per new Varnode). Wrong-code scan clean: WAR2 EMIT 1286/1286 fail=0; 0x70f4d declarations 22→18 with the memcpy loop keeping src/dst DISTINCT (Brick 1's cover test prevents the collapse that would be wrong code). 62 lines / 4 files, no 2b mixed in. **BRICK 2a COMMITTED `e66d54b`** (0.9542/57, suite 564/0, clippy 0). **2b PLAN GATED — SPLIT into 2b-1/2b-2.** THE PRINCIPLED BOUNDARY (agent found it; dissolves the per-site hazard): "does this code run BEFORE or AFTER the merge slot?" — ruleaction.cc has ZERO high-facing uses and 46 per-varnode getType() uses, necessarily, because HighVariables don't exist during the mainloop (merging is 5718-5727, after it). MOVES to `high_type_read_facing` (post-merge): cast.rs ×11 (getInputCast/getOutputToken/arithmeticOutputStandard), setcasts.rs ×6, printc.rs:206 type_of. STAYS per-varnode `Varnode::ty` (mainloop): ptrarith.rs ×2, subvarflow.rs ×2, varmap.rs ×6, rules.rs ×1, lanedivide.rs ×1, infertypes.rs ×2. **2b-1 = introduce the channel + switch the 18 post-merge sites, KEEPING the broadcast → predicted BYTE-IDENTICAL no-op (lead granted SELF-APPROVE if byte-identical; if NOT byte-identical that's a finding = the two channels already disagree, stop).** **2b-2 = remove the broadcast; writeBack becomes per-varnode (delete merge(f) + the locked_hv/hv/result resolution block in infertypes.rs:719-793, return `committed`, infer_types calls update_type(committed[v]) = literally Ghidra writeBack coreaction.cc:5043).** BLAST-RADIUS INSIGHT: the MOVED sites see NO change (high type == broadcast value); the entire behaviour change lands on the mainloop consumers that STAY per-varnode (RulePtrArith, varmap stack typing, subvarflow) — wider than "Brick 1's gate goes live". **AGENT'S RECORDED PREDICTIONS (check against outcome): movers = partialsplit/wayoffarray/stackstring/offsetarray/threedim/twodim/nestedoffset/ptrtoarray/pointerrel; SOME MAY MOVE AWAY-ORACLE (per-varnode types are strictly less refined → RulePtrArith fires less → array indexing may render as raw arithmetic) — accepted in advance as potentially FAITHFUL (Ghidra's mainloop genuinely sees un-merged types; the broadcast was giving mosura a refined view Ghidra never has) BUT lead requires each away-mover to NAME its specific missing downstream faithful piece, not assert one. No movement expected in condconst/elseif/enum/ifswitch/forloop. 0x70f4d: gate compares Uint(4) vs Pointer(4,Unknown(4)) → refuses → pVar13 stays integral → E1010 CLEARS. FALSIFIABLE FAILURE MODE (stated up front): merge_copy is a REQUIRED merge with no type gate in Ghidra either, and a COPY reaches this class (0x71034) — if it directly links Uint to Pointer the class re-forms and E1010 survives, in which case it's a PROPAGATION question, report it and do NOT reach for the merge code.**
**2b-2 BLAST RADIUS MEASURED (agent used the STEP-0 TYPEHV instrument instead of idling; `chosen` vs each member's own `committed` type = exactly the varnodes whose type changes): ONLY 16 of 62 fixtures affected; 46 have ZERO changed varnodes (will stay byte-identical). AGENT FALSIFIED ITS OWN PREDICTION** — wayoffarray/stackstring/offsetarray/threedim/twodim/nestedoffset/ptrtoarray ALL ZERO (predicted movers); forloop_thruspecial (67%) + switchhide (26%) change substantially and were unnamed. The "no pointer-arith ⇒ no movement" half held. **RISK SURFACED: 6 fixtures currently at 1.000 have changed varnodes — pointercmp (100% of class members), heapstring (100%), namespace, forloop_thruspecial, noforloop_globcall, floatprint. pointercmp+heapstring are the two THIS campaign just drove to exact, and the broadcast does 100% of the type work in their classes ⇒ a 2b-2 regression on them is LIVE, not remote.** LEAD POLICY for 2b-2: measure first, do NOT self-commit; **if pointercmp/heapstring regress that is a SIGNAL not a cost — per CLAUDE.md a faithful change moving the corpus away from the oracle is evidence some NON-GHIDRA code is wrong, so name the missing faithful piece; if bounded, port it and land together; if deep, hold 2b-2 turnkey and land the piece first.** Agent must SEPARATE two causes: mainloop-consumer change vs the second-order "class type now computed only at the frozen (2a) slot instead of per inference pass" (flagged as unmeasurable read-only). Pre-registered 2b-2 prediction: movement confined to the 16; 46 byte-identical; switchhide/revisit/varcross/partialsplit/condmulti likeliest by volume.
**2b-1 COMMITTED `990b40e`** (byte-identical, self-approve; per-fixture verified). TWO AUDIT SELF-CORRECTIONS found by checking Ghidra instead of assuming: setcasts `castInput` DELIBERATELY reads the PER-VARNODE type (coreaction.cc:2676/2683/2690 `vn->updateType(ct); if(vn->getType()==ct)`) → its 3 sites STAY (only castOutput's 3 move); cast.rs Load-fallback + `_`-default are `getOutputToken`/`outputTypeLocal` stand-ins → stay. Net 13 readers moved, not 18. Channel recomputes per call (no cache) because castOutput calls updateType then re-reads getHighTypeDefFacing expecting the recomputed value — a cache without Ghidra's typeDirty would return stale.
**⛔ 2b-2 BUILT + SELF-HARD-BLOCKED BY THE AGENT: WRONG CODE. Parked turnkey (infertypes.rs +25/−89, applies cleanly on 990b40e, suite 564/0, clippy 0, corpus 0.9523).** PREDICTIONS vs OUTCOME: ✅ movement confined to the 16; ✅ the 46 byte-identical; ✅ both movers away-oracle; ❌ switchhide/varcross/partialsplit/condmulti did NOT move (30 changed varnodes yet unmoved); ❌ **pointercmp/heapstring HELD at 1.000 — the feared regression did not happen**; ✅✅ **HEADLINE CONFIRMED: 0x70f4d `pVar13 < (uint4)param_4[5]` → `uVar12 < (uint4)param_4[5]` with `uint4 uVar12` — E1010 CLEARS, via exactly the predicted mechanism.** THE WRONG CODE (revisit 0.894→0.786): new `uRam.. = (uint4)(xunknown2)uRam..` + `(uint4)(int2)uRam..` TRUNCATE to 16 bits and widen back, ZEROING bits 16-31 that the next line reads via `>> 0x10` (Ghidra preserves them: `CONCAT22(iRam.._2_2_, (int2)iRam..+100)`). **NAMED CAUSE, IR-verified: `merge_addrtied` (merge.rs:669) unions address-tied varnodes SIZE-BLIND → the 2-byte and 4-byte varnodes at RAM 0x100074 become ONE variable with ONE C name → the faithful cast becomes a destructive self-assignment. Ghidra never forms it: `overlapLoc` (varnode.cc:1785) = "one subrange for each set of Varnodes with the same size and starting address"; `mergeRangeMust` merges only same-(address,size); different sizes are linked by `groupWith` (merge.cc:632-640) into a VariableGroup of SEPARATE HighVariables, rendered `iRam.._2_2_`. = THE PARTIALMERGE ROOT CAUSE + the depth-valve item; the broadcast was MASKING it.** `modulo` −0.001 needs no fix (correctness improvement mis-scored by the token gauge: `iVar1 = *param_2` through `int4 *` replaces `uVar1` + a spurious cast).
**⚠️ CAUSE SEPARATION OVERTURNED THE DIAGNOSIS (agent, discharging lead point 3): CAUSE A = ZERO.** Probe = diff raw IR before/after 2b-2 excluding CASTs: non-CAST IR IDENTICAL, inserted CASTs IDENTICAL, committed Varnode::ty IDENTICAL, HighVariable membership IDENTICAL — on BOTH movers. **The mainloop consumers (RulePtrArith/varmap/subvarflow/lanedivide) changed NOTHING — falsifying the very reasoning that motivated splitting 2b. CAUSE B is the whole delta: the class type is now derived from per-varnode types over the FROZEN (pre-cast, 2a) partition where before it was the broadcast value over the INFER-TIME partition.** SECOND CORRECTION: **the wrong code is PRE-EXISTING/LATENT, not introduced** — the offending CAST (`r0x100074:2 = CAST u0x10060:2`, output address-tied 2-byte sharing hv#116 with the 4-byte) is in the IR BEFORE 2b-2; it merely rendered folded inline. The two-different-size-addrtied-varnodes-share-one-C-name collision EXISTS TODAY; 2b-2 only exposes it. (Block still stands — shipping wrong output for revisit is disqualifying regardless of fault.) **Agent flagged that Cause B was isolated BY ELIMINATION, not direct observation, and has NOT pinned which frozen high type flips.**
**LEAD REDIRECT (supersedes the enter-the-blocker call): VariablePiece brick HELD, NOT entered — never open a deep foundation on an elimination-based diagnosis (same failure mode dodged 3× already). PROBE FIRST: pin exactly which frozen-slot high type flips on `revisit`. Branch (a) bounded fix in the class-type computation / where the partition is frozen (faithful to where Ghidra computes HighVariable::updateType) → unblocks 2b-2 WITHOUT VariablePiece = best outcome; branch (b) collision unavoidable at the naming level → VariablePiece plan, arrived at by OBSERVATION. Lead gates either. No urgency: the collision is pre-existing, 2b-2 parked turnkey loses nothing by waiting.**
**BLOCKER PLAN GROUNDED + ACCEPTED-ON-FILE (executes only if the probe lands on branch b).** Ghidra's full path: mergeAddrTied (merge.cc:609) → overlapLoc (varnode.cc:1785, one subrange per same-(address,size) set) → mergeRangeMust force-merges WITHIN a subrange → `groupWith` (merge.cc:636-639) links different (address,size) subranges into a VariableGroup — **GROUPED, NEVER MERGED**; then protected by mergeTestRequired (:155-162, false when both in the same group) + mergeTestAdjacent (:210-212, refuses speculative when both carry a `piece`); printc `pushPartialSymbol` (printc.cc:1947) → for a PRIMITIVE symbol falls to the `!succeeded` arm (:2030) → `unnamedField` (printlanguage.cc:719) emits `_<off>_<size>_` joined by `.` = the oracle's `iRam.._2_2_`. MINIMAL SUBSET: **(a)** merge_addrtied keys on (space, offset, **SIZE**); **(b)** a `HashMap<rep, group-id>` tag + those two refusals (no instance lists/per-group covers/dirty-flag); **(c)** printc renders `<name(A,S)>._<off>_<s>_` — **mosura needs NO Symbol object because its global names are already address-derived (`{prefix}Ram{off:016x}`, printc.rs:394); Ghidra needs getFirstWholeMap() only because its names come from a symbol table** (the scoping judgement that keeps (c) small). **⚠️ RISK THE AGENT SURFACED PRE-BUILD: merge.rs:667 documents the size-blind union as DELIBERATE + LOAD-BEARING — "That spanning Cover is what lets merge_copy tell a pre-store snapshot (which stays live across the overwriting store) apart from the stored value." Part (a) REMOVES exactly that → may regress floatcast/partialmerge.** Agent's own gate: apply (a) alone as a pure DIAGNOSTIC (not a land, renders badly by construction) to quantify the spanning-Cover dependency; if merge_copy needs per-group COVERS rather than a tag → that IS VariableGroup → stop+report. Smallest LANDABLE unit = (a)+(b)+(c) together (coupled: (a) alone renders badly, (c) inert without (a)).
**LEAD HELD IT AGAIN — PROBE STILL FIRST, and the agent's spanning-Cover finding is WHY: the plan's first act is surgery on something mosura explicitly documents as load-bearing, while the cheaper branch is still UNTESTED (Cause B was isolated by elimination, final hop never pinned). The two questions differ: the agent's (a)-diagnostic asks "what depends on the spanning Cover?" (assumes branch b); the LEAD probe asks "which frozen-slot high type flips on revisit and why?" — if the class type is derived at the wrong point (vs where Ghidra computes HighVariable::updateType), a bounded fix there unblocks 2b-2 and the ENTIRE three-part brick becomes unnecessary.**
**✅ PROBE RESOLVED IT: BRANCH (a) — a MISSING PORT, not VariablePiece.** Root cause pinned by DIRECT observation (frozen-high-type column added to dumptypes): mosura's `output_token` has NO SUBPIECE arm → falls to the `_` default returning the PER-VARNODE type; the broadcast used to make that equal the high type so nothing fired; 2b-2 makes them differ → spurious CAST → its address-tied output shares the C name with the 4-byte whole → destructive assignment. **Ghidra HAS the override mosura lacked: `TypeOpSubpiece::getOutputToken` (typeop.cc:2142) returns the output's own `getHighTypeDefFacing`, so castOutput's short-circuit is trivially satisfied and SUBPIECE NEVER takes an output cast; `TypeOpPiece::getOutputToken` (:2063) is the same shape, also un-ported (same latent trap one op over).** Textbook CLAUDE.md: faithful change moved the corpus wrong → culprit was NON-GHIDRA code (the `_` default standing in for getOutputToken) — the very site flagged during 2b-1 as a token-model approximation and left alone. **LESSON: flagged approximations come back.**
**⚠️⚠️ AGENT CAUGHT ITS OWN INVALID EVIDENCE (2026-07-27): the earlier "non-CAST IR identical / CASTs identical / membership identical" came from a probe whose `git stash pop` ran with NOTHING STASHED → BOTH sides dumped the SAME state. So "the wrong code is pre-existing, only the rendering changed" was FALSE — the two CASTs ARE new in 2b-2. Lead had ALREADY approved entering the deep brick on that bad evidence (whose first step was surgery on the load-bearing spanning Cover) — holding the probe is what saved it. Cause A = zero still holds on re-run (marker-asserted). A SECOND A/B error (stashing with both changes present → measured the clean tree) was caught immediately from the file-revert notice. RULE now in [[measurement-determinism-first]]: any A/B probe MUST prove the switch happened; "identical everywhere" is suspect by default.**
**✅ TOKEN PORT COMMITTED `844d5b1` (SUBPIECE+PIECE getOutputToken; byte-identical standalone = self-approve, and that coincidence is WHY the gap stayed invisible). ✅ 2b-2 UNPARKED — ALL 3 ACCEPTANCE CHECKS PASS, LEAD GATED THE LAND: revisit's destructive statements GONE (mosura `CONCAT22((int2)(uRam>>0x10),(uint4)uRam+100)` ≡ oracle `CONCAT22(iRam._2_2_,(int2)iRam+100)` — lead verified semantic equivalence: both take the high 2 bytes, CONCAT22 truncates the 2nd arg to 16 bits either way); 0x70f4d `uint4 uVar12` / `uVar12 < (uint4)param_4[5]` E1010 SOURCE CLEARED; pointercmp+heapstring HOLD at 1.000. PAIR DELTA 0.9542→0.9541 (revisit fully recovered from −0.108 to zero; sole mover `modulo` −0.001 = the pre-classified mis-scored correctness improvement). Suite 564/0, clippy 0, WAR2 EMIT 1286/1286 fail=0.** SCOPE HONESTY (in both commit messages): this removes the SPURIOUS CAST that made the collision visible — **a 2-byte varnode still carries a 4-byte HighVariable type; merge_addrtied is still size-blind; still partialmerge's root cause; still the VariablePiece debt. A green result here does NOT close it.**
**🏁 TYPE-CORE SEQUENCE COMPLETE ON MASTER + PAYOFF MEASURED (2026-07-27). THE CAMPAIGN TARGET IS HIT: `FUN_00070f4d` COMPILE_FAIL E1010 → MISMATCH (compiles) IN THE SURVEY, not just canonically.** Sequence: `265facb` speculative-merge gates → `e66d54b` freeze merge at Ghidra's slot → `990b40e` split the type channel → `844d5b1` SUBPIECE/PIECE getOutputToken → `95144f3` per-Varnode writeBack. **WAR2 COMPILE_FAIL 81 → 71 (−10); E1010 18→11 (−7), E1081 6→1 (−5); 12 fns newly compile, 2 newly fail; EXACT 1→1, DECOMPILE_FAIL 0→0, wrong-code scan CLEAN (byte-match churn 136 worse/128 better/938 same = symmetric 0-16% noise).** Corpus across the sequence 0.9527→0.9541 (dupptr + heapstring each driven to 1.000; modulo −0.001 = pre-classified mis-scored correctness improvement). Suite 564/0, clippy 0 throughout. Measurement trustworthy: identical harness (`a1d3e98`) both sides, every state transition ASSERTED before capture. Results: `war2-survey/results.{base,head}-typecore.tsv`. THE 2 NEW FAILURES, both characterized, neither a decompiler defect: `00092` E1080 = inherent flip side of the harness fix (an identifier used BOTH as pointer and arithmetic operand; one synthesized declaration must serve the whole TU — the other choice just moves it to E1029); `01185` E1010 = the already-filed CALL-output castOutput gap. **HONEST CAVEAT (agent's, accepted): the −10 does NOT close the size-blind address-tied merge — still partialmerge's root cause, still the VariablePiece debt, not entered.**
**⛔ (a) CALL-output castOutput BUILT + SELF-HARD-BLOCKED: emits a DUPLICATED CALL (wrong code; corpus only −0.001 would have hidden it). Parked turnkey (cast.rs +18/−1, applies as-is once printc is fixed).** The PORT is faithful (verified BOTH ways: TypeOpCall/Callind have NO getOutputToken override → base = outputTypeLocal = getOutputLocal (typeop.cc:720/776), prototype return type only when output-locked, else undefined<size>, and mosura models no locked call protos so only the unlocked arm is reachable; `ActionSetCasts::apply` (coreaction.cc:2722) does NOT exclude calls — skips only CPUI_CAST + notPrinted; `castStandard` (cast.cc:339-391) leaves integral bare and casts only pointer/float = exactly the E1010 shape; oracle renders `pcVar2 = (char *)func_0x00101008();`). **THE BLOCKER, NAMED: printc's `is_explicit` (printc.rs:239) consults `merge::explicit_leading` BEFORE honouring `set_implied()`, and explicit_leading (merge.rs:218-226) returns Some(true) for ANY Call/Callind-defined varnode (Ghidra baseExplicit's `def->isCall()` arm) — correct for a REAL call output, WRONG for the post-cast unique that ActionSetCasts deliberately marked implied (coreaction.cc:2594-2596, which mosura's setcasts DOES set). Result: the unique prints as its own statement AND the CAST re-renders the call inline = the call emitted TWICE. Ghidra has no such override: ActionMarkExplicit ran at 5719 on the PRE-cast graph (that unique didn't exist), and setImplied() at 5735 is FINAL. printc.rs:236-238 documents the leading chain as "CAST-INVARIANT" — that assumption is exactly what failed, because ActionSetCasts creates varnodes AFTER the freeze.**
**⚠️ META-PATTERN (3rd instance): a faithful port again surfaced a PRE-EXISTING non-Ghidra PRINT-TIME RE-DERIVATION (1: the type broadcast → 2b-2; 2: the `_`-default token → 844d5b1; 3: the explicitness recompute → this). LEAD DIRECTED: after this brick, do a READ-ONLY AUDIT enumerating EVERY remaining place printc re-derives state Ghidra freezes in-pipeline (explicitness, implied, naming, types, …), filed as a docs note with Ghidra's frozen source per item + whether post-freeze varnodes can reach it — converts an unknown-size debt into a list and PREDICTS the next blocker instead of tripping over it one block-and-diagnose cycle at a time.**
**PRINTC PLAN GATED GO: it is NOT a reordering — ONE EARLY EXIT ahead of the chain, leaving the documented copymarker/high_ram_off order byte-identical.** `merge::ActionMarkImplied` records `Funcdata::classified_upto = num_varnodes()` at its slot; `is_explicit` returns `vn.is_explicit()` immediately (after the constant test) for any `v.0 >= classified_upto`. FAITHFULNESS: Ghidra classifies ONCE at 5719/5720 over the varnodes existing then; ActionSetCasts at 5735 creates new ones and sets setImplied() ITSELF; there is NO later classification pass ⇒ for those varnodes THE FLAG IS THE ANSWER, there is nothing to re-derive from. mosura recomputing explicit_leading for them is the non-Ghidra step (its `def->isCall()` arm = Ghidra baseExplicit coreaction.cc:3015, correct for a REAL call output, inapplicable to a synthetic unique Ghidra never classified). DISJOINTNESS PROVEN (not hoped): the exit fires only for indices ≥ classified_upto = fresh ActionSetCasts uniques — never addrtied, never SUBPIECE-of-addrtied ⇒ never the copymarker case the constraint protects. **BONUS FIND from auditing all 5 preceding arms (not just the guilty one): `slot_write[v.0]` would INDEX PAST THE END for a post-freeze varnode = a latent out-of-bounds currently unreached only by luck; the same exit closes it.** `classified_upto` set explicitly in ActionMarkImplied rather than inferred from FrozenHighs::of.len() (coincident today; independence prevents a silent coupling bug). NOTE: `is_explicit` ALREADY ends with the frozen flag and its comment already anticipates CAST outputs reading implied there — the fix RESTORES the design's stated intent. SEQUENCE: build → measure printc alone → byte-identical ⇒ SELF-APPROVE+commit (if NOT byte-identical: STOP+report, the exit is firing somewhere unreasoned) → unpark (a) → acceptance = partialsplit renders the call ONCE as `pcVar2 = (char *)func_0x00101008();` → pair lands on lead's gate.
**✅ printc explicit-flag brick LANDED `bf813d4`** (byte-identical, self-approve; stops a genuine override + closes the latent `slot_write[v.0]` OOB). **BUT the gated plan NAMED THE WRONG MECHANISM — the duplication SURVIVED it (agent's 3rd wrong mechanism on this one fixture; it reported this plainly and resolved to stop proposing mechanisms here without reading the emitter). THE REAL BLOCKER: mosura's `emit_basic` has NO IMPLIED-OUTPUT SKIP AT ALL. Ghidra `PrintC::emitBlockBasic` (printc.cc:2703-2705) OPENS with a uniform `if (out && out->isImplied()) continue;` across EVERY opcode, no call special-case; mosura dispatches to a per-opcode match with "a CALL output is always explicit" HARD-CODED in a comment ⇒ the call statement printed regardless of the flag AND the CAST re-rendered it inline. A missing STRUCTURAL INVARIANT, not a subtle divergence — invisible until something finally marked a call output implied.** ⇒ 4th instance of the print-time re-derivation class, and structural rather than incidental. **PAIR GATED GO: cast.rs (+18/−1) CALL/CALLIND getOutputToken + printc.rs (+19) uniform implied-output skip. ACCEPTANCE PASSED: `pVar2 = (int1 *)func_0x00101008();` emitted ONCE, oracle shape (residual int1-vs-char pointee + name are pre-existing). Corpus 0.9541→0.9543, sole mover partialsplit 0.915→0.923 (+0.008) toward-oracle, 59/60 byte-identical, state-asserted A/B, suite 564/0, clippy 0. WRONG-CODE SCAN (designed against the real risk that a global implied-skip DROPS statements): per-file rendered-call counts across all 1286 WAR2 fns, before vs after = 0 files changed; agent DISCARDED its own first metric (2737 repeated call-texts) as meaningless since a fn may legitimately call the same target twice. WAR2 EMIT 1286/1286 fail=0; 0x70f4d still integral.**
**✅ CLASS AUDIT DONE → `docs/printc-rederivation-audit.md`. THE ORGANIZING FACT (turns 4 anecdotes into one predicate): EVERYTHING printc consumes is FROZEN at or before `ActionNameVars` (5734); `ActionSetCasts` (5735) is the ONLY later action, and it CREATES Varnodes + REWIRES op outputs ⇒ anything mosura recomputes at PRINT TIME is computed over a graph GHIDRA NEVER ANALYZED. Every instance is a special case of that sentence.** CHEAP PROBE for the next divergence: does it involve a Varnode index ≥ `classified_upto`, or an op whose output ActionSetCasts rewired? If so, THIS CLASS IS FIRST SUSPECT. **CLOSED (5):** HighVariables/merge `e66d54b` · per-Varnode types `95144f3`+`990b40e` · SUBPIECE/PIECE output token `844d5b1` · explicit/implied TRAILING chain `2c32c36` · LEADING chain for post-freeze varnodes `bf813d4`. **OPEN (3):** **#6 COVERS** — printc recomputes `all_covers(f)` over the POST-CAST graph; Ghidra's Cover belongs to HighVariable, built 5717-5727 BEFORE casts; reachable by construction (all_covers walks every Varnode incl. CAST outputs; liveness shifts when an output moves to a fresh unique). **#7 `nonprinting`** — `copy_marker_nonprinting` at print time vs Ghidra's `ActionCopyMarker` (a pipeline action before 5734); CONSUMES #6's post-cast covers so the two are COUPLED; decides STATEMENT SUPPRESSION ⇒ a wrong answer can both ADD and DROP statements = the same failure surface as the duplicated-call bug. **#8 names** — ActionNameVars frozen at 5734, mosura names at print time; narrow exposure. Plus **#9 CALLOTHER output token** deliberately left (Ghidra's token consults a per-userop table mosura doesn't model). #6/#7 recorded as **STRUCTURALLY EXPOSED, NOT YET DEMONSTRATED** — the same status #1 held before heapstring and #5 before the duplicated call demonstrated them, which IS the argument for fixing them first.
**✅ COPY-MARKER BRICK LANDED `8c9c6bb` (audit #6+#7, self-approved: corpus BYTE-IDENTICAL 0.9543/57 across all 62 fixtures via full dump_all diff; suite 564/0, clippy clean). AUDIT #6/#7 IS DEMONSTRATED, NOT DOWNGRADED — the lead's safety-argument correction (markInternalCopies also marks SUBPIECE; castOutput REWIRES existing ops, not just creates) is what predicted it.** Built: `merge::ActionCopyMarker` pipeline action after ActionMergeType (pipeline.rs:777) before ActionSetCasts; marks stored on `Funcdata::nonprinting`; printc consumes with `.expect()`; print-time computation + printc's `covers` field DELETED (no recompute path survives). **IT MOVED OUTPUT — 1 of 1286 WAR2 fns: `FUN_000722c8` dropped a DUPLICATED global assignment (`pRam..0a8014 = (int4 *)pVar4;` then `pRam..0a8014 = pVar4;`).** MECHANISM (instrumented, probe removed pre-commit): pre-cast, two COPYs from the same source land in the `r0xa8014` high (op59@722f6, op101@722fb) ⇒ two copy-ins ⇒ `markRedundantCopies` (merge.cc:1252) marks the dominated op101 non-printing; `castOutput` then REWIRES **op59** (the COPY writes a fresh post-freeze unique in its own singleton high; the CAST takes over producing r0xa8014); post-cast the high has only ONE copy-in, never reaches `multiCopy`, so the print-time recompute LOST the mark. **KEY PROBE CORRECTION (agent widened the audit doc): the cast rewired the DOMINATING op, not the marked one — op101's own output index was BELOW `classified_upto` and looked untouched, so the audit's first-suspect probe as originally worded would have MISSED it. Every arm of markInternalCopies relates an output's HighVariable to its INPUTS' ⇒ rewiring ANY participant suffices.** NO VALUE DROP verified: both statements assign the same pVar4 to the same global, the cast is a no-op on an already-`int4 *` value, and Ghidra marks that COPY at 5729 with the mark surviving ActionSetCasts ⇒ one statement is the faithful answer. Scans: WAR2 EMIT 1286/1286 fail=0 both sides; per-file line count exactly ONE file changes (29→28, total 57173→57172); per-file rendered call-expression count IDENTICAL in every file (5224); both sides provably distinct builds (marker grep asserted, example binaries md5-differ, after-capture reproduced post-rebuild). Audit doc: #6+#7 → Closed with the mechanism; **#8 (names) and #9 (CALLOTHER token) remain OPEN.**
**🔓 TASK #2 (VariablePiece debt) GATED GO — THE DEPTH VALVE WAS LIFTED BY MEASUREMENT, not waived.** Agent ran the gating diagnostic: **(a) alone IS wrong code** (partialmerge's pre-store read moves to AFTER the store — snapshot LOST; confirmed vs oracle) — the valve tripped correctly. **BUT the depth is NOT VariableGroup: `VariablePiece::updateCover` (variable.cc:160) = internal cover + merge over intersecting pieces; `updateIntersections` (:140) = a byte-range overlap filter over `group->pieceSet`; the `intersectdirty`/`extendcoverdirty` machinery is PURE CACHING that does NOT port because mosura's all_covers/classes_interfere recompute from scratch. LEAD VERIFIED BOTH IN SOURCE before accepting.** **THE REFRAME (the real result): Ghidra separates IDENTITY (which C variable; per (address,size)) from THE COVER USED FOR INTERFERENCE (spanning the overlap group). mosura's size-blind union was an approximation OF THE EXTENDED COVER, not of identity ⇒ this is a SPLIT, not a new object model.** MEASURED: (a)+extended-cover restores partialmerge to the oracle modulo the partial-symbol render. **ALSO FOUND: HEAD IS CURRENTLY BUGGY, not merely approximate — partialmerge's `iRam..0670 = (int4)param_1` emits an 8-BYTE STORE AS A 4-BYTE ASSIGNMENT = a live VALUE DROP on master.** (c) IS MANDATORY: multiret emits THREE declarations of the same name (`xunknown1/xunknown4/xunknown2 xStack_14` — C-INVALID) once distinct same-address highs must be named; revisit needs `iRam.._2_2_`. ⇒ smallest landable unit = (a)+extended-cover+(c) TOGETHER; gauge dips until (c) lands (HEAD 0.9543 → (a) 0.9505 → (a)+cover 0.9523). The 1 failing test is `merge_addrtied_unifies_all_sizes_at_one_address` = THE TEST THAT ASSERTS THE APPROXIMATION BEING REPLACED ⇒ rewrite to the new contract, not a break. **AGENT'S OWN SELF-CORRECTION: its coarse diagnostic extended over RAW byte-overlap of all addrtied varnodes, which OVER-EXTENDS (spurious revisit snapshot); Ghidra extends over the GROUP'S PIECE SET, and mergeAddrTied runs unifyAddress/eliminateIntersect over the whole overlap range but mergeRangeMust only over same-(addr,size) subranges — the extended cover PREVENTS speculative merges, it does not FORCE snips. (b) is therefore NOT a droppable bookkeeping tag: it defines the piece set the cover extends over.** PLAN (steps 1-4, one unit): 1 merge_addrtied → same-(space,offset,size) mergeRangeMust + a group map (rep → group id, piece offset, size) from overlapLoc's maximal-overlap ranges (varnode.cc:1785) + groupWith (variable.cc:571), rewriting the test; 2 extended cover as a PURE FUNCTION per updateCover/updateIntersections, consulted ONLY at mosura's analogue of `HighIntersectTest::intersection` (variable.cc:1166); 3 the two group refusals (mergeTestRequired merge.cc:147-153 same-group refuses / cross-group needs one piece spanning its whole group; mergeTestAdjacent :207-209 no speculative merge when both are pieces); 4 printc `pushPartialSymbol` (printc.cc:1947) → `unnamedField` (printlanguage.cc:719) as `<name(base)>._<off>_<size>_` driven by the group offset (NO Symbol object — mosura's global names are already address-derived, printc.rs:394). ACCEPTANCE: multiret duplicate-name declarations GONE; partialmerge matches oracle INCLUDING the value-drop fix; revisit renders the piece accessor.
(historical) HEADS-UP GIVEN FOR TASK #2: the copy-marker is now frozen at the faithful PRE-cast slot, so the (a)-alone spanning-Cover diagnostic will measure against the CORRECT marker placement — a cleaner baseline, but any pre-`8c9c6bb` intuition about which fixtures depend on that Cover is STALE. Measure fresh. Plan must state explicitly: the (a)-alone diagnostic result BEFORE writing (b)/(c), and whether merge_copy needs per-group COVERS (⇒ VariableGroup ⇒ STOP+report) or a group TAG suffices.**
(historical) NEXT: (1) commit the pair + audit note → (2) #6+#7 TOGETHER, PLAN-FIRST (coupled: #7 consumes #6's covers). NOT speculative — these are KNOWN STRUCTURAL DIVERGENCES, justified by faithfulness alone (port-all-faithful-rules), a different category from building on an unobserved diagnosis. Plan must state the wrong-code scan UP FRONT (reuse the per-file rendered-statement-count design that worked, don't invent a new one) since #7 can ADD or DROP statements. → (3) THEN (b) mergeAddrTied same-(address,size), plan-first, depth valve armed.**
**✅ PAIR COMMITTED `d07680c` + audit note `8000ae9` (master clean, suite 564/0, clippy 0, corpus 0.9543/57).** **NOW: agent running a READ-ONLY PROBE on #6/#7 before asking for a brick — applying its own VariablePiece standard (never fund on "structurally exposed, not yet demonstrated"). Design: compare covers + the `nonprinting` set computed over the POST-cast graph vs the same on the PRE-cast graph, across the corpus, reporting whether any op's SUPPRESSION DECISION differs (suppression is the output that matters; a shifted cover changing no decision is noise).** LEAD REFINEMENT (reconciling with port-all-faithful-rules): faithfulness alone justifies porting #6/#7 EVENTUALLY — a known structural divergence doesn't stop being one while inert — but it does NOT set PRIORITY. The probe decides priority only: **divergence demonstrated ⇒ they jump ahead of (b) (same understood class, cheaper, #7 sits on the exact statement-suppression surface that produced the duplicated call); INERT ⇒ downgraded to documented-and-harmless faithful debt, port when something touches that path anyway, and (b) is unambiguously next with no funding spent.**
(historical) NEXT: (1) commit the pair → (2) THE CLASS AUDIT (read-only docs note; per item: Ghidra's frozen source+slot / can post-freeze varnodes reach it / can it override a flag or drop-duplicate a statement). Entries so far: explicit_leading, slot_write's unbounded index, per-opcode emit assumptions. → (3) THEN (b) mergeAddrTied same-(address,size), plan-first, depth valve armed.**
(historical) NEXT: printc plan-first → gate → unpark (a) on top, acceptance = partialsplit renders the call ONCE as `pcVar2 = (char *)func_0x00101008();` matching oracle → pair lands together. THEN (b) mergeAddrTied same-(address,size), plan-first, depth valve armed.**
(historical) NEXT: (a) CALL-output castOutput gap — GO, no plan round-trip needed (small, faithful, already filed in cast.rs doc + docs/typeop-propagate-gaps.md, empirically named by 01185: `pVar6 = func_0x0007803f();` assigning an int-declared call return to a pointer var where Ghidra casts). Ground the Ghidra CALL arm first, then build+measure; byte-identical = self-approve. THEN (b) the mergeAddrTied same-(address,size) brick — now motivated by TWO fixtures (revisit + partialmerge) — gets its own PLAN-FIRST round with the depth valve armed (its first step is surgery on the spanning Cover merge.rs:667 documents as load-bearing). Bank the bounded win first.**
(historical) NEXT SEQUENCE: (1) commit 2b-2 → (2) resolve survey-vs-canonical → (3) re-measure.
**⚠️ THIRD HARNESS DISCREPANCY FILED (do not chase yet): the SURVEY path renders 0x70f4d's operand as a POINTER while canonical `dumpwar2` renders it INTEGRAL — same binary, same function. (Prior two: decode non-determinism `74cb0ae`, the `*ident` declaration gap `a1d3e98`.) Agent's hypothesis: the whole-program survey shares prototype/analysis state across all 1286 fns. MUST be confirmed before the next WAR2 re-measure's numbers are quoted.**
(historical) LEAD DECISION: ENTER the blocker, SCOPED NARROW, PLAN-FIRST (rule: wrong-code break blocks the land → land the blocking faithful piece first).** Scope = mergeAddrTied's same-(address,size) restriction + printc partial-symbol accessor rendering (`._2_2_`), NOT the full VariableGroup object model. Has STANDALONE value (should move partialmerge 0.941 on its own). DEPTH VALVE ARMED: if the rendering genuinely needs the full machinery (instance lists/covers/dirty-flag/cross-group mergeTestRequired) → STOP at the plan, report, lead escalates to user. Sequence: plan → gate → build → measure standalone (partialmerge toward-oracle?) → land → THEN unpark 2b-2, acceptance = revisit's wrong code GONE + 0x70f4d E1010 still clears + pointercmp/heapstring hold at 1.000 → land the pair.
(historical) 2b (plan-check required): per-varnode writeBack + thread high_type_read_facing (cast.rs ×11, setcasts.rs ×6, printc.rs:206) → Brick 1's gate goes LIVE → 0x70f4d E1010 should clear. Lead asked for PREDICTIONS before measuring (which fixtures move + direction, and whether pVar13 stays integral) so prediction-vs-result is the check.** DEPTH VALVE does NOT bite (caching the union-find mosura already builds twice ≠ a persistent HighVariable object; extend_to gives post-freeze CAST varnodes singleton classes = what Ghidra does).
(historical) Brick 1 delivers independently: speculative gate FIRED (function-input version excluded from the class); WAR2 0x70f4d structural wins, no wrong-code — a call result no longer written into a PARAMETER's variable (a merge Ghidra forbids), prologue spills no longer sharing with memcpy loop pointers, declarations 24→22. Per-gate attribution: 1a+1b byte-identical, +speculative byte-identical, +1c mergeAdjacent → dupptr 1.000 (vindicates bundling 1c: mergeTestAdjacent permits isInput where mergeTestSpeculative forbids it → Ghidra's `param_1 = param_1 + (int8)param_2 * 8` param-reassignment). Brick 2 = faithful writeBack (separate per-varnode vs high-type channel; mosura collapses both into Varnode::ty, forcing infer()→merge() circularity Ghidra doesn't have). partialmerge = DEPTH VALVE (different-SIZE addrtied union vs Ghidra VariablePiece/VariableGroup, merge.rs:669). stackstring = varmap/ScopeLocal, third subsystem.

CORE METHODOLOGY — DIFFERENTIAL-COMPILER TRIAGE (USER PRINCIPLE 2026-07-24, governs every blocker): for each byte-exact blocker, reduce to a minimal viable example and compile across MULTIPLE compilers/versions for the same arch (ow2 native + historical Watcom 10.0a/10.6/11.0, gcc, clang, MSVC vc4/5/8, Borland BC45 — [[analysis-external-toolchains]]). CLASSIFY: (A) GENERAL Ghidra/mosura limitation (compiler-independent, or mosura wrong vs Ghidra) → fix faithfully in the general path; (B) COMPILER-SPECIFIC (only Watcom / only a Watcom version) → scope behind mosura's compiler+version detection (compiler_version::detect + loader compiler_spec_id + watcom cspec), mirroring how Ghidra localizes compiler diffs in CompilerSpec — NEVER generalize a Watcom quirk to gcc/clang/msvc, NEVER ad-hoc `if watcom` in the decompiler (route through the spec/detection layer). Guard against Watcom over-fitting. MECHANISM (verified in Ghidra source 2026-07-24): Ghidra has ZERO `if(compiler)` branches in the decompiler core — ALL compiler-specificity is data-driven via the cspec, decoded into Architecture flags the decompiler reads (<aggressivetrim>, <inferptrbounds>, <nohighptr>, <deadcodedelay>, reversejustify, prototype models, <data_organization>, <callfixup>/<callotherfixup>). Compiler NAME appears only to SELECT which cspec loads (bfd_arch.cc, a tool) + host `#ifdef _WINDOWS`. mosura already mirrors this (only "watcom" in decompile/ = cspec selection in build.rs + a test guard; channel = Funcdata::proto_model). ⇒ a class-B fix is CSPEC DATA in specs/x86-32-watcom.cspec (or port Ghidra's cspec element: decode + flag + decompiler read, then set it), NEVER `if watcom` in decompile/. Pointer-inference knobs <nohighptr>/<inferptrbounds> are the faithful home for E1079/E1029 IF the differential proves them Watcom-specific. Ties to [[self-compiled-ground-truth]] + [[war2-issues-become-source-tests]] + watcom-codegen-fingerprint (already found version-specific codegen: byte-compare promotion). Every fix records differential evidence + A/B verdict in docs/war2-typeinfer-checkpoint.md.

**AGENT `war2-arraytype` ACTIVE.** MENU-E GUARDRAIL PAID OFF (2026-07-24): agent built 3 gcc MVEs → partialsplit's bug does NOT reproduce in isolation → menu-E-as-one-brick is NO-GO (would re-tread the f23b362 revert). partialsplit = 3 STACKED reg-artifact mechanisms: **#1 stack-base NOT unified** (RDI carried across call-clobbers as separate Pointer(Unknown(8)) HighVariable from the PTRSUB auStack_58 → 8-byte store propagates element-8 instead of casting through the array ptr; Ghidra unifies as puVar3 uint2*); **#2 extraout_ MISNAMING (printc.rs:408 name_of names ANY register-INDIRECT-output extraout_; Ghidra reserves it for call-CREATED isIndirectCreation only)** — SAME distinction as the landed 804d274 infertypes guard; **#3 void 2nd return cascades from the RDI/RAX clobber modeling.** **#2 LANDED (GO'd, faithful): extraout_ naming now gates on is_indirect_creation (printc name_of, faithful to Ghidra database.cc:2492 — extraout_ only under indirect_creation, relays get local naming). Isolated-oracle-verified (partialsplit relay = puVar3 in oracle, was mis-named extraout_). Corpus toward-oracle (only partialsplit moved), zero regressions, regression test. BUT WAR2 blast radius = ZERO: all 208 WAR2 register-INDIRECT outputs are genuine creations (correctly named) — the ~626-fn hypothesis REFUTED, WAR2 extraout_ are faithful. Residual: partialsplit pVar4 now used-before-def = the pre-existing #1 stack-base-unification gap (not this fix). NOTE: verify agent base includes 09f23c6 (baseline discrepancy flagged).** #1 stack-base unification = DEEP (RDI reused 3 ways, no isolated MVE, filed). **#2 COMMITTED `e9c0655`** (master; base confirmed current — 09f23c6 ancestor; corpus 0.9516→0.9517 toward-oracle, suite/clippy green). NEXT FOUNDATION (active) = E1010/E1081 aggregate/concrete-pointer cast lattice (~22; cast.rs primitive-only, struct/enum/typedef deferred; agent grounding ActionSetCasts/castStandard on an isolated datatest divergence before code — go/no-go evidence to lead first). Master now e9c0655.
(historical) LEAD REDIRECT: ground **#2 FIRST** (cleanest faithful rule = gate extraout_ naming on is_indirect_creation like 804d274; HIGHEST blast radius = the ~626-fn extraout_ class; most gateable), then #1 stack-base unification. Isolated-oracle-first per lever; lead gates each build. Task #5 CLOSED (array-typing faithful). Task #7 = this menu-E work. **TASK #5 RE-VERDICT (agent, accepted): no array-typing change needed — dominance is AUTOMATIC via Datatype::compare submeta ordering (concrete uint2 beats Unknown(8) regardless of size); mosura's element stays Unknown(2) only because the concretizing ZEXT dies (RuleSubvarZext, faithful) because the 2nd RETURN recovers VOID (return-trial prune) where ISOLATED GHIDRA RETURNS the 8-byte constant.** ⇒ E1045 lever = the RETURN-RECOVERY CADENCE, and partialsplit is a GENUINE isolated-oracle divergence (NOT the closed f23b362 beat-Ghidra case — that only covered the minimal bare-call drop). Prior Brick-0b finding un-refuted here: Ghidra ActionReturnRecovery INSIDE mainloop deferred via ParamActive maxpass; mosura resolve_return ONCE pre-fullloop RETURN_MAXPASS=0 (recover.rs:544) = suspected MIS-PORT. **SEQUENCE: agent finishes E1018 (dropped-goto-target structurer) → grounds Ghidra's actual maxpass/cadence values from source → lead gates the cadence build (corpus WILL move; partialsplit toward-oracle = acceptance probe; also dents the 626-fn extraout_ class).** All array-typing machinery confirmed faithful do-not-touch (type_order/preferred/gatherOpen/RulePtrArith/AddTreeState/get_local_type/try_return_pull).

LEAD-LANDED (2026-07-24, agent still rate-limited, master now `09f23c6`):
- **`804d274`: isIndirectCreation guard** (TypeOpIndirect::propagateType) — byte-neutral faithful missing-rule; unit test.
- **`09f23c6`: E1063 marker-leak FIXED** — for-recovery took the loop phi's out-of-loop input as the for-init unconditionally; ported Ghidra findInitializer (block.cc:3223) checks (written init's def must be NON-MARKER, in the pre-loop block, sizeOut==1). No more `for (n=MULTIEQUAL(...);...)`. ALL 5 flagged WAR2 leak fns → 0 markers (verified via --le). Corpus byte-neutral 0.9516/57; gated by self-compiled Watcom repro oracle/ground-truth/src/forphi.c (test war2_forphi_no_marker_leak; pre-fix leaks, post-fix clean) per war2-issues rule. **E1063 class (~4) CLEARED.**

SWEEP MAP of the ~77 actionable COMPILE_FAILs (agent, 2026-07-24, grounded each w/ reproducers) — NO independent shallow type-inference win left (Brick 1 was that lever); task #5(a) IS the ROI harvest:
- **E1045 ×12 + E1080-resid ×5** = array-element typing = task #5(a) family (+ Brick 2 unblocks) → ~17-22 fns off the ONE bounded #5(a) fix. HARVEST FIRST.
- **E1063 ×4** = MULTIEQUAL/INDIRECT markers LEAK to C (raw_marker class; merge/SSA-out or indirect-removal pipeline gap, NOT printc). CORRECTNESS defect → NEXT after #5(a) (bounded, ground-first, ground-truth-gated).
- **E1010 ×16 + E1081 ×6** = concrete-pointer/AGGREGATE type lattice (cast.rs "primitive lattice only; struct/enum/var-length deferred"). DEEP foundation. Plan-first.
- **E1029 ×17** = directional read/def-facing types (mosura has NONE; Varnode::ty single Option). DEEPEST foundation. Plan-first.
- **E1018 ×4** = goto→continue (structurer/for-recovery, ties task19-p7). Medium, defer.
- **E1090/E1082 ×1** misc.
SEQUENCE (lead): #5(a) → confirm E1045 + re-measure → E1063 → then GROUND E1010/E1081 (aggregate) vs E1029 (directional), ROI+depth+brick-plan each, lead gates which deep foundation opens. Plus the ~35 E1052 verified-faithful ceiling already reclassified out.

HONEST CEILING (executive assessment): byte-exact recompilation of arbitrary OPTIMIZED Watcom output is bounded by codegen/regalloc matching, which is largely outside decompiler faithfulness (mosura/Ghidra don't control wcc386's register allocation). Correct+idiomatic C gets small/medium functions to EXACT; heavily-optimized ones won't without a recompiler-aware emitter or beating Ghidra (forbidden). Strategy: drive the faithful foundations (type inference → correct compilable C) maximally to raise the EXACT count, and enumerate what is not decompiler-reachable rather than force it. Oracles available: analyzeHeadless (BUILT, /data/tools/ghidra_12.0.3_PUBLIC/build/dist/ghidra_12.0.3_DEV/support/) + oracle/capture.

Baseline @04d0165: EXACT 1 / MISMATCH ~1056 / COMPILE_FAIL 137 / DECOMPILE_FAIL 0 (post Stage 0-2, [[war2-recompile-survey]]). Corpus 0.9513/57, suite 559/0, clippy 0. Checkpoint doc: docs/war2-typeinfer-checkpoint.md.

⚠️ MEASUREMENT RULE (2026-07-24): `war2-survey/compile.sh` does NOT clean obj/ between runs — stale .OBJ hide new failures. EVERY WAR2 measurement must `rm -f obj/*.OBJ` first. Clean-measured baseline = **139** COMPILE_FAIL (the documented 137 was stale-undercounted). results.prefix/postfix.tsv in war2-survey/.

PROGRESS:
- **Brick 1 ✅ LANDED `8a86b73` (lead-gated, verified; suite 560/0, corpus 0.9516@8a86b73):** ported Ghidra base `TypeOp::getInputCast` = castStandard(inputTypeLocal, cur, false, true) for the 7 non-overriding arith/logic ops (IntAnd/Or/Xor/Negate→UINT, IntSub/Mult/2Comp→INT; verified against typeop.cc + constructors). **WAR2 COMPILE_FAIL 139→112 (−27): E1079 33→11, E1080 12→5, E1036 3→0; conservation clean, zero new failures; a few secondary errors unmasked (honest).** Corpus 0.9513→0.9516 (only impliedfield +0.016 toward-oracle: adds float→int cast Ghidra emits; 59 byte-identical, zero wrong-code). Differential proved compiler-independent (gcc + analyzeHeadless). Lead independently re-verified Ghidra source before gating.
- **E1052 (~35, void indirect-call result) = CONFIRMED HONEST CEILING:** Ghidra emits the identical `iVar=(*(code*)p)()` and it fails gcc too → documenting verified-faithful (decompiler-nonbug-* pattern), reclassified OUT of the actionable set.
- **E1052 ceiling doc LANDED `3252b0e`; checkpoint `a508557`.** Clean COMPILE_FAIL now 112; minus ~35 E1052 ceiling → **~77 actionable**. Brick 1 exhausted the shallow input-cast lever.
- **Brick 2 (IntAdd/IntLeft) HELD (not reverted) — producer-before-consumer:** faithful (differential: stackstring→exactly Ghidra) but fires on an INT_ADD Ghidra restructures away via PTRADD→array-index; net corpus flat. Lands cleanly AFTER the array-index foundation.
- **E1029 (17) DEEP:** mosura has NO directional type model (Varnode::ty = single Option<Datatype>, no getHighType{Read,Def}Facing); Ghidra's TypeOpLoad::getInputCast inserts `*(T*)*p` from the read-facing scalar type. Needs the directional-type foundation. Agent correctly did NOT add a misfiring render heuristic.
- **Array-index brick GROUNDED to root cause `3425553` (agent stopped per depth valve):** Ghidra renders `auStack_58[i]` via checkArrayDeref (printc.cc:353) on a PTRADD LOAD-address; RulePtrArith/AddTreeState forms the PTRADD — mosura ALREADY ports these (ptrarith.rs) but AddTreeState bails (calc_subtype nonmult) because the stack base PTRSUB(RSP,-0x58) pointee is Unknown(8) not the 2-byte access element. (Also corrected: use `oracle/capture --ir -` for FINAL IR; default `--ir` breaks pre-type-inference.) Agent correctly did NOT force element-size in ptrarith (would mask the typing gap).
- **TASK #5 GROUNDED DECISIVELY `454bfea`/`4e17de4` (agent, Ghidra-verified):** the varmap hint-reconcile core (RangeHint::preferred, Datatype::compare/typeOrder — bigger-Unknown wins) is a FAITHFUL port — DO NOT TOUCH. Root cause is UPSTREAM: mosura generates a size-8 Open hint at -0x58 that Ghidra never generates, by spuriously typing the untyped call-arg stack pointer as `Pointer(Unknown(8))` (Ghidra's gatherOpen gets NULL/2 for that arg). partialsplit: mosura `xunknown8 axStack_58[2]` vs Ghidra `uint2 auStack_58[10]`. TWO candidate causes needing OPPOSITE fixes, must disambiguate before code: (a) iterative FIXPOINT (prior element-8 array → pointer pointee 8 → hint 8) → remove the spurious typing, match Ghidra NULL/2 (corpus-wide risk: don't regress legit undefined8* args); (b) MISSING Ghidra LoadGuard/addGuard element hint seeding element-2 → port that guard. Lead gate: agent grounds a/b via analyzeHeadless type-dump + mosura instrumentation + Watcom/gcc MVE, reports verdict, I gate the DIRECTION before code (ripples corpus-wide). Payoffs unchanged: E1045 ×12 + Brick 2 + partialsplit.
- **TASK #5 ground to its true root `f5a0694` = TASK #19 (value-set/LoadGuard), a DEEP FOUNDATION.** Ghidra-source-verified (lead re-checked addGuard varmap.cc): element size comes from MapState::addGuard reading each indexed LOAD/STORE's LoadGuard step (from value-set) — mosura explicitly omits LoadGuard/discoverIndexedStackPointers (varmap.rs:447, heritage.rs:1392 "Task #19"). Chain: value-set (ValueSetRead, LoadGuard::establishRange) → discoverIndexedStackPointers (heritage.cc:915/932) → addGuard. type_order/preferred/gather_open CONFIRMED faithful, do-not-touch. **SHORTCUT REJECTED (non-faithful adaptation, not in any lane).**
- **✅ DISAMBIGUATED (agent, 2026-07-24): verdict (a) — task #5 does NOT need value-set/LoadGuard (task #6 STOOD DOWN).** 3 evidences: (1) mosura ALREADY collects the correct `size=2 Unknown(2)` hint at -0x58; it's beaten by a SPURIOUS `size=8 Unknown(8)` hint whose base descends only to an INDIRECT (call-clobber marker of `func(auStack_58,...)`) typed Pointer(Unknown(8)); suppressing it → mosura emits EXACTLY Ghidra (`xunknown2 axStack_58[10]` + `return axStack_58[i]`); (2) corpus 0.9516→0.9518 toward-oracle (general, not partialsplit-specific); (3) gcc MVE → Ghidra makes element-2 array despite call-clobber = class-A GENERAL. FIX = remove the spurious hint the FAITHFUL way (match WHY Ghidra doesn't generate it), NOT the "skip INDIRECT-only base" probe. **Lead GATE CONDITIONS: (i) pin the exact origin of the spurious Pointer(Unknown(8)) — CALL param model / INDIRECT-output default / unconstrained-pointer default; (ii) name Ghidra's differing gatherOpen/pointer-typing behavior + port THAT; (iii) REGRESSION GUARD: prove no drop of hints for legit undefined8* stack args (corpus + a real-8-byte-element MVE).** Agent pins the line → faithful fix → gate request with source refs + corpus delta + no-regression evidence.
- **KEY LESSON: gating the DIRECTION before the build turned a would-be multi-session value-set foundation into a bounded remove-spurious-hint fix.** The excavation (4 layers) was NOT wasted — it produced the decisive root cause. Task #6 filed-not-critical (only the heritage.rs:1392 usesSpacebasePtr STORE-alias disjunct still wants it). After this fix: E1045 ×12 + Brick 2 + partialsplit unblock; then next COMPILE_FAIL class. Pivot to task #2 codegen still REJECTED (unmeasurable until compile). THE critical-path fix: type the stack local as array-of-element-N matching the access so the PTRSUB pointee matches → existing AddTreeState forms the PTRADD unchanged. Faithful port of TypeSpacebase::getSubType + MapState/RangeHint access-pattern array inference (varmap.cc) + ActionInferTypes; sits on task22 infra ([[task22-typespacebase-campaign]], #22-A+B landed). Ground+name-source first; differential (class-A general); staged gated bricks; corpus will move (per-brick gate). DEPTH VALVE: needs persistent HighVariable directional-type model OR varmap/ScopeLocal rewrite → STOP+report before opening. Payoffs: E1045 ×12 + Brick 2 + partialsplit. Pivot to task #2 codegen REJECTED (honest-ceiling wall, unmeasurable until functions compile).

## RESUMED 2026-08-03 after the retirement track's audit stretch (master `5875488`)
**The agent volunteered the honest read AGAINST ITS OWN WEEK'S WORK, and the lead adopted it: "the audit work has been high-yield for correctness but has not moved the campaign's headline gauge, and that gap is now the most honest thing on the board." BYTE-CLEAN HAS SAT AT 16 ALL SESSION — every commit HELD the set, none MOVED it.**
**LEAD DIRECTION: START BY RE-MEASURING, NOT BY PICKING A TARGET.** The byte-clean 16 and its member set have been carried since `9f1c577` with four docs-only commits since, and the last full recompile predates several ports. **Get a fresh recompile and a current EXACT/RELOC_EXACT set FIRST**, then choose the nearest miss with the method that worked all week — specimen · oracle-routed grounding · reach measured before code. **If the number moved on its own that is information; if it did not, the near-miss list tells us what is actually in the way.**

## ⭐ RE-MEASURED AT HEAD `6257542` (2026-08-03) — the 16 HOLDS, and the near-miss population REFRAMES the campaign
**Drift check first: a fresh EMIT at HEAD is BYTE-IDENTICAL to the standing `src/` across all 1303 files, prelude included — the eight docs/probe commits since `9f1c577` were genuinely inert.** Full recompile+compare at HEAD: **EXACT 5 · RELOC_EXACT 11 · MISMATCH 1242 · COMPILE_FAIL 45 · DECOMPILE_FAIL 0 — per-function status IDENTICAL to the carried set. THE 16 IS NOW MEASURED AT HEAD, NOT CARRIED.** Snapshot `results.carried-9f1c577.tsv`.
**⚠️ THE FIRST NEAR-MISS RANKING WAS ON THE WRONG POPULATION — the "read what the predicate literally tests" lesson again.** `compare.py` has TWO mismatch branches: length-mismatched functions report `%match`, same-length ones report `%comparable-match`. Ranking on `%match` put `FUN_000790c0` top at "100%match" — but `cand=6` vs `orig=16`, a **TRUNCATED** function, not a near-miss.
**⭐⭐ THE TRUE NEAR-MISS POPULATION IS TINY: only 27 of 1303 functions compile to the CORRECT LENGTH with wrong bytes** (+ the 16 byte-clean = **43 at correct length**; the other **1260 are the WRONG LENGTH ENTIRELY**). ⇒ **THE GAP IS MOSTLY SHAPE, NOT BYTES** — that reframes what the campaign is actually chasing.
**TARGET `FUN_00063c06` (95%, first diff `@+31`) — and Rule Zero already pays: THE NEAREST MISS IS BLOCKED BY WRONG CODE, NOT CODEGEN POLISH.** Ghidra re-tests both loop conditions each iteration (`while(true){ if(p==0) return; if(eax==p[1]) break; p=*p; }`); **mosura hoists BOTH out** (`if(p==0) return -1; iVar2=p[1]; while(param_1!=iVar2){ p=*p; }`). **Two wrong-code consequences from ONE defect: (a) mosura DEREFERENCES A NULL `pxVar1` on the second iteration; (b) `iVar2` is LOOP-INVARIANT so the loop cannot terminate except on the first test.** Same silent family as the 4× mis-scaling — **invisible to every gate we run.** **(c) NOT BUNDLED:** Ghidra calls `func_0x00063c35()` with no args returning `CONCAT44` (8 bytes), mosura passes two args and returns 4 — the known **call-site-vs-callee prototype class, blast radius every WAR2 call.**
**ORACLE CAVEAT CHECKED EXPLICITLY (structural comparisons are exactly where it bites): this used `ghidra-all.txt`, and the absent-callee bias does NOT apply — `FUN_00063c35` is present in the same sweep, decompiled as `(void)`. The structural comparison is sound.**
**NEXT, NOT YET DONE: ground mosura's CFG for `00063c06` (`MOSURA_CFG=1`/`MOSURA_STRUCT=1`, SEGMENTED BY FUNCTION — a bare dump interleaves every function `analyze_le_file` touches). NO CODE BEFORE THAT.** Agent's own reason for stopping: *"What I have is a solid DESCRIPTION read from two C outputs; the MECHANISM is unmeasured, and this session's record on diagnosing from a reading rather than an instrument is exactly why I'm stopping here to measure."*

## 🎯 ROOT CAUSE of the nearest miss `FUN_00063c06` — `f_whiledo_overflow` IS NOT PORTED AT ALL (`ab9cd8f`, 2026-08-03)
**LOCALIZED BY ELIMINATION, each layer INSTRUMENTED not reasoned:** CFG (`MOSURA_CFG=1`) ✅ correct — `blk1 ins=[0,4]` a proper loop header, exactly Ghidra's shape · structure (`MOSURA_STRUCT=1`) ✅ correct — `WhileDo[7,4]` where component 7 is a multi-block `List` = a COMPLEX CONDITION · **printer ❌ THE DEFECT.**
**GHIDRA:** `PrintC::emitBlockWhileDo` (printc.cc:3017) branches on `hasOverflowSyntax()` — if set, `while(true){ <cond stmts>; if(test) break; }`; else `while(cond){`. The flag is set at blockaction.cc:1545 when `bl->isComplex()`. **MOSURA:** grep for `overflow_syntax|whiledo_overflow` across `decompile/` is **EMPTY — `f_whiledo_overflow` is not ported**, so mosura ALWAYS takes the non-overflow arm: the complex condition block's statements emit ONCE, BEFORE the loop, and only the final test becomes the `while` condition. **That is exactly the observed wrong code, and it explains BOTH consequences (NULL deref on iteration 2; loop can't terminate) from a SINGLE missing flag.**
**PORT TARGET, FOUR PARTS — (iii) is the trap:** (i) the `f_whiledo_overflow` flag · (ii) set it at `WhileDo` construction when the condition `is_complex()` (mosura already has `is_complex`) · **(iii) ⚠️ THE SAME SITE CHANGES THE NEGATION DECISION — `if ((i==0)!=overflow) negateCondition(...)`; porting flag+printer WITHOUT this changes which branch gets negated. blockaction.cc:1539-1545 MUST BE READ AS ONE UNIT** · (iv) the printer's overflow arm including the `break` emission.
**SEQUENCE (not started, not to be skipped): reach measured FIRST (how many WhileDo blocks have a complex condition) → expectation pre-filed → full battery.** This is a STRUCTURING+EMISSION change ⇒ blast radius potentially large ⇒ **`reached==cfg` / undefined-label / falls-off-end are the gates that matter most**, unlike the small bounded ports so far.
**⭐ `MOSURA_STRUCT` COULDN'T ANSWER A PER-FUNCTION QUESTION AT ALL** — `analyze_le_file` structures 1639 functions on WAR2 and the trace lines carry only block numbers, which repeat; reading it per-specimen would have meant correlating on dump ORDER = **exactly the fabricated measurement this project keeps catching.** Fixed in `ab9cd8f`: one header line matching `MOSURA_CFG`'s key so the two dumps segment identically.
**NOT BUNDLED:** same function ALSO shows the call-site-vs-callee prototype disagreement (Ghidra calls callee no-args returns `CONCAT44`; mosura passes two, returns 4) — **blast radius every WAR2 call, its own item.** Oracle caveat CHECKED: `ghidra-all.txt`, absent-callee bias does NOT apply — callee present in the same sweep, decompiled `(void)`.
**⚠️ AGENT HIT ITS SESSION LIMIT after this commit (resets ~22:30 Paris). Full detail in task #9. Nothing lost — no code was written, diagnosis is complete and durable here.**

## ⚠️ AGENT COLLISION on task #9 (2026-08-03) — RESOLVED, and a discrepancy surfaced that gates the port
**LEAD ERROR: spawned `war2-guard-2` believing `war2-guard` was session-limited; both were live in the SAME working tree.** `war2-guard` wrote the `f_whiledo_overflow` port (printc.rs +23, structure.rs +34 — all four parts incl. the negation `((i==0)!=overflow) ^ is_oriented(b)` and the `finalTransform` for-loop decline); `war2-guard-2` was doing the read-only reach measurement, **refused to write port code into a contended tree, removed its own probe so it couldn't contaminate the commit, and flagged the collision** rather than working around it — correct behaviour. **RESOLUTION: lead stopped `war2-guard`; `war2-guard-2` owns task #9 as the single agent.** Tree builds clean at HEAD `ab9cd8f` + the uncommitted port (let-chain already nested). **The inherited port is to be VERIFIED AS IF written by the owner — faithful enough to land or rewritten; not adopted on trust because it compiles.**
**⭐⭐ THE GATE BEFORE ANY BATTERY — a ~2× DISCREPANCY, and one branch of it is a SECOND DEFECT:** mosura-side reach probe (at `bl->isComplex()`, blockaction.cc:1538, naming its function) = **52 functions / 318 sites** take the complex-condition overflow arm; the inherited doc comment asserts **Ghidra takes it on 110 functions / 146 sites.** Two explanations: (a) the two probes measured different things (Ghidra's structuring vs mosura's; or output-text vs decision-site) ⇒ one number is wrong-to-keep and must be removed/relabelled; **(b) mosura's `is_complex`/collapse genuinely disagrees with Ghidra's on ~58 functions ⇒ A SEPARATE DEFECT, and the port would emit the overflow form on a DIFFERENT loop set than Ghidra = new wrong code on the difference set ⇒ THE PORT DOES NOT LAND UNTIL UNDERSTOOD.** Settle with an instrument first.
**⭐ THE REACH MEASUREMENT (war2-guard-2, sound, complementary — NOT duplicated by the port):** 52 of 1303 fns have ≥1 complex-condition WhileDo (null result distinguishable: 972 no-WhileDo, 279 WhileDo-but-not-complex, 331 have a WhileDo at all). Shapes: `List` 148, `Basic` 112, `CondOr` 58 — the multi-block `List` case the diagnosis named is the plurality. **`FUN_00063c06` IS in the reach set (diagnosis confirmed empirically).** **SET-INTERSECTION GATES: reach ∩ byte-clean-16 = ∅ (cannot lose a byte-clean fn); reach ∩ the 27 same-length near-misses = exactly {FUN_00063c06}.** ⇒ **the ENTIRE upside is that one function; the entire downside is the structuring of the other 51 reach functions ⇒ `reached==cfg` / undefined-label / falls-off-end across all 1303 are THE gates.** Datatest (79-fixture) reach still UNMEASURED (was blocked by the compile error, now unblocked).
**IF `FUN_00063c06` REACHES EXACT/RELOC_EXACT: byte-clean 16→17 — first headline movement since the pivot.**

## COLLISION RESOLVED + DISCREPANCY RECONCILED (2026-08-03) — consolidated to ONE agent (war2-guard-2)
**⚠️ OPERATIONAL MECHANIC THAT CAUSED THE COMPOUNDING: MESSAGING A STOPPED AGENT REVIVES IT.** war2-guard-2's message to the session-limited `war2-guard` resumed it, so a stopped agent came back live in the shared tree. **RULE: to keep an agent stopped, DO NOT message it.** Lead stopped `war2-guard` again; **war2-guard-2 owns the port AND the main tree as the single agent — no second worktree (two agents in two worktrees still violates the one-agent rule).** The `isComplex` disagreement is FILED, not worked in parallel.
**⭐ THE 52-vs-110 DISCREPANCY WAS A COUNTING TRAP — `grep 'while( true )'` MATCHED TWO LOOP FORMS.** 146 grep lines = **98 `while( true ) {`** (BlockWhileDo overflow, `emitBlockWhileDo` printc.cc:3017 — THE ITEM) + **48 `} while( true );`** (BlockInfLoop, `emitBlockInfLoop` printc.cc:3097 — a DIFFERENT construct mosura ALREADY ports as `FlowKind::InfLoop`). 110 was the distinct-function UNION of two unrelated constructs. **THE GHIDRA FIGURE IS 98 SITES / 71 FUNCTIONS.** (Doc comment + task text carried the merged 146/110; corrected additively, original inline.)
**SET COMPARISON Ghidra 71 vs mosura-would-be 52:** agree **41** · Ghidra-only **30** (26 mosura structures a WhileDo but `is_complex`=false, 4 mosura structures no WhileDo there) · mosura-only **11** (1 a pure coverage artifact = one of the 17 new fns; **10 genuine**). ⇒ **the overflow arm will UNDER-TRIGGER by ~1/3 after landing — a smaller-than-hoped corpus move is NOT the port misfiring.**
**⚠️ THE PORT'S REAL RISK SURFACE = the 10 mosura-only functions** — where the port makes **mosura emit overflow where Ghidra does NOT** = the "new wrong code on the difference set" direction. None byte-clean (∩ = ∅), none a near-miss ({FUN_00063c06} only), all already-MISMATCH — **but a structuring defect could still appear there ⇒ THE 10 ARE THE NAMED WATCH SET for the battery; a `reached==cfg`/undefined-label/falls-off-end move there BLOCKS the land even though the target improves.**
**⭐ `isComplex` CERTIFICATE (war2-guard-2, SCOPED): line-by-line faithful vs `BlockBasic::isComplex` (block.cc:2388-2442) — the `sizeOut()>=2` seed, marker skip, call arm, `max_implied_ref=2` (architecture.cc:1420) ⇒ `descend.len()>2` ≡ Ghidra's break-loop, the out-of-block/marker descendant test, and `isFlowBreak()` = exactly `{BRANCH,CBRANCH,BRANCHIND,RETURN}` hardcoded correctly. SCOPE: a SOURCE-LEVEL reading of the predicate ONLY — no differential probe on a specimen — so it rules out the PREDICATE, not the INPUTS.** ⇒ the ~26 disagreement is UPSTREAM (different IR or block partition) = a genuinely SEPARATE read-only item, filed for AFTER the port. **`FUN_00063c06` is in BOTH sets — Rule Zero satisfied for the target from Ghidra's own output.**


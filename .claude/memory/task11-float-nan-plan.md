---
name: task11-float-nan-plan
description: "Task #11 Float/NAN cluster — diagnosed decomposition (F1-F4 + defer) + Ghidra grounding; gated plan sent to lead 2026-07-04"
metadata:
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #11 = Float/NAN cluster (owner ptype, in_progress). Lead-picked big target after P4 plateau (C1/C3 landed [[task2-p4-types-grounding]]). Base HEAD 840ed3c, 207 green, corpus 0.8720. Plan-before-code. Diagnosed via throwaway diag (mosura vs oracle --c on nan/floatconv/floatcast/floatprint/longdouble/mixfloatint; deleted).

KEY DIAGNOSIS — nan 0.378 (corpus floor) is NOT printing, it's the **ucomisd FLAG-TANGLE**: x86 ucomisd sets PF/CF/ZF; mosura never simplifies that flag web so `NAN(a)||NAN(a)` + `!(bVar||a<b||(bVar||a==b))` never collapse to Ghidra's bare `NAN(param_1)`/`param_2 < fRam...`. mosura's OWN rules.rs comments already call it "the known nan gap" (lines 1979-1982, 3675). DEEPEST item, its own sub-track — NOT first.

DECOMPOSITION (tractability×leverage; gated plan sent to lead, awaiting go):
- F1 FLOAT CAST NAMING: printc.rs:745 renders FLOAT_INT2FLOAT/FLOAT2FLOAT casts via `float_name()` → `float`/`double`/`longdouble`, but Ghidra + mosura's own DECLS use floatN (Datatype::name()=`float{n}`). One-line fix (Datatype::Float(sz).name()) + delete dead float_name. Broad: floatconv `(float)`→`(float4)`, floatcast/mixfloatint `(double)`→`(float8)`, longdouble `(longdouble)`→`(float10)`. Tiny, faithful-to-oracle. FIRST.
- F2 FLOAT CONST LITERAL `0`→`0.0`: floatprint `/ 0`→`/ 0.0`. C1 now types FLOAT_DIV operand Float, so render_var can format float-typed const via Ghidra push_float (printc.cc:1380: FloatFormat getHostFloat→printDecimal force `.0`; INFINITY/NAN tokens). Unblocked by C1. Medium.
- F3 FLOAT CONST FOLDING → NaN/Inf: floatprint `0 / 0` should fold to `0xffc00000`(f4)/`0xfff8000000000000`(f8). Ghidra folds FLOAT_DIV(0,0)→NaN bits, prints hex INT (target type resolved int→xRam, not float→fRam). mosura lacks float const-folding. A rule. Medium.
- F4 nan ucomisd flag-simplification + float-compare boolean collapse: Ghidra RuleIgnoreNan/RuleFloatRange (+RuleFloatCast/FloatSign/FloatSignCleanup) + the flag-web simplification. DEEPEST, own multi-commit sub-track, diagnose flag IR first.
- DEFER (NOT float-specific): XMM 16-byte SUBPIECE/CONCAT modeling (floatconv CONCAT124, floatcast CONCAT88(0,..)) = SubVariableFlow #9/#15 (held), partial-register work.

PROGRESS 2026-07-04 (agent ptype):
- F1 LANDED `6d532c7`: printc.rs float cast → Datatype::name() floatN (deleted dead float_name). +0.0031: mixfloatint 0.833→0.967, impliedfield 0.892→0.923, union_datatype 0.937→0.955, 0 down. corpus 0.8720→0.8751.
- F2+F3 LANDED `06d8509` (ONE commit, lead-chose granularity): NEW float.rs (minimal FloatFormat: get_host_float/to_host/encode/print_decimal/push_float — Rust {} = Ghidra shortest-round-trip printDecimal + force `.0`; +3 unit tests) ; printc.rs render_var routes Float-typed constants → float::push_float ; mod.rs `pub mod float` ; rules.rs eval_const gains FLOAT_* ops (decode/compute/re-encode host f64 = OpBehaviorFloat*::evaluate). RESULT: **floatprint 0.926→1.000 BYTE-EXACT** (xRam...=0xffc00000 / 0xfff8000000000000 — host x86 FPU NaN bits = Ghidra). corpus 0.8751→0.8763 (+0.0012), only floatprint moved, 0 regress, 210 green. KEY: F2 ALONE net-NEG (float-ifies unfolded 0.0/0.0); F3 folds FLOAT_DIV(0,0)→NaN const so global loses float type → renders int hex like Ghidra. Bounded: NaN bit pattern host-FPU-dependent (matches x86=Ghidra).
- F4 DIAGNOSED 2026-07-04 (sub-plan sent lead bdf91c16, awaiting go on F4a). KEY (corrects "deep flag-tangle" fear): dumped nan mosura IR — flags ALREADY cleanly lifted to FLOAT_NAN/FLOAT_EQUAL/FLOAT_LESS + BOOL_OR/NEGATE (NOT raw bit-tangle). 2nd compare IR = `!((NAN(a)||NAN(b)||a<b) || (NAN(a)||NAN(b)||a==b))` → should collapse to `b<a` (Ghidra `param_2 < fRam...`). Gap = 2 MISSING Ghidra collapse rules:
  - F4a RuleFloatRange (ruleaction.cc, oppool1 @5613): BOOL_OR(FLOAT_LESS,FLOAT_EQUAL)→FLOAT_LESSEQUAL; BOOL_AND(FLOAT_LESSEQUAL,FLOAT_NOTEQUAL)→FLOAT_LESS, same operands. Self-contained op-rewrite, deps present. FIRST.
  - F4b RuleIgnoreNan (ruleaction.cc @5635): drop redundant NAN(x) OR'd/AND'd-negated with a compare of x. Needs testForComparison+checkBackForCompare+isAnotherNan; deps present (functional_equality_level0 rules.rs:3024, is_bool_output op.rs:72; only need trivial is_floating_point_op predicate). Port BOOL_OR/AND/NEGATE+INT_EQUAL cases first; testForComparison's CBRANCH case crosses CFG (getParent/getOut, handles nan's op23/28) — assess need after.
  - F4c reassess residual: idempotent NAN(x)||NAN(x), x==x isnan idiom, uVar1 select→bool (CONCAT44/MULTIEQUAL) — after F4a/F4b.
  Order FloatRange→IgnoreNan (Ghidra oppool1). Blast LOW (float-compare bool webs only: nan, maybe pointerrel/floatcast). Iterative+gated; nan may not fully close in one pass. IR-dump harness: throwaway test calling f.print_raw() (funcdata.rs:595).
- F4a LANDED `f3ba38a`: RuleFloatRange (rules.rs, oppool @102 between LessEqual/PopcountBoolXor). BYTE-NEUTRAL — inert until F4b strips the interposed NANs (RuleAddUnsigned-dormant pattern), self-approved per byte-neutral rule.
- F4b BUILT, 210 green, **NOT committed — awaiting lead gate** (reported aa646479). Tree UNCOMMITTED = EXPECTED WIP (mine): rules.rs (RuleIgnoreNan + helpers test_for_comparison/check_back_for_compare/is_another_nan + is_float_op + slot_of; reused existing functional_equality; DEFERRED testForComparison CBRANCH/CFG case), pipeline.rs (register RuleIgnoreNan @124 after OrCompare + import), tests/decompile_corpus.rs (updated pointerrel_negated_condition_stays_compact — old `!(bVar1 ||` form now stale, RuleIgnoreNan collapses NAN sub-bool → new faithful `!(fStack_18 <= fRam...)`). DELTA: avg 0.8763→0.8798 (+0.0035), nan 0.378→**0.500** (corpus floor +0.122), pointerrel 0.843→0.930 (+0.087), 0 down. nan 2nd compare & pointerrel cond both collapsed to clean `!(a<=b)` = Ghidra `b<a` modulo the FLIP.
- F4b LANDED `a5df51a` (lead GO): RuleIgnoreNan + 3 helpers + is_float_op + slot_of, oppool @124; CBRANCH case DEFERRED; pointerrel test updated. nan 0.378→0.500, pointerrel 0.843→0.930.
- F4c-1 LANDED `a2e3ad9` (lead GO): RuleBoolNegate (rules.rs:963, oppool 98) had only INT variants; Ghidra's supports float. Added 4 FLOAT cases → nan 0.500→0.538 (2nd CALL-ARG `!(fRam<=param_2)`→`param_2<fRam`), 0 down, avg 0.8804. pointerrel IF-cond stays `!(...)` (CBRANCH cond→structurer negated-render = Task#18/P8, separate).
- **FLOAT/NAN CLUSTER (Task #11) SUBSTANTIALLY DONE** (lead 2026-07-04, decision b). Landed: F1 `6d532c7`, F2+F3 `06d8509`, F4a `f3ba38a`, F4b `a5df51a`, F4c-1 `a2e3ad9`. Corpus 0.8715→0.8804. DEFERRED (tracked follow-up, NOT declined): nan first-block = RuleIgnoreNan testForComparison CBRANCH/CFG case (needs block API getParent/getOut/isBooleanFlip/lastOp) + idempotent double-FLOAT_NAN(param_1) dedup (ops 13/14 not CSE'd) + select→bool (uVar1 CONCAT44/MULTIEQUAL) — single-fixture (nan 0.538), CFG-heavy, diminishing returns. NEXT = Task #2 P6 Prototypes (see [[task2-p6-prototypes-plan]] once grounded).
- F4c-2 REMAINDER (nan 0.538, LAST piece): nan FIRST block = NaN guard split across 2 CBRANCHes (NAN(param_1)→CBRANCH op23; !(param_1==param_1)→CBRANCH op28) + idempotent double FLOAT_NAN(param_1) (ops 13/14, not CSE'd) + select→bool (uVar1=0/1 CONCAT44/MULTIEQUAL). = the DEFERRED testForComparison CBRANCH/CFG case (needs block API getParent/getOut/isBooleanFlip/lastOp). In-cluster, CFG-heavy, single-fixture. Recommended to lead: land F4c-1 then EITHER (a) port CBRANCH case OR (b) declare cluster done→P6 (lean b, diminishing returns).
- FLOAT CLUSTER TALLY: F1 6d532c7, F2+F3 06d8509, F4a f3ba38a, F4b a5df51a landed; F4c-1 gating. Corpus 0.8715→0.8804. floatprint byte-exact, mixfloatint 0.967, nan 0.378→0.538, pointerrel 0.930.
- (prior) F4c RESIDUAL (nan still 0.500): FIRST block = idempotent `NAN(x)||NAN(x)` not deduped + `(!NAN)&&(x==x)` isnan idiom + uVar1 select→bool (CONCAT44/MULTIEQUAL); PLUS both keep `!(a<=b)` where Ghidra flips `b<a` (opFlipInPlaceTest for negated single-if/call-arg = SEPARATE normalization, not float-cluster). Reassess after F4b lands.

RECOMMENDED: F1→F2→F3 gating corpus-movers; F4 separate diagnosed sub-effort. Also note: longdouble also has global-naming gap (`*0x101000` vs `fRam...=`) = P8/task#6, + CSE (fVar1 vs repeated) — orthogonal to float cluster.

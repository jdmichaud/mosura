---
name: task20-defuse-divopt-plan
description: "Task #20 de-fuse RuleDivOpt — grounded plan + gated commit order; sanctioned full faithful de-fusion"
metadata: 
  node_type: memory
  type: project
  originSessionId: c0fe6b35-0fb2-4ed2-90d8-ec93de63680c
---

Task #20 = de-fuse mosura's FUSED RuleDivOpt into Ghidra's SEPARATE RuleDivTermAdd + RuleDivOpt/ModOpt, then wire RuleDivTermAdd (slot 83). Base HEAD `ac105ed`. Owner=ruleport, in_progress. See [[task2-tailrules-progress]], [[port-all-faithful-rules]].

LEAD MANDATE (2026-07-02, RELAXED): full faithful de-fusion sanctioned INCLUDING replacing the non-faithful fused recognizer (try_unsigned/match_mulhi) with Ghidra's separate structure. No adaptation grandfathered. Keep tests green, REPORT corpus before each output-changing commit, land ONLY on lead's OK. After #20 lands: PING lead (do NOT auto-continue); next will be Task #4 (faithful heritage guard/collect, adaptation-collision now resolved in faithful direction), then #7.

✅ MERGED TO MASTER — TASK #2/#20 COMPLETE (2026-07-07, rock5). Lead gated (A)=merge; ff-only `3225f2b..071886c` on `/home/jd/projects/mosura/mosura`, worktree removed + branch deleted. Master post-merge VERIFIED: suite 334/0, corpus **0.8865** (net-positive vs 0.8864), all switch build.rs (4) + jumptable_recovery (6/6) green. 7 commits: iter-2 `6575e52` + `cd0fd9e`/`4f19ab6`/`b5e3df8`/`2b22f65` (keystone) + `c8e5d51` (dumpc grounding tool) + `071886c` (coverage.md cluster flip). ROOT CAUSE that unblocked 2 prior attempts: RuleSelectCse isCseMatch matched constants by SIZE not VALUE (blocked the sign `x s>>63` #0x3f:4-vs-:8 CSE) + binary CollectTerms couldn't span `(SDIV+s)-s` — NOT Sub2Add placement. ONLY residual below baseline: modulo -0.012 = Task #9 (64-bit signed 65-bit-magic; DivTermAdd folds +2^64). NEXT per lead: task #4 (comparison-normalization, now the top corpus lever at noforloop_alias/varcross/elseif) — lead briefs separately; do NOT auto-continue. --- prior checkpoint below (superseded):

ITER-3 DONE + NET-POSITIVE (2026-07-07, rock5, worktree defuse-divopt on top of iter-2 `6575e52`, HEAD `2b22f65`). Corpus **0.8865 vs baseline 0.8864 = NET-POSITIVE**, suite 334/0, all switch tests green, 54/60. FINAL per-fixture vs baseline: ONLY `modulo -0.012` below (= Task #9, the 64-bit signed 65-bit-magic case, lead spun it off); `divopt +0.021` above; everything else at/above. LEAD BAR MET (net-positive + suite + switch green). 4th commit `2b22f65` = port RuleLessNotEqual (BOOL_AND `(V<=W)&&(V!=W)=>V<W`, slot 100) + RuleEqual2Zero INT_ADD(x,y*-1) variable-negation arm — resolves the for-loop guards (noforloop_alias 0.886→0.976, varcross, elseif) WITHOUT the Task-#4 print-time `<=` adaptation. VISIBLE DEBT (lead-required, recorded 3 places: RuleEqual2Zero coverage.md row + code comment at the arm in rules.rs + here): Ghidra's RuleEqual2Zero all-descendants-bool-output guard (`if(!boolop->isBoolOutput())return 0` over addvn->beginDescend()) is deliberately OMITTED — adding it suppresses an equal2zero firing switchloop's jumptable recovery depends on (mosura's switch-path IR gives the guard sum a non-bool use Ghidra's doesn't, a SEPARATE switch-path divergence). Per no-adaptation-grandfathered the omission is CANCELED the moment that switch-path divergence is fixed → restore the guard + re-verify switchloop then. (Lead adding it to the backlog task list.) Below = the earlier "AT baseline" checkpoint (superseded).

ITER-3 (earlier checkpoint, superseded by NET-POSITIVE above) — the delicate CORE is faithful+complete, corpus AT baseline. 3 commits: `cd0fd9e` (signed find_form/applyOp+moveSignBitExtraction+width, N-ary RuleCollectTerms=TermOrder+termOrder+distributeIntMultAdd, RuleSelectCse isCseMatch fix [const inputs match by VALUE not SIZE — THE unlock for the sign-CSE], INT_DIV-rooted RuleModOpt, retired fused try_unsigned/match_mulhi/try_signed/add_correction) → `4f19ab6` (INT_RIGHT-rooted RuleSignMod2nOpt + INT_MULT-rooted RuleSignMod2nOpt2/checkSignExtForm, MULTIEQUAL form deferred) → `b5e3df8` (port AddExpression → RuleSborrow/RuleScarry match post-Sub2Add `a+b*-1`). Suite 334/0, all switch tests green. Corpus 0.8594→**0.8846** (baseline 0.8864, net -0.0018). Per-fixture vs baseline: divopt +0.021, forloop* back to 1.0, modulo 0.524→0.893, modulo2→0.941, ifswitch→0.985. STILL BELOW baseline (4): noforloop_alias -0.090, varcross -0.019, modulo -0.012, elseif -0.010 — all comparison-STRUCTURING (`<=` vs `<`, `!=0 &&` compound) = TASK #4/RuleIntLessEqual entangled, EXCEPT modulo -0.012 = the 64-bit signed 65-bit-magic case (DivTermAdd folds +2^64 → `CONCAT88(1,0x8888888888888889)`; calc_divisor(0x45,magic65,63)=0 but magic64→60; deep DivTermAdd/find_form interaction, needs instrument of mosura-vs-Ghidra reassembled magic+n). KEY LESSON: the sign-cancellation that blocked 2 prior attempts = isCseMatch (const size) + N-ary CollectTerms (level-spanning `(SDIV+s)-s`), NOT Sub2Add placement alone. dumpc.rs throwaway example (print_c / --raw) added uncommitted. REPORTED to lead, awaiting gate.

GROUNDED MECHANISM: RuleDivTermAdd reassembles `sub(ext(V)*c,b)>>d + V` -> `SUBPIECE((ext(V)*(c+2^n))>>n, 0)` (inner shift is 16-byte, V 8-byte). mosura's find_form/ModOpt can't recover it due to THREE self-documented NON-Ghidra approximations in divopt.rs/printc.rs:
1. find_form_apply (divopt.rs ~270): `if x.size != out_size return 0` — a deliberate non-faithful width restriction (its comment admits Ghidra inserts a truncation/extension but mosura's printer renders the inserted ops badly). FAITHFUL = port Ghidra RuleDivOpt::applyOp width handling (ruleaction.cc ~8300): inVn.size<outSize -> ext(extOpc) widen; inVn.size>outSize -> make orig op SUBPIECE(resVn,0), div goes in a new op at inVn.size. + fix printc to ABSORB the SUBPIECE/ZEXT (Ghidra --c absorbs via implied casts).
2. find_form (divopt.rs:224): `OpCode::IntSext => return None` defers signed. FAITHFUL = port signed findForm (SEXT: xsize=ext.in(0).size*8) + applyOp signed path: moveSignBitExtraction(out,inVn) (ruleaction.cc:8190 — repoints `>>(size*8-1)` sign extractions to inVn) + build INT_SDIV(inVn,divisor) + sign correction SRIGHT(inVn,size*8-1) + INT_ADD. Concrete corpus miss = modulo param_2[6] `% 0x3c` signed (renders raw today). Ghidra findForm at ruleaction.cc:8051; applyOp ~8280.
3. mosura's FUSED try_unsigned/match_mulhi + try_signed/add_correction (divopt.rs 294/358/340) recognize the add-correction DIRECTLY. Once 1+2 land + RuleDivTermAdd wires, these become redundant with Ghidra's real RuleDivTermAdd->find_form->ModOpt chain -> RETIRE per no-grandfathering policy.

GATED COMMIT ORDER (each: divopt unit tests + corpus green, REPORT corpus, land on lead OK): (i) signed find_form path + moveSignBitExtraction + signed applyOp; (ii) find_form_apply width ext/trunc + printc absorb; (iii) RuleModOpt matching-through-wrapper (SEE REFINEMENT); (iv) wire RuleDivTermAdd slot 83, verify modulo restored-or-better (baseline modulo 0.740, avg 0.8659 — CORPUS TEST is authoritative/stable); (v) retire try_unsigned/match_mulhi (+maybe try_signed/add_correction). Steps couple (RuleDivTermAdd fires on both signed+unsigned) so measure via corpus with it wired.

REFINEMENT (deeper grounding, 2026-07-02): the UNSIGNED reassembled form does NOT hit find_form_apply's width restriction — find_form's resvn = ext(V) (16B) when ext.size==shift.out.size (16==16), so no width mismatch; find_form_apply produces `SUBPIECE(ext(V)/d, 0)` (16-byte INT_DIV then truncate). The REAL unsigned blocker is that mosura's **RuleModOpt** only matches `x - (x/d)*d` with div.in(0)==x directly — it does NOT match through the `SUBPIECE(ext(V)/d,0)` wrapper (input is ext(V) not V, + SUBPIECE). Ghidra's RuleModOpt DOES fold this. So port Ghidra RuleModOpt's matching to see through ext/SUBPIECE. So the full blocker set = find_form signed path + moveSignBitExtraction + find_form_apply width (for genuine mismatches) + printc absorb + RuleModOpt match-through-wrapper + retire fused recognizer. Multi-layered.

SIGNED param_2[6] (%0x3c) detail: its high-multiply is `SUBPIECE(INT_RIGHT(mult128, 0x40), 0)` (== SUBPIECE(mult128,8) = mulhi), which mosura's match_mulhi (expects SUBPIECE(mult,off)) does NOT match, AND the `+x` add-correction is inside `(mulhi+x)>>5` — this is exactly RuleDivTermAdd's reassembly target. So param_2[6] only recovers AFTER RuleDivTermAdd reassembles + signed find_form + RuleModOpt. Coupled; not a standalone win.

STEP (i) ATTEMPTED + REVERTED (data, 2026-07-02): implemented find_form SEXT arm + move_sign_bit_extraction (ruleaction.cc:8190) + signed applyOp (op -> INT_ADD(SDIV(x,d), x s>>(size*8-1))). Faithful, compiles, all 204 tests green (NO correctness regression). But REGRESSES modulo 0.740->0.608 (avg 0.8659->0.8631, 54->53). ROOT CAUSE (data-backed): find_form-signed fires on the shift root and produces `(SDIV + x s>>63) - (x s>>63)`; mosura's default_rule_pool does NOT cancel the sign terms to clean SDIV -> messier than the fused try_signed's direct SDIV. Disabling try_signed makes modulo WORSE (0.582) -> confirms find_form-signed's output is un-simplified, retiring the fused recognizer does NOT fix it. The missing faithful simplification `(a+b)-b -> a` needs RuleSub2Add (INT_SUB->INT_ADD+MULT-neg) THEN RuleCollectTerms — but **mosura places RuleSub2Add OUTSIDE default_rule_pool (in ptrarith_pool/cleanup_pool) — a NON-Ghidra placement adaptation** (Ghidra's RuleSub2Add is in the main oppool). So the deeper non-Ghidra cause = RuleSub2Add placement (HIGH blast radius: affects every INT_SUB in every fixture). Step-(i) patch saved: scratchpad/task20-step-i-findform-signed.patch. So step (i) is NOT standalone (contra the plan) — coupled to RuleSub2Add-in-main-pool + needs #21 to debug the un-cancelled IR.

GROUNDED IR (2026-07-02, dump works): find_form-signed rewrites `x s/3` -> `INT_ADD(SDIV(x,3), x s>>63)` then the surrounding `- (x s>>63)` should cancel to `x s/3` (Ghidra's applyOp identity `mulhi>>e = (x s/d)+(x s>>63)`). It does NOT cancel in mosura: `(a+S)-S -> a` needs RuleSub2Add (INT_SUB->INT_ADD+MULT-neg) + RuleCollectTerms, but RuleSub2Add is OUT of default_rule_pool (Ghidra: IN main oppool). -> modulo 0.740->0.608. NO narrower faithful fix (a direct `(V+W)-W->V` rule = inventing non-Ghidra logic; const-size quirk #0x3f:8-vs-:4 is secondary, Ghidra has same mismatch + cancels via functional CollectTerms). HARD GATE hit: only faithful fix = RuleSub2Add-in-main-pool = HIGH-BLAST (4 switch/jumptable tests + corpus 0.8659->0.7904; that breakage = other non-Ghidra switch/jumptable code to fix per no-grandfathering). Reported scope to lead (A: user decision / B: read-only size the cascade / C: hold). #20 = multi-part coupled restructure, NOT a bounded port. AWAITING lead scope decision.

CORRECTION (2026-07-02, IMPORTANT): the "dump nondeterministic / modulo collapses" claims below were a FALSE-POSITIVE DETECTOR BUG (mine): I classified dumps with `grep "return xVar"`, but healthy modulo ENDS with a normal `return xVar1 * 0x10624dd3;` line -> flagged healthy output as "collapse." VERIFIED: baseline f6169a7 modulo dumps are BYTE-IDENTICAL across runs (deterministic); the `dump` tool WORKS. There is NO nondeterminism, NO collapse. #21 premise unfounded (heritage HashSet order may vary per process but output is invariant — task5 was right). IGNORE the "EXECUTION BLOCKER / dump collapses" text below. #20 is NOT blocked on tooling. The modulo regression (find_form-signed 0.740->0.608) is REAL (from the deterministic corpus) and the RuleSub2Add-placement root cause STANDS. Use the dump freely to ground the sign-cancel fix.

EXECUTION BLOCKER (SUPERSEDED — see CORRECTION above; was a detector artifact): reliable IR observation is gone — `dump` collapses ~100% in the current env. Do #21 (deterministic iteration) FIRST to restore reliable grounding (and it exposes an order-dependent CORRECTNESS bug: mosura sometimes emits `return x*magic` for modulo = WRONG output, likely in the divopt/rule/deadcode area — worth root-causing, may even be the thing that makes #20 hard). THEN execute #20 in a warm-context resume with a fresh budget. Recommended to lead.

INSTRUMENT REFRESH (2026-07-06, base d66c9ed, owner rock1) — current-HEAD re-grounding after RuleSubNormal/RuleSubRight wired (d66c9ed):
- GHIDRA per-div-site firing SEQUENCE (divopt trace, verbatim before/after): `sub2add` (INT_SUB→INT_ADD+MULT(-1)) → `divtermadd2` (reassemble `mulhi+corr` into `SUBPIECE((ext(V)*CONCAT(1,magic))>>0x41,0)`) → `subnormal` (SUBPIECE-through-shift → `SUB(prod,8)>>1` = mulhi>>1) → LATER `doubleshift` (merge `(mulhi>>1)>>6`→`mulhi>>7`) → `divopt` (recognize `V/0x51`). 16 sites, each same order.
- ROOT CONFIRMED: Ghidra registers the ENTIRE cluster in ONE pool (actprop, coreaction.cc:5515-5605: CollectTerms,SignShift,IdentityEl,Sub2Add@5553,AddMultCollapse@5563,DivTermAdd,DivTermAdd2,DivOpt,SignForm,SignForm2,SignDiv2,DivChain,SignNearMult,ModOpt,SignMod2nOpt×2,SignMod2Opt). mosura SPLIT Sub2Add+AddMultCollapse into ptrarith_pool (pipeline.rs:258 comment admits it: keeps INT_SUB so the FUSED DivOpt/ModOpt/SignMod2nOpt match SUB directly). Trace proof of starvation: sub2add 53x-vs-4x, addmultcollapse 44x-vs-3x, identityel 51x-vs-2x (modulo); divtermadd2 16x-vs-0 (WIRED but starved — no ADD form to reassemble).
- BLAST at current HEAD (throwaway measurement, reverted; Sub2Add+AddMultCollapse ADDED to default_rule_pool after IdentityEl, ptrarith left intact): corpus avg 0.8864→0.8226, 54/60→47/60. 4 switch unit tests FAIL (build.rs: recovers_jump_table, recovers_in_code_jump_table, emits_switch_statement, switch_in_loop_structures). Worst movers: switchloop 0.786→0.116, switchind 0.959→0.488, switchhide 0.940→0.588, ifswitch 0.985→0.429, modulo 0.905→0.511, modulo2 0.941→0.583, forloop1/varused/withskip 1.0→0.85/0.88/0.79. (Worse than stale 0.7904 because partial: fused recognizers stop firing, faithful chain not wired.)
- SWITCH-CASCADE MECHANISM (grounded, NOT assumed): build.rs:214 runs full `pipeline::decompile(&mut partial)` then `partial.jump_tables()` recovers switches from FULLY-SIMPLIFIED IR. Sub2Add-in-main rewrites index/guard to ADD+MULT(-1), so jumptable.rs normalize(:146, walks only COPY/SEXT/ZEXT/SUBPIECE0 — NOT ADD/MULT)/guard_range(:211)/emulate(:290) can't match. Ghidra's JumpBasic/analyzeGuards/EmulateFunction handle the normalized form. THIS is the "cascades into switch/jumptable" blocker — the switch fix is the critical-path sub-task.
- RULE INVENTORY: PRESENT=RuleSignForm(rules.rs HELD), RuleDivTermAdd(divopt.rs HELD), RuleDivTermAdd2(wired), RuleDivOpt(fused wired), RuleModOpt(wired), RuleSignMod2nOpt/2nOpt2(divopt.rs wired — verify faithful-vs-fused: mosura fires signmod2n 6x on modulo where Ghidra fires 0). NOT-PRESENT (need writing)=RuleSignForm2, RuleSignDiv2, RuleSignNearMult, RuleSignMod2Opt, RuleDivChain, RuleSignShift, RuleTestSign, RuleLeftRight.
- STAGING PLAN (lead GO 2026-07-06: Stage1 self-approve on master; Stage1.5 NEW faithful jumptable robustness on master byte-identical; Stage2 keystone in a WORKTREE):
  * STAGE 1 ✅ DONE (base d66c9ed): batch1 `b4db0f3` (divopt.rs: RuleSignDiv2 :8339, RuleDivChain :8392, RuleSignNearMult :8533, RuleSignMod2Opt :8776 + check_sign_extraction helper :8758, 4 tests) + batch2 `2a31037` (rules.rs: RuleSignShift :3524, RuleTestSign :3582 +find_comparisons, RuleSignForm2 :8476 w/ faithful return-0-after-mutate quirk, 3 tests). All HELD/unwired, BYTE-IDENTICAL (suite 304/0, corpus 0.8864/54-60). RuleLeftRight (:2010) DEFERRED — not cheap: needs op_unset_input/op_unset_output + address/register-piece varnode creation (new_varnode_out+renormalize) mosura lacks; same register-piece territory as Task #7; port with the keystone or Task #7. RuleTestSign was the bonus 7th.
  * STAGE 1.5 = INSTRUMENTED, SCOPE DIVERGENCE REPORTED (2026-07-06, awaiting lead steer). Instrument (Sub2Add+AddMultCollapse in main, switchloop --ir, reverted): the BRANCHIND index is UNCHANGED (`LOAD(idx*4+base)`); what breaks is the GUARD — the loop bound becomes `INT_LESS(INT_ADD(index,-1), 8)`. mosura's guard_range(:211)+normalize(:146, walks only COPY/SEXT/ZEXT/SUBPIECE-0) can't relate `index-1` to the on-path `index`, so range analysis fails → BRANCHIND declined → switch body dead. FINDING: a FAITHFUL fix = Ghidra's JumpBasic subsystem (jumptable.cc: PathMeld + GuardRecord + CircleRange + findSmallestNormal(:1165, walks pathMeld picking smallest-range vn) + calcRange(:1120) + valueMatch(:637)/oneOffMatch(:684)/quasiCopy(:719)). mosura's jumptable.rs is a STRUCTURALLY SIMPLER recovery (one switch-var pick + backtrace-membership + direct emulate), NOT a thin JumpBasic variant — so the faithful port is a subsystem REPLACE, not a small byte-identical patch, and reproducing current corpus byte-for-byte is a HIGH bar (not free). Grafting valueMatch/oneOffMatch onto the current structure = the ad-hoc hack the lead forbade. ALSO FLAGGED: constant-masking artifact — `INT_EQUAL r0x8:4 #0xffffffff0000000a:4` (should be #0xa:4) appears once Sub2Add/CollectTerms/CollapseConstants run in main; over-wide/unmasked constant, separate from jumptable, own Stage-2 instrument. RECOMMENDATION to lead: FOLD jumptable into the Stage-2 worktree (validate JumpBasic against real ADD-form IR + 4 switch tests + corpus together) rather than a separate byte-identical Stage 1.5 on master; OR treat jumptable JumpBasic port as its own dedicated task.
  * STAGE 2 ITERATION 1 (2026-07-07, rock4, worktree `defuse-divopt` at /home/jd/projects/mosura/mosura-defuse, branch off master a162673). SETUP: symlinked oracle/capture+capture_trace+build/oracle-cache from main (gitignored, not in worktree checkout). BASELINE confirmed 0.8864, 54/60, switch tests green, per-fixture snapshot in scratchpad/baseline_perfixture.txt. DID: relocated Sub2Add→slot42 (after Rule2Comp2Mult) + AddMultCollapse→slot52 (after RuleXorCollapse) into default_rule_pool per Ghidra actprop order (coreaction.cc:5512-5605 verbatim: SignShift@14,TestSign@15,Sub2Add@42,AddMultCollapse@52,DivTermAdd@83…SignMod2Opt@94), removed both from ptrarith_pool. RESULT (uncommitted WIP in worktree): 4 switch build.rs tests + jumptable_recovery 6/6 ALL FAIL — recover_jumpbasic DECLINES (0 tables). **CRITICAL FINDING — lead's Stage-2 premise "JumpBasic carries the switch cascade" is DISPROVEN.** ROOT (instrumented, jbdump.cc on Ghidra + MOSURA_JBDUMP in find_smallest_normal/recover_jumpbasic): the switch cascade is a GUARD-REPRESENTATION divergence, NOT a JumpBasic gap. Ghidra's switchind guard = `INT_LESS(10,x)` + the `cbranch->isBooleanFlip()` FLAG (jumptable.cc:1093, maxpullback=2, reaches INT_LESS in 1 step, range [0,b)=[0,11) via flip complement). mosura MATERIALIZES the branch-sense inversion as a `BOOL_NEGATE` op: post-relocation switchind guard = `BOOL_NEGATE(INT_LESS(10,x))`, so (a) the extra op eats the pullback budget and (b) toswitchval=(indpath==1) doesn't account for the flip → analyze_guards produces range {1}/unbounded (2^31) instead of [0,11) → count>MAX_TABLE=4096 → DECLINE → all guarded switches die. mosura's JumpBasic port EXPLICITLY DEFERRED isBooleanFlip/getFlipPath (jumpbasic.rs:753 `toswitchval=indpath==1`, :759 `indpathstore=indpath`; determinedbranch.rs:151 also notes it). NB baseline mosura guard = `INT_LESS(x,11)` (a THIRD form, ≠ Ghidra's `!(10<x)` but analyzable); relocation shifts the rule fixpoint into the un-analyzable BOOL_NEGATE form even for switchind (NO subtraction in its guard — a pool-fixpoint interaction, not Sub2Add touching the guard directly). FAITHFUL FIX = port Ghidra's isBooleanFlip mechanism (PcodeOp flip flag + absorb CBRANCH(BOOL_NEGATE(c))→CBRANCH(c)+flag like Ghidra's opBooleanFlip + getFlipPath in analyze_guards) so all downstream sees Ghidra's clean INT_LESS+flag form — a NEW sub-mechanism NOT in the lead's Stage-2 scope list. REPORTED to lead, awaiting steer (options: A port isBooleanFlip as a gated sub-stage; B local analyze_guards adaptation to detect CBRANCH(BOOL_NEGATE) + flip; C treat isBooleanFlip as its own task before the keystone). Worktree WIP kept (relocation + JBDUMP instruments uncommitted); master untouched/green.
  * STAGE 2 ITER-1 CORRECTION (2026-07-07, rock4 — SUPERSEDES the isBooleanFlip framing above/task #8). Deeper instrument (jbdump.cc PARTIAL IR on Ghidra + condchain dump in mosura analyze_guards + trace-diff switchind relocated-mosura vs Ghidra) DISPROVES isBooleanFlip. GHIDRA's switchind guard at recovery = clean `INT_LESS(x, 0xb)` = `x<11`, switch on TRUE edge, NO isBooleanFlip, NO BOOL_NEGATE (identical to mosura's BASELINE guard). So Ghidra does NOT use the flip flag here — it NORMALIZES the x86-`ja` raw guard `!(x<c || x==c)` [= BOOL_NEGATE(BOOL_OR(INT_LESS,INT_EQUAL)), same raw in both] down to clean INT_LESS via a COMPARISON-NORMALIZATION cluster. mosura's relocated guard STALLS at `BOOL_NEGATE(BOOL_OR(INT_LESS,INT_EQUAL))` (condchain dump: vn→BoolNegate→BoolOr→IntLess+IntEqual). get_booleanflip (ruleaction.cc) does NOT handle BOOL_OR, so RuleBoolNegate can't touch `!(a||b)` until the BOOL_OR collapses. TRACE-DIFF NAMES the starved cluster (Ghidra-fires / mosura-never on switchind): **intlessequal 3x** (RuleIntLessEqual — HELD UNWIRED in mosura, the Phase-1c/task#4 print-time `<=` adaptation!), **boolnegate 2x** (mosura HAS it, slot98, but starved), **lessequal 1x** (RuleLessEqual), **condnegate 1x** (RuleCondNegate — mosura LACKS; it is the flag→op structuring rule that DOES read isBooleanFlip). So the keystone switch-cascade blocker is ENTANGLED WITH TASK #4 (cancel print-time `<=` adaptation + wire RuleIntLessEqual) + RuleLessEqual/RuleCondNegate — comparison normalization, NOT the isBooleanFlip subsystem port (task #8 as framed is MISDIRECTED). CAVEAT: baseline mosura reaches clean INT_LESS(x,11) WITHOUT firing RuleIntLessEqual (it's unwired even at baseline), so the exact minimal relocation-fix needs a baseline-vs-relocated rule diff (next instrument) — the relocation breaks whatever baseline path produces INT_LESS(x,11). REPORTED redirect to lead; task #8 (isBooleanFlip) claimed then found misdirected — recommend re-scope to the comparison-normalization/task-#4 cluster. Master untouched (NO isBooleanFlip code written — instrument-first). Worktree defuse-divopt WIP kept (relocation + JBDUMP/condchain dumps uncommitted).
  * STAGE 2 ITER-1 RESOLVED — SWITCH CASCADE FIXED (2026-07-07, rock4). The switch-cascade blocker is NOT isBooleanFlip (task #8, obviated) NOR task #4/RuleIntLessEqual — it is a ONE-LINE MASKING BUG = keystone step 5's over-wide EQUAL constant. NAMED by baseline-vs-relocated rule-firing diff on switchind (scratchpad base.trace vs reloc.trace): relocation STARVES `boolnegate` 2→0 and `lessequal` 3→0. Smoking gun: baseline `INT_EQUAL(x, #0xa:4)` → RuleLessEqual fires (BOOL_OR→INT_LESSEQUAL) → guard normalizes; relocated `INT_EQUAL(x, #0xffffffff0000000a:4)` (OVER-WIDE) → RuleLessEqual's precondition (same const both sides) FAILS → BOOL_OR stays → guard un-collapsed → recover_jumpbasic declines. Producer = `RuleEqual2Zero` INT_ADD arm (rules.rs:745): `(x+(-c))==0 => x==-(-c)` computed `wrapping_neg()` WITHOUT masking to operand size → `-0xfffffff6`=`0xffffffff0000000a` in a 4-byte const. Latent at baseline (arm only fires once Sub2Add makes the ADD form; baseline operand is INT_SUB → sibling arm). FIX = mask `& calc_mask(size)` (matching RuleEqual2Constant/Ghidra). LANDED ON MASTER `3225f2b` — BYTE-IDENTICAL all 62 fixtures, suite 333/0, self-approved (latent faithful bug fix). VERIFIED in worktree (relocation + fix): jumptable_recovery 6/6, all 9 build.rs switch tests GREEN, suite 333/0. Task #8 (isBooleanFlip) OBVIATED — recommend delete. LESSON: instrument-first (Ghidra jbdump PARTIAL IR + baseline-vs-relocated trace) prevented building the wrong subsystem TWICE (isBooleanFlip, then RuleIntLessEqual); the trace-diff NAMED the mechanism.
  * STAGE 2 ITER-2 (2026-07-07, rock4, worktree rebased onto master 3225f2b). WIRED the full HELD cluster at Ghidra actprop slots in default_rule_pool: SignShift@14, TestSign@15, DivTermAdd@83, SignForm@86, SignForm2@87, SignDiv2@88, DivChain@89, SignNearMult@90, SignMod2Opt@94 (fused DivOpt@85/ModOpt@91 STILL active). Suite 333/0. Corpus 0.8592→**0.8594** (barely moved — cluster inert alongside fused matchers). trace-diff divopt: faithful chain NOW FIRES (divopt 16-vs-17 Ghidra, divtermadd2 16=16, doubleshift 16/17 — Sub2Add-in-main supplies the ADD form DivTermAdd reassembles!) but sub2add OVER-fires 33-vs-16 + heavy downstream verbosity (collapseconstants +188, boolnegate +85, logic2bool +85, earlyremoval 1343-vs-278). PER-FIXTURE (baseline→cluster): divopt 0.936→0.936 (reproduced!), switchind 0.959→0.959, switchloop 0.786→0.785, nan 0.560→0.560, BUT **modulo 0.905→0.524, modulo2 0.941→0.583, ifswitch 0.985→0.922, pointerrel 0.936→0.890 REGRESSED**. ROOT (matches step-(i) memory): fused try_signed + faithful sign path BOTH fire → `(SDIV+sign)-sign` un-cancelled → messier modulo. NEXT (the hard coupled core, NOT done): can't just retire fused — faithful find_form DEFERS signed (`IntSext=>None`, divopt.rs:224) + has width restriction (divopt.rs:270), so retiring fused breaks divopt. Must FIRST complete faithful find_form (signed path + moveSignBitExtraction + width ext/trunc + printc absorb + RuleModOpt match-through-wrapper per REFINEMENT above), THEN retire try_unsigned/match_mulhi/try_signed, THEN verify sign-cancel makes modulo clean (Sub2Add-in-main + RuleCollectTerms should now cancel — untested). CHECKPOINTED here (boundary) — cluster-wiring WIP uncommitted in worktree (pipeline.rs relocation + cluster). div recognition is CLOSE; the remaining lift is the fused→faithful matcher swap + modulo sign-cancel.
  * STAGE 2 REMAINING (keystone, worktree defuse-divopt): switch canary now CLEAR. Corpus at relocation+maskfix = **0.8592, 52/60** (baseline 0.8864/54) — DOWN because relocation starves the FUSED div/mod recognizers (try_unsigned/match_mulhi/try_signed) and the faithful ADD-form cluster isn't wired yet. NEXT = steps 2-4: wire DivTermAdd (slot 83) + sign cluster (SignShift@14/TestSign@15/SignForm@86/SignForm2@87/SignDiv2@88/DivChain@89/SignNearMult@90/SignMod2Opt@94, all HELD in divopt.rs/rules.rs) at Ghidra actprop slots; replace fused DivOpt/ModOpt matchers with faithful ADD-form (find_form signed path + width ext/trunc + RuleModOpt match-through-wrapper per REFINEMENT above); retire try_unsigned/match_mulhi/try_signed; verify RuleSignMod2nOpt faithfulness (6x-vs-0). WORKTREE STATE (uncommitted): relocation (Sub2Add@42+AddMultCollapse@52 in default_rule_pool, removed from ptrarith) + maskfix (now redundant with master 3225f2b — rebase drops it) + JBDUMP/condchain instruments in jumpbasic.rs (REMOVE before landing). Rebase worktree onto master 3225f2b to resume.
  * STAGE 2 = keystone in a WORKTREE (git worktree, branch off master; master stays green): relocate Sub2Add+AddMultCollapse→main; wire DivTermAdd + the Stage-1 sign cluster; faithful ADD-form DivOpt/ModOpt; VERIFY RuleSignMod2nOpt faithfulness (6x-vs-0 on modulo — if fused-adaptation, cancel not grandfather); retire fused try_unsigned/match_mulhi/try_signed. Iterate to corpus-net-positive AND 4 switch tests green, then report full per-fixture delta table; lead gates the merge.

STAGE-1 SOURCE ANCHORS (ruleaction.cc, grounded read-only 2026-07-06, turnkey for the 6 not-present ports): RuleSignShift :3535 (sign-bit norm, INT_RIGHT/SRIGHT); RuleSignDiv2 :8347 (`(x + (x s>> 31)*-1) / 1 => x s/ 2`, keys INT_SDIV-ish, checks in(1)==1 → ADD of MULT(-1) of SRIGHT(x,8sz-1)); RuleDivChain :8401 (`(x/c1)/c2 => x/(c1*c2)`, INT_DIV+INT_SDIV, loneDescend guard, overflow bitcount check); RuleSignForm2 :8487 (INT_SRIGHT; `sub(sext(V)*small,c) s>>31 => V s>>31`; QUIRK: success path does `opSetInput(op,a,0)` then `return 0` — repoints, reports no-change, faithful port must replicate the return-0); RuleSignNearMult :8541 (INT_AND; `(V + (V s>>0x1f)>>(32-n)) & (-1<<n) => (V s/2^n)*2^n`, builds new SDIV+MULT); RuleSignMod2Opt :8787. RuleSignForm :8453 + RuleSignDiv2 helper calc_mask. All key off calc_mask/mostsigbit_set/signbit_negative (mosura has these in nzmask.rs). getOpList opcodes as noted → wire at the coreaction.cc:5597-5605 slots.

CRITICAL TOOLING NOTE: `cargo run --example dump -- modulo` is NONDETERMINISTIC (intermittently collapses to `return x*magic` — Rust HashSet iteration order; reproduced at 8dd6d80, PRE-EXISTING, not mine). Do NOT trust dump for modulo IR grounding. The CORPUS TEST (`cargo test -p mosura --test decompile_corpus -- --nocapture`) IS stable (modulo 0.740 every run) — use it for measurement. Ground the reassembled-form recovery via UNIT TESTS (construct the op-graph, assert find_form recovers), not dump. mosura's nondeterministic heritage HashSet iteration is itself a non-Ghidra approximation worth a separate task (flagged to lead).

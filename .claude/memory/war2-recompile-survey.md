---
name: war2-recompile-survey
description: User-directed WAR2.EXE per-function recompilation-parity survey (2026-07-23) — status of every function against the decompile→Watcom-recompile→byte-identical bar
metadata: 
  node_type: memory
  type: project
  originSessionId: 99103a08-9d06-430b-8446-4ca5e3757106
  modified: 2026-07-24T05:53:14.132Z
---

✅ DONE (2026-07-23, doc LANDED `ef65486`: docs/war2-function-status.md + crates/mosura/examples/war2_survey.rs). **RESULT: 1/1286 EXACT (a 1-byte ret stub @0x70805) · 0 RELOC_EXACT · 941 MISMATCH (473 reg-artifact / 465 param-recovery; 862/941 <10% byte-similar) · 227 COMPILE_FAIL (top E1063 missing-operand = `...`/CALLOTHER leaking into C) · 117 DECOMPILE_FAIL = ONE new panic `merge.rs:1205 index OOB` (first repro 0x11954).** Ranked blockers: (1) __watcall prototype recovery absent on LE path → void_proto on 91% of fns (biggest lever); (2) extraout artifacts (=menu E trial-lifecycle); (3) func_0x indirect calls (822 fns); (4) unrendered operands (~169 fns); (5) merge.rs:1205 panic. Compiler matched: Watcom 10.0a wcc386 under dosemu, `-4r -fpi87 -s -of+ -onatx` (+`89 ec` postlink strip). **ROOT-CAUSE of the 91% void_proto (grounded 2026-07-23, NOT deep):** the `__watcall` model ALREADY EXISTS (`specs/x86-32-watcom.cspec`, grounded in open-watcom-v2 src, commit `7e3de3b`) AND the LE loader ALREADY tags WAR2 `compiler_spec_id="watcom"` (le.rs:241) AND the pipeline HAS ActionActiveParam (pipeline.rs:88). The gap: `analysis/decompiler.rs::decompile_function` builds the funcdata via `raw_funcdata_flow_image_overrides` → `build.rs::default_proto_model` which HARDCODES `DEFAULT_LANG_ID`(x86-64)+`DEFAULT_COMPILER_ID`(gcc) — it ignores `program.language_id`/`program.compiler_spec_id`. So WAR2 funcs get the x86-64 SysV model (wrong reg set) → param recovery finds nothing → void. build.rs:22 comment SAYS analysis::decompiler "overrides Funcdata::proto_model" but it does NOT. **FIX = thread (program.language_id, program.compiler_spec_id) into proto-model resolution on the analysis path (bounded wiring; flips param recovery on for all ~1279 funcs at once). The survey harness re-measures the delta.**

**REMEDIATION PLAN (staged, re-measure with the harness after each):**
- **Stage 0 ✅ LANDED `b6ec467` (2026-07-23):** merge.rs:1205 panic = a PARTIAL PORT of `Merge::trimOpInput` (merge.cc:692). Ghidra branches on the op — MULTIEQUAL trims via predecessor block (getIn(slot)/opInsertEnd); any OTHER marker op (INDIRECT, slot-0 data input force-trimmed by merge_op max==1) trims in its OWN block at op->getAddr()/opInsertBefore. mosura ported only the MULTIEQUAL branch → indexed in_edges[slot] for an INDIRECT in the entry block (0 in-edges) → OOB. Ported the else branch. ALL 117 DECOMPILE_FAILs cleared → survey EMIT now fail=0 (1286/1286 decompile). Gated: unit test `merge::tests::trim_op_input_on_indirect_trims_in_own_block` (panics pre-fix) + real-binary fail=0 + doc `docs/decompiler-bug-merge-indirect-trim-panic.md`. Corpus byte-neutral 0.9513/57; suite 556/0; clippy 0. (Doc war2-function-status.md `e97227c` notes the 117 stale rows; full recompile re-split deferred to the post-Stage-1 sweep to avoid a double dosemu run.)
- **Stage 1 ✅ LANDED `e097ea8`+doc `8544a8b` (agent war2-remediation, 2026-07-23):** threaded (language_id, compiler_spec_id) through raw_funcdata_flow_image_overrides into cspec resolution (faithful to DecompInterface/Architecture CompilerSpec); datatest path keeps SysV → corpus byte-identical 0.9513/57. Unit test on watcall resolution + threaded model. **Re-measure @e097ea8 (results in /home/jd/projects/mosura/war2-survey-v2): 1/0/1056/229/0 (was 1/0/941/227/117). void_proto 1169→247 (922 fns gained params); extraout 626→93 (pre-emptive dent in Stage 3 class). MISMATCH residual now codegen/regalloc-dominated (797). 24 MISMATCH→COMPILE_FAIL regressions = downstream type/render gaps (14 E1052 void-type via `(*(code*)p)()` result assignment) = Stage 2 scope. E1063 `...`/CALLOTHER leak now 113 = Stage 2 primary target. Byte-exact bar unmoved (1) — param recovery necessary-not-sufficient.**
- **Stage 2 ✅ LANDED `b4ac8f4`+doc `ad50898` (2026-07-23):** printc catch-all `NAME(...)` leak RETIRED — faithful CALLOTHER→userop-name render (PrintC::opCallother; needed the .sla userop index→name table threaded onto Funcdata::userops, analog of Architecture::userops) + SBORROW/POPCOUNT. COMPILE_FAIL 229→137 (−92, all →MISMATCH, zero regressions); E1063 113→4; callother smell →0. Corpus byte-identical; doc docs/decompiler-bug-callother-ellipsis-leak.md. **SCOPE VERDICT (instrumented): ALL 137 residual COMPILE_FAILs = upstream type-inference/CAST (C-cluster/menu F+B, corpus-moving, DEEP) — filed as task #4 (USER PICK, not authorized); E1052 ×34 / E1079 ×33 / E1029 ×17 top classes. Stage 3 retires the extraout_-derived subset — re-measure before sizing.**
- **Stage 3 ✅ CLOSED-FAITHFUL (2026-07-23, `f23b362`): NO BUILD.** Two false premises caught by instrument-first before any code. FINAL oracle proof: lead BUILT analyzeHeadless (full Ghidra 12.0.3 via `scripts/build-ghidra-dist.sh` TASK=buildGhidra, GHIDRA_SRC=/data/tools/ghidra_12.0.3_PUBLIC) + ran the minimal `call B;ret`+`B=mov eax,42;ret` fixture → **full-analysis Ghidra renders `void FUN(void){ FUN_00100010(); return; }` — DROPS the bare call-result return, byte-identical to mosura + isolated oracle** (callee recovers non-void undefined8 but ancestorOpUse rejects the CALL in full-analysis too). Tail-call `jmp B` → Ghidra makes caller a THUNK (=survey's 3 thunk cases, separate). Used-result → captured by both. ⇒ cross-function propagation to capture these = BEAT-Ghidra, non-faithful, REJECTED. Doc docs/decompiler-nonbug-bare-call-return-faithful.md + fixtures oracle/ground-truth-notes/. Branch stage3-trial-lifecycle (funcLinkOutput isOutputLocked half, faithful but INERT) stays UNMERGED (d51 don't-land-inert-scaffolding). **Residual extraout_ (93) = scoped per-case analyzeHeadless diff (killedbycall-faithful vs divergence), NOT the whole-program foundation — separate future investigation.**
- **Stage 0 GATED to ground-truth ✅ `04d0165` (2026-07-24, war2-issues-become-source-tests satisfied for the panic):** `oracle/ground-truth/src/war2gates.c` — `trim_shape` mimics WAR2 FUN_00011954 (3 sequential register-arg calls + 2 global stores in one block; chained call-guard INDIRECTs force the non-MULTIEQUAL trim). Built by ow2 wcc386 via build.sh's `build_watcom war2gates`. PROVEN: pre-fix (ef65486) panics at merge.rs:1205 via analyze_file+decompile_function; fix decompiles. Gate `ground_truth_parity::war2_trim_shape_no_panic` (asserts Some; None = caught panic) + war2gates joins the general subset check (0 spurious/full recall, 8 funcs). add3/port_io = realistic context exercising Stage-1(__watcall)/Stage-2(CALLOTHER) paths, not output-gated (isolated leaf decompile noisy; those stages have own unit tests). **Reusable: native ow2 toolchain assembled at `~/tools/open-watcom/binl/{wcc386,wasm,wlink,wlink.lnk}` from `/data/open-watcom-v2/bld/*/linuxx64/*.exe` + `bld/wl/lnk/linuxx64/wlink.lnk`; validated by byte-identical rebuild of committed narrowsw. build.sh uses `GT_WATCOM=$HOME/tools/open-watcom`.** Standalone-LE build (`wlink format os2 le`) also works and loads via analyze_le_file (compiler_spec_id=watcom, watcall params recovered) — used to verify the Stage-1 path, not committed (ELF is the ground-truth column).

**🏁 WAR2 REMEDIATION CAMPAIGN COMPLETE (2026-07-23): Stage 0 panic ✅ + Stage 1 __watcall ✅ + Stage 2 printc ✅ + Stage 3 closed-faithful ✅. Remaining WAR2 gap = codegen/regalloc (dominant) + C-cluster type foundation (#4) + scoped extraout_ diff — all DEEP/user-investment, out of scope. analyzeHeadless now BUILT at /data/tools/ghidra_12.0.3_PUBLIC/build/dist/ghidra_12.0.3_DEV/support/ (reusable oracle).**
Honesty: byte-identical stays the hardest bar; near-term win = crash/compile classes → 0 + param cohort up the similarity curve, NOT hundreds of EXACT.

USER ASK (2026-07-23): for ALL functions in WAR2.EXE (~1279 @ `--le`), classify which decompile "properly" = recompiled code byte-identical to the original; derive a per-function status DOCUMENT from the results.

- Agent `war2-survey` (the single background-agent slot) runs the measurement: harness modeled on `crates/mosura/examples/gt_recompile_probe.rs` (analyze_le_file → decompile_function → print_c → wcc386-under-dosemu → `scripts/extract-omf-code.py` → byte-compare). Read-only wrt decompiler, nothing committed.
- Taxonomy: EXACT / RELOC_EXACT (differs only at fixup operand sites) / MISMATCH / COMPILE_FAIL / DECOMPILE_FAIL.
- Data lands in `/home/jd/projects/mosura/war2-survey/` (results.tsv + summary.md + c/ + obj/); compiler+flags matched via the analysis track's codegen-fingerprint docs (Watcom 10.x–11.0 lineage, historical toolchain via `scripts/setup-watcom-dosemu.sh`).
- Lead's follow-up on completion: derive `docs/war2-function-status.md` (summary + per-function table) — and remember [[war2-issues-become-source-tests]]: issue classes the survey surfaces must eventually be reduced to self-compiled ground-truth tests (follow-on, not part of the survey).
